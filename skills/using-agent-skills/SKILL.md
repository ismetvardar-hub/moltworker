---
name: using-agent-skills
description: Route user intent to the right LİKYA skill and slash command.
---

# Using Agent Skills

1. Kullanıcı “araştır / trend / repo oku” derse → `agent-reach-internet-access` + `.agents/agent-reach.md`
2. Yeni özellik → `/spec` → `/plan` → `/build` → `/test` → `/ship`
3. “Kod çürük / temizle” → `stop-slop` + `/review`
4. Metin / espri → `ethos-voice` (küfür yok)
5. Daze* veya Extreme Park istenirse → **davranışı koru**, gerekmedikçe dosyaya dokunma
