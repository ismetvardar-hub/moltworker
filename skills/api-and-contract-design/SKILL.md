---
name: api-and-contract-design
description: Contract-first API shapes before platform.js wiring.
---

# API & Contract Design

1. Path + method + body/response şeması
2. Auth: `requireUser` / `authHeaders`
3. OpenAPI notu (`server/openapi.js`)
4. Smoke/e2e assert planı
5. `events.js` / `integrations.js` mühürlü — yeni entegrasyonu oraya sıkıştırma
