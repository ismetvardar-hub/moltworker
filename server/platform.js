/**
 * AŞAMA 4–5 — Platform API: auth + arşiv + hub + ayarlar + audit
 */

import {
  login,
  logout,
  sessionFromToken,
  listDemoUsers,
  ROLE_PAGES,
  setActiveBrand,
} from './auth.js';
import {
  readCollection,
  writeCollection,
  prependItem,
  deleteItem,
  clearCollection,
} from './store.js';
import { appendAudit, readAudit } from './audit.js';
import { applySettingsToEnv, getPublicSettings, saveSettings } from './settings.js';
import {
  cancelJob,
  claimDirective,
  createJob,
  deleteJob,
  getJob,
  jobsSummary,
  listJobs,
  runJob,
  startJobTicker,
  tickJobs,
} from './jobs.js';
import { addSseClient, sseClientCount } from './events.js';
import {
  ackReportEthos,
  ackReportFlag,
  buildOpsReport,
  cancelReportJobs,
  reportToMarkdown,
  runReportSweep,
  snapshotReportAudit,
} from './report.js';
import {
  createVenue,
  getVenue,
  listVenues,
  removeVenue,
  updateVenue,
  venuesSummary,
} from './venues.js';
import {
  clearOpsDegraded,
  createBackup,
  healthCheck,
  healthCheckAsync,
  listDataFiles,
  quarantineOpsFile,
  restoreBackup,
  rotateOpsBackup,
  runOpsIntegritySweep,
} from './ops.js';
import {
  listNotifications,
  markAllRead,
  markRead,
  unreadCount,
} from './notifications.js';
import {
  admitPass,
  listAccessEvents,
  listGates,
  listHolders,
  passStats,
  verifyPass,
} from './pass.js';
import { buildMetrics } from './metrics.js';
import {
  brandsForRole,
  brandsSummary,
  createBrand,
  getBrand,
  listBrands,
  removeBrand,
  updateBrand,
} from './brands.js';
import {
  getGuest,
  guestTimeline,
  guestsSummary,
  listGuests,
  syncGuestsFromSources,
  upsertGuest,
} from './guests.js';
import { createPlaybook, listPlaybooks, removePlaybook } from './playbooks.js';
import {
  createWebhook,
  listDeliveries,
  listWebhooks,
  removeWebhook,
} from './webhooks.js';
import { buildOpenApi } from './openapi.js';
import {
  ackInventoryFlag,
  adjustStock,
  inventorySummary,
  listInventory,
  quarantineInventorySku,
  receiveInventoryDelivery,
  restockInventoryLows,
  runInventorySweep,
} from './inventory.js';
import {
  createShift,
  listShifts,
  removeShift,
  shiftsSummary,
  updateShift,
} from './shifts.js';
import {
  createReservation,
  listReservations,
  removeReservation,
  reservationsSummary,
  updateReservation,
} from './reservations.js';
import {
  adjustPoints,
  listLedger,
  listLoyaltyAccounts,
  loyaltySummary,
} from './loyalty.js';
import {
  ackIncident,
  createIncident,
  incidentsSummary,
  listIncidents,
} from './incidents.js';
import {
  createPurchaseOrder,
  createSupplier,
  listPurchaseOrders,
  listSuppliers,
  receivePurchaseOrder,
  removePurchaseOrder,
  suppliersSummary,
  updatePurchaseOrder,
} from './suppliers.js';
import { createFeedback, feedbackSummary, listFeedback } from './feedback.js';
import { buildExport, listExportCatalog } from './exports.js';
import { consentSummary, listConsents, recordConsent } from './consent.js';
import {
  announcementsSummary,
  createAnnouncement,
  listAnnouncements,
  removeAnnouncement,
  updateAnnouncement,
} from './announcements.js';
import {
  cookRecipe,
  createRecipe,
  listRecipes,
  recipesSummary,
  removeRecipe,
  updateRecipe,
} from './recipes.js';
import {
  checklistsSummary,
  listChecklistRuns,
  listChecklistTemplates,
  startChecklistRun,
  toggleChecklistItem,
} from './checklists.js';
import {
  createLostFound,
  listLostFound,
  lostFoundSummary,
  updateLostFound,
} from './lostfound.js';
import { addTip, tipSummary } from './tips.js';
import {
  ackMaintenanceFlag,
  closeCriticalMaintenance,
  createPreventiveMaintenance,
  createTicket,
  escalateOverdueMaintenance,
  listMaintenance,
  maintenanceSummary,
  runMaintenanceSweep,
  updateTicket,
} from './maintenance.js';
import {
  ackBriefFlag,
  ackBriefIncidents,
  buildDailyBrief,
  closeBriefMaintenance,
  restockBriefInventory,
  runBriefSweep,
} from './brief.js';
import {
  createMenuItem,
  listMenu,
  menuSummary,
  removeMenuItem,
  updateMenuItem,
} from './menu.js';
import {
  campaignsSummary,
  createCampaign,
  listCampaigns,
  removeCampaign,
  updateCampaign,
} from './campaigns.js';
import {
  i18nSummary,
  listI18nNotes,
  removeI18nNote,
  upsertI18nNote,
} from './i18nNotes.js';
import {
  coldchainSummary,
  listColdAssets,
  listColdReadings,
  logColdReading,
} from './coldchain.js';
import { createHandover, handoverSummary, listHandover } from './handover.js';
import { cashSummary, postCash } from './cash.js';
import {
  assetsSummary,
  createAsset,
  listAssets,
  updateAsset,
} from './assets.js';
import {
  energySummary,
  listEnergyReadings,
  listMeters,
  logEnergyReading,
} from './energy.js';
import {
  getQuiz,
  listAttempts,
  listQuizzes,
  submitAttempt,
  trainingSummary,
} from './training.js';
import {
  createValet,
  listValet,
  updateValet,
  valetSummary,
} from './valet.js';
import {
  createMusicRequest,
  listMusic,
  musicSummary,
  updateMusicRequest,
} from './music.js';
import {
  createDocument,
  documentsSummary,
  listDocuments,
  removeDocument,
} from './documents.js';
import {
  listVendorScores,
  upsertVendorScore,
  vendorScoreSummary,
} from './vendorscore.js';
import { listWaste, logWaste, wasteSummary } from './waste.js';
import {
  createSeat,
  listSeating,
  seatingSummary,
  updateSeat,
} from './seating.js';
import {
  createWaitlistEntry,
  listWaitlist,
  updateWaitlist,
  waitlistSummary,
} from './waitlist.js';
import {
  complaintsSummary,
  createComplaint,
  listComplaints,
  updateComplaint,
} from './complaints.js';
import { createKudos, kudosSummary, listKudos } from './kudos.js';
import { hoursSummary, listHours, updateHours } from './hours.js';
import {
  ackWeatherExtreme,
  ackWeatherFlag,
  buildWeatherBrief,
  opsRefreshWeather,
  refreshWeather,
  runWeatherSweep,
  setWeatherAdvisoryHold,
} from './weather.js';
import {
  ackReadinessDimension,
  buildReadiness,
  escalateReadinessGap,
  refreshReadinessSnapshot,
  resolveReadinessGap,
  setReadinessThreshold,
} from './readiness.js';

import {
  createSpa, listSpa, spaSummary, updateSpa,
} from './spa.js';
import {
  createEventcal, eventcalSummary, listEventcal, updateEventcal,
} from './eventcal.js';
import {
  createGiftcards, giftcardsSummary, listGiftcards, updateGiftcards,
} from './giftcards.js';
import {
  createDelivery, deliverySummary, listDelivery, updateDelivery,
} from './delivery.js';
import {
  cleaningSummary, createCleaning, listCleaning, updateCleaning,
} from './cleaning.js';
import {
  createLaundry, laundrySummary, listLaundry, updateLaundry,
} from './laundry.js';
import {
  createWifi, listWifi, updateWifi, wifiSummary,
} from './wifi.js';
import {
  contentSummary, createContent, listContent, updateContent,
} from './content.js';
import {
  createPulse, listPulse, pulseSummary, updatePulse,
} from './pulse.js';
import {
  budgetSummary, createBudget, listBudget, updateBudget,
} from './budget.js';
import {
  contractsSummary, createContracts, listContracts, updateContracts,
} from './contracts.js';
import {
  createPassstock, listPassstock, passstockSummary, updatePassstock,
} from './passstock.js';
import {
  createKds, kdsSummary, listKds, updateKds,
} from './kds.js';
import {
  createEmergency, emergencySummary, listEmergency, updateEmergency,
} from './emergency.js';
import {
  ackDigestFlag,
  buildDigest,
  escalateDigestGap,
  refreshDigestReadiness,
  resolveDigestGap,
  runDigestSweep,
} from './digest.js';
import {
  createLockers,
  listLockers,
  lockersSummary,
  updateLockers,
} from './lockers.js';
import {
  createKidsclub,
  listKidsclub,
  kidsclubSummary,
  updateKidsclub,
} from './kidsclub.js';
import {
  createBeachbeds,
  listBeachbeds,
  beachbedsSummary,
  updateBeachbeds,
} from './beachbeds.js';
import {
  createTransfers,
  listTransfers,
  transfersSummary,
  updateTransfers,
} from './transfers.js';
import {
  createBadgeprint,
  listBadgeprint,
  badgeprintSummary,
  updateBadgeprint,
} from './badgeprint.js';
import {
  createMeetingrooms,
  listMeetingrooms,
  meetingroomsSummary,
  updateMeetingrooms,
} from './meetingrooms.js';
import {
  createMediakit,
  listMediakit,
  mediakitSummary,
  updateMediakit,
} from './mediakit.js';
import {
  createSustain,
  listSustain,
  sustainSummary,
  updateSustain,
} from './sustain.js';
import {
  createAllergens,
  listAllergens,
  allergensSummary,
  updateAllergens,
} from './allergens.js';
import {
  createWinecellar,
  listWinecellar,
  winecellarSummary,
  updateWinecellar,
} from './winecellar.js';
import {
  createLounge,
  listLounge,
  loungeSummary,
  updateLounge,
} from './lounge.js';
import {
  createShuttle,
  listShuttle,
  shuttleSummary,
  updateShuttle,
} from './shuttle.js';
import {
  createPartners,
  listPartners,
  partnersSummary,
  updatePartners,
} from './partners.js';
import {
  createMysteryshop,
  listMysteryshop,
  mysteryshopSummary,
  updateMysteryshop,
} from './mysteryshop.js';
import {
  ackBoardpackFlag,
  buildBoardpack,
  rebalanceBoardpackBudget,
  renewBoardpackContract,
  runBoardpackSweep,
  snapshotBoardpack,
} from './boardpack.js';
import {
  createConcierge,
  listConcierge,
  conciergeSummary,
  updateConcierge,
} from './concierge.js';
import {
  createMinibar,
  listMinibar,
  minibarSummary,
  updateMinibar,
} from './minibar.js';
import {
  createFolio,
  listFolio,
  folioSummary,
  updateFolio,
} from './folio.js';
import {
  createBanquet,
  listBanquet,
  banquetSummary,
  updateBanquet,
} from './banquet.js';
import {
  createTours,
  listTours,
  toursSummary,
  updateTours,
} from './tours.js';
import {
  createMarina,
  listMarina,
  marinaSummary,
  updateMarina,
} from './marina.js';
import {
  createHammam,
  listHammam,
  hammamSummary,
  updateHammam,
} from './hammam.js';
import {
  createTowels,
  listTowels,
  towelsSummary,
  updateTowels,
} from './towels.js';
import {
  createBands,
  listBands,
  bandsSummary,
  updateBands,
} from './bands.js';
import {
  createHaccp,
  listHaccp,
  haccpSummary,
  updateHaccp,
} from './haccp.js';
import {
  createPatrol,
  listPatrol,
  patrolSummary,
  updatePatrol,
} from './patrol.js';
import {
  createFleet,
  listFleet,
  fleetSummary,
  updateFleet,
} from './fleet.js';
import {
  createPayroll,
  listPayroll,
  payrollSummary,
  updatePayroll,
} from './payroll.js';
import {
  createFlash,
  listFlash,
  flashSummary,
  updateFlash,
} from './flash.js';
import {
  ackWarroomFlag,
  buildWarroom,
  clearWarroomHaccp,
  clearWarroomPatrol,
  closeWarroomConcierge,
  runWarroomSweep,
} from './warroom.js';
import {
  createUpsell,
  listUpsell,
  upsellSummary,
  updateUpsell,
} from './upsell.js';
import {
  createOtareviews,
  listOtareviews,
  otareviewsSummary,
  updateOtareviews,
} from './otareviews.js';
import {
  createGroups,
  listGroups,
  groupsSummary,
  updateGroups,
} from './groups.js';
import {
  createVipnotes,
  listVipnotes,
  vipnotesSummary,
  updateVipnotes,
} from './vipnotes.js';
import {
  createPhotoshoot,
  listPhotoshoot,
  photoshootSummary,
  updatePhotoshoot,
} from './photoshoot.js';
import {
  createDive,
  listDive,
  diveSummary,
  updateDive,
} from './dive.js';
import {
  createBikerent,
  listBikerent,
  bikerentSummary,
  updateBikerent,
} from './bikerent.js';
import {
  createCinema,
  listCinema,
  cinemaSummary,
  updateCinema,
} from './cinema.js';
import {
  createRetail,
  listRetail,
  retailSummary,
  updateRetail,
} from './retail.js';
import {
  createBakery,
  listBakery,
  bakerySummary,
  updateBakery,
} from './bakery.js';
import {
  createBreakfast,
  listBreakfast,
  breakfastSummary,
  updateBreakfast,
} from './breakfast.js';
import {
  createLateout,
  listLateout,
  lateoutSummary,
  updateLateout,
} from './lateout.js';
import {
  createAmenities,
  listAmenities,
  amenitiesSummary,
  updateAmenities,
} from './amenities.js';
import {
  createNightlog,
  listNightlog,
  nightlogSummary,
  updateNightlog,
} from './nightlog.js';
import {
  ackNightlyFlag,
  approveNightlyLate,
  buildNightly,
  closeNightlyFolio,
  closeNightlyLog,
  runNightlySweep,
} from './nightly.js';
import {
  createKeycards,
  listKeycards,
  keycardsSummary,
  updateKeycards,
} from './keycards.js';
import {
  createRoomstatus,
  listRoomstatus,
  roomstatusSummary,
  updateRoomstatus,
} from './roomstatus.js';
import {
  createBedding,
  listBedding,
  beddingSummary,
  updateBedding,
} from './bedding.js';
import {
  createWakeups,
  listWakeups,
  wakeupsSummary,
  updateWakeups,
} from './wakeups.js';
import {
  createParcels,
  listParcels,
  parcelsSummary,
  updateParcels,
} from './parcels.js';
import {
  createQrcheckin,
  listQrcheckin,
  qrcheckinSummary,
  updateQrcheckin,
} from './qrcheckin.js';
import {
  createGuestapp,
  listGuestapp,
  guestappSummary,
  updateGuestapp,
} from './guestapp.js';
import {
  createKaraoke,
  listKaraoke,
  karaokeSummary,
  updateKaraoke,
} from './karaoke.js';
import {
  createArtwall,
  listArtwall,
  artwallSummary,
  updateArtwall,
} from './artwall.js';
import {
  createFlorals,
  listFlorals,
  floralsSummary,
  updateFlorals,
} from './florals.js';
import {
  createPrivatechef,
  listPrivatechef,
  privatechefSummary,
  updatePrivatechef,
} from './privatechef.js';
import {
  createMocktails,
  listMocktails,
  mocktailsSummary,
  updateMocktails,
} from './mocktails.js';
import {
  createPromos,
  listPromos,
  promosSummary,
  updatePromos,
} from './promos.js';
import {
  createDawnservice,
  listDawnservice,
  dawnserviceSummary,
  updateDawnservice,
} from './dawnservice.js';
import {
  ackOrbitFlag,
  buildOrbit,
  cleanOrbitRooms,
  encodeOrbitKeys,
  retryOrbitGuestapp,
  runOrbitSweep,
} from './orbit.js';
import {
  createRosters,
  listRosters,
  rostersSummary,
  updateRosters,
} from './rosters.js';
import {
  createOvertime,
  listOvertime,
  overtimeSummary,
  updateOvertime,
} from './overtime.js';
import {
  createUniforms,
  listUniforms,
  uniformsSummary,
  updateUniforms,
} from './uniforms.js';
import {
  createHealthcards,
  listHealthcards,
  healthcardsSummary,
  updateHealthcards,
} from './healthcards.js';
import {
  createVisitors,
  listVisitors,
  visitorsSummary,
  updateVisitors,
} from './visitors.js';
import {
  createCctvlog,
  listCctvlog,
  cctvlogSummary,
  updateCctvlog,
} from './cctvlog.js';
import {
  createFiredrill,
  listFiredrill,
  firedrillSummary,
  updateFiredrill,
} from './firedrill.js';
import {
  createInsurance,
  listInsurance,
  insuranceSummary,
  updateInsurance,
} from './insurance.js';
import {
  createInvoices,
  listInvoices,
  invoicesSummary,
  updateInvoices,
} from './invoices.js';
import {
  createTaxpack,
  listTaxpack,
  taxpackSummary,
  updateTaxpack,
} from './taxpack.js';
import {
  createForecast,
  listForecast,
  forecastSummary,
  updateForecast,
} from './forecast.js';
import {
  createCapex,
  listCapex,
  capexSummary,
  updateCapex,
} from './capex.js';
import {
  createLicenses,
  listLicenses,
  licensesSummary,
  updateLicenses,
} from './licenses.js';
import {
  createSlabreaches,
  listSlabreaches,
  slabreachesSummary,
  updateSlabreaches,
} from './slabreaches.js';
import {
  ackApexFlag,
  approveApexOvertime,
  buildApex,
  clearApexInvoices,
  renewApexLicenses,
  runApexSweep,
} from './apex.js';
import {
  createExtlinks,
  listExtlinks,
  extlinksSummary,
  updateExtlinks,
} from './extlinks.js';
import {
  createApikeys,
  listApikeys,
  apikeysSummary,
  updateApikeys,
} from './apikeys.js';
import {
  createBackupsched,
  listBackupsched,
  backupschedSummary,
  updateBackupsched,
} from './backupsched.js';
import {
  createSysalerts,
  listSysalerts,
  sysalertsSummary,
  updateSysalerts,
} from './sysalerts.js';
import {
  createBugtracker,
  listBugtracker,
  bugtrackerSummary,
  updateBugtracker,
} from './bugtracker.js';
import {
  createReleasenotes,
  listReleasenotes,
  releasenotesSummary,
  updateReleasenotes,
} from './releasenotes.js';
import {
  createRunbooks,
  listRunbooks,
  runbooksSummary,
  updateRunbooks,
} from './runbooks.js';
import {
  createBiometrics,
  listBiometrics,
  biometricsSummary,
  updateBiometrics,
} from './biometrics.js';
import {
  createSecretsrot,
  listSecretsrot,
  secretsrotSummary,
  updateSecretsrot,
} from './secretsrot.js';
import {
  createDnscheck,
  listDnscheck,
  dnscheckSummary,
  updateDnscheck,
} from './dnscheck.js';
import {
  createMailqueue,
  listMailqueue,
  mailqueueSummary,
  updateMailqueue,
} from './mailqueue.js';
import {
  createSmsqueue,
  listSmsqueue,
  smsqueueSummary,
  updateSmsqueue,
} from './smsqueue.js';
import {
  ackAlertrulesFlag,
  alertrulesSummary,
  createAlertrules,
  disableAlertrules,
  enableAlertrules,
  fireAlertrules,
  listAlertrules,
  runAlertrulesSweep,
  updateAlertrules,
} from './alertrules.js';
import {
  createEdgecache,
  listEdgecache,
  edgecacheSummary,
  updateEdgecache,
} from './edgecache.js';
import {
  ackPyramidFlag, buildPyramid, flushPyramidComms, healPyramidNet, resolvePyramidSys, runPyramidSweep,
} from './pyramid.js';
import {
  createSignage,
  listSignage,
  signageSummary,
  updateSignage,
} from './signage.js';
import {
  createWayfind,
  listWayfind,
  wayfindSummary,
  updateWayfind,
} from './wayfind.js';
import {
  createBeaconmap,
  listBeaconmap,
  beaconmapSummary,
  updateBeaconmap,
} from './beaconmap.js';
import {
  createIotgates,
  listIotgates,
  iotgatesSummary,
  updateIotgates,
} from './iotgates.js';
import {
  createPowerops,
  listPowerops,
  poweropsSummary,
  updatePowerops,
} from './powerops.js';
import {
  createWaterops,
  listWaterops,
  wateropsSummary,
  updateWaterops,
} from './waterops.js';
import {
  createGreenops,
  listGreenops,
  greenopsSummary,
  updateGreenops,
} from './greenops.js';
import {
  createPestctrl,
  listPestctrl,
  pestctrlSummary,
  updatePestctrl,
} from './pestctrl.js';
import {
  createChemlog,
  listChemlog,
  chemlogSummary,
  updateChemlog,
} from './chemlog.js';
import {
  createPoolops,
  listPoolops,
  poolopsSummary,
  updatePoolops,
} from './poolops.js';
import {
  createSaunaops,
  listSaunaops,
  saunaopsSummary,
  updateSaunaops,
} from './saunaops.js';
import {
  createSteamops,
  listSteamops,
  steamopsSummary,
  updateSteamops,
} from './steamops.js';
import {
  createIcebath,
  listIcebath,
  icebathSummary,
  updateIcebath,
} from './icebath.js';
import {
  createRecovslots,
  listRecovslots,
  recovslotsSummary,
  updateRecovslots,
} from './recovslots.js';
import {
  ackSignalhubFlag, buildSignalhub, clearSignalhubChem, clearSignalhubGate, clearSignalhubWater, runSignalhubSweep,
} from './signalhub.js';
import {
  createHelipad,
  listHelipad,
  helipadSummary,
  updateHelipad,
} from './helipad.js';
import {
  createJetski,
  listJetski,
  jetskiSummary,
  updateJetski,
} from './jetski.js';
import {
  createYacht,
  listYacht,
  yachtSummary,
  updateYacht,
} from './yacht.js';
import {
  createSurfschool,
  listSurfschool,
  surfschoolSummary,
  updateSurfschool,
} from './surfschool.js';
import {
  createPaddle,
  listPaddle,
  paddleSummary,
  updatePaddle,
} from './paddle.js';
import {
  createClimwall,
  listClimwall,
  climwallSummary,
  updateClimwall,
} from './climwall.js';
import {
  createEscaperoom,
  listEscaperoom,
  escaperoomSummary,
  updateEscaperoom,
} from './escaperoom.js';
import {
  createArcade,
  listArcade,
  arcadeSummary,
  updateArcade,
} from './arcade.js';
import {
  createBowling,
  listBowling,
  bowlingSummary,
  updateBowling,
} from './bowling.js';
import {
  createBilliards,
  listBilliards,
  billiardsSummary,
  updateBilliards,
} from './billiards.js';
import {
  createPokertable,
  listPokertable,
  pokertableSummary,
  updatePokertable,
} from './pokertable.js';
import {
  createTrivia,
  listTrivia,
  triviaSummary,
  updateTrivia,
} from './trivia.js';
import {
  createDjbooth,
  listDjbooth,
  djboothSummary,
  updateDjbooth,
} from './djbooth.js';
import {
  createSoundcheck,
  listSoundcheck,
  soundcheckSummary,
  updateSoundcheck,
} from './soundcheck.js';
import {
  ackSkylineFlag, buildSkyline, endSkylineEscape, passSkylineSound, runSkylineSweep, serviceSkylineJet,
} from './skyline.js';
import {
  createCrowddens,
  listCrowddens,
  crowddensSummary,
  updateCrowddens,
} from './crowddens.js';
import {
  createQueuetimes,
  listQueuetimes,
  queuetimesSummary,
  updateQueuetimes,
} from './queuetimes.js';
import {
  createLostchild,
  listLostchild,
  lostchildSummary,
  updateLostchild,
} from './lostchild.js';
import {
  createFirstaid,
  listFirstaid,
  firstaidSummary,
  updateFirstaid,
} from './firstaid.js';
import {
  createAedcheck,
  listAedcheck,
  aedcheckSummary,
  updateAedcheck,
} from './aedcheck.js';
import {
  createEvacdrill,
  listEvacdrill,
  evacdrillSummary,
  updateEvacdrill,
} from './evacdrill.js';
import {
  createCrowdctrl,
  listCrowdctrl,
  crowdctrlSummary,
  updateCrowdctrl,
} from './crowdctrl.js';
import {
  createRadiolog,
  listRadiolog,
  radiologSummary,
  updateRadiolog,
} from './radiolog.js';
import {
  createGatequeue,
  listGatequeue,
  gatequeueSummary,
  updateGatequeue,
} from './gatequeue.js';
import {
  createWristscan,
  listWristscan,
  wristscanSummary,
  updateWristscan,
} from './wristscan.js';
import {
  createFacepass,
  listFacepass,
  facepassSummary,
  updateFacepass,
} from './facepass.js';
import {
  createBagcheck,
  listBagcheck,
  bagcheckSummary,
  updateBagcheck,
} from './bagcheck.js';
import {
  createMetaldet,
  listMetaldet,
  metaldetSummary,
  updateMetaldet,
} from './metaldet.js';
import {
  createWatchlist,
  listWatchlist,
  watchlistSummary,
  updateWatchlist,
} from './watchlist.js';
import {
  ackSentinelFlag, buildSentinel, flowSentinelGate, resolveSentinelLost, runSentinelSweep, serviceSentinelAed,
} from './sentinel.js';
import {
  createMenuboard,
  listMenuboard,
  menuboardSummary,
  updateMenuboard,
} from './menuboard.js';
import {
  createAllergenalert,
  listAllergenalert,
  allergenalertSummary,
  updateAllergenalert,
} from './allergenalert.js';
import {
  createTempprobe,
  listTempprobe,
  tempprobeSummary,
  updateTempprobe,
} from './tempprobe.js';
import {
  createPrepqueue,
  listPrepqueue,
  prepqueueSummary,
  updatePrepqueue,
} from './prepqueue.js';
import {
  createVoidlog,
  listVoidlog,
  voidlogSummary,
  updateVoidlog,
} from './voidlog.js';
import {
  createComps,
  listComps,
  compsSummary,
  updateComps,
} from './comps.js';
import {
  createSplitbill,
  listSplitbill,
  splitbillSummary,
  updateSplitbill,
} from './splitbill.js';
import {
  createTabopen,
  listTabopen,
  tabopenSummary,
  updateTabopen,
} from './tabopen.js';
import {
  createCorkage,
  listCorkage,
  corkageSummary,
  updateCorkage,
} from './corkage.js';
import {
  createSommelier,
  listSommelier,
  sommelierSummary,
  updateSommelier,
} from './sommelier.js';
import {
  createChefnote,
  listChefnote,
  chefnoteSummary,
  updateChefnote,
} from './chefnote.js';
import {
  createPassticket,
  listPassticket,
  passticketSummary,
  updatePassticket,
} from './passticket.js';
import {
  createZoneheat,
  listZoneheat,
  zoneheatSummary,
  updateZoneheat,
} from './zoneheat.js';
import {
  createRevpulse,
  listRevpulse,
  revpulseSummary,
  updateRevpulse,
} from './revpulse.js';
import {
  ackHorizonFlag,
  approveHorizonComps,
  buildHorizon,
  clearHorizonAllergy,
  closeHorizonTabs,
  runHorizonSweep,
} from './horizon.js';
import {
  createStayext,
  listStayext,
  stayextSummary,
  updateStayext,
} from './stayext.js';
import {
  createRoommove,
  listRoommove,
  roommoveSummary,
  updateRoommove,
} from './roommove.js';
import {
  createEarlyin,
  listEarlyin,
  earlyinSummary,
  updateEarlyin,
} from './earlyin.js';
import {
  createLuggage,
  listLuggage,
  luggageSummary,
  updateLuggage,
} from './luggage.js';
import {
  createTurndown,
  listTurndown,
  turndownSummary,
  updateTurndown,
} from './turndown.js';
import {
  createPillowmenu,
  listPillowmenu,
  pillowmenuSummary,
  updatePillowmenu,
} from './pillowmenu.js';
import {
  createScenting,
  listScenting,
  scentingSummary,
  updateScenting,
} from './scenting.js';
import {
  createDndflags,
  listDndflags,
  dndflagsSummary,
  updateDndflags,
} from './dndflags.js';
import {
  createBathstock,
  listBathstock,
  bathstockSummary,
  updateBathstock,
} from './bathstock.js';
import {
  createIronreq,
  listIronreq,
  ironreqSummary,
  updateIronreq,
} from './ironreq.js';
import {
  createPressing,
  listPressing,
  pressingSummary,
  updatePressing,
} from './pressing.js';
import {
  createShoeshine,
  listShoeshine,
  shoeshineSummary,
  updateShoeshine,
} from './shoeshine.js';
import {
  createBabycot,
  listBabycot,
  babycotSummary,
  updateBabycot,
} from './babycot.js';
import {
  createPetstay,
  listPetstay,
  petstaySummary,
  updatePetstay,
} from './petstay.js';
import {
  ackMeridianFlag,
  approveMeridianEarly,
  approveMeridianMove,
  buildMeridian,
  clearMeridianTurndown,
  runMeridianSweep,
} from './meridian.js';
import {
  createArbill,
  listArbill,
  arbillSummary,
  updateArbill,
} from './arbill.js';
import {
  createApbill,
  listApbill,
  apbillSummary,
  updateApbill,
} from './apbill.js';
import {
  createBankrec,
  listBankrec,
  bankrecSummary,
  updateBankrec,
} from './bankrec.js';
import {
  createFxrates,
  listFxrates,
  fxratesSummary,
  updateFxrates,
} from './fxrates.js';
import {
  createTipout,
  listTipout,
  tipoutSummary,
  updateTipout,
} from './tipout.js';
import {
  createDeposit,
  listDeposit,
  depositSummary,
  updateDeposit,
} from './deposit.js';
import {
  createRefunds,
  listRefunds,
  refundsSummary,
  updateRefunds,
} from './refunds.js';
import {
  createChargeback,
  listChargeback,
  chargebackSummary,
  updateChargeback,
} from './chargeback.js';
import {
  createGiftred,
  listGiftred,
  giftredSummary,
  updateGiftred,
} from './giftred.js';
import {
  createMemberbill,
  listMemberbill,
  memberbillSummary,
  updateMemberbill,
} from './memberbill.js';
import {
  createRateplan,
  listRateplan,
  rateplanSummary,
  updateRateplan,
} from './rateplan.js';
import {
  createChannelmgr,
  listChannelmgr,
  channelmgrSummary,
  updateChannelmgr,
} from './channelmgr.js';
import {
  createOverbook,
  listOverbook,
  overbookSummary,
  updateOverbook,
} from './overbook.js';
import {
  createYieldrule,
  listYieldrule,
  yieldruleSummary,
  updateYieldrule,
} from './yieldrule.js';
import {
  ackLedgerFlag,
  buildLedger,
  clearLedgerChannel,
  collectLedgerAr,
  resolveLedgerChargeback,
  runLedgerSweep,
} from './ledger.js';
import {
  createOnboarding,
  listOnboarding,
  onboardingSummary,
  updateOnboarding,
} from './onboarding.js';
import {
  createOffboarding,
  listOffboarding,
  offboardingSummary,
  updateOffboarding,
} from './offboarding.js';
import {
  createInterviews,
  listInterviews,
  interviewsSummary,
  updateInterviews,
} from './interviews.js';
import {
  createCertifications,
  listCertifications,
  certificationsSummary,
  updateCertifications,
} from './certifications.js';
import {
  createLangskill,
  listLangskill,
  langskillSummary,
  updateLangskill,
} from './langskill.js';
import {
  createShiftswap,
  listShiftswap,
  shiftswapSummary,
  updateShiftswap,
} from './shiftswap.js';
import {
  createLeaverequest,
  listLeaverequest,
  leaverequestSummary,
  updateLeaverequest,
} from './leaverequest.js';
import {
  createAttendance,
  listAttendance,
  attendanceSummary,
  updateAttendance,
} from './attendance.js';
import {
  createPerformance,
  listPerformance,
  performanceSummary,
  updatePerformance,
} from './performance.js';
import {
  createRecognition,
  listRecognition,
  recognitionSummary,
  updateRecognition,
} from './recognition.js';
import {
  createHandbook,
  listHandbook,
  handbookSummary,
  updateHandbook,
} from './handbook.js';
import {
  createSafetybrief,
  listSafetybrief,
  safetybriefSummary,
  updateSafetybrief,
} from './safetybrief.js';
import {
  createNearmiss,
  listNearmiss,
  nearmissSummary,
  updateNearmiss,
} from './nearmiss.js';
import {
  createWhistle,
  listWhistle,
  whistleSummary,
  updateWhistle,
} from './whistle.js';
import {
  ackPeoplehubFlag,
  approvePeoplehubLeave,
  buildPeoplehub,
  closePeoplehubNearmiss,
  completePeoplehubOnboarding,
  runPeoplehubSweep,
} from './peoplehub.js';
import {
  createCarbonlog,
  listCarbonlog,
  carbonlogSummary,
  updateCarbonlog,
} from './carbonlog.js';
import {
  createWateraudit,
  listWateraudit,
  waterauditSummary,
  updateWateraudit,
} from './wateraudit.js';
import {
  createAirquality,
  listAirquality,
  airqualitySummary,
  updateAirquality,
} from './airquality.js';
import {
  createSolarops,
  listSolarops,
  solaropsSummary,
  updateSolarops,
} from './solarops.js';
import {
  createBiodiversity,
  listBiodiversity,
  biodiversitySummary,
  updateBiodiversity,
} from './biodiversity.js';
import {
  createRecycling,
  listRecycling,
  recyclingSummary,
  updateRecycling,
} from './recycling.js';
import {
  createGreencert,
  listGreencert,
  greencertSummary,
  updateGreencert,
} from './greencert.js';
import {
  createAuditfind,
  listAuditfind,
  auditfindSummary,
  updateAuditfind,
} from './auditfind.js';
import {
  createPolicyack,
  listPolicyack,
  policyackSummary,
  updatePolicyack,
} from './policyack.js';
import {
  createDataprotect,
  listDataprotect,
  dataprotectSummary,
  updateDataprotect,
} from './dataprotect.js';
import {
  createRetention,
  listRetention,
  retentionSummary,
  updateRetention,
} from './retention.js';
import {
  createAccessreview,
  listAccessreview,
  accessreviewSummary,
  updateAccessreview,
} from './accessreview.js';
import {
  createVendorrisk,
  listVendorrisk,
  vendorriskSummary,
  updateVendorrisk,
} from './vendorrisk.js';
import {
  createLegalhold,
  listLegalhold,
  legalholdSummary,
  updateLegalhold,
} from './legalhold.js';
import {
  ackEcosphereFlag,
  buildEcosphere,
  clearEcosphereVendorHigh,
  fulfillEcosphereDataprotect,
  mitigateEcosphereFinding,
  runEcosphereSweep,
} from './ecosphere.js';
import {
  createPresskit,
  listPresskit,
  presskitSummary,
  updatePresskit,
} from './presskit.js';
import {
  createInfluencer,
  listInfluencer,
  influencerSummary,
  updateInfluencer,
} from './influencer.js';
import {
  createUgcmod,
  listUgcmod,
  ugcmodSummary,
  updateUgcmod,
} from './ugcmod.js';
import {
  createSeoaudit,
  listSeoaudit,
  seoauditSummary,
  updateSeoaudit,
} from './seoaudit.js';
import {
  createAdspend,
  listAdspend,
  adspendSummary,
  updateAdspend,
} from './adspend.js';
import {
  createBrandguard,
  listBrandguard,
  brandguardSummary,
  updateBrandguard,
} from './brandguard.js';
import {
  createStoryboard,
  listStoryboard,
  storyboardSummary,
  updateStoryboard,
} from './storyboard.js';
import {
  createLivestream,
  listLivestream,
  livestreamSummary,
  updateLivestream,
} from './livestream.js';
import {
  createPodcastshow,
  listPodcastshow,
  podcastshowSummary,
  updatePodcastshow,
} from './podcastshow.js';
import {
  createNewsletter,
  listNewsletter,
  newsletterSummary,
  updateNewsletter,
} from './newsletter.js';
import {
  createTagmap,
  listTagmap,
  tagmapSummary,
  updateTagmap,
} from './tagmap.js';
import {
  createSocialinbox,
  listSocialinbox,
  socialinboxSummary,
  updateSocialinbox,
} from './socialinbox.js';
import {
  createMediaembargo,
  listMediaembargo,
  mediaembargoSummary,
  updateMediaembargo,
} from './mediaembargo.js';
import {
  createCreativereq,
  listCreativereq,
  creativereqSummary,
  updateCreativereq,
} from './creativereq.js';
import {
  ackBrandpulseFlag,
  actionBrandpulseGuard,
  approveBrandpulseUgc,
  buildBrandpulse,
  runBrandpulseSweep,
  triageBrandpulseInbox,
} from './brandpulse.js';
import {
  createModelops,
  listModelops,
  modelopsSummary,
  updateModelops,
} from './modelops.js';
import {
  createPromptlib,
  listPromptlib,
  promptlibSummary,
  updatePromptlib,
} from './promptlib.js';
import {
  createAgenteval,
  listAgenteval,
  agentevalSummary,
  updateAgenteval,
} from './agenteval.js';
import {
  createTokenbudget,
  listTokenbudget,
  tokenbudgetSummary,
  updateTokenbudget,
} from './tokenbudget.js';
import {
  createRagindex,
  listRagindex,
  ragindexSummary,
  updateRagindex,
} from './ragindex.js';
import {
  createToolpermit,
  listToolpermit,
  toolpermitSummary,
  updateToolpermit,
} from './toolpermit.js';
import {
  createSandboxrun,
  listSandboxrun,
  sandboxrunSummary,
  updateSandboxrun,
} from './sandboxrun.js';
import {
  createHallucheck,
  listHallucheck,
  hallucheckSummary,
  updateHallucheck,
} from './hallucheck.js';
import {
  createDatasetcur,
  listDatasetcur,
  datasetcurSummary,
  updateDatasetcur,
} from './datasetcur.js';
import {
  createRedteam,
  listRedteam,
  redteamSummary,
  updateRedteam,
} from './redteam.js';
import {
  createSlaagent,
  listSlaagent,
  slaagentSummary,
  updateSlaagent,
} from './slaagent.js';
import {
  createCostguard,
  listCostguard,
  costguardSummary,
  updateCostguard,
} from './costguard.js';
import {
  createLatencylog,
  listLatencylog,
  latencylogSummary,
  updateLatencylog,
} from './latencylog.js';
import {
  createDriftmonitor,
  listDriftmonitor,
  driftmonitorSummary,
  updateDriftmonitor,
} from './driftmonitor.js';
import {
  ackCognisphereFlag,
  buildCognisphere,
  clearCognisphereDrift,
  haltCognisphereCost,
  mitigateCognisphereRisk,
  runCognisphereSweep,
} from './cognisphere.js';
import {
  createPosbridge,
  listPosbridge,
  posbridgeSummary,
  updatePosbridge,
} from './posbridge.js';
import {
  createDynamint,
  listDynamint,
  dynamintSummary,
  updateDynamint,
} from './dynamint.js';
import {
  createCouriertrack,
  listCouriertrack,
  couriertrackSummary,
  updateCouriertrack,
} from './couriertrack.js';
import {
  createAutocheckout,
  listAutocheckout,
  autocheckoutSummary,
  updateAutocheckout,
} from './autocheckout.js';
import {
  createLoyaltyburn,
  listLoyaltyburn,
  loyaltyburnSummary,
  updateLoyaltyburn,
} from './loyaltyburn.js';
import {
  createTreatoffer,
  listTreatoffer,
  treatofferSummary,
  updateTreatoffer,
} from './treatoffer.js';
import {
  createOmnimarket,
  listOmnimarket,
  omnimarketSummary,
  updateOmnimarket,
} from './omnimarket.js';
import {
  createClickcollect,
  listClickcollect,
  clickcollectSummary,
  updateClickcollect,
} from './clickcollect.js';
import {
  createLastmile,
  listLastmile,
  lastmileSummary,
  updateLastmile,
} from './lastmile.js';
import {
  createInvsync,
  listInvsync,
  invsyncSummary,
  updateInvsync,
} from './invsync.js';
import {
  createPricepush,
  listPricepush,
  pricepushSummary,
  updatePricepush,
} from './pricepush.js';
import {
  createQrpay,
  listQrpay,
  qrpaySummary,
  updateQrpay,
} from './qrpay.js';
import {
  createCourierpool,
  listCourierpool,
  courierpoolSummary,
  updateCourierpool,
} from './courierpool.js';
import {
  createGiftrelay,
  listGiftrelay,
  giftrelaySummary,
  updateGiftrelay,
} from './giftrelay.js';
import {
  ackVanguardFlag,
  buildVanguard,
  clearVanguardInvDrift,
  flushVanguardMintQueue,
  forceVanguardPosOnline,
  runVanguardSweep,
} from './vanguard.js';
import {
  createEdgegate,
  listEdgegate,
  edgegateSummary,
  updateEdgegate,
} from './edgegate.js';
import {
  createMeshlink,
  listMeshlink,
  meshlinkSummary,
  updateMeshlink,
} from './meshlink.js';
import {
  createRadiomesh,
  listRadiomesh,
  radiomeshSummary,
  updateRadiomesh,
} from './radiomesh.js';
import {
  createSensorfuse,
  listSensorfuse,
  sensorfuseSummary,
  updateSensorfuse,
} from './sensorfuse.js';
import {
  createOtafirm,
  listOtafirm,
  otafirmSummary,
  updateOtafirm,
} from './otafirm.js';
import {
  createDevinventory,
  listDevinventory,
  devinventorySummary,
  updateDevinventory,
} from './devinventory.js';
import {
  createPowerbudget,
  listPowerbudget,
  powerbudgetSummary,
  updatePowerbudget,
} from './powerbudget.js';
import {
  createBackhaul,
  listBackhaul,
  backhaulSummary,
  updateBackhaul,
} from './backhaul.js';
import {
  createEdgecache2,
  listEdgecache2,
  edgecache2Summary,
  updateEdgecache2,
} from './edgecache2.js';
import {
  createSyncrepl,
  listSyncrepl,
  syncreplSummary,
  updateSyncrepl,
} from './syncrepl.js';
import {
  createFailover,
  listFailover,
  failoverSummary,
  updateFailover,
} from './failover.js';
import {
  createTelemetry,
  listTelemetry,
  telemetrySummary,
  updateTelemetry,
} from './telemetry.js';
import {
  createNetslice,
  listNetslice,
  netsliceSummary,
  updateNetslice,
} from './netslice.js';
import {
  createSatlink,
  listSatlink,
  satlinkSummary,
  updateSatlink,
} from './satlink.js';
import {
  ackLatticeFlag, buildLattice, clearLatticeFailback, healLatticeGate, retryLatticeOta, runLatticeSweep,
} from './lattice.js';
import {
  createGuesttwin,
  listGuesttwin,
  guesttwinSummary,
  updateGuesttwin,
} from './guesttwin.js';
import {
  createPrefgraph,
  listPrefgraph,
  prefgraphSummary,
  updatePrefgraph,
} from './prefgraph.js';
import {
  createIntentscore,
  listIntentscore,
  intentscoreSummary,
  updateIntentscore,
} from './intentscore.js';
import {
  createNextbest,
  listNextbest,
  nextbestSummary,
  updateNextbest,
} from './nextbest.js';
import {
  createJourneymap,
  listJourneymap,
  journeymapSummary,
  updateJourneymap,
} from './journeymap.js';
import {
  createMicroseg,
  listMicroseg,
  microsegSummary,
  updateMicroseg,
} from './microseg.js';
import {
  createOfferlab,
  listOfferlab,
  offerlabSummary,
  updateOfferlab,
} from './offerlab.js';
import {
  createConsentgraph,
  listConsentgraph,
  consentgraphSummary,
  updateConsentgraph,
} from './consentgraph.js';
import {
  createEmotionpulse,
  listEmotionpulse,
  emotionpulseSummary,
  updateEmotionpulse,
} from './emotionpulse.js';
import {
  createServicememory,
  listServicememory,
  servicememorySummary,
  updateServicememory,
} from './servicememory.js';
import {
  createRecoverypath,
  listRecoverypath,
  recoverypathSummary,
  updateRecoverypath,
} from './recoverypath.js';
import {
  createLifetimeval,
  listLifetimeval,
  lifetimevalSummary,
  updateLifetimeval,
} from './lifetimeval.js';
import {
  createChurnrisk,
  listChurnrisk,
  churnriskSummary,
  updateChurnrisk,
} from './churnrisk.js';
import {
  createWowmoment,
  listWowmoment,
  wowmomentSummary,
  updateWowmoment,
} from './wowmoment.js';
import {
  ackMirrorFlag, acceptMirrorNba, buildMirror, closeMirrorRecovery, refreshMirrorTwin, runMirrorSweep,
} from './mirror.js';
import {
  createIncidentbus,
  listIncidentbus,
  incidentbusSummary,
  updateIncidentbus,
} from './incidentbus.js';
import {
  createPlaytrigger,
  listPlaytrigger,
  playtriggerSummary,
  updatePlaytrigger,
} from './playtrigger.js';
import {
  createEscalation,
  listEscalation,
  escalationSummary,
  updateEscalation,
} from './escalation.js';
import {
  createWarroomseat,
  listWarroomseat,
  warroomseatSummary,
  updateWarroomseat,
} from './warroomseat.js';
import {
  createDecisionlog,
  listDecisionlog,
  decisionlogSummary,
  updateDecisionlog,
} from './decisionlog.js';
import {
  createSlotrack,
  listSlotrack,
  slotrackSummary,
  updateSlotrack,
} from './slotrack.js';
import {
  createErrorbudget,
  listErrorbudget,
  errorbudgetSummary,
  updateErrorbudget,
} from './errorbudget.js';
import {
  createChangewindow,
  listChangewindow,
  changewindowSummary,
  updateChangewindow,
} from './changewindow.js';
import {
  createBlameless,
  listBlameless,
  blamelessSummary,
  updateBlameless,
} from './blameless.js';
import {
  createPagerduty,
  listPagerduty,
  pagerdutySummary,
  updatePagerduty,
} from './pagerduty.js';
import {
  createStatuspage,
  listStatuspage,
  statuspageSummary,
  updateStatuspage,
} from './statuspage.js';
import {
  createRunbooklink,
  listRunbooklink,
  runbooklinkSummary,
  updateRunbooklink,
} from './runbooklink.js';
import {
  createCommsbridge,
  listCommsbridge,
  commsbridgeSummary,
  updateCommsbridge,
} from './commsbridge.js';
import {
  createAfteraction,
  listAfteraction,
  afteractionSummary,
  updateAfteraction,
} from './afteraction.js';
import {
  ackKeystoneFlag, buildKeystone, clearKeystoneBus, closeKeystoneEsc, healKeystoneSlo, runKeystoneSweep,
} from './keystone.js';
import {
  createRevstream,
  listRevstream,
  revstreamSummary,
  updateRevstream,
} from './revstream.js';
import {
  createPackagemix,
  listPackagemix,
  packagemixSummary,
  updatePackagemix,
} from './packagemix.js';
import {
  createAncillary,
  listAncillary,
  ancillarySummary,
  updateAncillary,
} from './ancillary.js';
import {
  createDynamicbundle,
  listDynamicbundle,
  dynamicbundleSummary,
  updateDynamicbundle,
} from './dynamicbundle.js';
import {
  createPricefloor,
  listPricefloor,
  pricefloorSummary,
  updatePricefloor,
} from './pricefloor.js';
import {
  createCompset,
  listCompset,
  compsetSummary,
  updateCompset,
} from './compset.js';
import {
  createPickuppace,
  listPickuppace,
  pickuppaceSummary,
  updatePickuppace,
} from './pickuppace.js';
import {
  createNoshowrisk,
  listNoshowrisk,
  noshowriskSummary,
  updateNoshowrisk,
} from './noshowrisk.js';
import {
  createWalkinflow,
  listWalkinflow,
  walkinflowSummary,
  updateWalkinflow,
} from './walkinflow.js';
import {
  createTableturn,
  listTableturn,
  tableturnSummary,
  updateTableturn,
} from './tableturn.js';
import {
  createBeatrevenue,
  listBeatrevenue,
  beatrevenueSummary,
  updateBeatrevenue,
} from './beatrevenue.js';
import {
  createCashforecast,
  listCashforecast,
  cashforecastSummary,
  updateCashforecast,
} from './cashforecast.js';
import {
  createMarginwatch,
  listMarginwatch,
  marginwatchSummary,
  updateMarginwatch,
} from './marginwatch.js';
import {
  createPromoattr,
  listPromoattr,
  promoattrSummary,
  updatePromoattr,
} from './promoattr.js';
import {
  ackZenithFlag, buildZenith, catchZenithPace, coolZenithDemand, healZenithMargin, runZenithSweep,
} from './zenith.js';
import {
  createMasterplan,
  listMasterplan,
  masterplanSummary,
  updateMasterplan,
} from './masterplan.js';
import {
  createOkrrack,
  listOkrrack,
  okrrackSummary,
  updateOkrrack,
} from './okrrack.js';
import {
  createRoadmap,
  listRoadmap,
  roadmapSummary,
  updateRoadmap,
} from './roadmap.js';
import {
  createBetboard,
  listBetboard,
  betboardSummary,
  updateBetboard,
} from './betboard.js';
import {
  createPortfoliorisk,
  listPortfoliorisk,
  portfolioriskSummary,
  updatePortfoliorisk,
} from './portfoliorisk.js';
import {
  createCaptable,
  listCaptable,
  captableSummary,
  updateCaptable,
} from './captable.js';
import {
  createBoardmotion,
  listBoardmotion,
  boardmotionSummary,
  updateBoardmotion,
} from './boardmotion.js';
import {
  createAlliance,
  listAlliance,
  allianceSummary,
  updateAlliance,
} from './alliance.js';
import {
  createExpansion,
  listExpansion,
  expansionSummary,
  updateExpansion,
} from './expansion.js';
import {
  createLegacyarc,
  listLegacyarc,
  legacyarcSummary,
  updateLegacyarc,
} from './legacyarc.js';
import {
  createCulturecode,
  listCulturecode,
  culturecodeSummary,
  updateCulturecode,
} from './culturecode.js';
import {
  createTalentbet,
  listTalentbet,
  talentbetSummary,
  updateTalentbet,
} from './talentbet.js';
import {
  createMoatwatch,
  listMoatwatch,
  moatwatchSummary,
  updateMoatwatch,
} from './moatwatch.js';
import {
  createNorthstar,
  listNorthstar,
  northstarSummary,
  updateNorthstar,
} from './northstar.js';
import {
  ackOdysseyFlag, buildOdyssey, coolOdysseyRisk, hitOdysseyStar, recoverOdysseyOkr, runOdysseySweep,
} from './odyssey.js';
import {
  createTidewatch,
  listTidewatch,
  tidewatchSummary,
  updateTidewatch,
} from './tidewatch.js';
import {
  createDuneops,
  listDuneops,
  duneopsSummary,
  updateDuneops,
} from './duneops.js';
import {
  createSnorkelbay,
  listSnorkelbay,
  snorkelbaySummary,
  updateSnorkelbay,
} from './snorkelbay.js';
import {
  createCliffpath,
  listCliffpath,
  cliffpathSummary,
  updateCliffpath,
} from './cliffpath.js';
import {
  createCampglow,
  listCampglow,
  campglowSummary,
  updateCampglow,
} from './campglow.js';
import {
  createLookout,
  listLookout,
  lookoutSummary,
  updateLookout,
} from './lookout.js';
import {
  createReefguard,
  listReefguard,
  reefguardSummary,
  updateReefguard,
} from './reefguard.js';
import {
  createPierops,
  listPierops,
  pieropsSummary,
  updatePierops,
} from './pierops.js';
import {
  createSaildesk,
  listSaildesk,
  saildeskSummary,
  updateSaildesk,
} from './saildesk.js';
import {
  createUmbrellamap,
  listUmbrellamap,
  umbrellamapSummary,
  updateUmbrellamap,
} from './umbrellamap.js';
import {
  createSandclean,
  listSandclean,
  sandcleanSummary,
  updateSandclean,
} from './sandclean.js';
import {
  createNightswim,
  listNightswim,
  nightswimSummary,
  updateNightswim,
} from './nightswim.js';
import {
  createStargaze,
  listStargaze,
  stargazeSummary,
  updateStargaze,
} from './stargaze.js';
import {
  createCoastpatrol,
  listCoastpatrol,
  coastpatrolSummary,
  updateCoastpatrol,
} from './coastpatrol.js';
import {
  ackTideFlag, buildTide, clearTideReef, freeTidePier, openTideCliff, runTideSweep,
} from './tide.js';
import {
  createHarborlane,
  listHarborlane,
  harborlaneSummary,
  updateHarborlane,
} from './harborlane.js';
import {
  createDockslot,
  listDockslot,
  dockslotSummary,
  updateDockslot,
} from './dockslot.js';
import {
  createCraneops,
  listCraneops,
  craneopsSummary,
  updateCraneops,
} from './craneops.js';
import {
  createContainer,
  listContainer,
  containerSummary,
  updateContainer,
} from './container.js';
import {
  createColdbay,
  listColdbay,
  coldbaySummary,
  updateColdbay,
} from './coldbay.js';
import {
  createYardmove,
  listYardmove,
  yardmoveSummary,
  updateYardmove,
} from './yardmove.js';
import {
  createGatepass,
  listGatepass,
  gatepassSummary,
  updateGatepass,
} from './gatepass.js';
import {
  createBolthold,
  listBolthold,
  boltholdSummary,
  updateBolthold,
} from './bolthold.js';
import {
  createManifest,
  listManifest,
  manifestSummary,
  updateManifest,
} from './manifest.js';
import {
  createDemurrage,
  listDemurrage,
  demurrageSummary,
  updateDemurrage,
} from './demurrage.js';
import {
  createPilotage,
  listPilotage,
  pilotageSummary,
  updatePilotage,
} from './pilotage.js';
import {
  createTugassist,
  listTugassist,
  tugassistSummary,
  updateTugassist,
} from './tugassist.js';
import {
  createWharfage,
  listWharfage,
  wharfageSummary,
  updateWharfage,
} from './wharfage.js';
import {
  createStevedore,
  listStevedore,
  stevedoreSummary,
  updateStevedore,
} from './stevedore.js';
import {
  ackHarborFlag, buildHarbor, clearHarborCold, invoiceHarborDemurrage, releaseHarborHold, runHarborSweep,
} from './harbor.js';
import {
  createAuroradeck,
  listAuroradeck,
  auroradeckSummary,
  updateAuroradeck,
} from './auroradeck.js';
import {
  createLightshow,
  listLightshow,
  lightshowSummary,
  updateLightshow,
} from './lightshow.js';
import {
  createSoundscape,
  listSoundscape,
  soundscapeSummary,
  updateSoundscape,
} from './soundscape.js';
import {
  createScentzone,
  listScentzone,
  scentzoneSummary,
  updateScentzone,
} from './scentzone.js';
import {
  createMoodlight,
  listMoodlight,
  moodlightSummary,
  updateMoodlight,
} from './moodlight.js';
import {
  createFogscene,
  listFogscene,
  fogsceneSummary,
  updateFogscene,
} from './fogscene.js';
import {
  createProjection,
  listProjection,
  projectionSummary,
  updateProjection,
} from './projection.js';
import {
  createImmersive,
  listImmersive,
  immersiveSummary,
  updateImmersive,
} from './immersive.js';
import {
  createHapticcue,
  listHapticcue,
  hapticcueSummary,
  updateHapticcue,
} from './hapticcue.js';
import {
  createAtmosmix,
  listAtmosmix,
  atmosmixSummary,
  updateAtmosmix,
} from './atmosmix.js';
import {
  createGuestflow,
  listGuestflow,
  guestflowSummary,
  updateGuestflow,
} from './guestflow.js';
import {
  createScenectrl,
  listScenectrl,
  scenectrlSummary,
  updateScenectrl,
} from './scenectrl.js';
import {
  createNightmode,
  listNightmode,
  nightmodeSummary,
  updateNightmode,
} from './nightmode.js';
import {
  createDawnmode,
  listDawnmode,
  dawnmodeSummary,
  updateDawnmode,
} from './dawnmode.js';
import {
  ackAuroraFlag,
  buildAurora,
  clearAuroraFlow,
  dayAuroraNightmode,
  endAuroraLight,
  runAuroraSweep,
} from './aurora.js';
import {
  createMiseplan,
  listMiseplan,
  miseplanSummary,
  updateMiseplan,
} from './miseplan.js';
import {
  createPassrail,
  listPassrail,
  passrailSummary,
  updatePassrail,
} from './passrail.js';
import {
  createPlateup,
  listPlateup,
  plateupSummary,
  updatePlateup,
} from './plateup.js';
import {
  createGardebay,
  listGardebay,
  gardebaySummary,
  updateGardebay,
} from './gardebay.js';
import {
  createPastrylab,
  listPastrylab,
  pastrylabSummary,
  updatePastrylab,
} from './pastrylab.js';
import {
  createTastingmenu,
  listTastingmenu,
  tastingmenuSummary,
  updateTastingmenu,
} from './tastingmenu.js';
import {
  createCellarbox,
  listCellarbox,
  cellarboxSummary,
  updateCellarbox,
} from './cellarbox.js';
import {
  createBarrail,
  listBarrail,
  barrailSummary,
  updateBarrail,
} from './barrail.js';
import {
  createRoomservice,
  listRoomservice,
  roomserviceSummary,
  updateRoomservice,
} from './roomservice.js';
import {
  createCaterdesk,
  listCaterdesk,
  caterdeskSummary,
  updateCaterdesk,
} from './caterdesk.js';
import {
  createAllergenmap,
  listAllergenmap,
  allergenmapSummary,
  updateAllergenmap,
} from './allergenmap.js';
import {
  createWastekitchen,
  listWastekitchen,
  wastekitchenSummary,
  updateWastekitchen,
} from './wastekitchen.js';
import {
  createChefbrief,
  listChefbrief,
  chefbriefSummary,
  updateChefbrief,
} from './chefbrief.js';
import {
  createSupplypull,
  listSupplypull,
  supplypullSummary,
  updateSupplypull,
} from './supplypull.js';
import {
  ackHearthFlag,
  buildHearth,
  clearHearthAllergen,
  runHearthPass,
  runHearthSweep,
  sendHearthPlate,
} from './hearth.js';
import {
  createSpaflow,
  listSpaflow,
  spaflowSummary,
  updateSpaflow,
} from './spaflow.js';
import {
  createThermalbay,
  listThermalbay,
  thermalbaySummary,
  updateThermalbay,
} from './thermalbay.js';
import {
  createSaunalog,
  listSaunalog,
  saunalogSummary,
  updateSaunalog,
} from './saunalog.js';
import {
  createCryochamber,
  listCryochamber,
  cryochamberSummary,
  updateCryochamber,
} from './cryochamber.js';
import {
  createFloatpod,
  listFloatpod,
  floatpodSummary,
  updateFloatpod,
} from './floatpod.js';
import {
  createMassagebook,
  listMassagebook,
  massagebookSummary,
  updateMassagebook,
} from './massagebook.js';
import {
  createYogamat,
  listYogamat,
  yogamatSummary,
  updateYogamat,
} from './yogamat.js';
import {
  createBreathwork,
  listBreathwork,
  breathworkSummary,
  updateBreathwork,
} from './breathwork.js';
import {
  createRecoverybay,
  listRecoverybay,
  recoverybaySummary,
  updateRecoverybay,
} from './recoverybay.js';
import {
  createIvlounge,
  listIvlounge,
  ivloungeSummary,
  updateIvlounge,
} from './ivlounge.js';
import {
  createSleepcoach,
  listSleepcoach,
  sleepcoachSummary,
  updateSleepcoach,
} from './sleepcoach.js';
import {
  createNutrition,
  listNutrition,
  nutritionSummary,
  updateNutrition,
} from './nutrition.js';
import {
  createBiomarker,
  listBiomarker,
  biomarkerSummary,
  updateBiomarker,
} from './biomarker.js';
import {
  createWellnesskit,
  listWellnesskit,
  wellnesskitSummary,
  updateWellnesskit,
} from './wellnesskit.js';
import {
  ackSanctumFlag,
  buildSanctum,
  clearSanctumBio,
  clearSanctumSpa,
  completeSanctumSession,
  runSanctumSweep,
} from './sanctum.js';
import {
  createAssetmap,
  listAssetmap,
  assetmapSummary,
  updateAssetmap,
} from './assetmap.js';
import {
  createPlantroom,
  listPlantroom,
  plantroomSummary,
  updatePlantroom,
} from './plantroom.js';
import {
  createHvacloop,
  listHvacloop,
  hvacloopSummary,
  updateHvacloop,
} from './hvacloop.js';
import {
  createWaterloop,
  listWaterloop,
  waterloopSummary,
  updateWaterloop,
} from './waterloop.js';
import {
  createPowergrid,
  listPowergrid,
  powergridSummary,
  updatePowergrid,
} from './powergrid.js';
import {
  createElevatorlog,
  listElevatorlog,
  elevatorlogSummary,
  updateElevatorlog,
} from './elevatorlog.js';
import {
  createSpareparts,
  listSpareparts,
  sparepartsSummary,
  updateSpareparts,
} from './spareparts.js';
import {
  createWorkorder,
  listWorkorder,
  workorderSummary,
  updateWorkorder,
} from './workorder.js';
import {
  createLeasehold,
  listLeasehold,
  leaseholdSummary,
  updateLeasehold,
} from './leasehold.js';
import {
  createTenantops,
  listTenantops,
  tenantopsSummary,
  updateTenantops,
} from './tenantops.js';
import {
  createFacilitytour,
  listFacilitytour,
  facilitytourSummary,
  updateFacilitytour,
} from './facilitytour.js';
import {
  createCapexdesk,
  listCapexdesk,
  capexdeskSummary,
  updateCapexdesk,
} from './capexdesk.js';
import {
  createOpexdesk,
  listOpexdesk,
  opexdeskSummary,
  updateOpexdesk,
} from './opexdesk.js';
import {
  createEstatescan,
  listEstatescan,
  estatescanSummary,
  updateEstatescan,
} from './estatescan.js';
import {
  ackCitadelFlag,
  buildCitadel,
  clearCitadelHvac,
  clearCitadelPlant,
  closeCitadelWorkorder,
  runCitadelSweep,
} from './citadel.js';
import {
  createTalentdesk,
  listTalentdesk,
  talentdeskSummary,
  updateTalentdesk,
} from './talentdesk.js';
import {
  createShiftbid,
  listShiftbid,
  shiftbidSummary,
  updateShiftbid,
} from './shiftbid.js';
import {
  createSkillmatrix,
  listSkillmatrix,
  skillmatrixSummary,
  updateSkillmatrix,
} from './skillmatrix.js';
import {
  createCerttrack,
  listCerttrack,
  certtrackSummary,
  updateCerttrack,
} from './certtrack.js';
import {
  createTraininghub,
  listTraininghub,
  traininghubSummary,
  updateTraininghub,
} from './traininghub.js';
import {
  createMentorship,
  listMentorship,
  mentorshipSummary,
  updateMentorship,
} from './mentorship.js';
import {
  createSuccession,
  listSuccession,
  successionSummary,
  updateSuccession,
} from './succession.js';
import {
  createPayrollrun,
  listPayrollrun,
  payrollrunSummary,
  updatePayrollrun,
} from './payrollrun.js';
import {
  createPerformnote,
  listPerformnote,
  performnoteSummary,
  updatePerformnote,
} from './performnote.js';
import {
  createReviewcycle,
  listReviewcycle,
  reviewcycleSummary,
  updateReviewcycle,
} from './reviewcycle.js';
import {
  createHeadcount,
  listHeadcount,
  headcountSummary,
  updateHeadcount,
} from './headcount.js';
import {
  createAttrition,
  listAttrition,
  attritionSummary,
  updateAttrition,
} from './attrition.js';
import {
  createCulturepulse,
  listCulturepulse,
  culturepulseSummary,
  updateCulturepulse,
} from './culturepulse.js';
import {
  createShifttrade,
  listShifttrade,
  shifttradeSummary,
  updateShifttrade,
} from './shifttrade.js';
import {
  ackForgeFlag,
  advanceForgeTalent,
  approveForgeShift,
  buildForge,
  renewForgeCert,
  runForgeSweep,
} from './forge.js';
import {
  createSafetylog,
  listSafetylog,
  safetylogSummary,
  updateSafetylog,
} from './safetylog.js';
import {
  createIncidentlog,
  listIncidentlog,
  incidentlogSummary,
  updateIncidentlog,
} from './incidentlog.js';
import {
  createHazardnote,
  listHazardnote,
  hazardnoteSummary,
  updateHazardnote,
} from './hazardnote.js';
import {
  createEvacroute,
  listEvacroute,
  evacrouteSummary,
  updateEvacroute,
} from './evacroute.js';
import {
  createDrillrun,
  listDrillrun,
  drillrunSummary,
  updateDrillrun,
} from './drillrun.js';
import {
  createAidkit,
  listAidkit,
  aidkitSummary,
  updateAidkit,
} from './aidkit.js';
import {
  createHazmatbay,
  listHazmatbay,
  hazmatbaySummary,
  updateHazmatbay,
} from './hazmatbay.js';
import {
  createPpekit,
  listPpekit,
  ppekitSummary,
  updatePpekit,
} from './ppekit.js';
import {
  createLockouttag,
  listLockouttag,
  lockouttagSummary,
  updateLockouttag,
} from './lockouttag.js';
import {
  createPermitwork,
  listPermitwork,
  permitworkSummary,
  updatePermitwork,
} from './permitwork.js';
import {
  createCompliancerow,
  listCompliancerow,
  compliancerowSummary,
  updateCompliancerow,
} from './compliancerow.js';
import {
  createAudittrail2,
  listAudittrail2,
  audittrail2Summary,
  updateAudittrail2,
} from './audittrail2.js';
import {
  createCctvreview,
  listCctvreview,
  cctvreviewSummary,
  updateCctvreview,
} from './cctvreview.js';
import {
  createGuestsafety,
  listGuestsafety,
  guestsafetySummary,
  updateGuestsafety,
} from './guestsafety.js';
import {
  ackAegisFlag,
  buildAegis,
  clearAegisEvac,
  clearAegisSafety,
  closeAegisIncident,
  runAegisSweep,
} from './aegis.js';
import {
  createDatalake,
  listDatalake,
  datalakeSummary,
  updateDatalake,
} from './datalake.js';
import {
  createFeatureflag,
  listFeatureflag,
  featureflagSummary,
  updateFeatureflag,
} from './featureflag.js';
import {
  createModelcard,
  listModelcard,
  modelcardSummary,
  updateModelcard,
} from './modelcard.js';
import {
  createPromptlab,
  listPromptlab,
  promptlabSummary,
  updatePromptlab,
} from './promptlab.js';
import {
  createEvalbench,
  listEvalbench,
  evalbenchSummary,
  updateEvalbench,
} from './evalbench.js';
import {
  createDatasetcat,
  listDatasetcat,
  datasetcatSummary,
  updateDatasetcat,
} from './datasetcat.js';
import {
  createLineage,
  listLineage,
  lineageSummary,
  updateLineage,
} from './lineage.js';
import {
  createVectorstore,
  listVectorstore,
  vectorstoreSummary,
  updateVectorstore,
} from './vectorstore.js';
import {
  createInsightboard,
  listInsightboard,
  insightboardSummary,
  updateInsightboard,
} from './insightboard.js';
import {
  createAnomaly,
  listAnomaly,
  anomalySummary,
  updateAnomaly,
} from './anomaly.js';
import {
  createDemandcast,
  listDemandcast,
  demandcastSummary,
  updateDemandcast,
} from './demandcast.js';
import {
  createAbtest,
  listAbtest,
  abtestSummary,
  updateAbtest,
} from './abtest.js';
import {
  createScorecard,
  listScorecard,
  scorecardSummary,
  updateScorecard,
} from './scorecard.js';
import {
  createDecidlog,
  listDecidlog,
  decidlogSummary,
  updateDecidlog,
} from './decidlog.js';
import {
  ackOracleFlag,
  buildOracle,
  clearOracleScoreRed,
  promoteOracleCanary,
  resolveOracleAnomaly,
  runOracleSweep,
} from './oracle.js';
import {
  createMemberdesk,
  listMemberdesk,
  memberdeskSummary,
  updateMemberdesk,
} from './memberdesk.js';
import {
  createTierladder,
  listTierladder,
  tierladderSummary,
  updateTierladder,
} from './tierladder.js';
import {
  createPointledger,
  listPointledger,
  pointledgerSummary,
  updatePointledger,
} from './pointledger.js';
import {
  createPerkshop,
  listPerkshop,
  perkshopSummary,
  updatePerkshop,
} from './perkshop.js';
import {
  createReferral,
  listReferral,
  referralSummary,
  updateReferral,
} from './referral.js';
import {
  createGiftcard,
  listGiftcard,
  giftcardSummary,
  updateGiftcard,
} from './giftcard.js';
import {
  createVipdesk,
  listVipdesk,
  vipdeskSummary,
  updateVipdesk,
} from './vipdesk.js';
import {
  createStayhistory,
  listStayhistory,
  stayhistorySummary,
  updateStayhistory,
} from './stayhistory.js';
import {
  createPrefernote,
  listPrefernote,
  prefernoteSummary,
  updatePrefernote,
} from './prefernote.js';
import {
  createNpspulse,
  listNpspulse,
  npspulseSummary,
  updateNpspulse,
} from './npspulse.js';
import {
  createGuestcase,
  listGuestcase,
  guestcaseSummary,
  updateGuestcase,
} from './guestcase.js';
import {
  createPraise,
  listPraise,
  praiseSummary,
  updatePraise,
} from './praise.js';
import {
  createWinback,
  listWinback,
  winbackSummary,
  updateWinback,
} from './winback.js';
import {
  createClubnight,
  listClubnight,
  clubnightSummary,
  updateClubnight,
} from './clubnight.js';
import {
  ackCrownFlag, buildCrown, closeCrownCase, houseCrownVip, liveCrownWinback, runCrownSweep,
} from './crown.js';
import {
  createCampdesk,
  listCampdesk,
  campdeskSummary,
  updateCampdesk,
} from './campdesk.js';
import {
  createContentcal,
  listContentcal,
  contentcalSummary,
  updateContentcal,
} from './contentcal.js';
import {
  createSocialqueue,
  listSocialqueue,
  socialqueueSummary,
  updateSocialqueue,
} from './socialqueue.js';
import {
  createCreatordesk,
  listCreatordesk,
  creatordeskSummary,
  updateCreatordesk,
} from './creatordesk.js';
import {
  createUtmtrack,
  listUtmtrack,
  utmtrackSummary,
  updateUtmtrack,
} from './utmtrack.js';
import {
  createLanding,
  listLanding,
  landingSummary,
  updateLanding,
} from './landing.js';
import {
  createAbcopy,
  listAbcopy,
  abcopySummary,
  updateAbcopy,
} from './abcopy.js';
import {
  createSeopage,
  listSeopage,
  seopageSummary,
  updateSeopage,
} from './seopage.js';
import {
  createPushdesk,
  listPushdesk,
  pushdeskSummary,
  updatePushdesk,
} from './pushdesk.js';
import {
  createEmailblast,
  listEmailblast,
  emailblastSummary,
  updateEmailblast,
} from './emailblast.js';
import {
  createPresspack,
  listPresspack,
  presspackSummary,
  updatePresspack,
} from './presspack.js';
import {
  createBrandkit,
  listBrandkit,
  brandkitSummary,
  updateBrandkit,
} from './brandkit.js';
import {
  createMediabuy,
  listMediabuy,
  mediabuySummary,
  updateMediabuy,
} from './mediabuy.js';
import {
  createLeadmagnet,
  listLeadmagnet,
  leadmagnetSummary,
  updateLeadmagnet,
} from './leadmagnet.js';
import {
  ackBeaconFlag, buildBeacon, fixBeaconSocial, healBeaconSeo, liveBeaconCamp, runBeaconSweep,
} from './beacon.js';
import {
  createTreasury,
  listTreasury,
  treasurySummary,
  updateTreasury,
} from './treasury.js';
import {
  createCashflow,
  listCashflow,
  cashflowSummary,
  updateCashflow,
} from './cashflow.js';
import {
  createApdesk,
  listApdesk,
  apdeskSummary,
  updateApdesk,
} from './apdesk.js';
import {
  createArdesk,
  listArdesk,
  ardeskSummary,
  updateArdesk,
} from './ardesk.js';
import {
  createInvoicedesk,
  listInvoicedesk,
  invoicedeskSummary,
  updateInvoicedesk,
} from './invoicedesk.js';
import {
  createTaxdesk,
  listTaxdesk,
  taxdeskSummary,
  updateTaxdesk,
} from './taxdesk.js';
import {
  createBudgetline,
  listBudgetline,
  budgetlineSummary,
  updateBudgetline,
} from './budgetline.js';
import {
  createFxdesk,
  listFxdesk,
  fxdeskSummary,
  updateFxdesk,
} from './fxdesk.js';
import {
  createBankrecon,
  listBankrecon,
  bankreconSummary,
  updateBankrecon,
} from './bankrecon.js';
import {
  createPayout,
  listPayout,
  payoutSummary,
  updatePayout,
} from './payout.js';
import {
  createPettycash,
  listPettycash,
  pettycashSummary,
  updatePettycash,
} from './pettycash.js';
import {
  createCostcenter,
  listCostcenter,
  costcenterSummary,
  updateCostcenter,
} from './costcenter.js';
import {
  createGlmap,
  listGlmap,
  glmapSummary,
  updateGlmap,
} from './glmap.js';
import {
  createClosebook,
  listClosebook,
  closebookSummary,
  updateClosebook,
} from './closebook.js';
import {
  ackVaultFlag, buildVault, clearVaultRecon, closeVaultAp, healVaultTreasury, runVaultSweep,
} from './vault.js';
import {
  createShuttlelane,
  listShuttlelane,
  shuttlelaneSummary,
  updateShuttlelane,
} from './shuttlelane.js';
import {
  createFleetdesk,
  listFleetdesk,
  fleetdeskSummary,
  updateFleetdesk,
} from './fleetdesk.js';
import {
  createDriverost,
  listDriverost,
  driverostSummary,
  updateDriverost,
} from './driverost.js';
import {
  createFuelcard,
  listFuelcard,
  fuelcardSummary,
  updateFuelcard,
} from './fuelcard.js';
import {
  createRouteplan,
  listRouteplan,
  routeplanSummary,
  updateRouteplan,
} from './routeplan.js';
import {
  createDispatchboard,
  listDispatchboard,
  dispatchboardSummary,
  updateDispatchboard,
} from './dispatchboard.js';
import {
  createGpsping,
  listGpsping,
  gpspingSummary,
  updateGpsping,
} from './gpsping.js';
import {
  createVehiclemaint,
  listVehiclemaint,
  vehiclemaintSummary,
  updateVehiclemaint,
} from './vehiclemaint.js';
import {
  createValetops,
  listValetops,
  valetopsSummary,
  updateValetops,
} from './valetops.js';
import {
  createParkingbay,
  listParkingbay,
  parkingbaySummary,
  updateParkingbay,
} from './parkingbay.js';
import {
  createTransferjob,
  listTransferjob,
  transferjobSummary,
  updateTransferjob,
} from './transferjob.js';
import {
  createPickupdrop,
  listPickupdrop,
  pickupdropSummary,
  updatePickupdrop,
} from './pickupdrop.js';
import {
  createTollpass,
  listTollpass,
  tollpassSummary,
  updateTollpass,
} from './tollpass.js';
import {
  createLanecontrol,
  listLanecontrol,
  lanecontrolSummary,
  updateLanecontrol,
} from './lanecontrol.js';
import {
  ackConvoyFlag, buildConvoy, clearConvoyDispatch, freeConvoyCurb, readyConvoyFleet, runConvoySweep,
} from './convoy.js';
import {
  createRoomrack,
  listRoomrack,
  roomrackSummary,
  updateRoomrack,
} from './roomrack.js';
import {
  createHkboard,
  listHkboard,
  hkboardSummary,
  updateHkboard,
} from './hkboard.js';
import {
  createLinenroom,
  listLinenroom,
  linenroomSummary,
  updateLinenroom,
} from './linenroom.js';
import {
  createMinibarbay,
  listMinibarbay,
  minibarbaySummary,
  updateMinibarbay,
} from './minibarbay.js';
import {
  createFoundlog,
  listFoundlog,
  foundlogSummary,
  updateFoundlog,
} from './foundlog.js';
import {
  createKeydesk,
  listKeydesk,
  keydeskSummary,
  updateKeydesk,
} from './keydesk.js';
import {
  createTurnup,
  listTurnup,
  turnupSummary,
  updateTurnup,
} from './turnup.js';
import {
  createDeepclean,
  listDeepclean,
  deepcleanSummary,
  updateDeepclean,
} from './deepclean.js';
import {
  createInspectroom,
  listInspectroom,
  inspectroomSummary,
  updateInspectroom,
} from './inspectroom.js';
import {
  createVipprep,
  listVipprep,
  vipprepSummary,
  updateVipprep,
} from './vipprep.js';
import {
  createOutoforder,
  listOutoforder,
  outoforderSummary,
  updateOutoforder,
} from './outoforder.js';
import {
  createGuestrequest,
  listGuestrequest,
  guestrequestSummary,
  updateGuestrequest,
} from './guestrequest.js';
import {
  createAmensave,
  listAmensave,
  amensaveSummary,
  updateAmensave,
} from './amensave.js';
import {
  createPublicarea,
  listPublicarea,
  publicareaSummary,
  updatePublicarea,
} from './publicarea.js';
import {
  ackLinenFlag, buildLinen, completeLinenHk, passLinenInspect, releaseLinenOoo, runLinenSweep,
} from './linen.js';
import {
  createNightaudit,
  listNightaudit,
  nightauditSummary,
  updateNightaudit,
} from './nightaudit.js';
import {
  createFoliodesk,
  listFoliodesk,
  foliodeskSummary,
  updateFoliodesk,
} from './foliodesk.js';
import {
  createDeskqueue,
  listDeskqueue,
  deskqueueSummary,
  updateDeskqueue,
} from './deskqueue.js';
import {
  createWakeall,
  listWakeall,
  wakeallSummary,
  updateWakeall,
} from './wakeall.js';
import {
  createEarlycheck,
  listEarlycheck,
  earlycheckSummary,
  updateEarlycheck,
} from './earlycheck.js';
import {
  createLatecheck,
  listLatecheck,
  latecheckSummary,
  updateLatecheck,
} from './latecheck.js';
import {
  createBagstore,
  listBagstore,
  bagstoreSummary,
  updateBagstore,
} from './bagstore.js';
import {
  createCallsheet,
  listCallsheet,
  callsheetSummary,
  updateCallsheet,
} from './callsheet.js';
import {
  createArrivalboard,
  listArrivalboard,
  arrivalboardSummary,
  updateArrivalboard,
} from './arrivalboard.js';
import {
  createDepartureboard,
  listDepartureboard,
  departureboardSummary,
  updateDepartureboard,
} from './departureboard.js';
import {
  createViparrive,
  listViparrive,
  viparriveSummary,
  updateViparrive,
} from './viparrive.js';
import {
  createFrontlog,
  listFrontlog,
  frontlogSummary,
  updateFrontlog,
} from './frontlog.js';
import {
  createMoveticket,
  listMoveticket,
  moveticketSummary,
  updateMoveticket,
} from './moveticket.js';
import {
  createConciergejob,
  listConciergejob,
  conciergejobSummary,
  updateConciergejob,
} from './conciergejob.js';
import {
  ackAtlasFlag, buildAtlas, closeAtlasFolio, runAtlasAudit, runAtlasSweep, serveAtlasDesk,
} from './atlas.js';
import {
  createTybridge,
  listTybridge,
  tybridgeSummary,
  updateTybridge,
} from './tybridge.js';
import {
  createDolaplist,
  listDolaplist,
  dolaplistSummary,
  updateDolaplist,
} from './dolaplist.js';
import {
  createHephapick,
  listHephapick,
  hephapickSummary,
  updateHephapick,
} from './hephapick.js';
import {
  createTourpack,
  listTourpack,
  tourpackSummary,
  updateTourpack,
} from './tourpack.js';
import {
  createRentgear,
  listRentgear,
  rentgearSummary,
  updateRentgear,
} from './rentgear.js';
import {
  createWareserve,
  listWareserve,
  wareserveSummary,
  updateWareserve,
} from './wareserve.js';
import {
  createStaybook,
  listStaybook,
  staybookSummary,
  updateStaybook,
} from './staybook.js';
import {
  createDazeroom,
  listDazeroom,
  dazeroomSummary,
  updateDazeroom,
} from './dazeroom.js';
import {
  createKeylessdoor,
  listKeylessdoor,
  keylessdoorSummary,
  updateKeylessdoor,
} from './keylessdoor.js';
import {
  createSportslot,
  listSportslot,
  sportslotSummary,
  updateSportslot,
} from './sportslot.js';
import {
  createArenabook,
  listArenabook,
  arenabookSummary,
  updateArenabook,
} from './arenabook.js';
import {
  createNexusgate,
  listNexusgate,
  nexusgateSummary,
  updateNexusgate,
} from './nexusgate.js';
import {
  createMintbundle,
  listMintbundle,
  mintbundleSummary,
  updateMintbundle,
} from './mintbundle.js';
import {
  createPackfolio,
  listPackfolio,
  packfolioSummary,
  updatePackfolio,
} from './packfolio.js';
import {
  ackEmpireFlag, buildEmpire, departEmpireTour, runEmpireSweep, shipEmpireHepha, syncEmpireTy,
} from './empire.js';
import {
  createRetailfloor,
  listRetailfloor,
  retailfloorSummary,
  updateRetailfloor,
} from './retailfloor.js';
import {
  createPlanogram,
  listPlanogram,
  planogramSummary,
  updatePlanogram,
} from './planogram.js';
import {
  createShelfscan,
  listShelfscan,
  shelfscanSummary,
  updateShelfscan,
} from './shelfscan.js';
import {
  createPriceaudit,
  listPriceaudit,
  priceauditSummary,
  updatePriceaudit,
} from './priceaudit.js';
import {
  createShrinklog,
  listShrinklog,
  shrinklogSummary,
  updateShrinklog,
} from './shrinklog.js';
import {
  createVendorportal,
  listVendorportal,
  vendorportalSummary,
  updateVendorportal,
} from './vendorportal.js';
import {
  createAssortmix,
  listAssortmix,
  assortmixSummary,
  updateAssortmix,
} from './assortmix.js';
import {
  createPromoplane,
  listPromoplane,
  promoplaneSummary,
  updatePromoplane,
} from './promoplane.js';
import {
  createCategorybuy,
  listCategorybuy,
  categorybuySummary,
  updateCategorybuy,
} from './categorybuy.js';
import {
  createDemandplan,
  listDemandplan,
  demandplanSummary,
  updateDemandplan,
} from './demandplan.js';
import {
  createStockhealth,
  listStockhealth,
  stockhealthSummary,
  updateStockhealth,
} from './stockhealth.js';
import {
  createReturnbay,
  listReturnbay,
  returnbaySummary,
  updateReturnbay,
} from './returnbay.js';
import {
  createDarkstore,
  listDarkstore,
  darkstoreSummary,
  updateDarkstore,
} from './darkstore.js';
import {
  createPoslane,
  listPoslane,
  poslaneSummary,
  updatePoslane,
} from './poslane.js';
import {
  ackBazaarFlag,
  buildBazaar,
  dispatchBazaarDark,
  healBazaarStock,
  reviewBazaarShrink,
  runBazaarSweep,
} from './bazaar.js';
import {
  createMediawall,
  listMediawall,
  mediawallSummary,
  updateMediawall,
} from './mediawall.js';
import {
  createContentrights,
  listContentrights,
  contentrightsSummary,
  updateContentrights,
} from './contentrights.js';
import {
  createAdslot,
  listAdslot,
  adslotSummary,
  updateAdslot,
} from './adslot.js';
import {
  createSponsorpack,
  listSponsorpack,
  sponsorpackSummary,
  updateSponsorpack,
} from './sponsorpack.js';
import {
  createBrandambass,
  listBrandambass,
  brandambassSummary,
  updateBrandambass,
} from './brandambass.js';
import {
  createCreatorpay,
  listCreatorpay,
  creatorpaySummary,
  updateCreatorpay,
} from './creatorpay.js';
import {
  createUgcqueue,
  listUgcqueue,
  ugcqueueSummary,
  updateUgcqueue,
} from './ugcqueue.js';
import {
  createPressroom,
  listPressroom,
  pressroomSummary,
  updatePressroom,
} from './pressroom.js';
import {
  createLivecast,
  listLivecast,
  livecastSummary,
  updateLivecast,
} from './livecast.js';
import {
  createEventstream,
  listEventstream,
  eventstreamSummary,
  updateEventstream,
} from './eventstream.js';
import {
  createAffiliatenet,
  listAffiliatenet,
  affiliatenetSummary,
  updateAffiliatenet,
} from './affiliatenet.js';
import {
  createBoostdesk,
  listBoostdesk,
  boostdeskSummary,
  updateBoostdesk,
} from './boostdesk.js';
import {
  createAssetlib,
  listAssetlib,
  assetlibSummary,
  updateAssetlib,
} from './assetlib.js';
import {
  createBriefdesk,
  listBriefdesk,
  briefdeskSummary,
  updateBriefdesk,
} from './briefdesk.js';
import {
  ackStudioFlag,
  approveStudioUgc,
  buildStudio,
  deliverStudioBrief,
  endStudioLive,
  runStudioSweep,
} from './studio.js';
import {
  createCarbonledger,
  listCarbonledger,
  carbonledgerSummary,
  updateCarbonledger,
} from './carbonledger.js';
import {
  createWateruse,
  listWateruse,
  wateruseSummary,
  updateWateruse,
} from './wateruse.js';
import {
  createWastesort,
  listWastesort,
  wastesortSummary,
  updateWastesort,
} from './wastesort.js';
import {
  createEnergybid,
  listEnergybid,
  energybidSummary,
  updateEnergybid,
} from './energybid.js';
import {
  createSolaryield,
  listSolaryield,
  solaryieldSummary,
  updateSolaryield,
} from './solaryield.js';
import {
  createGreenteam,
  listGreenteam,
  greenteamSummary,
  updateGreenteam,
} from './greenteam.js';
import {
  createEsgaudit,
  listEsgaudit,
  esgauditSummary,
  updateEsgaudit,
} from './esgaudit.js';
import {
  createBiosurvey,
  listBiosurvey,
  biosurveySummary,
  updateBiosurvey,
} from './biosurvey.js';
import {
  createOffsetbuy,
  listOffsetbuy,
  offsetbuySummary,
  updateOffsetbuy,
} from './offsetbuy.js';
import {
  createClimategoal,
  listClimategoal,
  climategoalSummary,
  updateClimategoal,
} from './climategoal.js';
import {
  createGreenbond,
  listGreenbond,
  greenbondSummary,
  updateGreenbond,
} from './greenbond.js';
import {
  createPlasticaudit,
  listPlasticaudit,
  plasticauditSummary,
  updatePlasticaudit,
} from './plasticaudit.js';
import {
  createEvcharger,
  listEvcharger,
  evchargerSummary,
  updateEvcharger,
} from './evcharger.js';
import {
  createReefwatch,
  listReefwatch,
  reefwatchSummary,
  updateReefwatch,
} from './reefwatch.js';
import {
  ackVerdantFlag,
  buildVerdant,
  clearVerdantWater,
  closeVerdantEsg,
  fixVerdantEv,
  runVerdantSweep,
} from './verdant.js';
import {
  createAccessgate,
  listAccessgate,
  accessgateSummary,
  updateAccessgate,
} from './accessgate.js';
import {
  createIdproof,
  listIdproof,
  idproofSummary,
  updateIdproof,
} from './idproof.js';
import {
  createRolegrant,
  listRolegrant,
  rolegrantSummary,
  updateRolegrant,
} from './rolegrant.js';
import {
  createSessionguard,
  listSessionguard,
  sessionguardSummary,
  updateSessionguard,
} from './sessionguard.js';
import {
  createDevicetrust,
  listDevicetrust,
  devicetrustSummary,
  updateDevicetrust,
} from './devicetrust.js';
import {
  createSecretvault,
  listSecretvault,
  secretvaultSummary,
  updateSecretvault,
} from './secretvault.js';
import {
  createMfareg,
  listMfareg,
  mfaregSummary,
  updateMfareg,
} from './mfareg.js';
import {
  createSsobridge,
  listSsobridge,
  ssobridgeSummary,
  updateSsobridge,
} from './ssobridge.js';
import {
  createPrivacypol,
  listPrivacypol,
  privacypolSummary,
  updatePrivacypol,
} from './privacypol.js';
import {
  createConsentrow,
  listConsentrow,
  consentrowSummary,
  updateConsentrow,
} from './consentrow.js';
import {
  createBreachlog,
  listBreachlog,
  breachlogSummary,
  updateBreachlog,
} from './breachlog.js';
import {
  createSocqueue,
  listSocqueue,
  socqueueSummary,
  updateSocqueue,
} from './socqueue.js';
import {
  createPatchdesk,
  listPatchdesk,
  patchdeskSummary,
  updatePatchdesk,
} from './patchdesk.js';
import {
  createZerohour,
  listZerohour,
  zerohourSummary,
  updateZerohour,
} from './zerohour.js';
import {
  ackBastionFlag,
  approveBastionRole,
  archiveBastionBreach,
  buildBastion,
  closeBastionAccess,
  runBastionSweep,
} from './bastion.js';
import {
  createPartnerdesk,
  listPartnerdesk,
  partnerdeskSummary,
  updatePartnerdesk,
} from './partnerdesk.js';
import {
  createFranchise,
  listFranchise,
  franchiseSummary,
  updateFranchise,
} from './franchise.js';
import {
  createChannelkit,
  listChannelkit,
  channelkitSummary,
  updateChannelkit,
} from './channelkit.js';
import {
  createRebate,
  listRebate,
  rebateSummary,
  updateRebate,
} from './rebate.js';
import {
  createCoinvest,
  listCoinvest,
  coinvestSummary,
  updateCoinvest,
} from './coinvest.js';
import {
  createSlatrack,
  listSlatrack,
  slatrackSummary,
  updateSlatrack,
} from './slatrack.js';
import {
  createJointpromo,
  listJointpromo,
  jointpromoSummary,
  updateJointpromo,
} from './jointpromo.js';
import {
  createLeadshare,
  listLeadshare,
  leadshareSummary,
  updateLeadshare,
} from './leadshare.js';
import {
  createB2border,
  listB2border,
  b2borderSummary,
  updateB2border,
} from './b2border.js';
import {
  createWholesale,
  listWholesale,
  wholesaleSummary,
  updateWholesale,
} from './wholesale.js';
import {
  createDealroom,
  listDealroom,
  dealroomSummary,
  updateDealroom,
} from './dealroom.js';
import {
  createContractrow,
  listContractrow,
  contractrowSummary,
  updateContractrow,
} from './contractrow.js';
import {
  createCommission,
  listCommission,
  commissionSummary,
  updateCommission,
} from './commission.js';
import {
  createOnboardkit,
  listOnboardkit,
  onboardkitSummary,
  updateOnboardkit,
} from './onboardkit.js';
import {
  ackAlliance2Flag, buildAlliance2, busyAlliance2Partner, closeAlliance2Channel, liveAlliance2Invest, runAlliance2Sweep,
} from './alliance2.js';
import {
  createInboundpo,
  listInboundpo,
  inboundpoSummary,
  updateInboundpo,
} from './inboundpo.js';
import {
  createOutboundso,
  listOutboundso,
  outboundsoSummary,
  updateOutboundso,
} from './outboundso.js';
import {
  createAsntrack,
  listAsntrack,
  asntrackSummary,
  updateAsntrack,
} from './asntrack.js';
import {
  createDockyard,
  listDockyard,
  dockyardSummary,
  updateDockyard,
} from './dockyard.js';
import {
  createCrossdock,
  listCrossdock,
  crossdockSummary,
  updateCrossdock,
} from './crossdock.js';
import {
  createColdchain2,
  listColdchain2,
  coldchain2Summary,
  updateColdchain2,
} from './coldchain2.js';
import {
  createSlotbook,
  listSlotbook,
  slotbookSummary,
  updateSlotbook,
} from './slotbook.js';
import {
  createCarrierbid,
  listCarrierbid,
  carrierbidSummary,
  updateCarrierbid,
} from './carrierbid.js';
import {
  createFreightbill,
  listFreightbill,
  freightbillSummary,
  updateFreightbill,
} from './freightbill.js';
import {
  createMilestonet,
  listMilestonet,
  milestonetSummary,
  updateMilestonet,
} from './milestonet.js';
import {
  createExceptionlog,
  listExceptionlog,
  exceptionlogSummary,
  updateExceptionlog,
} from './exceptionlog.js';
import {
  createInventoryage,
  listInventoryage,
  inventoryageSummary,
  updateInventoryage,
} from './inventoryage.js';
import {
  createReplenplan,
  listReplenplan,
  replenplanSummary,
  updateReplenplan,
} from './replenplan.js';
import {
  createSafetystock,
  listSafetystock,
  safetystockSummary,
  updateSafetystock,
} from './safetystock.js';
import {
  ackArteryFlag, buildArtery, busyArteryAsn, closeArteryDock, runArteryInbound, runArterySweep,
} from './artery.js';
import {
  createSupplierkpi,
  listSupplierkpi,
  supplierkpiSummary,
  updateSupplierkpi,
} from './supplierkpi.js';
import {
  createCmdpulse,
  listCmdpulse,
  cmdpulseSummary,
  updateCmdpulse,
} from './cmdpulse.js';
import {
  createBoardpulse,
  listBoardpulse,
  boardpulseSummary,
  updateBoardpulse,
} from './boardpulse.js';
import {
  createRiskheat,
  listRiskheat,
  riskheatSummary,
  updateRiskheat,
} from './riskheat.js';
import {
  createCashpulse,
  listCashpulse,
  cashpulseSummary,
  updateCashpulse,
} from './cashpulse.js';
import {
  createOpsheat,
  listOpsheat,
  opsheatSummary,
  updateOpsheat,
} from './opsheat.js';
import {
  createGuestheat,
  listGuestheat,
  guestheatSummary,
  updateGuestheat,
} from './guestheat.js';
import {
  createBrandheat,
  listBrandheat,
  brandheatSummary,
  updateBrandheat,
} from './brandheat.js';
import {
  createAgentpulse,
  listAgentpulse,
  agentpulseSummary,
  updateAgentpulse,
} from './agentpulse.js';
import {
  createSystempulse,
  listSystempulse,
  systempulseSummary,
  updateSystempulse,
} from './systempulse.js';
import {
  createAlertfuse,
  listAlertfuse,
  alertfuseSummary,
  updateAlertfuse,
} from './alertfuse.js';
import {
  createWarbrief,
  listWarbrief,
  warbriefSummary,
  updateWarbrief,
} from './warbrief.js';
import {
  createDecisionhub,
  listDecisionhub,
  decisionhubSummary,
  updateDecisionhub,
} from './decisionhub.js';
import {
  createSealnote,
  listSealnote,
  sealnoteSummary,
  updateSealnote,
} from './sealnote.js';
import {
  ackDominionFlag, buildDominion, busyDominionCash, liveDominionSupplier, runDominionBoard, runDominionSweep,
} from './dominion.js';
import {
  createLegacyflag,
  listLegacyflag,
  legacyflagSummary,
  updateLegacyflag,
} from './legacyflag.js';
import {
  createCalmroom,
  listCalmroom,
  calmroomSummary,
  updateCalmroom,
} from './calmroom.js';
import {
  createQuiethours,
  listQuiethours,
  quiethoursSummary,
  updateQuiethours,
} from './quiethours.js';
import {
  createScentmood,
  listScentmood,
  scentmoodSummary,
  updateScentmood,
} from './scentmood.js';
import {
  createPillowmenu2,
  listPillowmenu2,
  pillowmenu2Summary,
  updatePillowmenu2,
} from './pillowmenu2.js';
import {
  createBathritual,
  listBathritual,
  bathritualSummary,
  updateBathritual,
} from './bathritual.js';
import {
  createSleepscore,
  listSleepscore,
  sleepscoreSummary,
  updateSleepscore,
} from './sleepscore.js';
import {
  createWelcomeamen,
  listWelcomeamen,
  welcomeamenSummary,
  updateWelcomeamen,
} from './welcomeamen.js';
import {
  createFarewell,
  listFarewell,
  farewellSummary,
  updateFarewell,
} from './farewell.js';
import {
  createMemorybook,
  listMemorybook,
  memorybookSummary,
  updateMemorybook,
} from './memorybook.js';
import {
  createCarecall,
  listCarecall,
  carecallSummary,
  updateCarecall,
} from './carecall.js';
import {
  createSurprisegift,
  listSurprisegift,
  surprisegiftSummary,
  updateSurprisegift,
} from './surprisegift.js';
import {
  createLoyaltyhug,
  listLoyaltyhug,
  loyaltyhugSummary,
  updateLoyaltyhug,
} from './loyaltyhug.js';
import {
  createFeedbackloop,
  listFeedbackloop,
  feedbackloopSummary,
  updateFeedbackloop,
} from './feedbackloop.js';
import {
  ackSerenityFlag, buildSerenity, closeSerenityLegacy, liveSerenityQuiet, runSerenityPillow, runSerenitySweep,
} from './serenity.js';
import {
  createMomentmap,
  listMomentmap,
  momentmapSummary,
  updateMomentmap,
} from './momentmap.js';
import {
  createApigateway,
  listApigateway,
  apigatewaySummary,
  updateApigateway,
} from './apigateway.js';
import {
  createWebhookhub,
  listWebhookhub,
  webhookhubSummary,
  updateWebhookhub,
} from './webhookhub.js';
import {
  createRatelimit2,
  listRatelimit2,
  ratelimit2Summary,
  updateRatelimit2,
} from './ratelimit2.js';
import {
  createSchemareg,
  listSchemareg,
  schemaregSummary,
  updateSchemareg,
} from './schemareg.js';
import {
  createEventbus,
  listEventbus,
  eventbusSummary,
  updateEventbus,
} from './eventbus.js';
import {
  createJobqueue2,
  listJobqueue2,
  jobqueue2Summary,
  updateJobqueue2,
} from './jobqueue2.js';
import {
  createCachemesh,
  listCachemesh,
  cachemeshSummary,
  updateCachemesh,
} from './cachemesh.js';
import {
  createCdnedge,
  listCdnedge,
  cdnedgeSummary,
  updateCdnedge,
} from './cdnedge.js';
import {
  createObservemap,
  listObservemap,
  observemapSummary,
  updateObservemap,
} from './observemap.js';
import {
  createErrorbudget2,
  listErrorbudget2,
  errorbudget2Summary,
  updateErrorbudget2,
} from './errorbudget2.js';
import {
  createFeaturegate,
  listFeaturegate,
  featuregateSummary,
  updateFeaturegate,
} from './featuregate.js';
import {
  createCanaryrun,
  listCanaryrun,
  canaryrunSummary,
  updateCanaryrun,
} from './canaryrun.js';
import {
  createRollback,
  listRollback,
  rollbackSummary,
  updateRollback,
} from './rollback.js';
import {
  ackCircuitFlag, buildCircuit, busyCircuitMoment, closeCircuitHook, liveCircuitSchema, runCircuitSweep,
} from './circuit.js';
import {
  createChaosdrill,
  listChaosdrill,
  chaosdrillSummary,
  updateChaosdrill,
} from './chaosdrill.js';
import {
  createMemberhub,
  listMemberhub,
  memberhubSummary,
  updateMemberhub,
} from './memberhub.js';
import {
  createCircle,
  listCircle,
  circleSummary,
  updateCircle,
} from './circle.js';
import {
  createMeetup,
  listMeetup,
  meetupSummary,
  updateMeetup,
} from './meetup.js';
import {
  createForummod,
  listForummod,
  forummodSummary,
  updateForummod,
} from './forummod.js';
import {
  createPolldesk,
  listPolldesk,
  polldeskSummary,
  updatePolldesk,
} from './polldesk.js';
import {
  createBadgeearn,
  listBadgeearn,
  badgeearnSummary,
  updateBadgeearn,
} from './badgeearn.js';
import {
  createQuestline,
  listQuestline,
  questlineSummary,
  updateQuestline,
} from './questline.js';
import {
  createVolunteer,
  listVolunteer,
  volunteerSummary,
  updateVolunteer,
} from './volunteer.js';
import {
  createDonation,
  listDonation,
  donationSummary,
  updateDonation,
} from './donation.js';
import {
  createChapter,
  listChapter,
  chapterSummary,
  updateChapter,
} from './chapter.js';
import {
  createAmbassador2,
  listAmbassador2,
  ambassador2Summary,
  updateAmbassador2,
} from './ambassador2.js';
import {
  createStorywall,
  listStorywall,
  storywallSummary,
  updateStorywall,
} from './storywall.js';
import {
  createRitualcal,
  listRitualcal,
  ritualcalSummary,
  updateRitualcal,
} from './ritualcal.js';
import {
  ackAgoraFlag, buildAgora, busyAgoraCircle, liveAgoraBadge, runAgoraDrill, runAgoraSweep,
} from './agora.js';
import {
  createCohort,
  listCohort,
  cohortSummary,
  updateCohort,
} from './cohort.js';
import {
  createLabbench,
  listLabbench,
  labbenchSummary,
  updateLabbench,
} from './labbench.js';
import {
  createPilotrun,
  listPilotrun,
  pilotrunSummary,
  updatePilotrun,
} from './pilotrun.js';
import {
  createPrototype,
  listPrototype,
  prototypeSummary,
  updatePrototype,
} from './prototype.js';
import {
  createHypothesis,
  listHypothesis,
  hypothesisSummary,
  updateHypothesis,
} from './hypothesis.js';
import {
  createMetricslab,
  listMetricslab,
  metricslabSummary,
  updateMetricslab,
} from './metricslab.js';
import {
  createUserboard,
  listUserboard,
  userboardSummary,
  updateUserboard,
} from './userboard.js';
import {
  createPatentdesk,
  listPatentdesk,
  patentdeskSummary,
  updatePatentdesk,
} from './patentdesk.js';
import {
  createSandbox,
  listSandbox,
  sandboxSummary,
  updateSandbox,
} from './sandbox.js';
import {
  createHackday,
  listHackday,
  hackdaySummary,
  updateHackday,
} from './hackday.js';
import {
  createIncubate,
  listIncubate,
  incubateSummary,
  updateIncubate,
} from './incubate.js';
import {
  createSpindesk,
  listSpindesk,
  spindeskSummary,
  updateSpindesk,
} from './spindesk.js';
import {
  createResearchnote,
  listResearchnote,
  researchnoteSummary,
  updateResearchnote,
} from './researchnote.js';
import {
  createLabbudget,
  listLabbudget,
  labbudgetSummary,
  updateLabbudget,
} from './labbudget.js';
import {
  ackCrucibleFlag, buildCrucible, busyCrucibleLearn, liveCruciblePilot, runCrucibleSweep, shipCrucibleLab,
} from './crucible.js';
import {
  createIpvault,
  listIpvault,
  ipvaultSummary,
  updateIpvault,
} from './ipvault.js';
import {
  createLegaldesk,
  listLegaldesk,
  legaldeskSummary,
  updateLegaldesk,
} from './legaldesk.js';
import {
  createRiskreg,
  listRiskreg,
  riskregSummary,
  updateRiskreg,
} from './riskreg.js';
import {
  createPolicyhub,
  listPolicyhub,
  policyhubSummary,
  updatePolicyhub,
} from './policyhub.js';
import {
  createClaimdesk,
  listClaimdesk,
  claimdeskSummary,
  updateClaimdesk,
} from './claimdesk.js';
import {
  createInsurancet,
  listInsurancet,
  insurancetSummary,
  updateInsurancet,
} from './insurancet.js';
import {
  createLitigation,
  listLitigation,
  litigationSummary,
  updateLitigation,
} from './litigation.js';
import {
  createCompliance2,
  listCompliance2,
  compliance2Summary,
  updateCompliance2,
} from './compliance2.js';
import {
  createEthicsline,
  listEthicsline,
  ethicslineSummary,
  updateEthicsline,
} from './ethicsline.js';
import {
  createKycrow,
  listKycrow,
  kycrowSummary,
  updateKycrow,
} from './kycrow.js';
import {
  createSanctions,
  listSanctions,
  sanctionsSummary,
  updateSanctions,
} from './sanctions.js';
import {
  createDatapriv,
  listDatapriv,
  dataprivSummary,
  updateDatapriv,
} from './datapriv.js';
import {
  createRetentionpol,
  listRetentionpol,
  retentionpolSummary,
  updateRetentionpol,
} from './retentionpol.js';
import {
  createAuditevidence,
  listAuditevidence,
  auditevidenceSummary,
  updateAuditevidence,
} from './auditevidence.js';
import {
  ackCharterFlag, buildCharter, closeCharterEthics, doneCharterClaim, liveCharterRisk, runCharterSweep,
} from './charter.js';
import {
  createBoardresolve,
  listBoardresolve,
  boardresolveSummary,
  updateBoardresolve,
} from './boardresolve.js';
import {
  createDrplan,
  listDrplan,
  drplanSummary,
  updateDrplan,
} from './drplan.js';
import {
  createBackupjob,
  listBackupjob,
  backupjobSummary,
  updateBackupjob,
} from './backupjob.js';
import {
  createFailover2,
  listFailover2,
  failover2Summary,
  updateFailover2,
} from './failover2.js';
import {
  createRunbook,
  listRunbook,
  runbookSummary,
  updateRunbook,
} from './runbook.js';
import {
  createWarroom2,
  listWarroom2,
  warroom2Summary,
  updateWarroom2,
} from './warroom2.js';
import {
  createCommsbridge2,
  listCommsbridge2,
  commsbridge2Summary,
  updateCommsbridge2,
} from './commsbridge2.js';
import {
  createSiteevac,
  listSiteevac,
  siteevacSummary,
  updateSiteevac,
} from './siteevac.js';
import {
  createColdsite,
  listColdsite,
  coldsiteSummary,
  updateColdsite,
} from './coldsite.js';
import {
  createHotspare,
  listHotspare,
  hotspareSummary,
  updateHotspare,
} from './hotspare.js';
import {
  createDrillscore,
  listDrillscore,
  drillscoreSummary,
  updateDrillscore,
} from './drillscore.js';
import {
  createVendorfail,
  listVendorfail,
  vendorfailSummary,
  updateVendorfail,
} from './vendorfail.js';
import {
  createPowercut,
  listPowercut,
  powercutSummary,
  updatePowercut,
} from './powercut.js';
import {
  createNetsplit,
  listNetsplit,
  netsplitSummary,
  updateNetsplit,
} from './netsplit.js';
import {
  ackPhoenixFlag, buildPhoenix, closePhoenixBackup, closePhoenixDrill, livePhoenixRunbook, runPhoenixSweep,
} from './phoenix.js';
import {
  createRestorejob,
  listRestorejob,
  restorejobSummary,
  updateRestorejob,
} from './restorejob.js';
import {
  createMarketscan,
  listMarketscan,
  marketscanSummary,
  updateMarketscan,
} from './marketscan.js';
import {
  createSitehunt,
  listSitehunt,
  sitehuntSummary,
  updateSitehunt,
} from './sitehunt.js';
import {
  createCapextable,
  listCapextable,
  capextableSummary,
  updateCapextable,
} from './capextable.js';
import {
  createSoftopen,
  listSoftopen,
  softopenSummary,
  updateSoftopen,
} from './softopen.js';
import {
  createLaunchpad,
  listLaunchpad,
  launchpadSummary,
  updateLaunchpad,
} from './launchpad.js';
import {
  createLocalhire,
  listLocalhire,
  localhireSummary,
  updateLocalhire,
} from './localhire.js';
import {
  createPermitdesk,
  listPermitdesk,
  permitdeskSummary,
  updatePermitdesk,
} from './permitdesk.js';
import {
  createLandlease,
  listLandlease,
  landleaseSummary,
  updateLandlease,
} from './landlease.js';
import {
  createBuildphase,
  listBuildphase,
  buildphaseSummary,
  updateBuildphase,
} from './buildphase.js';
import {
  createFfespec,
  listFfespec,
  ffespecSummary,
  updateFfespec,
} from './ffespec.js';
import {
  createBrandrollout,
  listBrandrollout,
  brandrolloutSummary,
  updateBrandrollout,
} from './brandrollout.js';
import {
  createTrainwave,
  listTrainwave,
  trainwaveSummary,
  updateTrainwave,
} from './trainwave.js';
import {
  createGolive,
  listGolive,
  goliveSummary,
  updateGolive,
} from './golive.js';
import {
  ackFrontierFlag, buildFrontier, busyFrontierSite, liveFrontierHire, runFrontierRestore, runFrontierSweep,
} from './frontier.js';
import {
  createPostlaunch,
  listPostlaunch,
  postlaunchSummary,
  updatePostlaunch,
} from './postlaunch.js';
import {
  createQasample,
  listQasample,
  qasampleSummary,
  updateQasample,
} from './qasample.js';
import {
  createDefectlog,
  listDefectlog,
  defectlogSummary,
  updateDefectlog,
} from './defectlog.js';
import {
  createStandardop,
  listStandardop,
  standardopSummary,
  updateStandardop,
} from './standardop.js';
import {
  createMysteryguest,
  listMysteryguest,
  mysteryguestSummary,
  updateMysteryguest,
} from './mysteryguest.js';
import {
  createNpsdeep,
  listNpsdeep,
  npsdeepSummary,
  updateNpsdeep,
} from './npsdeep.js';
import {
  createServicemark,
  listServicemark,
  servicemarkSummary,
  updateServicemark,
} from './servicemark.js';
import {
  createCalibdesk,
  listCalibdesk,
  calibdeskSummary,
  updateCalibdesk,
} from './calibdesk.js';
import {
  createLabresult,
  listLabresult,
  labresultSummary,
  updateLabresult,
} from './labresult.js';
import {
  createCertrenew,
  listCertrenew,
  certrenewSummary,
  updateCertrenew,
} from './certrenew.js';
import {
  createIsotrack,
  listIsotrack,
  isotrackSummary,
  updateIsotrack,
} from './isotrack.js';
import {
  createGuestvoice,
  listGuestvoice,
  guestvoiceSummary,
  updateGuestvoice,
} from './guestvoice.js';
import {
  createFixgate,
  listFixgate,
  fixgateSummary,
  updateFixgate,
} from './fixgate.js';
import {
  createRootcause,
  listRootcause,
  rootcauseSummary,
  updateRootcause,
} from './rootcause.js';
import {
  ackPrismFlag, buildPrism, closePrismMark, livePrismPost, runPrismDefect, runPrismSweep,
} from './prism.js';
import {
  createCorrective,
  listCorrective,
  correctiveSummary,
  updateCorrective,
} from './corrective.js';
import {
  createArchivebox,
  listArchivebox,
  archiveboxSummary,
  updateArchivebox,
} from './archivebox.js';
import {
  createOralhistory,
  listOralhistory,
  oralhistorySummary,
  updateOralhistory,
} from './oralhistory.js';
import {
  createArtifact,
  listArtifact,
  artifactSummary,
  updateArtifact,
} from './artifact.js';
import {
  createTimeline,
  listTimeline,
  timelineSummary,
  updateTimeline,
} from './timeline.js';
import {
  createFoundersnote,
  listFoundersnote,
  foundersnoteSummary,
  updateFoundersnote,
} from './foundersnote.js';
import {
  createBrandbible,
  listBrandbible,
  brandbibleSummary,
  updateBrandbible,
} from './brandbible.js';
import {
  createMuseumdesk,
  listMuseumdesk,
  museumdeskSummary,
  updateMuseumdesk,
} from './museumdesk.js';
import {
  createHeritage,
  listHeritage,
  heritageSummary,
  updateHeritage,
} from './heritage.js';
import {
  createAnniversary,
  listAnniversary,
  anniversarySummary,
  updateAnniversary,
} from './anniversary.js';
import {
  createAlumni,
  listAlumni,
  alumniSummary,
  updateAlumni,
} from './alumni.js';
import {
  createScholarship,
  listScholarship,
  scholarshipSummary,
  updateScholarship,
} from './scholarship.js';
import {
  createFoundation,
  listFoundation,
  foundationSummary,
  updateFoundation,
} from './foundation.js';
import {
  createLegacygift,
  listLegacygift,
  legacygiftSummary,
  updateLegacygift,
} from './legacygift.js';
import {
  ackMonumentFlag, buildMonument, closeMonumentCorrective, liveMonumentOral, runMonumentSweep, runMonumentTimeline,
} from './monument.js';
import {
  createStoryvault,
  listStoryvault,
  storyvaultSummary,
  updateStoryvault,
} from './storyvault.js';
import {
  createOlympulse,
  listOlympulse,
  olympulseSummary,
  updateOlympulse,
} from './olympulse.js';
import {
  createHoldingseal,
  listHoldingseal,
  holdingsealSummary,
  updateHoldingseal,
} from './holdingseal.js';
import {
  createAgentcourt,
  listAgentcourt,
  agentcourtSummary,
  updateAgentcourt,
} from './agentcourt.js';
import {
  createFinalbrief,
  listFinalbrief,
  finalbriefSummary,
  updateFinalbrief,
} from './finalbrief.js';
import {
  createLegacycode,
  listLegacycode,
  legacycodeSummary,
  updateLegacycode,
} from './legacycode.js';
import {
  createEternallog,
  listEternallog,
  eternallogSummary,
  updateEternallog,
} from './eternallog.js';
import {
  createSummitnote,
  listSummitnote,
  summitnoteSummary,
  updateSummitnote,
} from './summitnote.js';
import {
  createConstellate,
  listConstellate,
  constellateSummary,
  updateConstellate,
} from './constellate.js';
import {
  createMythos,
  listMythos,
  mythosSummary,
  updateMythos,
} from './mythos.js';
import {
  createAegisfinal,
  listAegisfinal,
  aegisfinalSummary,
  updateAegisfinal,
} from './aegisfinal.js';
import {
  createCrownfinal,
  listCrownfinal,
  crownfinalSummary,
  updateCrownfinal,
} from './crownfinal.js';
import {
  createVaultfinal,
  listVaultfinal,
  vaultfinalSummary,
  updateVaultfinal,
} from './vaultfinal.js';
import {
  createEmpirefinal,
  listEmpirefinal,
  empirefinalSummary,
  updateEmpirefinal,
} from './empirefinal.js';
import {
  ackOlympusFlag, buildOlympus, busyOlympusStory, closeOlympusSeal, liveOlympusBrief, runOlympusSweep,
} from './olympus.js';
import {
  createAccessgate2,
  listAccessgate2,
  accessgate2Summary,
  updateAccessgate2,
} from './accessgate2.js';
import {
  createIdproof2,
  listIdproof2,
  idproof2Summary,
  updateIdproof2,
} from './idproof2.js';
import {
  createRolegrant2,
  listRolegrant2,
  rolegrant2Summary,
  updateRolegrant2,
} from './rolegrant2.js';
import {
  createSessionguard2,
  listSessionguard2,
  sessionguard2Summary,
  updateSessionguard2,
} from './sessionguard2.js';
import {
  createDevicetrust2,
  listDevicetrust2,
  devicetrust2Summary,
  updateDevicetrust2,
} from './devicetrust2.js';
import {
  createSecretvault2,
  listSecretvault2,
  secretvault2Summary,
  updateSecretvault2,
} from './secretvault2.js';
import {
  createMfareg2,
  listMfareg2,
  mfareg2Summary,
  updateMfareg2,
} from './mfareg2.js';
import {
  createSsobridge2,
  listSsobridge2,
  ssobridge2Summary,
  updateSsobridge2,
} from './ssobridge2.js';
import {
  createPrivacypol2,
  listPrivacypol2,
  privacypol2Summary,
  updatePrivacypol2,
} from './privacypol2.js';
import {
  createConsentrow2,
  listConsentrow2,
  consentrow2Summary,
  updateConsentrow2,
} from './consentrow2.js';
import {
  createBreachlog2,
  listBreachlog2,
  breachlog2Summary,
  updateBreachlog2,
} from './breachlog2.js';
import {
  createSocqueue2,
  listSocqueue2,
  socqueue2Summary,
  updateSocqueue2,
} from './socqueue2.js';
import {
  createPatchdesk2,
  listPatchdesk2,
  patchdesk2Summary,
  updatePatchdesk2,
} from './patchdesk2.js';
import {
  createZerohour2,
  listZerohour2,
  zerohour2Summary,
  updateZerohour2,
} from './zerohour2.js';
import {
  ackBastion2Flag, approveBastion2Role, archiveBastion2Breach, buildBastion2, closeBastion2Access, runBastion2Sweep,
} from './bastion2.js';
import {
  createPartnerdesk2,
  listPartnerdesk2,
  partnerdesk2Summary,
  updatePartnerdesk2,
} from './partnerdesk2.js';
import {
  createFranchise2,
  listFranchise2,
  franchise2Summary,
  updateFranchise2,
} from './franchise2.js';
import {
  createChannelkit2,
  listChannelkit2,
  channelkit2Summary,
  updateChannelkit2,
} from './channelkit2.js';
import {
  createRebate2,
  listRebate2,
  rebate2Summary,
  updateRebate2,
} from './rebate2.js';
import {
  createCoinvest2,
  listCoinvest2,
  coinvest2Summary,
  updateCoinvest2,
} from './coinvest2.js';
import {
  createSlatrack2,
  listSlatrack2,
  slatrack2Summary,
  updateSlatrack2,
} from './slatrack2.js';
import {
  createJointpromo2,
  listJointpromo2,
  jointpromo2Summary,
  updateJointpromo2,
} from './jointpromo2.js';
import {
  createLeadshare2,
  listLeadshare2,
  leadshare2Summary,
  updateLeadshare2,
} from './leadshare2.js';
import {
  createB2border2,
  listB2border2,
  b2border2Summary,
  updateB2border2,
} from './b2border2.js';
import {
  createWholesale2,
  listWholesale2,
  wholesale2Summary,
  updateWholesale2,
} from './wholesale2.js';
import {
  createDealroom2,
  listDealroom2,
  dealroom2Summary,
  updateDealroom2,
} from './dealroom2.js';
import {
  createContractrow2,
  listContractrow2,
  contractrow2Summary,
  updateContractrow2,
} from './contractrow2.js';
import {
  createCommission2,
  listCommission2,
  commission2Summary,
  updateCommission2,
} from './commission2.js';
import {
  createOnboardkit2,
  listOnboardkit2,
  onboardkit2Summary,
  updateOnboardkit2,
} from './onboardkit2.js';
import {
  ackAlliance3Flag, buildAlliance3, busyAlliance3Partner, closeAlliance3Channel, liveAlliance3Invest, runAlliance3Sweep,
} from './alliance3.js';
import {
  createInboundpo2,
  listInboundpo2,
  inboundpo2Summary,
  updateInboundpo2,
} from './inboundpo2.js';
import {
  createOutboundso2,
  listOutboundso2,
  outboundso2Summary,
  updateOutboundso2,
} from './outboundso2.js';
import {
  createAsntrack2,
  listAsntrack2,
  asntrack2Summary,
  updateAsntrack2,
} from './asntrack2.js';
import {
  createDockyard2,
  listDockyard2,
  dockyard2Summary,
  updateDockyard2,
} from './dockyard2.js';
import {
  createCrossdock2,
  listCrossdock2,
  crossdock2Summary,
  updateCrossdock2,
} from './crossdock2.js';
import {
  createColdchain22,
  listColdchain22,
  coldchain22Summary,
  updateColdchain22,
} from './coldchain22.js';
import {
  createSlotbook2,
  listSlotbook2,
  slotbook2Summary,
  updateSlotbook2,
} from './slotbook2.js';
import {
  createCarrierbid2,
  listCarrierbid2,
  carrierbid2Summary,
  updateCarrierbid2,
} from './carrierbid2.js';
import {
  createFreightbill2,
  listFreightbill2,
  freightbill2Summary,
  updateFreightbill2,
} from './freightbill2.js';
import {
  createMilestonet2,
  listMilestonet2,
  milestonet2Summary,
  updateMilestonet2,
} from './milestonet2.js';
import {
  createExceptionlog2,
  listExceptionlog2,
  exceptionlog2Summary,
  updateExceptionlog2,
} from './exceptionlog2.js';
import {
  createInventoryage2,
  listInventoryage2,
  inventoryage2Summary,
  updateInventoryage2,
} from './inventoryage2.js';
import {
  createReplenplan2,
  listReplenplan2,
  replenplan2Summary,
  updateReplenplan2,
} from './replenplan2.js';
import {
  createSafetystock2,
  listSafetystock2,
  safetystock2Summary,
  updateSafetystock2,
} from './safetystock2.js';
import {
  ackArtery2Flag, buildArtery2, busyArtery2Asn, closeArtery2Dock, runArtery2Inbound, runArtery2Sweep,
} from './artery2.js';
import {
  createSupplierkpi2,
  listSupplierkpi2,
  supplierkpi2Summary,
  updateSupplierkpi2,
} from './supplierkpi2.js';
import {
  createCmdpulse2,
  listCmdpulse2,
  cmdpulse2Summary,
  updateCmdpulse2,
} from './cmdpulse2.js';
import {
  createBoardpulse2,
  listBoardpulse2,
  boardpulse2Summary,
  updateBoardpulse2,
} from './boardpulse2.js';
import {
  createRiskheat2,
  listRiskheat2,
  riskheat2Summary,
  updateRiskheat2,
} from './riskheat2.js';
import {
  createCashpulse2,
  listCashpulse2,
  cashpulse2Summary,
  updateCashpulse2,
} from './cashpulse2.js';
import {
  createOpsheat2,
  listOpsheat2,
  opsheat2Summary,
  updateOpsheat2,
} from './opsheat2.js';
import {
  createGuestheat2,
  listGuestheat2,
  guestheat2Summary,
  updateGuestheat2,
} from './guestheat2.js';
import {
  createBrandheat2,
  listBrandheat2,
  brandheat2Summary,
  updateBrandheat2,
} from './brandheat2.js';
import {
  createAgentpulse2,
  listAgentpulse2,
  agentpulse2Summary,
  updateAgentpulse2,
} from './agentpulse2.js';
import {
  createSystempulse2,
  listSystempulse2,
  systempulse2Summary,
  updateSystempulse2,
} from './systempulse2.js';
import {
  createAlertfuse2,
  listAlertfuse2,
  alertfuse2Summary,
  updateAlertfuse2,
} from './alertfuse2.js';
import {
  createWarbrief2,
  listWarbrief2,
  warbrief2Summary,
  updateWarbrief2,
} from './warbrief2.js';
import {
  createDecisionhub2,
  listDecisionhub2,
  decisionhub2Summary,
  updateDecisionhub2,
} from './decisionhub2.js';
import {
  createSealnote2,
  listSealnote2,
  sealnote2Summary,
  updateSealnote2,
} from './sealnote2.js';
import {
  ackDominion2Flag, buildDominion2, busyDominion2Cash, liveDominion2Supplier, runDominion2Board, runDominion2Sweep,
} from './dominion2.js';
import {
  createLegacyflag2,
  listLegacyflag2,
  legacyflag2Summary,
  updateLegacyflag2,
} from './legacyflag2.js';
import {
  createCalmroom2,
  listCalmroom2,
  calmroom2Summary,
  updateCalmroom2,
} from './calmroom2.js';
import {
  createQuiethours2,
  listQuiethours2,
  quiethours2Summary,
  updateQuiethours2,
} from './quiethours2.js';
import {
  createScentmood2,
  listScentmood2,
  scentmood2Summary,
  updateScentmood2,
} from './scentmood2.js';
import {
  createPillowmenu3,
  listPillowmenu3,
  pillowmenu3Summary,
  updatePillowmenu3,
} from './pillowmenu3.js';
import {
  createBathritual2,
  listBathritual2,
  bathritual2Summary,
  updateBathritual2,
} from './bathritual2.js';
import {
  createSleepscore2,
  listSleepscore2,
  sleepscore2Summary,
  updateSleepscore2,
} from './sleepscore2.js';
import {
  createWelcomeamen2,
  listWelcomeamen2,
  welcomeamen2Summary,
  updateWelcomeamen2,
} from './welcomeamen2.js';
import {
  createFarewell2,
  listFarewell2,
  farewell2Summary,
  updateFarewell2,
} from './farewell2.js';
import {
  createMemorybook2,
  listMemorybook2,
  memorybook2Summary,
  updateMemorybook2,
} from './memorybook2.js';
import {
  createCarecall2,
  listCarecall2,
  carecall2Summary,
  updateCarecall2,
} from './carecall2.js';
import {
  createSurprisegift2,
  listSurprisegift2,
  surprisegift2Summary,
  updateSurprisegift2,
} from './surprisegift2.js';
import {
  createLoyaltyhug2,
  listLoyaltyhug2,
  loyaltyhug2Summary,
  updateLoyaltyhug2,
} from './loyaltyhug2.js';
import {
  createFeedbackloop2,
  listFeedbackloop2,
  feedbackloop2Summary,
  updateFeedbackloop2,
} from './feedbackloop2.js';
import {
  ackSerenity2Flag, buildSerenity2, closeSerenity2Legacy, liveSerenity2Quiet, runSerenity2Pillow, runSerenity2Sweep,
} from './serenity2.js';
import {
  createMomentmap2,
  listMomentmap2,
  momentmap2Summary,
  updateMomentmap2,
} from './momentmap2.js';
import {
  createApigateway2,
  listApigateway2,
  apigateway2Summary,
  updateApigateway2,
} from './apigateway2.js';
import {
  createWebhookhub2,
  listWebhookhub2,
  webhookhub2Summary,
  updateWebhookhub2,
} from './webhookhub2.js';
import {
  createRatelimit22,
  listRatelimit22,
  ratelimit22Summary,
  updateRatelimit22,
} from './ratelimit22.js';
import {
  createSchemareg2,
  listSchemareg2,
  schemareg2Summary,
  updateSchemareg2,
} from './schemareg2.js';
import {
  createEventbus2,
  listEventbus2,
  eventbus2Summary,
  updateEventbus2,
} from './eventbus2.js';
import {
  createJobqueue22,
  listJobqueue22,
  jobqueue22Summary,
  updateJobqueue22,
} from './jobqueue22.js';
import {
  createCachemesh2,
  listCachemesh2,
  cachemesh2Summary,
  updateCachemesh2,
} from './cachemesh2.js';
import {
  createCdnedge2,
  listCdnedge2,
  cdnedge2Summary,
  updateCdnedge2,
} from './cdnedge2.js';
import {
  createObservemap2,
  listObservemap2,
  observemap2Summary,
  updateObservemap2,
} from './observemap2.js';
import {
  createErrorbudget3,
  listErrorbudget3,
  errorbudget3Summary,
  updateErrorbudget3,
} from './errorbudget3.js';
import {
  createFeaturegate2,
  listFeaturegate2,
  featuregate2Summary,
  updateFeaturegate2,
} from './featuregate2.js';
import {
  createCanaryrun2,
  listCanaryrun2,
  canaryrun2Summary,
  updateCanaryrun2,
} from './canaryrun2.js';
import {
  createRollback2,
  listRollback2,
  rollback2Summary,
  updateRollback2,
} from './rollback2.js';
import {
  ackCircuit2Flag, buildCircuit2, busyCircuit2Moment, closeCircuit2Hook, liveCircuit2Schema, runCircuit2Sweep,
} from './circuit2.js';
import {
  createChaosdrill2,
  listChaosdrill2,
  chaosdrill2Summary,
  updateChaosdrill2,
} from './chaosdrill2.js';
import {
  createMemberhub2,
  listMemberhub2,
  memberhub2Summary,
  updateMemberhub2,
} from './memberhub2.js';
import {
  createCircle2,
  listCircle2,
  circle2Summary,
  updateCircle2,
} from './circle2.js';
import {
  createMeetup2,
  listMeetup2,
  meetup2Summary,
  updateMeetup2,
} from './meetup2.js';
import {
  createForummod2,
  listForummod2,
  forummod2Summary,
  updateForummod2,
} from './forummod2.js';
import {
  createPolldesk2,
  listPolldesk2,
  polldesk2Summary,
  updatePolldesk2,
} from './polldesk2.js';
import {
  createBadgeearn2,
  listBadgeearn2,
  badgeearn2Summary,
  updateBadgeearn2,
} from './badgeearn2.js';
import {
  createQuestline2,
  listQuestline2,
  questline2Summary,
  updateQuestline2,
} from './questline2.js';
import {
  createVolunteer2,
  listVolunteer2,
  volunteer2Summary,
  updateVolunteer2,
} from './volunteer2.js';
import {
  createDonation2,
  listDonation2,
  donation2Summary,
  updateDonation2,
} from './donation2.js';
import {
  createChapter2,
  listChapter2,
  chapter2Summary,
  updateChapter2,
} from './chapter2.js';
import {
  createAmbassador22,
  listAmbassador22,
  ambassador22Summary,
  updateAmbassador22,
} from './ambassador22.js';
import {
  createStorywall2,
  listStorywall2,
  storywall2Summary,
  updateStorywall2,
} from './storywall2.js';
import {
  createRitualcal2,
  listRitualcal2,
  ritualcal2Summary,
  updateRitualcal2,
} from './ritualcal2.js';
import {
  ackAgora2Flag, buildAgora2, busyAgora2Circle, liveAgora2Badge, runAgora2Drill, runAgora2Sweep,
} from './agora2.js';
import {
  createCohort2,
  listCohort2,
  cohort2Summary,
  updateCohort2,
} from './cohort2.js';
import {
  createLabbench2,
  listLabbench2,
  labbench2Summary,
  updateLabbench2,
} from './labbench2.js';
import {
  createPilotrun2,
  listPilotrun2,
  pilotrun2Summary,
  updatePilotrun2,
} from './pilotrun2.js';
import {
  createPrototype2,
  listPrototype2,
  prototype2Summary,
  updatePrototype2,
} from './prototype2.js';
import {
  createHypothesis2,
  listHypothesis2,
  hypothesis2Summary,
  updateHypothesis2,
} from './hypothesis2.js';
import {
  createMetricslab2,
  listMetricslab2,
  metricslab2Summary,
  updateMetricslab2,
} from './metricslab2.js';
import {
  createUserboard2,
  listUserboard2,
  userboard2Summary,
  updateUserboard2,
} from './userboard2.js';
import {
  createPatentdesk2,
  listPatentdesk2,
  patentdesk2Summary,
  updatePatentdesk2,
} from './patentdesk2.js';
import {
  createSandbox2,
  listSandbox2,
  sandbox2Summary,
  updateSandbox2,
} from './sandbox2.js';
import {
  createHackday2,
  listHackday2,
  hackday2Summary,
  updateHackday2,
} from './hackday2.js';
import {
  createIncubate2,
  listIncubate2,
  incubate2Summary,
  updateIncubate2,
} from './incubate2.js';
import {
  createSpindesk2,
  listSpindesk2,
  spindesk2Summary,
  updateSpindesk2,
} from './spindesk2.js';
import {
  createResearchnote2,
  listResearchnote2,
  researchnote2Summary,
  updateResearchnote2,
} from './researchnote2.js';
import {
  createLabbudget2,
  listLabbudget2,
  labbudget2Summary,
  updateLabbudget2,
} from './labbudget2.js';
import {
  ackCrucible2Flag, buildCrucible2, busyCrucible2Learn, liveCrucible2Pilot, runCrucible2Sweep, shipCrucible2Lab,
} from './crucible2.js';
import {
  createIpvault2,
  listIpvault2,
  ipvault2Summary,
  updateIpvault2,
} from './ipvault2.js';
import {
  createLegaldesk2,
  listLegaldesk2,
  legaldesk2Summary,
  updateLegaldesk2,
} from './legaldesk2.js';
import {
  createRiskreg2,
  listRiskreg2,
  riskreg2Summary,
  updateRiskreg2,
} from './riskreg2.js';
import {
  createPolicyhub2,
  listPolicyhub2,
  policyhub2Summary,
  updatePolicyhub2,
} from './policyhub2.js';
import {
  createClaimdesk2,
  listClaimdesk2,
  claimdesk2Summary,
  updateClaimdesk2,
} from './claimdesk2.js';
import {
  createInsurancet2,
  listInsurancet2,
  insurancet2Summary,
  updateInsurancet2,
} from './insurancet2.js';
import {
  createLitigation2,
  listLitigation2,
  litigation2Summary,
  updateLitigation2,
} from './litigation2.js';
import {
  createCompliance22,
  listCompliance22,
  compliance22Summary,
  updateCompliance22,
} from './compliance22.js';
import {
  createEthicsline2,
  listEthicsline2,
  ethicsline2Summary,
  updateEthicsline2,
} from './ethicsline2.js';
import {
  createKycrow2,
  listKycrow2,
  kycrow2Summary,
  updateKycrow2,
} from './kycrow2.js';
import {
  createSanctions2,
  listSanctions2,
  sanctions2Summary,
  updateSanctions2,
} from './sanctions2.js';
import {
  createDatapriv2,
  listDatapriv2,
  datapriv2Summary,
  updateDatapriv2,
} from './datapriv2.js';
import {
  createRetentionpol2,
  listRetentionpol2,
  retentionpol2Summary,
  updateRetentionpol2,
} from './retentionpol2.js';
import {
  createAuditevidence2,
  listAuditevidence2,
  auditevidence2Summary,
  updateAuditevidence2,
} from './auditevidence2.js';
import {
  ackCharter2Flag, buildCharter2, closeCharter2Ethics, doneCharter2Claim, liveCharter2Risk, runCharter2Sweep,
} from './charter2.js';
import {
  createBoardresolve2,
  listBoardresolve2,
  boardresolve2Summary,
  updateBoardresolve2,
} from './boardresolve2.js';
import {
  createDrplan2,
  listDrplan2,
  drplan2Summary,
  updateDrplan2,
} from './drplan2.js';
import {
  createBackupjob2,
  listBackupjob2,
  backupjob2Summary,
  updateBackupjob2,
} from './backupjob2.js';
import {
  createFailover3,
  listFailover3,
  failover3Summary,
  updateFailover3,
} from './failover3.js';
import {
  createRunbook2,
  listRunbook2,
  runbook2Summary,
  updateRunbook2,
} from './runbook2.js';
import {
  createWarroom22,
  listWarroom22,
  warroom22Summary,
  updateWarroom22,
} from './warroom22.js';
import {
  createCommsbridge22,
  listCommsbridge22,
  commsbridge22Summary,
  updateCommsbridge22,
} from './commsbridge22.js';
import {
  createSiteevac2,
  listSiteevac2,
  siteevac2Summary,
  updateSiteevac2,
} from './siteevac2.js';
import {
  createColdsite2,
  listColdsite2,
  coldsite2Summary,
  updateColdsite2,
} from './coldsite2.js';
import {
  createHotspare2,
  listHotspare2,
  hotspare2Summary,
  updateHotspare2,
} from './hotspare2.js';
import {
  createDrillscore2,
  listDrillscore2,
  drillscore2Summary,
  updateDrillscore2,
} from './drillscore2.js';
import {
  createVendorfail2,
  listVendorfail2,
  vendorfail2Summary,
  updateVendorfail2,
} from './vendorfail2.js';
import {
  createPowercut2,
  listPowercut2,
  powercut2Summary,
  updatePowercut2,
} from './powercut2.js';
import {
  createNetsplit2,
  listNetsplit2,
  netsplit2Summary,
  updateNetsplit2,
} from './netsplit2.js';
import {
  ackPhoenix2Flag, buildPhoenix2, closePhoenix2Backup, closePhoenix2Drill, livePhoenix2Runbook, runPhoenix2Sweep,
} from './phoenix2.js';
import {
  createAccessgate3,
  listAccessgate3,
  accessgate3Summary,
  updateAccessgate3,
} from './accessgate3.js';
import {
  createIdproof3,
  listIdproof3,
  idproof3Summary,
  updateIdproof3,
} from './idproof3.js';
import {
  createRolegrant3,
  listRolegrant3,
  rolegrant3Summary,
  updateRolegrant3,
} from './rolegrant3.js';
import {
  createSessionguard3,
  listSessionguard3,
  sessionguard3Summary,
  updateSessionguard3,
} from './sessionguard3.js';
import {
  createDevicetrust3,
  listDevicetrust3,
  devicetrust3Summary,
  updateDevicetrust3,
} from './devicetrust3.js';
import {
  createSecretvault3,
  listSecretvault3,
  secretvault3Summary,
  updateSecretvault3,
} from './secretvault3.js';
import {
  createMfareg3,
  listMfareg3,
  mfareg3Summary,
  updateMfareg3,
} from './mfareg3.js';
import {
  createSsobridge3,
  listSsobridge3,
  ssobridge3Summary,
  updateSsobridge3,
} from './ssobridge3.js';
import {
  createPrivacypol3,
  listPrivacypol3,
  privacypol3Summary,
  updatePrivacypol3,
} from './privacypol3.js';
import {
  createConsentrow3,
  listConsentrow3,
  consentrow3Summary,
  updateConsentrow3,
} from './consentrow3.js';
import {
  createBreachlog3,
  listBreachlog3,
  breachlog3Summary,
  updateBreachlog3,
} from './breachlog3.js';
import {
  createSocqueue3,
  listSocqueue3,
  socqueue3Summary,
  updateSocqueue3,
} from './socqueue3.js';
import {
  createPatchdesk3,
  listPatchdesk3,
  patchdesk3Summary,
  updatePatchdesk3,
} from './patchdesk3.js';
import {
  createZerohour3,
  listZerohour3,
  zerohour3Summary,
  updateZerohour3,
} from './zerohour3.js';
import {
  ackElysiumFlag, approveElysiumRole, archiveElysiumBreach, buildElysium, closeElysiumAccess, runElysiumSweep,
} from './elysium.js';
import {
  createPartnerdesk3,
  listPartnerdesk3,
  partnerdesk3Summary,
  updatePartnerdesk3,
} from './partnerdesk3.js';
import {
  createFranchise3,
  listFranchise3,
  franchise3Summary,
  updateFranchise3,
} from './franchise3.js';
import {
  createChannelkit3,
  listChannelkit3,
  channelkit3Summary,
  updateChannelkit3,
} from './channelkit3.js';
import {
  createRebate3,
  listRebate3,
  rebate3Summary,
  updateRebate3,
} from './rebate3.js';
import {
  createCoinvest3,
  listCoinvest3,
  coinvest3Summary,
  updateCoinvest3,
} from './coinvest3.js';
import {
  createSlatrack3,
  listSlatrack3,
  slatrack3Summary,
  updateSlatrack3,
} from './slatrack3.js';
import {
  createJointpromo3,
  listJointpromo3,
  jointpromo3Summary,
  updateJointpromo3,
} from './jointpromo3.js';
import {
  createLeadshare3,
  listLeadshare3,
  leadshare3Summary,
  updateLeadshare3,
} from './leadshare3.js';
import {
  createB2border3,
  listB2border3,
  b2border3Summary,
  updateB2border3,
} from './b2border3.js';
import {
  createWholesale3,
  listWholesale3,
  wholesale3Summary,
  updateWholesale3,
} from './wholesale3.js';
import {
  createDealroom3,
  listDealroom3,
  dealroom3Summary,
  updateDealroom3,
} from './dealroom3.js';
import {
  createContractrow3,
  listContractrow3,
  contractrow3Summary,
  updateContractrow3,
} from './contractrow3.js';
import {
  createCommission3,
  listCommission3,
  commission3Summary,
  updateCommission3,
} from './commission3.js';
import {
  createOnboardkit3,
  listOnboardkit3,
  onboardkit3Summary,
  updateOnboardkit3,
} from './onboardkit3.js';
import {
  ackAetherFlag,
  activateAetherPartner,
  buildAether,
  closeAetherDeal,
  liveAetherCoinvest,
  runAetherSweep,
} from './aether.js';
import {
  createInboundpo3,
  listInboundpo3,
  inboundpo3Summary,
  updateInboundpo3,
} from './inboundpo3.js';
import {
  createOutboundso3,
  listOutboundso3,
  outboundso3Summary,
  updateOutboundso3,
} from './outboundso3.js';
import {
  createAsntrack3,
  listAsntrack3,
  asntrack3Summary,
  updateAsntrack3,
} from './asntrack3.js';
import {
  createDockyard3,
  listDockyard3,
  dockyard3Summary,
  updateDockyard3,
} from './dockyard3.js';
import {
  createCrossdock3,
  listCrossdock3,
  crossdock3Summary,
  updateCrossdock3,
} from './crossdock3.js';
import {
  createColdchain23,
  listColdchain23,
  coldchain23Summary,
  updateColdchain23,
} from './coldchain23.js';
import {
  createSlotbook3,
  listSlotbook3,
  slotbook3Summary,
  updateSlotbook3,
} from './slotbook3.js';
import {
  createCarrierbid3,
  listCarrierbid3,
  carrierbid3Summary,
  updateCarrierbid3,
} from './carrierbid3.js';
import {
  createFreightbill3,
  listFreightbill3,
  freightbill3Summary,
  updateFreightbill3,
} from './freightbill3.js';
import {
  createMilestonet3,
  listMilestonet3,
  milestonet3Summary,
  updateMilestonet3,
} from './milestonet3.js';
import {
  createExceptionlog3,
  listExceptionlog3,
  exceptionlog3Summary,
  updateExceptionlog3,
} from './exceptionlog3.js';
import {
  createInventoryage3,
  listInventoryage3,
  inventoryage3Summary,
  updateInventoryage3,
} from './inventoryage3.js';
import {
  createReplenplan3,
  listReplenplan3,
  replenplan3Summary,
  updateReplenplan3,
} from './replenplan3.js';
import {
  createSafetystock3,
  listSafetystock3,
  safetystock3Summary,
  updateSafetystock3,
} from './safetystock3.js';
import {
  ackHeliosFlag, buildHelios, busyHeliosAsn, liveHeliosSlot, runHeliosInbound, runHeliosSweep,
} from './helios.js';
import {
  createSupplierkpi3,
  listSupplierkpi3,
  supplierkpi3Summary,
  updateSupplierkpi3,
} from './supplierkpi3.js';
import {
  createCmdpulse3,
  listCmdpulse3,
  cmdpulse3Summary,
  updateCmdpulse3,
} from './cmdpulse3.js';
import {
  createBoardpulse3,
  listBoardpulse3,
  boardpulse3Summary,
  updateBoardpulse3,
} from './boardpulse3.js';
import {
  createRiskheat3,
  listRiskheat3,
  riskheat3Summary,
  updateRiskheat3,
} from './riskheat3.js';
import {
  createCashpulse3,
  listCashpulse3,
  cashpulse3Summary,
  updateCashpulse3,
} from './cashpulse3.js';
import {
  createOpsheat3,
  listOpsheat3,
  opsheat3Summary,
  updateOpsheat3,
} from './opsheat3.js';
import {
  createGuestheat3,
  listGuestheat3,
  guestheat3Summary,
  updateGuestheat3,
} from './guestheat3.js';
import {
  createBrandheat3,
  listBrandheat3,
  brandheat3Summary,
  updateBrandheat3,
} from './brandheat3.js';
import {
  createAgentpulse3,
  listAgentpulse3,
  agentpulse3Summary,
  updateAgentpulse3,
} from './agentpulse3.js';
import {
  createSystempulse3,
  listSystempulse3,
  systempulse3Summary,
  updateSystempulse3,
} from './systempulse3.js';
import {
  createAlertfuse3,
  listAlertfuse3,
  alertfuse3Summary,
  updateAlertfuse3,
} from './alertfuse3.js';
import {
  createWarbrief3,
  listWarbrief3,
  warbrief3Summary,
  updateWarbrief3,
} from './warbrief3.js';
import {
  createDecisionhub3,
  listDecisionhub3,
  decisionhub3Summary,
  updateDecisionhub3,
} from './decisionhub3.js';
import {
  createSealnote3,
  listSealnote3,
  sealnote3Summary,
  updateSealnote3,
} from './sealnote3.js';
import {
  ackSeleneFlag, buildSelene, closeSeleneHeat, liveSeleneSupplier, runSeleneBoard, runSeleneSweep,
} from './selene.js';
import {
  createLegacyflag3,
  listLegacyflag3,
  legacyflag3Summary,
  updateLegacyflag3,
} from './legacyflag3.js';
import {
  createCalmroom3,
  listCalmroom3,
  calmroom3Summary,
  updateCalmroom3,
} from './calmroom3.js';
import {
  createQuiethours3,
  listQuiethours3,
  quiethours3Summary,
  updateQuiethours3,
} from './quiethours3.js';
import {
  createScentmood3,
  listScentmood3,
  scentmood3Summary,
  updateScentmood3,
} from './scentmood3.js';
import {
  createPillowmenu4,
  listPillowmenu4,
  pillowmenu4Summary,
  updatePillowmenu4,
} from './pillowmenu4.js';
import {
  createBathritual3,
  listBathritual3,
  bathritual3Summary,
  updateBathritual3,
} from './bathritual3.js';
import {
  createSleepscore3,
  listSleepscore3,
  sleepscore3Summary,
  updateSleepscore3,
} from './sleepscore3.js';
import {
  createWelcomeamen3,
  listWelcomeamen3,
  welcomeamen3Summary,
  updateWelcomeamen3,
} from './welcomeamen3.js';
import {
  createFarewell3,
  listFarewell3,
  farewell3Summary,
  updateFarewell3,
} from './farewell3.js';
import {
  createMemorybook3,
  listMemorybook3,
  memorybook3Summary,
  updateMemorybook3,
} from './memorybook3.js';
import {
  createCarecall3,
  listCarecall3,
  carecall3Summary,
  updateCarecall3,
} from './carecall3.js';
import {
  createSurprisegift3,
  listSurprisegift3,
  surprisegift3Summary,
  updateSurprisegift3,
} from './surprisegift3.js';
import {
  createLoyaltyhug3,
  listLoyaltyhug3,
  loyaltyhug3Summary,
  updateLoyaltyhug3,
} from './loyaltyhug3.js';
import {
  createFeedbackloop3,
  listFeedbackloop3,
  feedbackloop3Summary,
  updateFeedbackloop3,
} from './feedbackloop3.js';
import {
  ackGaiaFlag, buildGaia, closeGaiaLegacy, liveGaiaQuiet, runGaiaPillow, runGaiaSweep,
} from './gaia.js';
import {
  createMomentmap3,
  listMomentmap3,
  momentmap3Summary,
  updateMomentmap3,
} from './momentmap3.js';
import {
  createApigateway3,
  listApigateway3,
  apigateway3Summary,
  updateApigateway3,
} from './apigateway3.js';
import {
  createWebhookhub3,
  listWebhookhub3,
  webhookhub3Summary,
  updateWebhookhub3,
} from './webhookhub3.js';
import {
  createRatelimit23,
  listRatelimit23,
  ratelimit23Summary,
  updateRatelimit23,
} from './ratelimit23.js';
import {
  createSchemareg3,
  listSchemareg3,
  schemareg3Summary,
  updateSchemareg3,
} from './schemareg3.js';
import {
  createEventbus3,
  listEventbus3,
  eventbus3Summary,
  updateEventbus3,
} from './eventbus3.js';
import {
  createJobqueue23,
  listJobqueue23,
  jobqueue23Summary,
  updateJobqueue23,
} from './jobqueue23.js';
import {
  createCachemesh3,
  listCachemesh3,
  cachemesh3Summary,
  updateCachemesh3,
} from './cachemesh3.js';
import {
  createCdnedge3,
  listCdnedge3,
  cdnedge3Summary,
  updateCdnedge3,
} from './cdnedge3.js';
import {
  createObservemap3,
  listObservemap3,
  observemap3Summary,
  updateObservemap3,
} from './observemap3.js';
import {
  createErrorbudget4,
  listErrorbudget4,
  errorbudget4Summary,
  updateErrorbudget4,
} from './errorbudget4.js';
import {
  createFeaturegate3,
  listFeaturegate3,
  featuregate3Summary,
  updateFeaturegate3,
} from './featuregate3.js';
import {
  createCanaryrun3,
  listCanaryrun3,
  canaryrun3Summary,
  updateCanaryrun3,
} from './canaryrun3.js';
import {
  createRollback3,
  listRollback3,
  rollback3Summary,
  updateRollback3,
} from './rollback3.js';
import {
  ackChronosFlag, buildChronos, busyChronosMoment, closeChronosHook, liveChronosSchema, runChronosSweep,
} from './chronos.js';
import {
  createChaosdrill3,
  listChaosdrill3,
  chaosdrill3Summary,
  updateChaosdrill3,
} from './chaosdrill3.js';
import {
  createMemberhub3,
  listMemberhub3,
  memberhub3Summary,
  updateMemberhub3,
} from './memberhub3.js';
import {
  createCircle3,
  listCircle3,
  circle3Summary,
  updateCircle3,
} from './circle3.js';
import {
  createMeetup3,
  listMeetup3,
  meetup3Summary,
  updateMeetup3,
} from './meetup3.js';
import {
  createForummod3,
  listForummod3,
  forummod3Summary,
  updateForummod3,
} from './forummod3.js';
import {
  createPolldesk3,
  listPolldesk3,
  polldesk3Summary,
  updatePolldesk3,
} from './polldesk3.js';
import {
  createBadgeearn3,
  listBadgeearn3,
  badgeearn3Summary,
  updateBadgeearn3,
} from './badgeearn3.js';
import {
  createQuestline3,
  listQuestline3,
  questline3Summary,
  updateQuestline3,
} from './questline3.js';
import {
  createVolunteer3,
  listVolunteer3,
  volunteer3Summary,
  updateVolunteer3,
} from './volunteer3.js';
import {
  createDonation3,
  listDonation3,
  donation3Summary,
  updateDonation3,
} from './donation3.js';
import {
  createChapter3,
  listChapter3,
  chapter3Summary,
  updateChapter3,
} from './chapter3.js';
import {
  createAmbassador23,
  listAmbassador23,
  ambassador23Summary,
  updateAmbassador23,
} from './ambassador23.js';
import {
  createStorywall3,
  listStorywall3,
  storywall3Summary,
  updateStorywall3,
} from './storywall3.js';
import {
  createRitualcal3,
  listRitualcal3,
  ritualcal3Summary,
  updateRitualcal3,
} from './ritualcal3.js';
import {
  ackKairosFlag, buildKairos, busyKairosCircle, liveKairosBadge, runKairosDrill, runKairosSweep,
} from './kairos.js';
import {
  createCohort3,
  listCohort3,
  cohort3Summary,
  updateCohort3,
} from './cohort3.js';
import {
  createLabbench3,
  listLabbench3,
  labbench3Summary,
  updateLabbench3,
} from './labbench3.js';
import {
  createPilotrun3,
  listPilotrun3,
  pilotrun3Summary,
  updatePilotrun3,
} from './pilotrun3.js';
import {
  createPrototype3,
  listPrototype3,
  prototype3Summary,
  updatePrototype3,
} from './prototype3.js';
import {
  createHypothesis3,
  listHypothesis3,
  hypothesis3Summary,
  updateHypothesis3,
} from './hypothesis3.js';
import {
  createMetricslab3,
  listMetricslab3,
  metricslab3Summary,
  updateMetricslab3,
} from './metricslab3.js';
import {
  createUserboard3,
  listUserboard3,
  userboard3Summary,
  updateUserboard3,
} from './userboard3.js';
import {
  createPatentdesk3,
  listPatentdesk3,
  patentdesk3Summary,
  updatePatentdesk3,
} from './patentdesk3.js';
import {
  createSandbox3,
  listSandbox3,
  sandbox3Summary,
  updateSandbox3,
} from './sandbox3.js';
import {
  createHackday3,
  listHackday3,
  hackday3Summary,
  updateHackday3,
} from './hackday3.js';
import {
  createIncubate3,
  listIncubate3,
  incubate3Summary,
  updateIncubate3,
} from './incubate3.js';
import {
  createSpindesk3,
  listSpindesk3,
  spindesk3Summary,
  updateSpindesk3,
} from './spindesk3.js';
import {
  createResearchnote3,
  listResearchnote3,
  researchnote3Summary,
  updateResearchnote3,
} from './researchnote3.js';
import {
  createLabbudget3,
  listLabbudget3,
  labbudget3Summary,
  updateLabbudget3,
} from './labbudget3.js';
import {
  ackLogosFlag, buildLogos, busyLogosLearn, liveLogosPilot, runLogosSweep, shipLogosLab,
} from './logos.js';
import {
  createIpvault3,
  listIpvault3,
  ipvault3Summary,
  updateIpvault3,
} from './ipvault3.js';
import {
  createLegaldesk3,
  listLegaldesk3,
  legaldesk3Summary,
  updateLegaldesk3,
} from './legaldesk3.js';
import {
  createRiskreg3,
  listRiskreg3,
  riskreg3Summary,
  updateRiskreg3,
} from './riskreg3.js';
import {
  createPolicyhub3,
  listPolicyhub3,
  policyhub3Summary,
  updatePolicyhub3,
} from './policyhub3.js';
import {
  createClaimdesk3,
  listClaimdesk3,
  claimdesk3Summary,
  updateClaimdesk3,
} from './claimdesk3.js';
import {
  createInsurancet3,
  listInsurancet3,
  insurancet3Summary,
  updateInsurancet3,
} from './insurancet3.js';
import {
  createLitigation3,
  listLitigation3,
  litigation3Summary,
  updateLitigation3,
} from './litigation3.js';
import {
  createCompliance23,
  listCompliance23,
  compliance23Summary,
  updateCompliance23,
} from './compliance23.js';
import {
  createEthicsline3,
  listEthicsline3,
  ethicsline3Summary,
  updateEthicsline3,
} from './ethicsline3.js';
import {
  createKycrow3,
  listKycrow3,
  kycrow3Summary,
  updateKycrow3,
} from './kycrow3.js';
import {
  createSanctions3,
  listSanctions3,
  sanctions3Summary,
  updateSanctions3,
} from './sanctions3.js';
import {
  createDatapriv3,
  listDatapriv3,
  datapriv3Summary,
  updateDatapriv3,
} from './datapriv3.js';
import {
  createRetentionpol3,
  listRetentionpol3,
  retentionpol3Summary,
  updateRetentionpol3,
} from './retentionpol3.js';
import {
  createAuditevidence3,
  listAuditevidence3,
  auditevidence3Summary,
  updateAuditevidence3,
} from './auditevidence3.js';
import {
  ackPathosFlag, buildPathos, closePathosIp, livePathosRisk, runPathosClaim, runPathosSweep,
} from './pathos.js';
import {
  createBoardresolve3,
  listBoardresolve3,
  boardresolve3Summary,
  updateBoardresolve3,
} from './boardresolve3.js';
import {
  createDrplan3,
  listDrplan3,
  drplan3Summary,
  updateDrplan3,
} from './drplan3.js';
import {
  createBackupjob3,
  listBackupjob3,
  backupjob3Summary,
  updateBackupjob3,
} from './backupjob3.js';
import {
  createFailover4,
  listFailover4,
  failover4Summary,
  updateFailover4,
} from './failover4.js';
import {
  createRunbook3,
  listRunbook3,
  runbook3Summary,
  updateRunbook3,
} from './runbook3.js';
import {
  createWarroom23,
  listWarroom23,
  warroom23Summary,
  updateWarroom23,
} from './warroom23.js';
import {
  createCommsbridge23,
  listCommsbridge23,
  commsbridge23Summary,
  updateCommsbridge23,
} from './commsbridge23.js';
import {
  createSiteevac3,
  listSiteevac3,
  siteevac3Summary,
  updateSiteevac3,
} from './siteevac3.js';
import {
  createColdsite3,
  listColdsite3,
  coldsite3Summary,
  updateColdsite3,
} from './coldsite3.js';
import {
  createHotspare3,
  listHotspare3,
  hotspare3Summary,
  updateHotspare3,
} from './hotspare3.js';
import {
  createDrillscore3,
  listDrillscore3,
  drillscore3Summary,
  updateDrillscore3,
} from './drillscore3.js';
import {
  createVendorfail3,
  listVendorfail3,
  vendorfail3Summary,
  updateVendorfail3,
} from './vendorfail3.js';
import {
  createPowercut3,
  listPowercut3,
  powercut3Summary,
  updatePowercut3,
} from './powercut3.js';
import {
  createNetsplit3,
  listNetsplit3,
  netsplit3Summary,
  updateNetsplit3,
} from './netsplit3.js';
import {
  ackApotheosisFlag, buildApotheosis, closeApotheosisBackup, closeApotheosisDrill, liveApotheosisRunbook, runApotheosisSweep,
} from './apotheosis.js';
import {
  applyExtremeWeatherHold,
  cancelExtremeReservation,
  checkInExtremeReservation,
  clearExtremeWeatherHold,
  createExtremeMaas,
  expireExtremeWaitlist,
  extremeOverview,
  extremeSlotWeatherCheck,
  extremeUserSpec,
  extremeWalletSpend,
  issueExtremeGear,
  joinExtremeWaitlist,
  markExtremeNoShow,
  promoteExtremeWaitlist,
  renewExtremeMaas,
  reserveExtremeSlot,
  returnExtremeGear,
  runExtremeGearServiceSweep,
  runExtremeWeatherHoldSweep,
  signExtremeWaiver,
  topUpExtremeWallet,
  updateExtremeGear,
} from './extremepark.js';
import {
  addCampusIncident,
  assignCampusWorkOrder,
  campusCapacityRollup,
  campusCoreOverview,
  clearCampusZoneLockdown,
  completeCampusWorkOrder,
  createCampusWorkOrder,
  escalateCampusIncident,
  escalateCampusWorkOrder,
  lockdownCampusZone,
  resolveCampusIncident,
  runCampusCapacityAlertSweep,
  runCampusWorkOrderSweep,
  startCampusWorkOrder,
  transitionCampusZone,
  updateCampusZone,
} from './campuscore.js';
import {
  autoPostStayFolio,
  checkoutStay,
  completeStayGuestRequest,
  completeStayHk,
  createStayBooking,
  createStayGuestRequest,
  createStayHkTask,
  disputeStayFolioCharge,
  flagStayOverstay,
  issueStayKeyless,
  postStayFolioCharge,
  resolveStayOverstay,
  revokeStayKeyless,
  runStayNightAudit,
  scheduleStayLateCheckout,
  setStayWintering,
  settleStayFolio,
  stayNightRollup,
  stayRingOverview,
  updateStayUnit,
} from './stayring.js';
import {
  advanceReturnToPlay,
  assignAthleteCoach,
  athleteOsOverview,
  athleteReadinessRollup,
  clearAthleteForCompetition,
  clearAthleteMedicalHold,
  issueAthleteLicense,
  logAthleteSession,
  placeAthleteMedicalHold,
  registerAthleteCompetition,
  reportAthleteInjury,
  runAthleteCompetitionClearanceSweep,
  runAthleteRtpSweep,
  setAthleteClearance,
  upsertAthletePlan,
} from './athleteos.js';
import {
  clearLifeCrisis,
  completeLifeFollowUp,
  createLifePlan,
  flagLifeCrisis,
  ingestWearable,
  ingestWearableWebhook,
  lifeCoachCheckIn,
  lifeCoachOverview,
  lifeWeeklyDigest,
  processLifeFlags,
  registerLifeDevice,
  runLifeMissedCheckInSweep,
  scheduleLifeFollowUps,
  scoreLifePlanAdherence,
  verifyLifeWebhookSignature,
} from './lifecoach.js';
import {
  assessMarketRentalDamage,
  createMarketListing,
  createMarketPurchaseOrder,
  flagMarketRentalOverdue,
  marketCheckout,
  marketOsOverview,
  receiveMarketPurchaseOrder,
  reconcileMarketChannels,
  restockMarketListing,
  returnMarketRental,
  runMarketLowStockSweep,
  runMarketRentalSweep,
  settleMarketDeposit,
  syncMarketChannel,
} from './marketos.js';
import {
  disputeMallInvoice,
  generateMallCamRun,
  generateMallRentRun,
  holdMallLease,
  mallDayRollup,
  openMallOverview,
  pauseMallTenant,
  payMallInvoice,
  recordMallSale,
  releaseMallLease,
  resumeMallTenant,
  runMallDunningSweep,
  settleMallTenantFnb,
  updateMallTenant,
} from './openmall.js';
import {
  assignFamilyStaff,
  authorizedFamilyCheckout,
  bookFamilyProgram,
  cancelFamilyProgramBooking,
  familyCampOverview,
  familyCheckIn,
  familyCheckOut,
  familyEmergencyNote,
  flagFamilyPickupNoShow,
  issueFamilyPickupCode,
  runFamilyPickupExpirySweep,
  runFamilyRollCall,
  runFamilySafetySweep,
  runFamilyStaffRatioSweep,
  transferFamilyChild,
} from './familycamp.js';
import {
  agentBridgeBroadcast,
  agentBridgeOverview,
  agentBridgePing,
  closeAgentBridgeChannel,
  escalateAgentBridgeAlert,
  muteAgentBridgeAlert,
  openAgentBridgeChannel,
  pulseAgentBridgeChannel,
  resolveAgentBridgeAlert,
  routeAgentBridgeAlert,
  runAgentBridgeAlertSlaSweep,
  snoozeAgentBridgeChannel,
  unmuteAgentBridgeAlerts,
  wakeSnoozedAgentBridgeChannels,
} from './agentbridge.js';
import {
  ackCultureCrewCall,
  callCultureCrew,
  confirmCultureTicket,
  createCultureEvent,
  cultureBoxOfficeRollup,
  cultureSceneOverview,
  denyCultureDoor,
  endCultureStream,
  expireCultureHolds,
  holdCultureTicket,
  pulseCultureStream,
  refundCultureSale,
  releaseCultureHold,
  scanCultureDoor,
  setCultureLive,
  setCultureStageStatus,
  settleCultureEvent,
  startCultureStream,
  transferCultureHold,
  upgradeCultureSaleVip,
} from './culturescene.js';
import {
  applySportCompetitionHold,
  archiveSportBridgeLink,
  bridgeRecoveryPlan,
  completeBridgeRecovery,
  escalateSportGate,
  gateSportSlotAccess,
  linkSportProfiles,
  runSportEligibilitySweep,
  runSportPostCompSweep,
  snoozeSportHold,
  sportBridgeOverview,
  syncSlotToSession,
  wakeSportHolds,
} from './sportbridge.js';
import {
  agentQueueOverview,
  archiveAgentJobs,
  bumpAgentJobPriority,
  cancelAgentJob,
  claimAgentJob,
  completeAgentJob,
  enqueueAgentJob,
  rebalanceAgentQueue,
  reviveDeadAgentJobs,
  runAgentQueueSlaSweep,
  snoozeAgentJob,
  tickAgentQueue,
  wakeSnoozedAgentJobs,
} from './agentqueue.js';
import {
  addGreenIncident,
  approveGreenWorkPermit,
  batchRecordGreenMeters,
  clearGreenCurtailment,
  closeGreenWorkPermit,
  createGreenWorkPermit,
  greenPulseOverview,
  issueGreenCurtailment,
  recordGreenMeter,
  runGreenPermitExpirySweep,
  runGreenPulseAutomations,
  runWaterLeakTriage,
} from './greenpulse.js';
import {
  ackCampusBriefAction,
  assignCampusBriefAction,
  campusBriefOverview,
  campusHealthCheck,
  dismissCampusBriefAction,
  escalateCampusBriefAction,
  publishCampusBriefDigest,
  runCampusAutomations,
  snoozeCampusBriefAction,
  syncCampusBriefActions,
  wakeSnoozedCampusBriefActions,
} from './campusbrief.js';
import {
  acknowledgeFleetDirective,
  agentFleetOverview,
  closeFleetShift,
  dispatchFleetDirective,
  handoffFleetShift,
  parkFleetAgent,
  pingFleetAgent,
  retireFleetDirective,
  runFleetLoadBalance,
  startFleetShift,
  sweepFleetPresence,
  unparkFleetAgents,
} from './agentfleet.js';



applySettingsToEnv();
startJobTicker(5000);
// tesis / marka / misafir tohumu
listVenues();
listHolders();
listBrands();
listGuests();
listReservations();
listLoyaltyAccounts();
listIncidents();
listSuppliers();
listFeedback();
listConsents();
listAnnouncements();
listRecipes();
listChecklistTemplates();
listLostFound();
tipSummary();
listMaintenance();
listMenu();
listCampaigns();
listI18nNotes();
listColdAssets();
listHandover();
cashSummary();
listAssets();
listMeters();
listQuizzes();
listPlaybooks();
listInventory();
listShifts();
listValet();
listMusic();
listDocuments();
listVendorScores();
listSeating();
listWaitlist();
listComplaints();
listHours();
buildWeatherBrief();
buildReadiness();
listSpa();
listEventcal();
listGiftcards();
listDelivery();
listCleaning();
listLaundry();
listWifi();
listContent();
listPulse();
listBudget();
listContracts();
listPassstock();
listKds();
listEmergency();
listLockers();
listKidsclub();
listBeachbeds();
listTransfers();
listBadgeprint();
listMeetingrooms();
listMediakit();
listSustain();
listAllergens();
listWinecellar();
listLounge();
listShuttle();
listPartners();
listMysteryshop();



function sendJson(res, status, body) {
  res.statusCode = status;
  res.setHeader('Content-Type', 'application/json; charset=utf-8');
  res.setHeader('Access-Control-Allow-Origin', '*');
  res.setHeader('Access-Control-Allow-Methods', 'GET, POST, PATCH, DELETE, OPTIONS');
  res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization');
  res.setHeader('Cache-Control', 'no-store');
  res.end(JSON.stringify(body));
}

async function readBody(req) {
  const chunks = [];
  for await (const c of req) chunks.push(c);
  const raw = Buffer.concat(chunks).toString('utf8');
  if (!raw) return {};
  return JSON.parse(raw);
}

function getToken(req) {
  const h = req.headers.authorization || '';
  if (h.startsWith('Bearer ')) return h.slice(7);
  return null;
}

function requireUser(req, res) {
  const user = sessionFromToken(getToken(req));
  if (!user) {
    sendJson(res, 401, { error: 'Oturum gerekli' });
    return null;
  }
  return user;
}

function requireCeo(req, res) {
  const user = requireUser(req, res);
  if (!user) return null;
  if (user.role !== 'ceo') {
    sendJson(res, 403, { error: 'Yalnızca CEO erişebilir' });
    return null;
  }
  return user;
}

function hubSummary() {
  const archive = readCollection('archive', []);
  const whatsapp = readCollection('whatsapp', []);
  const nexus = readCollection('nexus-events', []);
  const audit = readAudit(12);
  const settings = getPublicSettings();
  const byAgent = {};
  for (const e of archive) {
    for (const a of e.agents ?? []) byAgent[a] = (byAgent[a] ?? 0) + 1;
  }
  const configuredKeys = settings.fields.filter((f) => f.configured).length;
  const jobs = jobsSummary();
  return {
    archiveCount: archive.length,
    whatsappCount: whatsapp.length,
    nexusEventCount: nexus.length,
    auditCount: readCollection('audit', []).length,
    settingsConfigured: configuredKeys,
    settingsTotal: settings.fields.length,
    jobsTotal: jobs.total,
    jobsByStatus: jobs.byStatus,
    upcomingJobs: jobs.upcoming,
    readyDirectives: jobs.readyDirectives,
    sseClients: sseClientCount(),
    recentArchive: archive.slice(0, 5),
    recentWhatsapp: whatsapp.slice(0, 5),
    recentNexus: nexus.slice(0, 8),
    recentAudit: audit,
    recentJobs: jobs.recent,
    venues: venuesSummary(),
    unreadNotifications: unreadCount(),
    health: healthCheck().status,
    agentHits: Object.entries(byAgent)
      .map(([agent, count]) => ({ agent, count }))
      .sort((a, b) => b.count - a.count)
      .slice(0, 12),
    generatedAt: new Date().toISOString(),
  };
}

/** Connect uyumlu middleware — hem Vite hem production sunucusu kullanır. */
export function createPlatformMiddleware() {
  return (req, res, next) => {
        const path = (req.url ?? '').split('?')[0];

        if (req.method === 'OPTIONS' && path.startsWith('/api/')) {
          sendJson(res, 204, {});
          return;
        }

        // Auth
        if (path === '/api/auth/demo-users' && req.method === 'GET') {
          sendJson(res, 200, { users: listDemoUsers(), rolePages: ROLE_PAGES });
          return;
        }
        if (path === '/api/auth/login' && req.method === 'POST') {
          void (async () => {
            try {
              const { username, password } = await readBody(req);
              const result = login(username, password);
              if (!result) {
                appendAudit({
                  actor: username || 'unknown',
                  action: 'auth.login_failed',
                  detail: 'Hatalı kimlik bilgisi',
                });
                sendJson(res, 401, { error: 'Kullanıcı adı veya şifre hatalı' });
                return;
              }
              appendAudit({
                actor: result.user.username,
                action: 'auth.login',
                detail: `${result.user.name} oturum açtı (${result.user.role})`,
              });
              sendJson(res, 200, result);
            } catch (err) {
              sendJson(res, 500, { error: err instanceof Error ? err.message : 'Login hatası' });
            }
          })();
          return;
        }
        if (path === '/api/auth/logout' && req.method === 'POST') {
          const user = sessionFromToken(getToken(req));
          logout(getToken(req));
          if (user) {
            appendAudit({
              actor: user.username,
              action: 'auth.logout',
              detail: 'Oturum kapatıldı',
            });
          }
          sendJson(res, 200, { ok: true });
          return;
        }
        if (path === '/api/auth/me' && req.method === 'GET') {
          const user = sessionFromToken(getToken(req));
          if (!user) {
            sendJson(res, 401, { error: 'Oturum yok' });
            return;
          }
          sendJson(res, 200, { user });
          return;
        }

        // Kalıcı arşiv
        if (path === '/api/archive' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, { entries: readCollection('archive', []) });
          return;
        }
        if (path === '/api/archive' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            try {
              const entry = await readBody(req);
              if (!entry?.id || !entry?.text) {
                sendJson(res, 400, { error: 'Geçersiz arşiv kaydı' });
                return;
              }
              const entries = prependItem('archive', entry, 200);
              appendAudit({
                actor: user.username,
                action: 'archive.save',
                detail: String(entry.text).slice(0, 100),
                meta: { id: entry.id, agents: entry.agents },
              });
              sendJson(res, 200, { ok: true, entries });
            } catch (err) {
              sendJson(res, 500, { error: err instanceof Error ? err.message : 'Arşiv yazılamadı' });
            }
          })();
          return;
        }
        if (path.startsWith('/api/archive/') && req.method === 'DELETE') {
          const user = requireUser(req, res);
          if (!user) return;
          const id = Number(path.split('/').pop());
          appendAudit({
            actor: user.username,
            action: 'archive.delete',
            detail: `Kayıt silindi: ${id}`,
          });
          sendJson(res, 200, { entries: deleteItem('archive', id) });
          return;
        }
        if (path === '/api/archive' && req.method === 'DELETE') {
          const user = requireUser(req, res);
          if (!user) return;
          clearCollection('archive');
          appendAudit({
            actor: user.username,
            action: 'archive.clear',
            detail: 'Arşiv temizlendi',
          });
          sendJson(res, 200, { entries: [] });
          return;
        }

        // Hub özeti
        if (path === '/api/hub/summary' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, hubSummary());
          return;
        }

        // Audit
        if (path === '/api/audit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          const limit = Number(url.searchParams.get('limit') || 50);
          sendJson(res, 200, { entries: readAudit(limit) });
          return;
        }

        // Ayarlar (CEO)
        if (path === '/api/settings' && req.method === 'GET') {
          if (!requireCeo(req, res)) return;
          sendJson(res, 200, getPublicSettings());
          return;
        }
        if (path === '/api/settings' && req.method === 'POST') {
          const user = requireCeo(req, res);
          if (!user) return;
          void (async () => {
            try {
              const patch = await readBody(req);
              const result = saveSettings(patch);
              appendAudit({
                actor: user.username,
                action: 'settings.update',
                detail: `Ayarlar güncellendi (${result.fields.filter((f) => f.configured).length}/${result.fields.length} yapılandırıldı)`,
              });
              sendJson(res, 200, result);
            } catch (err) {
              sendJson(res, 500, {
                error: err instanceof Error ? err.message : 'Ayarlar kaydedilemedi',
              });
            }
          })();
          return;
        }

        // Görevler / kuyruk (AŞAMA 6)
        if (path === '/api/jobs' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          sendJson(res, 200, {
            jobs: listJobs({
              status: url.searchParams.get('status') || undefined,
              kind: url.searchParams.get('kind') || undefined,
            }),
            summary: jobsSummary(),
          });
          return;
        }
        if (path === '/api/jobs' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            try {
              const body = await readBody(req);
              if (!body.kind) {
                sendJson(res, 400, { error: 'kind zorunlu (whatsapp.reminder | directive.queue)' });
                return;
              }
              if (
                body.kind !== 'whatsapp.reminder' &&
                body.kind !== 'directive.queue'
              ) {
                sendJson(res, 400, { error: 'Geçersiz kind' });
                return;
              }
              // kitchen yalnızca whatsapp; crew talimat kuyruğu oluşturamaz
              if (user.role === 'kitchen' && body.kind !== 'whatsapp.reminder') {
                sendJson(res, 403, { error: 'Mutfak yalnızca WhatsApp hatırlatması oluşturabilir' });
                return;
              }
              if (user.role === 'crew') {
                sendJson(res, 403, { error: 'Crew rolü görev oluşturamaz' });
                return;
              }
              const job = createJob({
                kind: body.kind,
                title: body.title,
                payload: body.payload,
                dueAt: body.dueAt,
                createdBy: user.username,
              });
              // dueAt geçmişse hemen tick
              if (new Date(job.dueAt).getTime() <= Date.now()) {
                const ran = await runJob(job.id, user.username);
                sendJson(res, 200, { job: ran });
                return;
              }
              sendJson(res, 200, { job });
            } catch (err) {
              sendJson(res, 500, {
                error: err instanceof Error ? err.message : 'Görev oluşturulamadı',
              });
            }
          })();
          return;
        }
        if (path === '/api/jobs/tick' && req.method === 'POST') {
          if (!requireCeo(req, res)) return;
          void (async () => {
            const results = await tickJobs();
            sendJson(res, 200, { ran: results.length, results });
          })();
          return;
        }
        if (path.startsWith('/api/jobs/') && path.endsWith('/run') && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          const id = path.split('/')[3];
          void (async () => {
            const job = await runJob(id, user.username);
            if (!job) {
              sendJson(res, 404, { error: 'Görev bulunamadı' });
              return;
            }
            sendJson(res, 200, { job });
          })();
          return;
        }
        if (path.startsWith('/api/jobs/') && path.endsWith('/cancel') && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          const id = path.split('/')[3];
          const job = cancelJob(id, user.username);
          if (!job) {
            sendJson(res, 404, { error: 'Görev bulunamadı' });
            return;
          }
          sendJson(res, 200, { job });
          return;
        }
        if (path.startsWith('/api/jobs/') && path.endsWith('/claim') && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          if (user.role === 'crew') {
            sendJson(res, 403, { error: 'Crew talimat claim edemez' });
            return;
          }
          const id = path.split('/')[3];
          void (async () => {
            const result = await claimDirective(id, user.username);
            if (!result) {
              sendJson(res, 404, { error: 'Talimat görevi bulunamadı' });
              return;
            }
            if (result.error) {
              sendJson(res, 409, { error: result.error, job: result.job });
              return;
            }
            sendJson(res, 200, { job: result.job, text: result.text });
          })();
          return;
        }
        // Operasyon raporu (AŞAMA 8)
        if (path === '/api/report' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          const format = url.searchParams.get('format') || 'json';
          const report = buildOpsReport();
          if (format === 'markdown' || format === 'md') {
            const md = reportToMarkdown(report);
            res.statusCode = 200;
            res.setHeader('Content-Type', 'text/markdown; charset=utf-8');
            res.setHeader(
              'Content-Disposition',
              `attachment; filename="likya-rapor-${report.generatedAt.slice(0, 10)}.md"`,
            );
            res.setHeader('Access-Control-Allow-Origin', '*');
            res.end(md);
            return;
          }
          sendJson(res, 200, report);
          return;
        }
        if (path === '/api/report/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runReportSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/report/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackReportFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/report/jobs/cancel' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, cancelReportJobs(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/report/ethos/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackReportEthos(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/report/audit/snapshot' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, snapshotReportAudit(await readBody(req), user.username)); })();
          return;
        }

        // SSE canlı olay akışı (AŞAMA 7) — token query ile (EventSource header desteklemez)
        if (path === '/api/events' && req.method === 'GET') {
          const url = new URL(req.url ?? '', 'http://local');
          const token = url.searchParams.get('token') || getToken(req);
          const user = sessionFromToken(token);
          if (!user) {
            sendJson(res, 401, { error: 'Oturum gerekli' });
            return;
          }
          res.writeHead(200, {
            'Content-Type': 'text/event-stream; charset=utf-8',
            'Cache-Control': 'no-cache, no-transform',
            Connection: 'keep-alive',
            'Access-Control-Allow-Origin': '*',
          });
          res.write(`: connected as ${user.username}\n\n`);
          addSseClient(res);
          return;
        }
        if (path.startsWith('/api/jobs/') && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const id = path.split('/')[3];
          const job = getJob(id);
          if (!job) {
            sendJson(res, 404, { error: 'Görev bulunamadı' });
            return;
          }
          sendJson(res, 200, { job });
          return;
        }
        if (path.startsWith('/api/jobs/') && req.method === 'DELETE') {
          const user = requireCeo(req, res);
          if (!user) return;
          const id = path.split('/')[3];
          sendJson(res, 200, { jobs: deleteJob(id) });
          return;
        }

        // ── AŞAMA 10: Tesisler ────────────────────────────────────────
        if (path === '/api/venues' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, venuesSummary());
          return;
        }
        if (path === '/api/venues' && req.method === 'POST') {
          const user = requireCeo(req, res);
          if (!user) return;
          void (async () => {
            try {
              const body = await readBody(req);
              if (!body.name) {
                sendJson(res, 400, { error: 'name zorunlu' });
                return;
              }
              const venue = createVenue(body, user.username);
              sendJson(res, 200, { venue });
            } catch (err) {
              sendJson(res, 500, {
                error: err instanceof Error ? err.message : 'Tesis oluşturulamadı',
              });
            }
          })();
          return;
        }
        if (path.startsWith('/api/venues/') && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const id = path.split('/')[3];
          const venue = getVenue(id);
          if (!venue) {
            sendJson(res, 404, { error: 'Tesis bulunamadı' });
            return;
          }
          sendJson(res, 200, { venue });
          return;
        }
        if (path.startsWith('/api/venues/') && req.method === 'PATCH') {
          const user = requireCeo(req, res);
          if (!user) return;
          void (async () => {
            const id = path.split('/')[3];
            const patch = await readBody(req);
            const venue = updateVenue(id, patch, user.username);
            if (!venue) {
              sendJson(res, 404, { error: 'Tesis bulunamadı' });
              return;
            }
            sendJson(res, 200, { venue });
          })();
          return;
        }
        if (path.startsWith('/api/venues/') && req.method === 'DELETE') {
          const user = requireCeo(req, res);
          if (!user) return;
          const id = path.split('/')[3];
          const venue = removeVenue(id, user.username);
          if (!venue) {
            sendJson(res, 404, { error: 'Tesis bulunamadı' });
            return;
          }
          sendJson(res, 200, { ok: true, venue });
          return;
        }

        // ── AŞAMA 11: Health + yedek ──────────────────────────────────
        if (path === '/api/health' && req.method === 'GET') {
          // health auth gerektirmez (load balancer / docker healthcheck)
          void healthCheckAsync().then((h) => sendJson(res, 200, h));
          return;
        }
        if (path === '/api/ops/files' && req.method === 'GET') {
          if (!requireCeo(req, res)) return;
          sendJson(res, 200, { files: listDataFiles() });
          return;
        }
        if (path === '/api/ops/backup' && req.method === 'GET') {
          const user = requireCeo(req, res);
          if (!user) return;
          const backup = createBackup();
          appendAudit({
            actor: user.username,
            action: 'ops.backup',
            detail: `Yedek alındı (${Object.keys(backup.collections).length} koleksiyon)`,
          });
          res.statusCode = 200;
          res.setHeader('Content-Type', 'application/json; charset=utf-8');
          res.setHeader(
            'Content-Disposition',
            `attachment; filename="likya-backup-${backup.createdAt.slice(0, 10)}.json"`,
          );
          res.setHeader('Access-Control-Allow-Origin', '*');
          res.end(JSON.stringify(backup, null, 2));
          return;
        }
        if (path === '/api/ops/restore' && req.method === 'POST') {
          const user = requireCeo(req, res);
          if (!user) return;
          void (async () => {
            try {
              const body = await readBody(req);
              const result = restoreBackup(body, user.username);
              sendJson(res, 200, result);
            } catch (err) {
              sendJson(res, 400, {
                error: err instanceof Error ? err.message : 'Geri yükleme başarısız',
              });
            }
          })();
          return;
        }
        if (path === '/api/ops/integrity' && req.method === 'POST') {
          const user = requireCeo(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runOpsIntegritySweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/ops/backup/rotate' && req.method === 'POST') {
          const user = requireCeo(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, rotateOpsBackup(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/ops/quarantine' && req.method === 'POST') {
          const user = requireCeo(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, quarantineOpsFile(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/ops/degraded/clear' && req.method === 'POST') {
          const user = requireCeo(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearOpsDegraded(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 12: Bildirimler ─────────────────────────────────────
        if (path === '/api/notifications' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          const unreadOnly = url.searchParams.get('unread') === '1';
          const limit = Number(url.searchParams.get('limit') || 50);
          sendJson(res, 200, {
            unread: unreadCount(),
            notifications: listNotifications(limit, unreadOnly),
          });
          return;
        }
        if (path === '/api/notifications/read-all' && req.method === 'POST') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, markAllRead());
          return;
        }
        if (path.startsWith('/api/notifications/') && path.endsWith('/read') && req.method === 'POST') {
          if (!requireUser(req, res)) return;
          const id = path.split('/')[3];
          const n = markRead(id);
          if (!n) {
            sendJson(res, 404, { error: 'Bildirim bulunamadı' });
            return;
          }
          sendJson(res, 200, { notification: n, unread: unreadCount() });
          return;
        }

        // ── AŞAMA 13: OlymposPass geçiş motoru ────────────────────────
        if (path === '/api/pass/holders' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, { holders: listHolders(), stats: passStats() });
          return;
        }
        if (path === '/api/pass/gates' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, { gates: listGates() });
          return;
        }
        if (path === '/api/pass/events' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          const limit = Number(url.searchParams.get('limit') || 40);
          sendJson(res, 200, { events: listAccessEvents(limit) });
          return;
        }
        if (path === '/api/pass/verify' && req.method === 'POST') {
          if (!requireUser(req, res)) return;
          void (async () => {
            try {
              const body = await readBody(req);
              sendJson(res, 200, verifyPass(body));
            } catch (err) {
              sendJson(res, 500, {
                error: err instanceof Error ? err.message : 'Doğrulama hatası',
              });
            }
          })();
          return;
        }
        if (path === '/api/pass/admit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            try {
              const body = await readBody(req);
              const result = admitPass({
                code: body.code,
                gateId: body.gateId,
                actor: user.username,
              });
              sendJson(res, 200, result);
            } catch (err) {
              sendJson(res, 500, {
                error: err instanceof Error ? err.message : 'Geçiş kaydı başarısız',
              });
            }
          })();
          return;
        }

        // ── AŞAMA 15: Metrikler ───────────────────────────────────────
        if (path === '/api/metrics' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildMetrics());
          return;
        }

        // ── AŞAMA 16: Markalar ────────────────────────────────────────
        if (path === '/api/brands' && req.method === 'GET') {
          const user = requireUser(req, res);
          if (!user) return;
          sendJson(res, 200, {
            ...brandsSummary(),
            mine: brandsForRole(user.role),
            activeBrandId: user.activeBrandId ?? null,
          });
          return;
        }
        if (path === '/api/brands/active' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const body = await readBody(req);
            const updated = setActiveBrand(getToken(req), body.brandId);
            if (!updated) {
              sendJson(res, 403, { error: 'Bu markaya erişim yok' });
              return;
            }
            sendJson(res, 200, { user: updated });
          })();
          return;
        }
        if (path === '/api/brands' && req.method === 'POST') {
          const user = requireCeo(req, res);
          if (!user) return;
          void (async () => {
            const body = await readBody(req);
            if (!body.name) {
              sendJson(res, 400, { error: 'name zorunlu' });
              return;
            }
            sendJson(res, 200, { brand: createBrand(body, user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/brands/') && req.method === 'PATCH') {
          const user = requireCeo(req, res);
          if (!user) return;
          void (async () => {
            const id = path.split('/')[3];
            const brand = updateBrand(id, await readBody(req), user.username);
            if (!brand) {
              sendJson(res, 404, { error: 'Marka bulunamadı' });
              return;
            }
            sendJson(res, 200, { brand });
          })();
          return;
        }
        if (path.startsWith('/api/brands/') && req.method === 'DELETE') {
          const user = requireCeo(req, res);
          if (!user) return;
          const id = path.split('/')[3];
          if (id === 'active') return; // reserved
          const brand = removeBrand(id, user.username);
          if (!brand) {
            sendJson(res, 404, { error: 'Marka bulunamadı' });
            return;
          }
          sendJson(res, 200, { ok: true, brand });
          return;
        }
        if (path.startsWith('/api/brands/') && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const id = path.split('/')[3];
          const brand = getBrand(id);
          if (!brand) {
            sendJson(res, 404, { error: 'Marka bulunamadı' });
            return;
          }
          sendJson(res, 200, { brand });
          return;
        }

        // ── AŞAMA 17: Misafir CRM ─────────────────────────────────────
        if (path === '/api/guests' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, guestsSummary());
          return;
        }
        if (path === '/api/guests/sync' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          sendJson(res, 200, syncGuestsFromSources(user.username));
          return;
        }
        if (path === '/api/guests' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const body = await readBody(req);
            if (!body.name) {
              sendJson(res, 400, { error: 'name zorunlu' });
              return;
            }
            sendJson(res, 200, { guest: upsertGuest(body, user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/guests/') && path.endsWith('/timeline') && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const id = path.split('/')[3];
          const data = guestTimeline(id);
          if (!data) {
            sendJson(res, 404, { error: 'Misafir bulunamadı' });
            return;
          }
          sendJson(res, 200, data);
          return;
        }
        if (path.startsWith('/api/guests/') && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const id = path.split('/')[3];
          const guest = getGuest(id);
          if (!guest) {
            sendJson(res, 404, { error: 'Misafir bulunamadı' });
            return;
          }
          sendJson(res, 200, { guest });
          return;
        }

        // ── AŞAMA 20: Playbooks ───────────────────────────────────────
        if (path === '/api/playbooks' && req.method === 'GET') {
          const user = requireUser(req, res);
          if (!user) return;
          const url = new URL(req.url ?? '', 'http://local');
          const brandId = url.searchParams.get('brandId') || user.activeBrandId || undefined;
          sendJson(res, 200, { playbooks: listPlaybooks(brandId) });
          return;
        }
        if (path === '/api/playbooks' && req.method === 'POST') {
          const user = requireCeo(req, res);
          if (!user) return;
          void (async () => {
            const body = await readBody(req);
            if (!body.prompt) {
              sendJson(res, 400, { error: 'prompt zorunlu' });
              return;
            }
            sendJson(res, 200, { playbook: createPlaybook(body, user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/playbooks/') && req.method === 'DELETE') {
          const user = requireCeo(req, res);
          if (!user) return;
          const id = path.split('/')[3];
          const pb = removePlaybook(id, user.username);
          if (!pb) {
            sendJson(res, 404, { error: 'Playbook bulunamadı' });
            return;
          }
          sendJson(res, 200, { ok: true, playbook: pb });
          return;
        }

        // ── AŞAMA 21: Webhooks ────────────────────────────────────────
        if (path === '/api/webhooks' && req.method === 'GET') {
          if (!requireCeo(req, res)) return;
          sendJson(res, 200, {
            webhooks: listWebhooks(),
            deliveries: listDeliveries(30),
          });
          return;
        }
        if (path === '/api/webhooks' && req.method === 'POST') {
          const user = requireCeo(req, res);
          if (!user) return;
          void (async () => {
            try {
              const body = await readBody(req);
              sendJson(res, 200, { webhook: createWebhook(body, user.username) });
            } catch (err) {
              sendJson(res, 400, {
                error: err instanceof Error ? err.message : 'Webhook oluşturulamadı',
              });
            }
          })();
          return;
        }
        if (path.startsWith('/api/webhooks/') && req.method === 'DELETE') {
          const user = requireCeo(req, res);
          if (!user) return;
          const id = path.split('/')[3];
          const hook = removeWebhook(id, user.username);
          if (!hook) {
            sendJson(res, 404, { error: 'Webhook bulunamadı' });
            return;
          }
          sendJson(res, 200, { ok: true, webhook: hook });
          return;
        }

        // ── AŞAMA 22: OpenAPI ─────────────────────────────────────────
        if ((path === '/api/openapi.json' || path === '/api/docs') && req.method === 'GET') {
          sendJson(res, 200, buildOpenApi());
          return;
        }

        // ── AŞAMA 23: Envanter ────────────────────────────────────────
        if (path === '/api/inventory' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          sendJson(res, 200, {
            ...inventorySummary(),
            items: listInventory({
              venueId: url.searchParams.get('venueId') || undefined,
              brandId: url.searchParams.get('brandId') || undefined,
            }),
          });
          return;
        }
        if (path === '/api/inventory/adjust' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          if (user.role === 'crew') {
            sendJson(res, 403, { error: 'Crew stok düşemez' });
            return;
          }
          void (async () => {
            const body = await readBody(req);
            const result = adjustStock(body, user.username);
            if (!result) {
              sendJson(res, 404, { error: 'SKU bulunamadı' });
              return;
            }
            sendJson(res, 200, result);
          })();
          return;
        }
        if (path === '/api/inventory/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runInventorySweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/inventory/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackInventoryFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/inventory/lows/restock' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, restockInventoryLows(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/inventory/sku/quarantine' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, quarantineInventorySku(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/inventory/delivery/receive' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, receiveInventoryDelivery(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 24: Vardiyalar ──────────────────────────────────────
        if (path === '/api/shifts' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          sendJson(res, 200, {
            ...shiftsSummary(),
            shifts: listShifts({
              date: url.searchParams.get('date') || undefined,
              venueId: url.searchParams.get('venueId') || undefined,
              brandId: url.searchParams.get('brandId') || undefined,
            }),
          });
          return;
        }
        if (path === '/api/shifts' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const body = await readBody(req);
            sendJson(res, 200, { shift: createShift(body, user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/shifts/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const id = path.split('/')[3];
            const shift = updateShift(id, await readBody(req), user.username);
            if (!shift) {
              sendJson(res, 404, { error: 'Vardiya bulunamadı' });
              return;
            }
            sendJson(res, 200, { shift });
          })();
          return;
        }
        if (path.startsWith('/api/shifts/') && req.method === 'DELETE') {
          const user = requireCeo(req, res);
          if (!user) return;
          const id = path.split('/')[3];
          const shift = removeShift(id, user.username);
          if (!shift) {
            sendJson(res, 404, { error: 'Vardiya bulunamadı' });
            return;
          }
          sendJson(res, 200, { ok: true, shift });
          return;
        }

        // ── AŞAMA 25: Rezervasyonlar ──────────────────────────────────
        if (path === '/api/reservations' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          sendJson(res, 200, {
            ...reservationsSummary(),
            reservations: listReservations({
              date: url.searchParams.get('date') || undefined,
              venueId: url.searchParams.get('venueId') || undefined,
              brandId: url.searchParams.get('brandId') || undefined,
              status: url.searchParams.get('status') || undefined,
            }),
          });
          return;
        }
        if (path === '/api/reservations' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const body = await readBody(req);
            sendJson(res, 200, { reservation: createReservation(body, user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/reservations/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const id = path.split('/')[3];
            const reservation = updateReservation(id, await readBody(req), user.username);
            if (!reservation) {
              sendJson(res, 404, { error: 'Rezervasyon bulunamadı' });
              return;
            }
            sendJson(res, 200, { reservation });
          })();
          return;
        }
        if (path.startsWith('/api/reservations/') && req.method === 'DELETE') {
          const user = requireUser(req, res);
          if (!user) return;
          if (user.role === 'crew') {
            sendJson(res, 403, { error: 'Crew rezervasyon silemez' });
            return;
          }
          const id = path.split('/')[3];
          const reservation = removeReservation(id, user.username);
          if (!reservation) {
            sendJson(res, 404, { error: 'Rezervasyon bulunamadı' });
            return;
          }
          sendJson(res, 200, { ok: true, reservation });
          return;
        }

        // ── AŞAMA 26: Sadakat / Daze-Gift ─────────────────────────────
        if (path === '/api/loyalty' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          sendJson(res, 200, {
            ...loyaltySummary(),
            accounts: listLoyaltyAccounts({
              brandId: url.searchParams.get('brandId') || undefined,
            }),
            ledger: listLedger(Number(url.searchParams.get('limit')) || 40),
          });
          return;
        }
        if (path === '/api/loyalty/adjust' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          if (user.role === 'crew') {
            sendJson(res, 403, { error: 'Crew puan düşemez' });
            return;
          }
          void (async () => {
            const body = await readBody(req);
            const result = adjustPoints(body, user.username);
            if (!result) {
              sendJson(res, 404, { error: 'Hesap bulunamadı' });
              return;
            }
            sendJson(res, 200, result);
          })();
          return;
        }

        // ── AŞAMA 27: Olay panosu ─────────────────────────────────────
        if (path === '/api/incidents' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, {
            ...incidentsSummary(),
            incidents: listIncidents(),
          });
          return;
        }
        if (path === '/api/incidents' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const body = await readBody(req);
            sendJson(res, 200, { incident: createIncident(body, user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/incidents/') && path.endsWith('/ack') && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const id = path.split('/')[3];
            const body = await readBody(req);
            const incident = ackIncident(id, body.status || 'acked', user.username);
            if (!incident) {
              sendJson(res, 404, { error: 'Olay bulunamadı' });
              return;
            }
            sendJson(res, 200, { incident });
          })();
          return;
        }

        // ── AŞAMA 28: Tedarik / satınalma ─────────────────────────────
        if (path === '/api/suppliers' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, {
            ...suppliersSummary(),
            suppliers: listSuppliers(),
            orders: listPurchaseOrders(),
          });
          return;
        }
        if (path === '/api/suppliers' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          if (user.role === 'crew') {
            sendJson(res, 403, { error: 'Crew tedarikçi ekleyemez' });
            return;
          }
          void (async () => {
            sendJson(res, 200, { supplier: createSupplier(await readBody(req), user.username) });
          })();
          return;
        }
        if (path === '/api/purchase-orders' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          sendJson(res, 200, {
            orders: listPurchaseOrders({
              status: url.searchParams.get('status') || undefined,
              supplierId: url.searchParams.get('supplierId') || undefined,
            }),
          });
          return;
        }
        if (path === '/api/purchase-orders' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          if (user.role === 'crew') {
            sendJson(res, 403, { error: 'Crew sipariş açamaz' });
            return;
          }
          void (async () => {
            sendJson(res, 200, {
              order: createPurchaseOrder(await readBody(req), user.username),
            });
          })();
          return;
        }
        if (path.startsWith('/api/purchase-orders/') && path.endsWith('/receive') && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          const id = path.split('/')[3];
          const result = receivePurchaseOrder(id, user.username);
          if (!result) {
            sendJson(res, 404, { error: 'Sipariş bulunamadı' });
            return;
          }
          sendJson(res, 200, result);
          return;
        }
        if (path.startsWith('/api/purchase-orders/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const id = path.split('/')[3];
            const order = updatePurchaseOrder(id, await readBody(req), user.username);
            if (!order) {
              sendJson(res, 404, { error: 'Sipariş bulunamadı' });
              return;
            }
            sendJson(res, 200, { order });
          })();
          return;
        }
        if (path.startsWith('/api/purchase-orders/') && req.method === 'DELETE') {
          const user = requireCeo(req, res);
          if (!user) return;
          const id = path.split('/')[3];
          const order = removePurchaseOrder(id, user.username);
          if (!order) {
            sendJson(res, 404, { error: 'Silinemedi (yok veya teslim alınmış)' });
            return;
          }
          sendJson(res, 200, { ok: true, order });
          return;
        }

        // ── AŞAMA 29: Geri bildirim / NPS ─────────────────────────────
        if (path === '/api/feedback' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          sendJson(res, 200, {
            ...feedbackSummary(),
            feedback: listFeedback({
              venueId: url.searchParams.get('venueId') || undefined,
              brandId: url.searchParams.get('brandId') || undefined,
              channel: url.searchParams.get('channel') || undefined,
            }),
          });
          return;
        }
        if (path === '/api/feedback' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { feedback: createFeedback(await readBody(req), user.username) });
          })();
          return;
        }

        // ── AŞAMA 30: CSV export ──────────────────────────────────────
        if (path === '/api/exports' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, { catalog: listExportCatalog() });
          return;
        }
        if (path.startsWith('/api/exports/') && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const id = path.split('/')[3];
          const file = buildExport(id);
          if (!file) {
            sendJson(res, 404, { error: 'Export türü bulunamadı', catalog: listExportCatalog() });
            return;
          }
          const url = new URL(req.url ?? '', 'http://local');
          if (url.searchParams.get('format') === 'json') {
            sendJson(res, 200, file);
            return;
          }
          res.statusCode = 200;
          res.setHeader('Content-Type', file.contentType);
          res.setHeader('Content-Disposition', `attachment; filename="${file.filename}"`);
          res.setHeader('Access-Control-Allow-Origin', '*');
          res.setHeader('Cache-Control', 'no-store');
          res.end(file.csv);
          return;
        }

        // ── AŞAMA 31: KVKK onay günlüğü ───────────────────────────────
        if (path === '/api/consents' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          sendJson(res, 200, {
            ...consentSummary(),
            consents: listConsents({
              purpose: url.searchParams.get('purpose') || undefined,
              granted: url.searchParams.get('granted') || undefined,
            }),
          });
          return;
        }
        if (path === '/api/consents' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { consent: recordConsent(await readBody(req), user.username) });
          })();
          return;
        }

        // ── AŞAMA 32: Duyurular ───────────────────────────────────────
        if (path === '/api/announcements' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          sendJson(res, 200, {
            ...announcementsSummary(),
            announcements: listAnnouncements({
              status: url.searchParams.get('status') || undefined,
              brandId: url.searchParams.get('brandId') || undefined,
            }),
          });
          return;
        }
        if (path === '/api/announcements' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          if (user.role === 'crew') {
            sendJson(res, 403, { error: 'Crew duyuru yayınlayamaz' });
            return;
          }
          void (async () => {
            sendJson(res, 200, {
              announcement: createAnnouncement(await readBody(req), user.username),
            });
          })();
          return;
        }
        if (path.startsWith('/api/announcements/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const id = path.split('/')[3];
            const announcement = updateAnnouncement(id, await readBody(req), user.username);
            if (!announcement) {
              sendJson(res, 404, { error: 'Duyuru bulunamadı' });
              return;
            }
            sendJson(res, 200, { announcement });
          })();
          return;
        }
        if (path.startsWith('/api/announcements/') && req.method === 'DELETE') {
          const user = requireCeo(req, res);
          if (!user) return;
          const id = path.split('/')[3];
          const announcement = removeAnnouncement(id, user.username);
          if (!announcement) {
            sendJson(res, 404, { error: 'Duyuru bulunamadı' });
            return;
          }
          sendJson(res, 200, { ok: true, announcement });
          return;
        }

        // ── AŞAMA 33: Reçeteler ───────────────────────────────────────
        if (path === '/api/recipes' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, {
            ...recipesSummary(),
            recipes: listRecipes(),
          });
          return;
        }
        if (path === '/api/recipes' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          if (user.role === 'crew') {
            sendJson(res, 403, { error: 'Crew reçete ekleyemez' });
            return;
          }
          void (async () => {
            sendJson(res, 200, { recipe: createRecipe(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/recipes/') && path.endsWith('/cook') && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const id = path.split('/')[3];
            const body = await readBody(req);
            const result = cookRecipe(id, body.portions || 1, user.username);
            if (!result) {
              sendJson(res, 404, { error: 'Reçete bulunamadı' });
              return;
            }
            sendJson(res, 200, result);
          })();
          return;
        }
        if (path.startsWith('/api/recipes/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const id = path.split('/')[3];
            const recipe = updateRecipe(id, await readBody(req), user.username);
            if (!recipe) {
              sendJson(res, 404, { error: 'Reçete bulunamadı' });
              return;
            }
            sendJson(res, 200, { recipe });
          })();
          return;
        }
        if (path.startsWith('/api/recipes/') && req.method === 'DELETE') {
          const user = requireCeo(req, res);
          if (!user) return;
          const id = path.split('/')[3];
          const recipe = removeRecipe(id, user.username);
          if (!recipe) {
            sendJson(res, 404, { error: 'Reçete bulunamadı' });
            return;
          }
          sendJson(res, 200, { ok: true, recipe });
          return;
        }

        // ── AŞAMA 34: Kontrol listeleri ───────────────────────────────
        if (path === '/api/checklists' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, {
            ...checklistsSummary(),
            templates: listChecklistTemplates(),
            runs: listChecklistRuns(),
          });
          return;
        }
        if (path === '/api/checklists/start' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const body = await readBody(req);
            const run = startChecklistRun(body.templateId, user.username);
            if (!run) {
              sendJson(res, 404, { error: 'Şablon bulunamadı' });
              return;
            }
            sendJson(res, 200, { run });
          })();
          return;
        }
        if (path.startsWith('/api/checklists/') && path.includes('/toggle') && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const runId = path.split('/')[3];
            const body = await readBody(req);
            const run = toggleChecklistItem(runId, body.checkId, body.done, user.username);
            if (!run) {
              sendJson(res, 404, { error: 'Çalıştırma bulunamadı' });
              return;
            }
            sendJson(res, 200, { run });
          })();
          return;
        }

        // ── AŞAMA 35: Kayıp eşya ──────────────────────────────────────
        if (path === '/api/lost-found' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          sendJson(res, 200, {
            ...lostFoundSummary(),
            items: listLostFound({
              status: url.searchParams.get('status') || undefined,
              venueId: url.searchParams.get('venueId') || undefined,
            }),
          });
          return;
        }
        if (path === '/api/lost-found' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLostFound(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/lost-found/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const id = path.split('/')[3];
            const item = updateLostFound(id, await readBody(req), user.username);
            if (!item) {
              sendJson(res, 404, { error: 'Kayıt bulunamadı' });
              return;
            }
            sendJson(res, 200, { item });
          })();
          return;
        }

        // ── AŞAMA 36: Bahşiş havuzu ───────────────────────────────────
        if (path === '/api/tips' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, tipSummary());
          return;
        }
        if (path === '/api/tips' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const body = await readBody(req);
            const result = addTip(body, user.username);
            if (!result) {
              sendJson(res, 400, { error: 'Geçersiz tutar veya yetersiz bakiye' });
              return;
            }
            sendJson(res, 200, result);
          })();
          return;
        }

        // ── AŞAMA 38: Bakım ticket ────────────────────────────────────
        if (path === '/api/maintenance' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          sendJson(res, 200, {
            ...maintenanceSummary(),
            tickets: listMaintenance({
              status: url.searchParams.get('status') || undefined,
              venueId: url.searchParams.get('venueId') || undefined,
            }),
          });
          return;
        }
        if (path === '/api/maintenance' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { ticket: createTicket(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/maintenance/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const id = path.split('/')[3];
            const ticket = updateTicket(id, await readBody(req), user.username);
            if (!ticket) {
              sendJson(res, 404, { error: 'Ticket bulunamadı' });
              return;
            }
            sendJson(res, 200, { ticket });
          })();
          return;
        }
        if (path === '/api/maintenance/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runMaintenanceSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/maintenance/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackMaintenanceFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/maintenance/critical/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeCriticalMaintenance(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/maintenance/overdue/escalate' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, escalateOverdueMaintenance(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/maintenance/preventive/create' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, createPreventiveMaintenance(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 39: Günlük brief ────────────────────────────────────
        if (path === '/api/brief' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildDailyBrief());
          return;
        }
        if (path === '/api/brief/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runBriefSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/brief/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackBriefFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/brief/incidents/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackBriefIncidents(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/brief/inventory/restock' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, restockBriefInventory(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/brief/maintenance/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeBriefMaintenance(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 40: Menü ────────────────────────────────────────────
        if (path === '/api/menu' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          sendJson(res, 200, {
            ...menuSummary(),
            items: listMenu({
              venueId: url.searchParams.get('venueId') || undefined,
              brandId: url.searchParams.get('brandId') || undefined,
              available: url.searchParams.get('available') || undefined,
            }),
          });
          return;
        }
        if (path === '/api/menu' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          if (user.role === 'crew') {
            sendJson(res, 403, { error: 'Crew menü ekleyemez' });
            return;
          }
          void (async () => {
            sendJson(res, 200, { item: createMenuItem(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/menu/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const id = path.split('/')[3];
            const item = updateMenuItem(id, await readBody(req), user.username);
            if (!item) {
              sendJson(res, 404, { error: 'Menü kalemi bulunamadı' });
              return;
            }
            sendJson(res, 200, { item });
          })();
          return;
        }
        if (path.startsWith('/api/menu/') && req.method === 'DELETE') {
          const user = requireCeo(req, res);
          if (!user) return;
          const id = path.split('/')[3];
          const item = removeMenuItem(id, user.username);
          if (!item) {
            sendJson(res, 404, { error: 'Menü kalemi bulunamadı' });
            return;
          }
          sendJson(res, 200, { ok: true, item });
          return;
        }

        // ── AŞAMA 41: Kampanyalar ─────────────────────────────────────
        if (path === '/api/campaigns' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          sendJson(res, 200, {
            ...campaignsSummary(),
            campaigns: listCampaigns({
              status: url.searchParams.get('status') || undefined,
              brandId: url.searchParams.get('brandId') || undefined,
            }),
          });
          return;
        }
        if (path === '/api/campaigns' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          if (user.role === 'crew') {
            sendJson(res, 403, { error: 'Crew kampanya açamaz' });
            return;
          }
          void (async () => {
            sendJson(res, 200, {
              campaign: createCampaign(await readBody(req), user.username),
            });
          })();
          return;
        }
        if (path.startsWith('/api/campaigns/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const id = path.split('/')[3];
            const campaign = updateCampaign(id, await readBody(req), user.username);
            if (!campaign) {
              sendJson(res, 404, { error: 'Kampanya bulunamadı' });
              return;
            }
            sendJson(res, 200, { campaign });
          })();
          return;
        }
        if (path.startsWith('/api/campaigns/') && req.method === 'DELETE') {
          const user = requireCeo(req, res);
          if (!user) return;
          const id = path.split('/')[3];
          const campaign = removeCampaign(id, user.username);
          if (!campaign) {
            sendJson(res, 404, { error: 'Kampanya bulunamadı' });
            return;
          }
          sendJson(res, 200, { ok: true, campaign });
          return;
        }

        // ── AŞAMA 42: Lokalizasyon notları ────────────────────────────
        if (path === '/api/i18n' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          sendJson(res, 200, {
            ...i18nSummary(),
            notes: listI18nNotes({
              locale: url.searchParams.get('locale') || undefined,
              status: url.searchParams.get('status') || undefined,
              module: url.searchParams.get('module') || undefined,
            }),
          });
          return;
        }
        if (path === '/api/i18n' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { note: upsertI18nNote(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/i18n/') && req.method === 'DELETE') {
          const user = requireCeo(req, res);
          if (!user) return;
          const id = path.split('/')[3];
          const note = removeI18nNote(id, user.username);
          if (!note) {
            sendJson(res, 404, { error: 'Not bulunamadı' });
            return;
          }
          sendJson(res, 200, { ok: true, note });
          return;
        }

        // ── AŞAMA 43: Soğuk zincir ────────────────────────────────────
        if (path === '/api/coldchain' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, {
            ...coldchainSummary(),
            assets: listColdAssets(),
            readings: listColdReadings(40),
          });
          return;
        }
        if (path === '/api/coldchain' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const reading = logColdReading(await readBody(req), user.username);
            if (!reading) {
              sendJson(res, 400, { error: 'Geçersiz varlık/sıcaklık' });
              return;
            }
            sendJson(res, 200, { reading });
          })();
          return;
        }

        // ── AŞAMA 44: Vardiya teslim ──────────────────────────────────
        if (path === '/api/handover' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          sendJson(res, 200, {
            ...handoverSummary(),
            notes: listHandover({
              venueId: url.searchParams.get('venueId') || undefined,
            }),
          });
          return;
        }
        if (path === '/api/handover' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, {
              note: createHandover(await readBody(req), user.username),
            });
          })();
          return;
        }

        // ── AŞAMA 45: Kasa ────────────────────────────────────────────
        if (path === '/api/cash' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          sendJson(res, 200, cashSummary(url.searchParams.get('venueId') || undefined));
          return;
        }
        if (path === '/api/cash' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          if (user.role === 'crew') {
            sendJson(res, 403, { error: 'Crew kasa hareketi giremez' });
            return;
          }
          void (async () => {
            const result = postCash(await readBody(req), user.username);
            if (!result) {
              sendJson(res, 400, { error: 'Geçersiz tutar veya yetersiz bakiye' });
              return;
            }
            sendJson(res, 200, result);
          })();
          return;
        }

        // ── AŞAMA 46: Varlıklar ───────────────────────────────────────
        if (path === '/api/assets' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const url = new URL(req.url ?? '', 'http://local');
          sendJson(res, 200, {
            ...assetsSummary(),
            assets: listAssets({
              venueId: url.searchParams.get('venueId') || undefined,
              status: url.searchParams.get('status') || undefined,
            }),
          });
          return;
        }
        if (path === '/api/assets' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { asset: createAsset(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/assets/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const id = path.split('/')[3];
            const asset = updateAsset(id, await readBody(req), user.username);
            if (!asset) {
              sendJson(res, 404, { error: 'Varlık bulunamadı' });
              return;
            }
            sendJson(res, 200, { asset });
          })();
          return;
        }

        // ── AŞAMA 47: Enerji ──────────────────────────────────────────
        if (path === '/api/energy' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, {
            ...energySummary(),
            meters: listMeters(),
            readings: listEnergyReadings(40),
          });
          return;
        }
        if (path === '/api/energy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const reading = logEnergyReading(await readBody(req), user.username);
            if (!reading) {
              sendJson(res, 400, { error: 'Geçersiz sayaç' });
              return;
            }
            sendJson(res, 200, { reading });
          })();
          return;
        }

        // ── AŞAMA 48: Eğitim quiz ─────────────────────────────────────
        if (path === '/api/training' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, trainingSummary());
          return;
        }
        if (path.startsWith('/api/training/') && path.endsWith('/quiz') && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          const id = path.split('/')[3];
          const quiz = getQuiz(id);
          if (!quiz) {
            sendJson(res, 404, { error: 'Quiz bulunamadı' });
            return;
          }
          // cevapları gizle
          sendJson(res, 200, {
            quiz: {
              id: quiz.id,
              title: quiz.title,
              questions: (quiz.questions || []).map((q) => ({
                id: q.id,
                prompt: q.prompt,
                options: q.options,
              })),
            },
          });
          return;
        }
        if (path === '/api/training/attempt' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const body = await readBody(req);
            const attempt = submitAttempt(body, user.username);
            if (!attempt) {
              sendJson(res, 404, { error: 'Quiz bulunamadı' });
              return;
            }
            sendJson(res, 200, { attempt, recent: listAttempts(10) });
          })();
          return;
        }

        // ── AŞAMA 49: Vale ────────────────────────────────────────────
        if (path === '/api/valet' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, { ...valetSummary(), tickets: listValet() });
          return;
        }
        if (path === '/api/valet' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { ticket: createValet(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/valet/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const ticket = updateValet(path.split('/')[3], await readBody(req), user.username);
            if (!ticket) {
              sendJson(res, 404, { error: 'Fiş bulunamadı' });
              return;
            }
            sendJson(res, 200, { ticket });
          })();
          return;
        }

        // ── AŞAMA 50: Müzik ───────────────────────────────────────────
        if (path === '/api/music' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, { ...musicSummary(), requests: listMusic() });
          return;
        }
        if (path === '/api/music' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, {
              request: createMusicRequest(await readBody(req), user.username),
            });
          })();
          return;
        }
        if (path.startsWith('/api/music/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const request = updateMusicRequest(
              path.split('/')[3],
              await readBody(req),
              user.username,
            );
            if (!request) {
              sendJson(res, 404, { error: 'İstek bulunamadı' });
              return;
            }
            sendJson(res, 200, { request });
          })();
          return;
        }

        // ── AŞAMA 51: Belgeler ────────────────────────────────────────
        if (path === '/api/documents' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, { ...documentsSummary(), documents: listDocuments() });
          return;
        }
        if (path === '/api/documents' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, {
              document: createDocument(await readBody(req), user.username),
            });
          })();
          return;
        }
        if (path.startsWith('/api/documents/') && req.method === 'DELETE') {
          const user = requireCeo(req, res);
          if (!user) return;
          const document = removeDocument(path.split('/')[3], user.username);
          if (!document) {
            sendJson(res, 404, { error: 'Belge bulunamadı' });
            return;
          }
          sendJson(res, 200, { ok: true, document });
          return;
        }

        // ── AŞAMA 52: Tedarikçi skor ──────────────────────────────────
        if (path === '/api/vendor-scores' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, { ...vendorScoreSummary(), scores: listVendorScores() });
          return;
        }
        if (path === '/api/vendor-scores' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, {
              score: upsertVendorScore(await readBody(req), user.username),
            });
          })();
          return;
        }

        // ── AŞAMA 53: Fire ────────────────────────────────────────────
        if (path === '/api/waste' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, wasteSummary());
          return;
        }
        if (path === '/api/waste' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { entry: logWaste(await readBody(req), user.username) });
          })();
          return;
        }

        // ── AŞAMA 54: Oturma ──────────────────────────────────────────
        if (path === '/api/seating' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, { ...seatingSummary(), tables: listSeating() });
          return;
        }
        if (path === '/api/seating' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { table: createSeat(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/seating/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const table = updateSeat(path.split('/')[3], await readBody(req), user.username);
            if (!table) {
              sendJson(res, 404, { error: 'Masa bulunamadı' });
              return;
            }
            sendJson(res, 200, { table });
          })();
          return;
        }

        // ── AŞAMA 55: Bekleme listesi ─────────────────────────────────
        if (path === '/api/waitlist' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, { ...waitlistSummary(), entries: listWaitlist() });
          return;
        }
        if (path === '/api/waitlist' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, {
              entry: createWaitlistEntry(await readBody(req), user.username),
            });
          })();
          return;
        }
        if (path.startsWith('/api/waitlist/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const entry = updateWaitlist(path.split('/')[3], await readBody(req), user.username);
            if (!entry) {
              sendJson(res, 404, { error: 'Kayıt bulunamadı' });
              return;
            }
            sendJson(res, 200, { entry });
          })();
          return;
        }

        // ── AŞAMA 56: Şikayetler ──────────────────────────────────────
        if (path === '/api/complaints' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, { ...complaintsSummary(), complaints: listComplaints() });
          return;
        }
        if (path === '/api/complaints' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, {
              complaint: createComplaint(await readBody(req), user.username),
            });
          })();
          return;
        }
        if (path.startsWith('/api/complaints/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const complaint = updateComplaint(
              path.split('/')[3],
              await readBody(req),
              user.username,
            );
            if (!complaint) {
              sendJson(res, 404, { error: 'Şikayet bulunamadı' });
              return;
            }
            sendJson(res, 200, { complaint });
          })();
          return;
        }

        // ── AŞAMA 57: Kudos ───────────────────────────────────────────
        if (path === '/api/kudos' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, kudosSummary());
          return;
        }
        if (path === '/api/kudos' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { entry: createKudos(await readBody(req), user.username) });
          })();
          return;
        }

        // ── AŞAMA 58: Çalışma saatleri ────────────────────────────────
        if (path === '/api/hours' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, hoursSummary());
          return;
        }
        if (path.startsWith('/api/hours/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const venueId = path.split('/')[3];
            const row = updateHours(venueId, await readBody(req), user.username);
            if (!row) {
              sendJson(res, 404, { error: 'Tesis saatleri bulunamadı' });
              return;
            }
            sendJson(res, 200, { hours: row });
          })();
          return;
        }

        // ── AŞAMA 59: Hava brifi ──────────────────────────────────────
        if (path === '/api/weather' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildWeatherBrief());
          return;
        }
        if (path === '/api/weather/refresh' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          sendJson(res, 200, refreshWeather(user.username));
          return;
        }
        if (path === '/api/weather/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runWeatherSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/weather/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackWeatherFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/weather/ops/refresh' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, opsRefreshWeather(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/weather/advisory/hold' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, setWeatherAdvisoryHold(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/weather/extreme/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackWeatherExtreme(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 60: Hazırlık skoru ──────────────────────────────────
        if (path === '/api/readiness' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildReadiness());
          return;
        }
        if (path === '/api/readiness/snapshot' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, refreshReadinessSnapshot(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/readiness/threshold' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, setReadinessThreshold(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/readiness/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackReadinessDimension(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/readiness/escalate' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, escalateReadinessGap(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/readiness/gap/resolve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, resolveReadinessGap(await readBody(req), user.username)); })();
          return;
        }


        // ── AŞAMA 61–75 ─────────────────────────────────────────────

        if (path === '/api/spa' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, spaSummary());
          return;
        }
        if (path === '/api/spa' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSpa(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/spa/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSpa(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/eventcal' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, eventcalSummary());
          return;
        }
        if (path === '/api/eventcal' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEventcal(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/eventcal/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEventcal(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/giftcards' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, giftcardsSummary());
          return;
        }
        if (path === '/api/giftcards' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGiftcards(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/giftcards/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGiftcards(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/delivery' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, deliverySummary());
          return;
        }
        if (path === '/api/delivery' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDelivery(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/delivery/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDelivery(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/cleaning' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cleaningSummary());
          return;
        }
        if (path === '/api/cleaning' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCleaning(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cleaning/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCleaning(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/laundry' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, laundrySummary());
          return;
        }
        if (path === '/api/laundry' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLaundry(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/laundry/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLaundry(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/wifi' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, wifiSummary());
          return;
        }
        if (path === '/api/wifi' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWifi(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/wifi/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWifi(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/content' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, contentSummary());
          return;
        }
        if (path === '/api/content' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createContent(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/content/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateContent(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/pulse' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pulseSummary());
          return;
        }
        if (path === '/api/pulse' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPulse(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pulse/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePulse(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/budget' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, budgetSummary());
          return;
        }
        if (path === '/api/budget' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBudget(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/budget/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBudget(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/contracts' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, contractsSummary());
          return;
        }
        if (path === '/api/contracts' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createContracts(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/contracts/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateContracts(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/passstock' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, passstockSummary());
          return;
        }
        if (path === '/api/passstock' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPassstock(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/passstock/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePassstock(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/kds' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, kdsSummary());
          return;
        }
        if (path === '/api/kds' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createKds(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/kds/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateKds(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/emergency' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, emergencySummary());
          return;
        }
        if (path === '/api/emergency' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEmergency(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/emergency/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEmergency(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/digest' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildDigest());
          return;
        }
        if (path === '/api/digest/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runDigestSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/digest/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackDigestFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/digest/readiness/refresh' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, refreshDigestReadiness(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/digest/gap/escalate' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, escalateDigestGap(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/digest/gap/resolve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, resolveDigestGap(await readBody(req), user.username)); })();
          return;
        }


        // ── AŞAMA 76–90 ─────────────────────────────────────────────

        if (path === '/api/lockers' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, lockersSummary());
          return;
        }
        if (path === '/api/lockers' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLockers(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/lockers/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLockers(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/kidsclub' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, kidsclubSummary());
          return;
        }
        if (path === '/api/kidsclub' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createKidsclub(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/kidsclub/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateKidsclub(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/beachbeds' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, beachbedsSummary());
          return;
        }
        if (path === '/api/beachbeds' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBeachbeds(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/beachbeds/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBeachbeds(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/transfers' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, transfersSummary());
          return;
        }
        if (path === '/api/transfers' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTransfers(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/transfers/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTransfers(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/badgeprint' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, badgeprintSummary());
          return;
        }
        if (path === '/api/badgeprint' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBadgeprint(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/badgeprint/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBadgeprint(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/meetingrooms' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, meetingroomsSummary());
          return;
        }
        if (path === '/api/meetingrooms' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMeetingrooms(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/meetingrooms/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMeetingrooms(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/mediakit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, mediakitSummary());
          return;
        }
        if (path === '/api/mediakit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMediakit(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/mediakit/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMediakit(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/sustain' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sustainSummary());
          return;
        }
        if (path === '/api/sustain' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSustain(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sustain/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSustain(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/allergens' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, allergensSummary());
          return;
        }
        if (path === '/api/allergens' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAllergens(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/allergens/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAllergens(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/winecellar' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, winecellarSummary());
          return;
        }
        if (path === '/api/winecellar' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWinecellar(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/winecellar/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWinecellar(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/lounge' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, loungeSummary());
          return;
        }
        if (path === '/api/lounge' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLounge(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/lounge/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLounge(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/shuttle' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, shuttleSummary());
          return;
        }
        if (path === '/api/shuttle' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createShuttle(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/shuttle/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateShuttle(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/partners' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, partnersSummary());
          return;
        }
        if (path === '/api/partners' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPartners(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/partners/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePartners(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/mysteryshop' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, mysteryshopSummary());
          return;
        }
        if (path === '/api/mysteryshop' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMysteryshop(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/mysteryshop/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMysteryshop(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/boardpack' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildBoardpack());
          return;
        }
        if (path === '/api/boardpack/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runBoardpackSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/boardpack/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackBoardpackFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/boardpack/snapshot' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, snapshotBoardpack(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/boardpack/contract/renew' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, renewBoardpackContract(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/boardpack/budget/rebalance' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, rebalanceBoardpackBudget(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 91–105 ──

        if (path === '/api/concierge' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, conciergeSummary());
          return;
        }
        if (path === '/api/concierge' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createConcierge(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/concierge/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateConcierge(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/minibar' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, minibarSummary());
          return;
        }
        if (path === '/api/minibar' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMinibar(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/minibar/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMinibar(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/folio' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, folioSummary());
          return;
        }
        if (path === '/api/folio' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFolio(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/folio/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFolio(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/banquet' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, banquetSummary());
          return;
        }
        if (path === '/api/banquet' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBanquet(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/banquet/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBanquet(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/tours' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, toursSummary());
          return;
        }
        if (path === '/api/tours' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTours(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/tours/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTours(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/marina' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, marinaSummary());
          return;
        }
        if (path === '/api/marina' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMarina(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/marina/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMarina(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/hammam' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, hammamSummary());
          return;
        }
        if (path === '/api/hammam' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHammam(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/hammam/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHammam(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/towels' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, towelsSummary());
          return;
        }
        if (path === '/api/towels' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTowels(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/towels/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTowels(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/bands' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, bandsSummary());
          return;
        }
        if (path === '/api/bands' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBands(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/bands/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBands(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/haccp' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, haccpSummary());
          return;
        }
        if (path === '/api/haccp' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHaccp(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/haccp/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHaccp(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/patrol' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, patrolSummary());
          return;
        }
        if (path === '/api/patrol' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPatrol(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/patrol/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePatrol(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/fleet' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, fleetSummary());
          return;
        }
        if (path === '/api/fleet' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFleet(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/fleet/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFleet(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/payroll' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, payrollSummary());
          return;
        }
        if (path === '/api/payroll' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPayroll(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/payroll/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePayroll(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/flash' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, flashSummary());
          return;
        }
        if (path === '/api/flash' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFlash(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/flash/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFlash(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/warroom' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildWarroom());
          return;
        }
        if (path === '/api/warroom/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runWarroomSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/warroom/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackWarroomFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/warroom/haccp/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearWarroomHaccp(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/warroom/patrol/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearWarroomPatrol(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/warroom/concierge/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeWarroomConcierge(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 106–120 ──

        if (path === '/api/upsell' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, upsellSummary());
          return;
        }
        if (path === '/api/upsell' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createUpsell(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/upsell/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateUpsell(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/otareviews' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, otareviewsSummary());
          return;
        }
        if (path === '/api/otareviews' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOtareviews(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/otareviews/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOtareviews(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/groups' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, groupsSummary());
          return;
        }
        if (path === '/api/groups' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGroups(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/groups/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGroups(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/vipnotes' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, vipnotesSummary());
          return;
        }
        if (path === '/api/vipnotes' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createVipnotes(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/vipnotes/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateVipnotes(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/photoshoot' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, photoshootSummary());
          return;
        }
        if (path === '/api/photoshoot' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPhotoshoot(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/photoshoot/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePhotoshoot(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/dive' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, diveSummary());
          return;
        }
        if (path === '/api/dive' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDive(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/dive/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDive(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/bikerent' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, bikerentSummary());
          return;
        }
        if (path === '/api/bikerent' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBikerent(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/bikerent/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBikerent(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cinema' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cinemaSummary());
          return;
        }
        if (path === '/api/cinema' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCinema(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cinema/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCinema(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/retail' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, retailSummary());
          return;
        }
        if (path === '/api/retail' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRetail(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/retail/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRetail(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/bakery' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, bakerySummary());
          return;
        }
        if (path === '/api/bakery' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBakery(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/bakery/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBakery(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/breakfast' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, breakfastSummary());
          return;
        }
        if (path === '/api/breakfast' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBreakfast(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/breakfast/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBreakfast(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/lateout' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, lateoutSummary());
          return;
        }
        if (path === '/api/lateout' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLateout(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/lateout/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLateout(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/amenities' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, amenitiesSummary());
          return;
        }
        if (path === '/api/amenities' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAmenities(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/amenities/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAmenities(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/nightlog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, nightlogSummary());
          return;
        }
        if (path === '/api/nightlog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createNightlog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/nightlog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateNightlog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/nightly' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildNightly());
          return;
        }
        if (path === '/api/nightly/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runNightlySweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/nightly/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackNightlyFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/nightly/log/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeNightlyLog(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/nightly/folio/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeNightlyFolio(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/nightly/late/approve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, approveNightlyLate(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 121–135 ──

        if (path === '/api/keycards' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, keycardsSummary());
          return;
        }
        if (path === '/api/keycards' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createKeycards(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/keycards/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateKeycards(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/roomstatus' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, roomstatusSummary());
          return;
        }
        if (path === '/api/roomstatus' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRoomstatus(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/roomstatus/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRoomstatus(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/bedding' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, beddingSummary());
          return;
        }
        if (path === '/api/bedding' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBedding(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/bedding/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBedding(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/wakeups' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, wakeupsSummary());
          return;
        }
        if (path === '/api/wakeups' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWakeups(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/wakeups/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWakeups(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/parcels' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, parcelsSummary());
          return;
        }
        if (path === '/api/parcels' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createParcels(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/parcels/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateParcels(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/qrcheckin' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, qrcheckinSummary());
          return;
        }
        if (path === '/api/qrcheckin' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createQrcheckin(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/qrcheckin/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateQrcheckin(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/guestapp' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, guestappSummary());
          return;
        }
        if (path === '/api/guestapp' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGuestapp(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/guestapp/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGuestapp(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/karaoke' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, karaokeSummary());
          return;
        }
        if (path === '/api/karaoke' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createKaraoke(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/karaoke/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateKaraoke(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/artwall' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, artwallSummary());
          return;
        }
        if (path === '/api/artwall' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createArtwall(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/artwall/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateArtwall(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/florals' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, floralsSummary());
          return;
        }
        if (path === '/api/florals' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFlorals(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/florals/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFlorals(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/privatechef' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, privatechefSummary());
          return;
        }
        if (path === '/api/privatechef' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPrivatechef(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/privatechef/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePrivatechef(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/mocktails' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, mocktailsSummary());
          return;
        }
        if (path === '/api/mocktails' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMocktails(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/mocktails/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMocktails(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/promos' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, promosSummary());
          return;
        }
        if (path === '/api/promos' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPromos(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/promos/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePromos(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/dawnservice' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, dawnserviceSummary());
          return;
        }
        if (path === '/api/dawnservice' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDawnservice(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/dawnservice/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDawnservice(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/orbit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildOrbit());
          return;
        }
        if (path === '/api/orbit/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runOrbitSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/orbit/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackOrbitFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/orbit/room/clean' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, cleanOrbitRooms(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/orbit/key/encode' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, encodeOrbitKeys(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/orbit/guestapp/retry' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, retryOrbitGuestapp(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 136–150 ──

        if (path === '/api/rosters' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, rostersSummary());
          return;
        }
        if (path === '/api/rosters' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRosters(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/rosters/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRosters(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/overtime' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, overtimeSummary());
          return;
        }
        if (path === '/api/overtime' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOvertime(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/overtime/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOvertime(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/uniforms' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, uniformsSummary());
          return;
        }
        if (path === '/api/uniforms' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createUniforms(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/uniforms/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateUniforms(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/healthcards' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, healthcardsSummary());
          return;
        }
        if (path === '/api/healthcards' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHealthcards(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/healthcards/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHealthcards(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/visitors' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, visitorsSummary());
          return;
        }
        if (path === '/api/visitors' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createVisitors(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/visitors/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateVisitors(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cctvlog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cctvlogSummary());
          return;
        }
        if (path === '/api/cctvlog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCctvlog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cctvlog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCctvlog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/firedrill' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, firedrillSummary());
          return;
        }
        if (path === '/api/firedrill' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFiredrill(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/firedrill/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFiredrill(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/insurance' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, insuranceSummary());
          return;
        }
        if (path === '/api/insurance' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createInsurance(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/insurance/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateInsurance(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/invoices' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, invoicesSummary());
          return;
        }
        if (path === '/api/invoices' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createInvoices(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/invoices/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateInvoices(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/taxpack' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, taxpackSummary());
          return;
        }
        if (path === '/api/taxpack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTaxpack(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/taxpack/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTaxpack(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/forecast' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, forecastSummary());
          return;
        }
        if (path === '/api/forecast' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createForecast(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/forecast/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateForecast(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/capex' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, capexSummary());
          return;
        }
        if (path === '/api/capex' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCapex(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/capex/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCapex(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/licenses' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, licensesSummary());
          return;
        }
        if (path === '/api/licenses' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLicenses(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/licenses/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLicenses(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/slabreaches' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, slabreachesSummary());
          return;
        }
        if (path === '/api/slabreaches' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSlabreaches(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/slabreaches/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSlabreaches(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/apex' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildApex());
          return;
        }
        if (path === '/api/apex/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runApexSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/apex/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackApexFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/apex/invoice/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearApexInvoices(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/apex/license/renew' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, renewApexLicenses(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/apex/overtime/approve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, approveApexOvertime(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 151–165 ──

        if (path === '/api/extlinks' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, extlinksSummary());
          return;
        }
        if (path === '/api/extlinks' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createExtlinks(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/extlinks/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateExtlinks(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/apikeys' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, apikeysSummary());
          return;
        }
        if (path === '/api/apikeys' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createApikeys(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/apikeys/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateApikeys(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/backupsched' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, backupschedSummary());
          return;
        }
        if (path === '/api/backupsched' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBackupsched(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/backupsched/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBackupsched(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sysalerts' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sysalertsSummary());
          return;
        }
        if (path === '/api/sysalerts' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSysalerts(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sysalerts/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSysalerts(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/bugtracker' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, bugtrackerSummary());
          return;
        }
        if (path === '/api/bugtracker' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBugtracker(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/bugtracker/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBugtracker(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/releasenotes' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, releasenotesSummary());
          return;
        }
        if (path === '/api/releasenotes' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createReleasenotes(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/releasenotes/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateReleasenotes(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/runbooks' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, runbooksSummary());
          return;
        }
        if (path === '/api/runbooks' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRunbooks(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/runbooks/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRunbooks(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/biometrics' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, biometricsSummary());
          return;
        }
        if (path === '/api/biometrics' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBiometrics(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/biometrics/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBiometrics(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/secretsrot' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, secretsrotSummary());
          return;
        }
        if (path === '/api/secretsrot' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSecretsrot(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/secretsrot/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSecretsrot(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/dnscheck' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, dnscheckSummary());
          return;
        }
        if (path === '/api/dnscheck' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDnscheck(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/dnscheck/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDnscheck(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/mailqueue' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, mailqueueSummary());
          return;
        }
        if (path === '/api/mailqueue' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMailqueue(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/mailqueue/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMailqueue(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/smsqueue' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, smsqueueSummary());
          return;
        }
        if (path === '/api/smsqueue' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSmsqueue(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/smsqueue/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSmsqueue(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/alertrules' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, alertrulesSummary());
          return;
        }
        if (path === '/api/alertrules' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAlertrules(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/alertrules/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAlertrules(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }
        if (path === '/api/alertrules/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runAlertrulesSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/alertrules/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackAlertrulesFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/alertrules/enable' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, enableAlertrules(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/alertrules/disable' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, disableAlertrules(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/alertrules/fire' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, fireAlertrules(await readBody(req), user.username)); })();
          return;
        }


        if (path === '/api/edgecache' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, edgecacheSummary());
          return;
        }
        if (path === '/api/edgecache' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEdgecache(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/edgecache/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEdgecache(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/pyramid' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildPyramid());
          return;
        }
        if (path === '/api/pyramid/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runPyramidSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/pyramid/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackPyramidFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/pyramid/sys/resolve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, resolvePyramidSys(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/pyramid/net/heal' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, healPyramidNet(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/pyramid/comms/flush' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, flushPyramidComms(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 166–180 ──

        if (path === '/api/signage' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, signageSummary());
          return;
        }
        if (path === '/api/signage' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSignage(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/signage/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSignage(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/wayfind' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, wayfindSummary());
          return;
        }
        if (path === '/api/wayfind' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWayfind(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/wayfind/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWayfind(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/beaconmap' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, beaconmapSummary());
          return;
        }
        if (path === '/api/beaconmap' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBeaconmap(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/beaconmap/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBeaconmap(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/iotgates' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, iotgatesSummary());
          return;
        }
        if (path === '/api/iotgates' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createIotgates(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/iotgates/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateIotgates(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/powerops' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, poweropsSummary());
          return;
        }
        if (path === '/api/powerops' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPowerops(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/powerops/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePowerops(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/waterops' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, wateropsSummary());
          return;
        }
        if (path === '/api/waterops' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWaterops(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/waterops/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWaterops(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/greenops' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, greenopsSummary());
          return;
        }
        if (path === '/api/greenops' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGreenops(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/greenops/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGreenops(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pestctrl' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pestctrlSummary());
          return;
        }
        if (path === '/api/pestctrl' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPestctrl(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pestctrl/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePestctrl(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/chemlog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, chemlogSummary());
          return;
        }
        if (path === '/api/chemlog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createChemlog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/chemlog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateChemlog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/poolops' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, poolopsSummary());
          return;
        }
        if (path === '/api/poolops' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPoolops(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/poolops/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePoolops(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/saunaops' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, saunaopsSummary());
          return;
        }
        if (path === '/api/saunaops' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSaunaops(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/saunaops/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSaunaops(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/steamops' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, steamopsSummary());
          return;
        }
        if (path === '/api/steamops' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSteamops(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/steamops/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSteamops(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/icebath' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, icebathSummary());
          return;
        }
        if (path === '/api/icebath' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createIcebath(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/icebath/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateIcebath(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/recovslots' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, recovslotsSummary());
          return;
        }
        if (path === '/api/recovslots' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRecovslots(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/recovslots/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRecovslots(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/signalhub' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildSignalhub());
          return;
        }
        if (path === '/api/signalhub/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runSignalhubSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/signalhub/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackSignalhubFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/signalhub/water/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearSignalhubWater(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/signalhub/chem/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearSignalhubChem(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/signalhub/gate/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearSignalhubGate(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 181–195 ──

        if (path === '/api/helipad' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, helipadSummary());
          return;
        }
        if (path === '/api/helipad' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHelipad(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/helipad/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHelipad(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/jetski' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, jetskiSummary());
          return;
        }
        if (path === '/api/jetski' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createJetski(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/jetski/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateJetski(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/yacht' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, yachtSummary());
          return;
        }
        if (path === '/api/yacht' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createYacht(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/yacht/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateYacht(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/surfschool' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, surfschoolSummary());
          return;
        }
        if (path === '/api/surfschool' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSurfschool(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/surfschool/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSurfschool(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/paddle' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, paddleSummary());
          return;
        }
        if (path === '/api/paddle' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPaddle(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/paddle/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePaddle(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/climwall' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, climwallSummary());
          return;
        }
        if (path === '/api/climwall' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createClimwall(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/climwall/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateClimwall(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/escaperoom' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, escaperoomSummary());
          return;
        }
        if (path === '/api/escaperoom' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEscaperoom(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/escaperoom/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEscaperoom(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/arcade' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, arcadeSummary());
          return;
        }
        if (path === '/api/arcade' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createArcade(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/arcade/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateArcade(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/bowling' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, bowlingSummary());
          return;
        }
        if (path === '/api/bowling' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBowling(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/bowling/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBowling(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/billiards' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, billiardsSummary());
          return;
        }
        if (path === '/api/billiards' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBilliards(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/billiards/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBilliards(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pokertable' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pokertableSummary());
          return;
        }
        if (path === '/api/pokertable' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPokertable(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pokertable/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePokertable(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/trivia' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, triviaSummary());
          return;
        }
        if (path === '/api/trivia' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTrivia(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/trivia/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTrivia(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/djbooth' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, djboothSummary());
          return;
        }
        if (path === '/api/djbooth' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDjbooth(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/djbooth/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDjbooth(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/soundcheck' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, soundcheckSummary());
          return;
        }
        if (path === '/api/soundcheck' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSoundcheck(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/soundcheck/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSoundcheck(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/skyline' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildSkyline());
          return;
        }
        if (path === '/api/skyline/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runSkylineSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/skyline/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackSkylineFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/skyline/sound/pass' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, passSkylineSound(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/skyline/escape/end' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, endSkylineEscape(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/skyline/jet/service' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, serviceSkylineJet(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 196–210 ──

        if (path === '/api/crowddens' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, crowddensSummary());
          return;
        }
        if (path === '/api/crowddens' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCrowddens(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/crowddens/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCrowddens(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/queuetimes' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, queuetimesSummary());
          return;
        }
        if (path === '/api/queuetimes' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createQueuetimes(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/queuetimes/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateQueuetimes(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/lostchild' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, lostchildSummary());
          return;
        }
        if (path === '/api/lostchild' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLostchild(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/lostchild/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLostchild(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/firstaid' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, firstaidSummary());
          return;
        }
        if (path === '/api/firstaid' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFirstaid(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/firstaid/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFirstaid(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/aedcheck' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, aedcheckSummary());
          return;
        }
        if (path === '/api/aedcheck' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAedcheck(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/aedcheck/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAedcheck(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/evacdrill' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, evacdrillSummary());
          return;
        }
        if (path === '/api/evacdrill' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEvacdrill(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/evacdrill/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEvacdrill(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/crowdctrl' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, crowdctrlSummary());
          return;
        }
        if (path === '/api/crowdctrl' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCrowdctrl(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/crowdctrl/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCrowdctrl(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/radiolog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, radiologSummary());
          return;
        }
        if (path === '/api/radiolog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRadiolog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/radiolog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRadiolog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/gatequeue' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, gatequeueSummary());
          return;
        }
        if (path === '/api/gatequeue' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGatequeue(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/gatequeue/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGatequeue(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/wristscan' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, wristscanSummary());
          return;
        }
        if (path === '/api/wristscan' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWristscan(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/wristscan/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWristscan(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/facepass' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, facepassSummary());
          return;
        }
        if (path === '/api/facepass' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFacepass(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/facepass/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFacepass(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/bagcheck' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, bagcheckSummary());
          return;
        }
        if (path === '/api/bagcheck' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBagcheck(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/bagcheck/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBagcheck(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/metaldet' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, metaldetSummary());
          return;
        }
        if (path === '/api/metaldet' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMetaldet(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/metaldet/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMetaldet(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/watchlist' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, watchlistSummary());
          return;
        }
        if (path === '/api/watchlist' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWatchlist(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/watchlist/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWatchlist(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/sentinel' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildSentinel());
          return;
        }
        if (path === '/api/sentinel/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runSentinelSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/sentinel/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackSentinelFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/sentinel/lost/resolve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, resolveSentinelLost(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/sentinel/aed/service' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, serviceSentinelAed(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/sentinel/gate/flow' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, flowSentinelGate(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 211–225 ──

        if (path === '/api/menuboard' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, menuboardSummary());
          return;
        }
        if (path === '/api/menuboard' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMenuboard(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/menuboard/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMenuboard(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/allergenalert' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, allergenalertSummary());
          return;
        }
        if (path === '/api/allergenalert' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAllergenalert(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/allergenalert/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAllergenalert(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/tempprobe' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, tempprobeSummary());
          return;
        }
        if (path === '/api/tempprobe' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTempprobe(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/tempprobe/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTempprobe(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/prepqueue' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, prepqueueSummary());
          return;
        }
        if (path === '/api/prepqueue' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPrepqueue(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/prepqueue/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePrepqueue(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/voidlog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, voidlogSummary());
          return;
        }
        if (path === '/api/voidlog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createVoidlog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/voidlog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateVoidlog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/comps' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, compsSummary());
          return;
        }
        if (path === '/api/comps' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createComps(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/comps/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateComps(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/splitbill' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, splitbillSummary());
          return;
        }
        if (path === '/api/splitbill' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSplitbill(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/splitbill/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSplitbill(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/tabopen' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, tabopenSummary());
          return;
        }
        if (path === '/api/tabopen' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTabopen(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/tabopen/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTabopen(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/corkage' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, corkageSummary());
          return;
        }
        if (path === '/api/corkage' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCorkage(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/corkage/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCorkage(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sommelier' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sommelierSummary());
          return;
        }
        if (path === '/api/sommelier' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSommelier(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sommelier/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSommelier(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/chefnote' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, chefnoteSummary());
          return;
        }
        if (path === '/api/chefnote' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createChefnote(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/chefnote/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateChefnote(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/passticket' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, passticketSummary());
          return;
        }
        if (path === '/api/passticket' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPassticket(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/passticket/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePassticket(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/zoneheat' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, zoneheatSummary());
          return;
        }
        if (path === '/api/zoneheat' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createZoneheat(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/zoneheat/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateZoneheat(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/revpulse' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, revpulseSummary());
          return;
        }
        if (path === '/api/revpulse' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRevpulse(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/revpulse/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRevpulse(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/horizon' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildHorizon());
          return;
        }
        if (path === '/api/horizon/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runHorizonSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/horizon/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackHorizonFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/horizon/allergy/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearHorizonAllergy(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/horizon/tab/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeHorizonTabs(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/horizon/comp/approve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, approveHorizonComps(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 226–240 ──

        if (path === '/api/stayext' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, stayextSummary());
          return;
        }
        if (path === '/api/stayext' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createStayext(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/stayext/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateStayext(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/roommove' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, roommoveSummary());
          return;
        }
        if (path === '/api/roommove' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRoommove(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/roommove/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRoommove(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/earlyin' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, earlyinSummary());
          return;
        }
        if (path === '/api/earlyin' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEarlyin(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/earlyin/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEarlyin(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/luggage' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, luggageSummary());
          return;
        }
        if (path === '/api/luggage' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLuggage(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/luggage/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLuggage(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/turndown' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, turndownSummary());
          return;
        }
        if (path === '/api/turndown' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTurndown(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/turndown/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTurndown(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pillowmenu' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pillowmenuSummary());
          return;
        }
        if (path === '/api/pillowmenu' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPillowmenu(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pillowmenu/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePillowmenu(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/scenting' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, scentingSummary());
          return;
        }
        if (path === '/api/scenting' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createScenting(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/scenting/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateScenting(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/dndflags' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, dndflagsSummary());
          return;
        }
        if (path === '/api/dndflags' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDndflags(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/dndflags/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDndflags(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/bathstock' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, bathstockSummary());
          return;
        }
        if (path === '/api/bathstock' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBathstock(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/bathstock/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBathstock(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ironreq' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ironreqSummary());
          return;
        }
        if (path === '/api/ironreq' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createIronreq(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ironreq/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateIronreq(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pressing' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pressingSummary());
          return;
        }
        if (path === '/api/pressing' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPressing(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pressing/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePressing(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/shoeshine' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, shoeshineSummary());
          return;
        }
        if (path === '/api/shoeshine' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createShoeshine(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/shoeshine/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateShoeshine(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/babycot' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, babycotSummary());
          return;
        }
        if (path === '/api/babycot' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBabycot(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/babycot/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBabycot(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/petstay' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, petstaySummary());
          return;
        }
        if (path === '/api/petstay' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPetstay(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/petstay/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePetstay(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/meridian' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildMeridian());
          return;
        }
        if (path === '/api/meridian/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runMeridianSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/meridian/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackMeridianFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/meridian/move/approve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, approveMeridianMove(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/meridian/early/approve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, approveMeridianEarly(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/meridian/turndown/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearMeridianTurndown(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 241–255 ──

        if (path === '/api/arbill' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, arbillSummary());
          return;
        }
        if (path === '/api/arbill' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createArbill(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/arbill/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateArbill(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/apbill' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, apbillSummary());
          return;
        }
        if (path === '/api/apbill' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createApbill(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/apbill/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateApbill(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/bankrec' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, bankrecSummary());
          return;
        }
        if (path === '/api/bankrec' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBankrec(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/bankrec/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBankrec(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/fxrates' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, fxratesSummary());
          return;
        }
        if (path === '/api/fxrates' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFxrates(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/fxrates/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFxrates(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/tipout' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, tipoutSummary());
          return;
        }
        if (path === '/api/tipout' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTipout(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/tipout/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTipout(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/deposit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, depositSummary());
          return;
        }
        if (path === '/api/deposit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDeposit(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/deposit/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDeposit(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/refunds' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, refundsSummary());
          return;
        }
        if (path === '/api/refunds' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRefunds(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/refunds/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRefunds(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/chargeback' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, chargebackSummary());
          return;
        }
        if (path === '/api/chargeback' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createChargeback(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/chargeback/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateChargeback(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/giftred' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, giftredSummary());
          return;
        }
        if (path === '/api/giftred' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGiftred(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/giftred/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGiftred(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/memberbill' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, memberbillSummary());
          return;
        }
        if (path === '/api/memberbill' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMemberbill(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/memberbill/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMemberbill(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/rateplan' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, rateplanSummary());
          return;
        }
        if (path === '/api/rateplan' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRateplan(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/rateplan/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRateplan(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/channelmgr' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, channelmgrSummary());
          return;
        }
        if (path === '/api/channelmgr' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createChannelmgr(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/channelmgr/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateChannelmgr(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/overbook' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, overbookSummary());
          return;
        }
        if (path === '/api/overbook' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOverbook(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/overbook/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOverbook(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/yieldrule' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, yieldruleSummary());
          return;
        }
        if (path === '/api/yieldrule' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createYieldrule(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/yieldrule/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateYieldrule(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/ledger' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildLedger());
          return;
        }
        if (path === '/api/ledger/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runLedgerSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/ledger/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackLedgerFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/ledger/ar/collect' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, collectLedgerAr(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/ledger/chargeback/resolve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, resolveLedgerChargeback(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/ledger/channel/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearLedgerChannel(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 256–270 ──

        if (path === '/api/onboarding' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, onboardingSummary());
          return;
        }
        if (path === '/api/onboarding' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOnboarding(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/onboarding/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOnboarding(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/offboarding' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, offboardingSummary());
          return;
        }
        if (path === '/api/offboarding' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOffboarding(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/offboarding/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOffboarding(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/interviews' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, interviewsSummary());
          return;
        }
        if (path === '/api/interviews' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createInterviews(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/interviews/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateInterviews(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/certifications' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, certificationsSummary());
          return;
        }
        if (path === '/api/certifications' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCertifications(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/certifications/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCertifications(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/langskill' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, langskillSummary());
          return;
        }
        if (path === '/api/langskill' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLangskill(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/langskill/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLangskill(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/shiftswap' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, shiftswapSummary());
          return;
        }
        if (path === '/api/shiftswap' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createShiftswap(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/shiftswap/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateShiftswap(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/leaverequest' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, leaverequestSummary());
          return;
        }
        if (path === '/api/leaverequest' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLeaverequest(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/leaverequest/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLeaverequest(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/attendance' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, attendanceSummary());
          return;
        }
        if (path === '/api/attendance' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAttendance(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/attendance/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAttendance(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/performance' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, performanceSummary());
          return;
        }
        if (path === '/api/performance' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPerformance(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/performance/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePerformance(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/recognition' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, recognitionSummary());
          return;
        }
        if (path === '/api/recognition' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRecognition(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/recognition/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRecognition(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/handbook' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, handbookSummary());
          return;
        }
        if (path === '/api/handbook' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHandbook(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/handbook/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHandbook(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/safetybrief' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, safetybriefSummary());
          return;
        }
        if (path === '/api/safetybrief' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSafetybrief(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/safetybrief/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSafetybrief(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/nearmiss' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, nearmissSummary());
          return;
        }
        if (path === '/api/nearmiss' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createNearmiss(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/nearmiss/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateNearmiss(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/whistle' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, whistleSummary());
          return;
        }
        if (path === '/api/whistle' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWhistle(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/whistle/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWhistle(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/peoplehub' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildPeoplehub());
          return;
        }
        if (path === '/api/peoplehub/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runPeoplehubSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/peoplehub/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackPeoplehubFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/peoplehub/leave/approve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, approvePeoplehubLeave(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/peoplehub/nearmiss/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closePeoplehubNearmiss(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/peoplehub/onboarding/complete' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, completePeoplehubOnboarding(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 271–285 ──

        if (path === '/api/carbonlog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, carbonlogSummary());
          return;
        }
        if (path === '/api/carbonlog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCarbonlog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/carbonlog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCarbonlog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/wateraudit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, waterauditSummary());
          return;
        }
        if (path === '/api/wateraudit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWateraudit(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/wateraudit/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWateraudit(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/airquality' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, airqualitySummary());
          return;
        }
        if (path === '/api/airquality' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAirquality(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/airquality/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAirquality(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/solarops' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, solaropsSummary());
          return;
        }
        if (path === '/api/solarops' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSolarops(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/solarops/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSolarops(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/biodiversity' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, biodiversitySummary());
          return;
        }
        if (path === '/api/biodiversity' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBiodiversity(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/biodiversity/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBiodiversity(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/recycling' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, recyclingSummary());
          return;
        }
        if (path === '/api/recycling' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRecycling(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/recycling/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRecycling(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/greencert' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, greencertSummary());
          return;
        }
        if (path === '/api/greencert' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGreencert(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/greencert/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGreencert(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/auditfind' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, auditfindSummary());
          return;
        }
        if (path === '/api/auditfind' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAuditfind(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/auditfind/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAuditfind(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/policyack' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, policyackSummary());
          return;
        }
        if (path === '/api/policyack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPolicyack(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/policyack/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePolicyack(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/dataprotect' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, dataprotectSummary());
          return;
        }
        if (path === '/api/dataprotect' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDataprotect(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/dataprotect/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDataprotect(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/retention' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, retentionSummary());
          return;
        }
        if (path === '/api/retention' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRetention(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/retention/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRetention(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/accessreview' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, accessreviewSummary());
          return;
        }
        if (path === '/api/accessreview' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAccessreview(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/accessreview/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAccessreview(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/vendorrisk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, vendorriskSummary());
          return;
        }
        if (path === '/api/vendorrisk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createVendorrisk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/vendorrisk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateVendorrisk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/legalhold' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, legalholdSummary());
          return;
        }
        if (path === '/api/legalhold' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLegalhold(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/legalhold/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLegalhold(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/ecosphere' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildEcosphere());
          return;
        }
        if (path === '/api/ecosphere/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runEcosphereSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/ecosphere/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackEcosphereFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/ecosphere/finding/mitigate' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, mitigateEcosphereFinding(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/ecosphere/dataprotect/fulfill' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, fulfillEcosphereDataprotect(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/ecosphere/vendor/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearEcosphereVendorHigh(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 286–300 ──

        if (path === '/api/presskit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, presskitSummary());
          return;
        }
        if (path === '/api/presskit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPresskit(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/presskit/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePresskit(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/influencer' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, influencerSummary());
          return;
        }
        if (path === '/api/influencer' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createInfluencer(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/influencer/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateInfluencer(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ugcmod' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ugcmodSummary());
          return;
        }
        if (path === '/api/ugcmod' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createUgcmod(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ugcmod/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateUgcmod(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/seoaudit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, seoauditSummary());
          return;
        }
        if (path === '/api/seoaudit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSeoaudit(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/seoaudit/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSeoaudit(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/adspend' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, adspendSummary());
          return;
        }
        if (path === '/api/adspend' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAdspend(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/adspend/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAdspend(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/brandguard' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, brandguardSummary());
          return;
        }
        if (path === '/api/brandguard' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBrandguard(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/brandguard/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBrandguard(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/storyboard' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, storyboardSummary());
          return;
        }
        if (path === '/api/storyboard' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createStoryboard(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/storyboard/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateStoryboard(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/livestream' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, livestreamSummary());
          return;
        }
        if (path === '/api/livestream' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLivestream(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/livestream/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLivestream(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/podcastshow' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, podcastshowSummary());
          return;
        }
        if (path === '/api/podcastshow' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPodcastshow(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/podcastshow/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePodcastshow(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/newsletter' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, newsletterSummary());
          return;
        }
        if (path === '/api/newsletter' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createNewsletter(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/newsletter/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateNewsletter(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/tagmap' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, tagmapSummary());
          return;
        }
        if (path === '/api/tagmap' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTagmap(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/tagmap/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTagmap(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/socialinbox' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, socialinboxSummary());
          return;
        }
        if (path === '/api/socialinbox' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSocialinbox(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/socialinbox/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSocialinbox(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/mediaembargo' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, mediaembargoSummary());
          return;
        }
        if (path === '/api/mediaembargo' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMediaembargo(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/mediaembargo/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMediaembargo(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/creativereq' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, creativereqSummary());
          return;
        }
        if (path === '/api/creativereq' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCreativereq(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/creativereq/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCreativereq(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/brandpulse' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildBrandpulse());
          return;
        }
        if (path === '/api/brandpulse/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runBrandpulseSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/brandpulse/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackBrandpulseFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/brandpulse/inbox/triage' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, triageBrandpulseInbox(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/brandpulse/ugc/approve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, approveBrandpulseUgc(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/brandpulse/guard/action' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, actionBrandpulseGuard(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 301–315 ──

        if (path === '/api/modelops' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, modelopsSummary());
          return;
        }
        if (path === '/api/modelops' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createModelops(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/modelops/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateModelops(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/promptlib' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, promptlibSummary());
          return;
        }
        if (path === '/api/promptlib' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPromptlib(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/promptlib/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePromptlib(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/agenteval' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, agentevalSummary());
          return;
        }
        if (path === '/api/agenteval' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAgenteval(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/agenteval/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAgenteval(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/tokenbudget' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, tokenbudgetSummary());
          return;
        }
        if (path === '/api/tokenbudget' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTokenbudget(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/tokenbudget/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTokenbudget(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ragindex' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ragindexSummary());
          return;
        }
        if (path === '/api/ragindex' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRagindex(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ragindex/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRagindex(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/toolpermit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, toolpermitSummary());
          return;
        }
        if (path === '/api/toolpermit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createToolpermit(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/toolpermit/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateToolpermit(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sandboxrun' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sandboxrunSummary());
          return;
        }
        if (path === '/api/sandboxrun' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSandboxrun(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sandboxrun/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSandboxrun(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/hallucheck' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, hallucheckSummary());
          return;
        }
        if (path === '/api/hallucheck' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHallucheck(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/hallucheck/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHallucheck(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/datasetcur' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, datasetcurSummary());
          return;
        }
        if (path === '/api/datasetcur' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDatasetcur(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/datasetcur/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDatasetcur(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/redteam' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, redteamSummary());
          return;
        }
        if (path === '/api/redteam' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRedteam(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/redteam/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRedteam(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/slaagent' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, slaagentSummary());
          return;
        }
        if (path === '/api/slaagent' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSlaagent(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/slaagent/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSlaagent(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/costguard' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, costguardSummary());
          return;
        }
        if (path === '/api/costguard' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCostguard(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/costguard/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCostguard(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/latencylog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, latencylogSummary());
          return;
        }
        if (path === '/api/latencylog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLatencylog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/latencylog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLatencylog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/driftmonitor' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, driftmonitorSummary());
          return;
        }
        if (path === '/api/driftmonitor' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDriftmonitor(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/driftmonitor/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDriftmonitor(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/cognisphere' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildCognisphere());
          return;
        }
        if (path === '/api/cognisphere/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runCognisphereSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/cognisphere/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackCognisphereFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/cognisphere/cost/halt' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, haltCognisphereCost(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/cognisphere/drift/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearCognisphereDrift(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/cognisphere/mitigate' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, mitigateCognisphereRisk(await readBody(req), user.username)); })();
          return;
        }

        // ── AŞAMA 316–330 · Omni-Channel & Autonomous Commerce ──

        if (path === '/api/posbridge' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, posbridgeSummary());
          return;
        }
        if (path === '/api/posbridge' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPosbridge(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/posbridge/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePosbridge(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/dynamint' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, dynamintSummary());
          return;
        }
        if (path === '/api/dynamint' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDynamint(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/dynamint/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDynamint(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/couriertrack' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, couriertrackSummary());
          return;
        }
        if (path === '/api/couriertrack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCouriertrack(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/couriertrack/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCouriertrack(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/autocheckout' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, autocheckoutSummary());
          return;
        }
        if (path === '/api/autocheckout' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAutocheckout(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/autocheckout/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAutocheckout(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/loyaltyburn' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, loyaltyburnSummary());
          return;
        }
        if (path === '/api/loyaltyburn' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLoyaltyburn(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/loyaltyburn/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLoyaltyburn(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/treatoffer' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, treatofferSummary());
          return;
        }
        if (path === '/api/treatoffer' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTreatoffer(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/treatoffer/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTreatoffer(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/omnimarket' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, omnimarketSummary());
          return;
        }
        if (path === '/api/omnimarket' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOmnimarket(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/omnimarket/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOmnimarket(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/clickcollect' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, clickcollectSummary());
          return;
        }
        if (path === '/api/clickcollect' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createClickcollect(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/clickcollect/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateClickcollect(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/lastmile' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, lastmileSummary());
          return;
        }
        if (path === '/api/lastmile' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLastmile(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/lastmile/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLastmile(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/invsync' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, invsyncSummary());
          return;
        }
        if (path === '/api/invsync' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createInvsync(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/invsync/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateInvsync(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pricepush' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pricepushSummary());
          return;
        }
        if (path === '/api/pricepush' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPricepush(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pricepush/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePricepush(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/qrpay' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, qrpaySummary());
          return;
        }
        if (path === '/api/qrpay' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createQrpay(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/qrpay/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateQrpay(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/courierpool' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, courierpoolSummary());
          return;
        }
        if (path === '/api/courierpool' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCourierpool(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/courierpool/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCourierpool(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/giftrelay' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, giftrelaySummary());
          return;
        }
        if (path === '/api/giftrelay' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGiftrelay(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/giftrelay/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGiftrelay(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/vanguard' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildVanguard());
          return;
        }
        if (path === '/api/vanguard/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runVanguardSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/vanguard/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackVanguardFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/vanguard/pos/online' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, forceVanguardPosOnline(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/vanguard/inv/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearVanguardInvDrift(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/vanguard/mint/flush' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, flushVanguardMintQueue(await readBody(req), user.username)); })();
          return;
        }

        // ── Edge Mesh · Lattice (AŞAMA 331–345) ──

        if (path === '/api/edgegate' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, edgegateSummary());
          return;
        }
        if (path === '/api/edgegate' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEdgegate(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/edgegate/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEdgegate(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/meshlink' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, meshlinkSummary());
          return;
        }
        if (path === '/api/meshlink' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMeshlink(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/meshlink/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMeshlink(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/radiomesh' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, radiomeshSummary());
          return;
        }
        if (path === '/api/radiomesh' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRadiomesh(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/radiomesh/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRadiomesh(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sensorfuse' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sensorfuseSummary());
          return;
        }
        if (path === '/api/sensorfuse' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSensorfuse(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sensorfuse/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSensorfuse(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/otafirm' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, otafirmSummary());
          return;
        }
        if (path === '/api/otafirm' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOtafirm(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/otafirm/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOtafirm(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/devinventory' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, devinventorySummary());
          return;
        }
        if (path === '/api/devinventory' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDevinventory(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/devinventory/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDevinventory(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/powerbudget' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, powerbudgetSummary());
          return;
        }
        if (path === '/api/powerbudget' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPowerbudget(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/powerbudget/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePowerbudget(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/backhaul' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, backhaulSummary());
          return;
        }
        if (path === '/api/backhaul' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBackhaul(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/backhaul/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBackhaul(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/edgecache2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, edgecache2Summary());
          return;
        }
        if (path === '/api/edgecache2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEdgecache2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/edgecache2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEdgecache2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/syncrepl' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, syncreplSummary());
          return;
        }
        if (path === '/api/syncrepl' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSyncrepl(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/syncrepl/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSyncrepl(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/failover' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, failoverSummary());
          return;
        }
        if (path === '/api/failover' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFailover(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/failover/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFailover(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/telemetry' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, telemetrySummary());
          return;
        }
        if (path === '/api/telemetry' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTelemetry(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/telemetry/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTelemetry(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/netslice' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, netsliceSummary());
          return;
        }
        if (path === '/api/netslice' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createNetslice(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/netslice/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateNetslice(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/satlink' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, satlinkSummary());
          return;
        }
        if (path === '/api/satlink' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSatlink(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/satlink/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSatlink(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/lattice' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildLattice());
          return;
        }
        if (path === '/api/lattice/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runLatticeSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/lattice/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackLatticeFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/lattice/gate/heal' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, healLatticeGate(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/lattice/ota/retry' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, retryLatticeOta(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/lattice/failback/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearLatticeFailback(await readBody(req), user.username)); })();
          return;
        }

        // ── Guest Twin · Mirror (AŞAMA 346–360) ──

        if (path === '/api/guesttwin' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, guesttwinSummary());
          return;
        }
        if (path === '/api/guesttwin' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGuesttwin(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/guesttwin/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGuesttwin(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/prefgraph' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, prefgraphSummary());
          return;
        }
        if (path === '/api/prefgraph' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPrefgraph(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/prefgraph/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePrefgraph(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/intentscore' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, intentscoreSummary());
          return;
        }
        if (path === '/api/intentscore' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createIntentscore(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/intentscore/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateIntentscore(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/nextbest' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, nextbestSummary());
          return;
        }
        if (path === '/api/nextbest' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createNextbest(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/nextbest/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateNextbest(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/journeymap' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, journeymapSummary());
          return;
        }
        if (path === '/api/journeymap' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createJourneymap(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/journeymap/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateJourneymap(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/microseg' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, microsegSummary());
          return;
        }
        if (path === '/api/microseg' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMicroseg(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/microseg/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMicroseg(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/offerlab' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, offerlabSummary());
          return;
        }
        if (path === '/api/offerlab' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOfferlab(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/offerlab/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOfferlab(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/consentgraph' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, consentgraphSummary());
          return;
        }
        if (path === '/api/consentgraph' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createConsentgraph(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/consentgraph/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateConsentgraph(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/emotionpulse' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, emotionpulseSummary());
          return;
        }
        if (path === '/api/emotionpulse' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEmotionpulse(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/emotionpulse/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEmotionpulse(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/servicememory' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, servicememorySummary());
          return;
        }
        if (path === '/api/servicememory' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createServicememory(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/servicememory/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateServicememory(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/recoverypath' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, recoverypathSummary());
          return;
        }
        if (path === '/api/recoverypath' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRecoverypath(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/recoverypath/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRecoverypath(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/lifetimeval' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, lifetimevalSummary());
          return;
        }
        if (path === '/api/lifetimeval' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLifetimeval(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/lifetimeval/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLifetimeval(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/churnrisk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, churnriskSummary());
          return;
        }
        if (path === '/api/churnrisk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createChurnrisk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/churnrisk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateChurnrisk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/wowmoment' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, wowmomentSummary());
          return;
        }
        if (path === '/api/wowmoment' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWowmoment(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/wowmoment/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWowmoment(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/mirror' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildMirror());
          return;
        }
        if (path === '/api/mirror/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runMirrorSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/mirror/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackMirrorFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/mirror/twin/refresh' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, refreshMirrorTwin(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/mirror/nba/accept' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, acceptMirrorNba(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/mirror/recovery/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeMirrorRecovery(await readBody(req), user.username)); })();
          return;
        }

        // ── Command Fabric · Keystone (AŞAMA 361–375) ──

        if (path === '/api/incidentbus' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, incidentbusSummary());
          return;
        }
        if (path === '/api/incidentbus' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createIncidentbus(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/incidentbus/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateIncidentbus(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/playtrigger' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, playtriggerSummary());
          return;
        }
        if (path === '/api/playtrigger' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPlaytrigger(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/playtrigger/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePlaytrigger(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/escalation' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, escalationSummary());
          return;
        }
        if (path === '/api/escalation' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEscalation(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/escalation/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEscalation(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/warroomseat' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, warroomseatSummary());
          return;
        }
        if (path === '/api/warroomseat' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWarroomseat(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/warroomseat/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWarroomseat(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/decisionlog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, decisionlogSummary());
          return;
        }
        if (path === '/api/decisionlog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDecisionlog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/decisionlog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDecisionlog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/slotrack' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, slotrackSummary());
          return;
        }
        if (path === '/api/slotrack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSlotrack(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/slotrack/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSlotrack(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/errorbudget' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, errorbudgetSummary());
          return;
        }
        if (path === '/api/errorbudget' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createErrorbudget(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/errorbudget/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateErrorbudget(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/changewindow' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, changewindowSummary());
          return;
        }
        if (path === '/api/changewindow' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createChangewindow(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/changewindow/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateChangewindow(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/blameless' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, blamelessSummary());
          return;
        }
        if (path === '/api/blameless' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBlameless(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/blameless/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBlameless(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pagerduty' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pagerdutySummary());
          return;
        }
        if (path === '/api/pagerduty' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPagerduty(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pagerduty/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePagerduty(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/statuspage' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, statuspageSummary());
          return;
        }
        if (path === '/api/statuspage' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createStatuspage(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/statuspage/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateStatuspage(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/runbooklink' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, runbooklinkSummary());
          return;
        }
        if (path === '/api/runbooklink' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRunbooklink(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/runbooklink/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRunbooklink(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/commsbridge' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, commsbridgeSummary());
          return;
        }
        if (path === '/api/commsbridge' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCommsbridge(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/commsbridge/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCommsbridge(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/afteraction' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, afteractionSummary());
          return;
        }
        if (path === '/api/afteraction' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAfteraction(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/afteraction/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAfteraction(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/keystone' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildKeystone());
          return;
        }
        if (path === '/api/keystone/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runKeystoneSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/keystone/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackKeystoneFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/keystone/bus/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearKeystoneBus(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/keystone/slo/heal' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, healKeystoneSlo(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/keystone/esc/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeKeystoneEsc(await readBody(req), user.username)); })();
          return;
        }

        // ── Revenue OS · Zenith (AŞAMA 376–390) ──

        if (path === '/api/revstream' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, revstreamSummary());
          return;
        }
        if (path === '/api/revstream' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRevstream(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/revstream/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRevstream(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/packagemix' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, packagemixSummary());
          return;
        }
        if (path === '/api/packagemix' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPackagemix(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/packagemix/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePackagemix(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ancillary' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ancillarySummary());
          return;
        }
        if (path === '/api/ancillary' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAncillary(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ancillary/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAncillary(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/dynamicbundle' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, dynamicbundleSummary());
          return;
        }
        if (path === '/api/dynamicbundle' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDynamicbundle(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/dynamicbundle/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDynamicbundle(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pricefloor' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pricefloorSummary());
          return;
        }
        if (path === '/api/pricefloor' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPricefloor(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pricefloor/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePricefloor(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/compset' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, compsetSummary());
          return;
        }
        if (path === '/api/compset' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCompset(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/compset/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCompset(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pickuppace' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pickuppaceSummary());
          return;
        }
        if (path === '/api/pickuppace' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPickuppace(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pickuppace/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePickuppace(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/noshowrisk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, noshowriskSummary());
          return;
        }
        if (path === '/api/noshowrisk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createNoshowrisk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/noshowrisk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateNoshowrisk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/walkinflow' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, walkinflowSummary());
          return;
        }
        if (path === '/api/walkinflow' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWalkinflow(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/walkinflow/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWalkinflow(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/tableturn' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, tableturnSummary());
          return;
        }
        if (path === '/api/tableturn' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTableturn(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/tableturn/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTableturn(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/beatrevenue' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, beatrevenueSummary());
          return;
        }
        if (path === '/api/beatrevenue' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBeatrevenue(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/beatrevenue/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBeatrevenue(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cashforecast' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cashforecastSummary());
          return;
        }
        if (path === '/api/cashforecast' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCashforecast(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cashforecast/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCashforecast(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/marginwatch' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, marginwatchSummary());
          return;
        }
        if (path === '/api/marginwatch' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMarginwatch(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/marginwatch/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMarginwatch(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/promoattr' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, promoattrSummary());
          return;
        }
        if (path === '/api/promoattr' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPromoattr(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/promoattr/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePromoattr(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/zenith' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildZenith());
          return;
        }
        if (path === '/api/zenith/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runZenithSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/zenith/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackZenithFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/zenith/pace/catch' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, catchZenithPace(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/zenith/margin/heal' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, healZenithMargin(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/zenith/demand/cool' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, coolZenithDemand(await readBody(req), user.username)); })();
          return;
        }

        // ── Finale · Odyssey (AŞAMA 391–405) ──

        if (path === '/api/masterplan' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, masterplanSummary());
          return;
        }
        if (path === '/api/masterplan' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMasterplan(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/masterplan/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMasterplan(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/okrrack' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, okrrackSummary());
          return;
        }
        if (path === '/api/okrrack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOkrrack(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/okrrack/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOkrrack(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/roadmap' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, roadmapSummary());
          return;
        }
        if (path === '/api/roadmap' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRoadmap(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/roadmap/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRoadmap(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/betboard' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, betboardSummary());
          return;
        }
        if (path === '/api/betboard' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBetboard(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/betboard/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBetboard(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/portfoliorisk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, portfolioriskSummary());
          return;
        }
        if (path === '/api/portfoliorisk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPortfoliorisk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/portfoliorisk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePortfoliorisk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/captable' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, captableSummary());
          return;
        }
        if (path === '/api/captable' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCaptable(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/captable/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCaptable(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/boardmotion' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, boardmotionSummary());
          return;
        }
        if (path === '/api/boardmotion' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBoardmotion(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/boardmotion/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBoardmotion(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/alliance' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, allianceSummary());
          return;
        }
        if (path === '/api/alliance' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAlliance(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/alliance/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAlliance(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/expansion' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, expansionSummary());
          return;
        }
        if (path === '/api/expansion' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createExpansion(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/expansion/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateExpansion(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/legacyarc' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, legacyarcSummary());
          return;
        }
        if (path === '/api/legacyarc' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLegacyarc(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/legacyarc/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLegacyarc(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/culturecode' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, culturecodeSummary());
          return;
        }
        if (path === '/api/culturecode' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCulturecode(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/culturecode/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCulturecode(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/talentbet' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, talentbetSummary());
          return;
        }
        if (path === '/api/talentbet' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTalentbet(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/talentbet/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTalentbet(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/moatwatch' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, moatwatchSummary());
          return;
        }
        if (path === '/api/moatwatch' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMoatwatch(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/moatwatch/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMoatwatch(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/northstar' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, northstarSummary());
          return;
        }
        if (path === '/api/northstar' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createNorthstar(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/northstar/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateNorthstar(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/odyssey' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildOdyssey());
          return;
        }
        if (path === '/api/odyssey/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runOdysseySweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/odyssey/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackOdysseyFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/odyssey/okr/recover' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, recoverOdysseyOkr(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/odyssey/risk/cool' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, coolOdysseyRisk(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/odyssey/star/hit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, hitOdysseyStar(await readBody(req), user.username)); })();
          return;
        }

        // ── Coastal Ops · Tide (AŞAMA 406–420) ──

        if (path === '/api/tidewatch' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, tidewatchSummary());
          return;
        }
        if (path === '/api/tidewatch' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTidewatch(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/tidewatch/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTidewatch(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/duneops' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, duneopsSummary());
          return;
        }
        if (path === '/api/duneops' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDuneops(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/duneops/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDuneops(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/snorkelbay' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, snorkelbaySummary());
          return;
        }
        if (path === '/api/snorkelbay' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSnorkelbay(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/snorkelbay/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSnorkelbay(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cliffpath' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cliffpathSummary());
          return;
        }
        if (path === '/api/cliffpath' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCliffpath(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cliffpath/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCliffpath(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/campglow' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, campglowSummary());
          return;
        }
        if (path === '/api/campglow' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCampglow(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/campglow/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCampglow(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/lookout' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, lookoutSummary());
          return;
        }
        if (path === '/api/lookout' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLookout(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/lookout/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLookout(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/reefguard' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, reefguardSummary());
          return;
        }
        if (path === '/api/reefguard' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createReefguard(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/reefguard/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateReefguard(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pierops' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pieropsSummary());
          return;
        }
        if (path === '/api/pierops' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPierops(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pierops/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePierops(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/saildesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, saildeskSummary());
          return;
        }
        if (path === '/api/saildesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSaildesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/saildesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSaildesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/umbrellamap' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, umbrellamapSummary());
          return;
        }
        if (path === '/api/umbrellamap' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createUmbrellamap(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/umbrellamap/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateUmbrellamap(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sandclean' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sandcleanSummary());
          return;
        }
        if (path === '/api/sandclean' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSandclean(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sandclean/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSandclean(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/nightswim' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, nightswimSummary());
          return;
        }
        if (path === '/api/nightswim' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createNightswim(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/nightswim/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateNightswim(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/stargaze' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, stargazeSummary());
          return;
        }
        if (path === '/api/stargaze' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createStargaze(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/stargaze/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateStargaze(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/coastpatrol' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, coastpatrolSummary());
          return;
        }
        if (path === '/api/coastpatrol' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCoastpatrol(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/coastpatrol/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCoastpatrol(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/tide' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildTide());
          return;
        }
        if (path === '/api/tide/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runTideSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/tide/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackTideFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/tide/reef/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearTideReef(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/tide/cliff/open' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, openTideCliff(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/tide/pier/free' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, freeTidePier(await readBody(req), user.username)); })();
          return;
        }

        // ── Port Logistics · Harbor (AŞAMA 421–435) ──

        if (path === '/api/harborlane' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, harborlaneSummary());
          return;
        }
        if (path === '/api/harborlane' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHarborlane(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/harborlane/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHarborlane(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/dockslot' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, dockslotSummary());
          return;
        }
        if (path === '/api/dockslot' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDockslot(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/dockslot/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDockslot(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/craneops' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, craneopsSummary());
          return;
        }
        if (path === '/api/craneops' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCraneops(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/craneops/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCraneops(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/container' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, containerSummary());
          return;
        }
        if (path === '/api/container' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createContainer(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/container/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateContainer(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/coldbay' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, coldbaySummary());
          return;
        }
        if (path === '/api/coldbay' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createColdbay(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/coldbay/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateColdbay(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/yardmove' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, yardmoveSummary());
          return;
        }
        if (path === '/api/yardmove' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createYardmove(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/yardmove/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateYardmove(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/gatepass' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, gatepassSummary());
          return;
        }
        if (path === '/api/gatepass' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGatepass(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/gatepass/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGatepass(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/bolthold' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, boltholdSummary());
          return;
        }
        if (path === '/api/bolthold' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBolthold(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/bolthold/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBolthold(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/manifest' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, manifestSummary());
          return;
        }
        if (path === '/api/manifest' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createManifest(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/manifest/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateManifest(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/demurrage' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, demurrageSummary());
          return;
        }
        if (path === '/api/demurrage' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDemurrage(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/demurrage/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDemurrage(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pilotage' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pilotageSummary());
          return;
        }
        if (path === '/api/pilotage' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPilotage(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pilotage/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePilotage(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/tugassist' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, tugassistSummary());
          return;
        }
        if (path === '/api/tugassist' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTugassist(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/tugassist/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTugassist(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/wharfage' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, wharfageSummary());
          return;
        }
        if (path === '/api/wharfage' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWharfage(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/wharfage/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWharfage(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/stevedore' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, stevedoreSummary());
          return;
        }
        if (path === '/api/stevedore' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createStevedore(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/stevedore/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateStevedore(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/harbor' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildHarbor());
          return;
        }
        if (path === '/api/harbor/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runHarborSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/harbor/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackHarborFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/harbor/cold/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearHarborCold(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/harbor/hold/release' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, releaseHarborHold(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/harbor/demurrage/invoice' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, invoiceHarborDemurrage(await readBody(req), user.username)); })();
          return;
        }

        // ── Experience Fabric · Aurora (AŞAMA 436–450) ──

        if (path === '/api/auroradeck' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, auroradeckSummary());
          return;
        }
        if (path === '/api/auroradeck' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAuroradeck(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/auroradeck/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAuroradeck(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/lightshow' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, lightshowSummary());
          return;
        }
        if (path === '/api/lightshow' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLightshow(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/lightshow/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLightshow(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/soundscape' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, soundscapeSummary());
          return;
        }
        if (path === '/api/soundscape' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSoundscape(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/soundscape/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSoundscape(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/scentzone' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, scentzoneSummary());
          return;
        }
        if (path === '/api/scentzone' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createScentzone(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/scentzone/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateScentzone(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/moodlight' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, moodlightSummary());
          return;
        }
        if (path === '/api/moodlight' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMoodlight(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/moodlight/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMoodlight(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/fogscene' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, fogsceneSummary());
          return;
        }
        if (path === '/api/fogscene' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFogscene(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/fogscene/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFogscene(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/projection' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, projectionSummary());
          return;
        }
        if (path === '/api/projection' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createProjection(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/projection/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateProjection(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/immersive' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, immersiveSummary());
          return;
        }
        if (path === '/api/immersive' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createImmersive(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/immersive/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateImmersive(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/hapticcue' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, hapticcueSummary());
          return;
        }
        if (path === '/api/hapticcue' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHapticcue(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/hapticcue/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHapticcue(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/atmosmix' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, atmosmixSummary());
          return;
        }
        if (path === '/api/atmosmix' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAtmosmix(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/atmosmix/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAtmosmix(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/guestflow' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, guestflowSummary());
          return;
        }
        if (path === '/api/guestflow' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGuestflow(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/guestflow/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGuestflow(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/scenectrl' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, scenectrlSummary());
          return;
        }
        if (path === '/api/scenectrl' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createScenectrl(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/scenectrl/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateScenectrl(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/nightmode' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, nightmodeSummary());
          return;
        }
        if (path === '/api/nightmode' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createNightmode(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/nightmode/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateNightmode(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/dawnmode' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, dawnmodeSummary());
          return;
        }
        if (path === '/api/dawnmode' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDawnmode(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/dawnmode/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDawnmode(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/aurora' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildAurora());
          return;
        }
        if (path === '/api/aurora/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runAuroraSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/aurora/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackAuroraFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/aurora/flow/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearAuroraFlow(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/aurora/light/end' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, endAuroraLight(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/aurora/night/day' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, dayAuroraNightmode(await readBody(req), user.username)); })();
          return;
        }

        // ── Culinary OS · Hearth (AŞAMA 451–465) ──

        if (path === '/api/miseplan' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, miseplanSummary());
          return;
        }
        if (path === '/api/miseplan' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMiseplan(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/miseplan/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMiseplan(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/passrail' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, passrailSummary());
          return;
        }
        if (path === '/api/passrail' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPassrail(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/passrail/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePassrail(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/plateup' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, plateupSummary());
          return;
        }
        if (path === '/api/plateup' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPlateup(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/plateup/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePlateup(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/gardebay' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, gardebaySummary());
          return;
        }
        if (path === '/api/gardebay' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGardebay(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/gardebay/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGardebay(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pastrylab' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pastrylabSummary());
          return;
        }
        if (path === '/api/pastrylab' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPastrylab(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pastrylab/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePastrylab(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/tastingmenu' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, tastingmenuSummary());
          return;
        }
        if (path === '/api/tastingmenu' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTastingmenu(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/tastingmenu/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTastingmenu(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cellarbox' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cellarboxSummary());
          return;
        }
        if (path === '/api/cellarbox' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCellarbox(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cellarbox/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCellarbox(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/barrail' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, barrailSummary());
          return;
        }
        if (path === '/api/barrail' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBarrail(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/barrail/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBarrail(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/roomservice' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, roomserviceSummary());
          return;
        }
        if (path === '/api/roomservice' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRoomservice(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/roomservice/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRoomservice(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/caterdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, caterdeskSummary());
          return;
        }
        if (path === '/api/caterdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCaterdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/caterdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCaterdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/allergenmap' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, allergenmapSummary());
          return;
        }
        if (path === '/api/allergenmap' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAllergenmap(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/allergenmap/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAllergenmap(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/wastekitchen' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, wastekitchenSummary());
          return;
        }
        if (path === '/api/wastekitchen' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWastekitchen(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/wastekitchen/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWastekitchen(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/chefbrief' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, chefbriefSummary());
          return;
        }
        if (path === '/api/chefbrief' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createChefbrief(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/chefbrief/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateChefbrief(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/supplypull' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, supplypullSummary());
          return;
        }
        if (path === '/api/supplypull' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSupplypull(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/supplypull/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSupplypull(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/hearth' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildHearth());
          return;
        }
        if (path === '/api/hearth/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runHearthSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/hearth/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackHearthFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/hearth/pass/run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runHearthPass(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/hearth/allergen/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearHearthAllergen(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/hearth/plate/send' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, sendHearthPlate(await readBody(req), user.username)); })();
          return;
        }

        // ── Wellness OS · Sanctum (AŞAMA 466–480) ──

        if (path === '/api/spaflow' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, spaflowSummary());
          return;
        }
        if (path === '/api/spaflow' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSpaflow(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/spaflow/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSpaflow(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/thermalbay' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, thermalbaySummary());
          return;
        }
        if (path === '/api/thermalbay' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createThermalbay(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/thermalbay/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateThermalbay(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/saunalog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, saunalogSummary());
          return;
        }
        if (path === '/api/saunalog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSaunalog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/saunalog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSaunalog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cryochamber' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cryochamberSummary());
          return;
        }
        if (path === '/api/cryochamber' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCryochamber(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cryochamber/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCryochamber(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/floatpod' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, floatpodSummary());
          return;
        }
        if (path === '/api/floatpod' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFloatpod(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/floatpod/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFloatpod(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/massagebook' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, massagebookSummary());
          return;
        }
        if (path === '/api/massagebook' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMassagebook(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/massagebook/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMassagebook(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/yogamat' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, yogamatSummary());
          return;
        }
        if (path === '/api/yogamat' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createYogamat(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/yogamat/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateYogamat(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/breathwork' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, breathworkSummary());
          return;
        }
        if (path === '/api/breathwork' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBreathwork(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/breathwork/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBreathwork(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/recoverybay' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, recoverybaySummary());
          return;
        }
        if (path === '/api/recoverybay' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRecoverybay(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/recoverybay/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRecoverybay(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ivlounge' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ivloungeSummary());
          return;
        }
        if (path === '/api/ivlounge' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createIvlounge(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ivlounge/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateIvlounge(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sleepcoach' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sleepcoachSummary());
          return;
        }
        if (path === '/api/sleepcoach' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSleepcoach(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sleepcoach/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSleepcoach(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/nutrition' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, nutritionSummary());
          return;
        }
        if (path === '/api/nutrition' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createNutrition(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/nutrition/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateNutrition(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/biomarker' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, biomarkerSummary());
          return;
        }
        if (path === '/api/biomarker' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBiomarker(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/biomarker/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBiomarker(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/wellnesskit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, wellnesskitSummary());
          return;
        }
        if (path === '/api/wellnesskit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWellnesskit(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/wellnesskit/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWellnesskit(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/sanctum' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildSanctum());
          return;
        }
        if (path === '/api/sanctum/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runSanctumSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/sanctum/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackSanctumFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/sanctum/spa/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearSanctumSpa(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/sanctum/bio/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearSanctumBio(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/sanctum/session/complete' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, completeSanctumSession(await readBody(req), user.username)); })();
          return;
        }

        // ── Estate Ops · Citadel (AŞAMA 481–495) ──

        if (path === '/api/assetmap' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, assetmapSummary());
          return;
        }
        if (path === '/api/assetmap' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAssetmap(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/assetmap/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAssetmap(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/plantroom' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, plantroomSummary());
          return;
        }
        if (path === '/api/plantroom' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPlantroom(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/plantroom/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePlantroom(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/hvacloop' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, hvacloopSummary());
          return;
        }
        if (path === '/api/hvacloop' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHvacloop(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/hvacloop/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHvacloop(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/waterloop' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, waterloopSummary());
          return;
        }
        if (path === '/api/waterloop' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWaterloop(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/waterloop/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWaterloop(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/powergrid' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, powergridSummary());
          return;
        }
        if (path === '/api/powergrid' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPowergrid(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/powergrid/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePowergrid(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/elevatorlog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, elevatorlogSummary());
          return;
        }
        if (path === '/api/elevatorlog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createElevatorlog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/elevatorlog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateElevatorlog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/spareparts' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sparepartsSummary());
          return;
        }
        if (path === '/api/spareparts' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSpareparts(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/spareparts/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSpareparts(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/workorder' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, workorderSummary());
          return;
        }
        if (path === '/api/workorder' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWorkorder(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/workorder/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWorkorder(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/leasehold' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, leaseholdSummary());
          return;
        }
        if (path === '/api/leasehold' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLeasehold(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/leasehold/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLeasehold(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/tenantops' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, tenantopsSummary());
          return;
        }
        if (path === '/api/tenantops' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTenantops(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/tenantops/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTenantops(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/facilitytour' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, facilitytourSummary());
          return;
        }
        if (path === '/api/facilitytour' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFacilitytour(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/facilitytour/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFacilitytour(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/capexdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, capexdeskSummary());
          return;
        }
        if (path === '/api/capexdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCapexdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/capexdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCapexdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/opexdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, opexdeskSummary());
          return;
        }
        if (path === '/api/opexdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOpexdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/opexdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOpexdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/estatescan' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, estatescanSummary());
          return;
        }
        if (path === '/api/estatescan' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEstatescan(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/estatescan/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEstatescan(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/citadel' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildCitadel());
          return;
        }
        if (path === '/api/citadel/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runCitadelSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/citadel/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackCitadelFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/citadel/plant/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearCitadelPlant(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/citadel/hvac/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearCitadelHvac(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/citadel/workorder/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeCitadelWorkorder(await readBody(req), user.username)); })();
          return;
        }

        // ── Talent OS · Forge (AŞAMA 496–510) ──

        if (path === '/api/talentdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, talentdeskSummary());
          return;
        }
        if (path === '/api/talentdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTalentdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/talentdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTalentdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/shiftbid' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, shiftbidSummary());
          return;
        }
        if (path === '/api/shiftbid' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createShiftbid(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/shiftbid/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateShiftbid(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/skillmatrix' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, skillmatrixSummary());
          return;
        }
        if (path === '/api/skillmatrix' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSkillmatrix(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/skillmatrix/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSkillmatrix(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/certtrack' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, certtrackSummary());
          return;
        }
        if (path === '/api/certtrack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCerttrack(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/certtrack/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCerttrack(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/traininghub' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, traininghubSummary());
          return;
        }
        if (path === '/api/traininghub' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTraininghub(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/traininghub/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTraininghub(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/mentorship' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, mentorshipSummary());
          return;
        }
        if (path === '/api/mentorship' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMentorship(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/mentorship/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMentorship(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/succession' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, successionSummary());
          return;
        }
        if (path === '/api/succession' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSuccession(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/succession/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSuccession(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/payrollrun' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, payrollrunSummary());
          return;
        }
        if (path === '/api/payrollrun' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPayrollrun(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/payrollrun/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePayrollrun(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/performnote' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, performnoteSummary());
          return;
        }
        if (path === '/api/performnote' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPerformnote(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/performnote/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePerformnote(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/reviewcycle' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, reviewcycleSummary());
          return;
        }
        if (path === '/api/reviewcycle' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createReviewcycle(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/reviewcycle/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateReviewcycle(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/headcount' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, headcountSummary());
          return;
        }
        if (path === '/api/headcount' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHeadcount(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/headcount/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHeadcount(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/attrition' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, attritionSummary());
          return;
        }
        if (path === '/api/attrition' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAttrition(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/attrition/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAttrition(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/culturepulse' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, culturepulseSummary());
          return;
        }
        if (path === '/api/culturepulse' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCulturepulse(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/culturepulse/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCulturepulse(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/shifttrade' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, shifttradeSummary());
          return;
        }
        if (path === '/api/shifttrade' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createShifttrade(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/shifttrade/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateShifttrade(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/forge' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildForge());
          return;
        }
        if (path === '/api/forge/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runForgeSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/forge/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackForgeFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/forge/talent/advance' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, advanceForgeTalent(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/forge/cert/renew' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, renewForgeCert(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/forge/shift/approve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, approveForgeShift(await readBody(req), user.username)); })();
          return;
        }

        // ── Safety OS · Aegis (AŞAMA 511–525) ──

        if (path === '/api/safetylog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, safetylogSummary());
          return;
        }
        if (path === '/api/safetylog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSafetylog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/safetylog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSafetylog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/incidentlog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, incidentlogSummary());
          return;
        }
        if (path === '/api/incidentlog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createIncidentlog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/incidentlog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateIncidentlog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/hazardnote' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, hazardnoteSummary());
          return;
        }
        if (path === '/api/hazardnote' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHazardnote(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/hazardnote/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHazardnote(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/evacroute' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, evacrouteSummary());
          return;
        }
        if (path === '/api/evacroute' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEvacroute(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/evacroute/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEvacroute(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/drillrun' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, drillrunSummary());
          return;
        }
        if (path === '/api/drillrun' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDrillrun(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/drillrun/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDrillrun(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/aidkit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, aidkitSummary());
          return;
        }
        if (path === '/api/aidkit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAidkit(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/aidkit/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAidkit(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/hazmatbay' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, hazmatbaySummary());
          return;
        }
        if (path === '/api/hazmatbay' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHazmatbay(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/hazmatbay/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHazmatbay(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ppekit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ppekitSummary());
          return;
        }
        if (path === '/api/ppekit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPpekit(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ppekit/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePpekit(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/lockouttag' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, lockouttagSummary());
          return;
        }
        if (path === '/api/lockouttag' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLockouttag(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/lockouttag/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLockouttag(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/permitwork' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, permitworkSummary());
          return;
        }
        if (path === '/api/permitwork' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPermitwork(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/permitwork/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePermitwork(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/compliancerow' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, compliancerowSummary());
          return;
        }
        if (path === '/api/compliancerow' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCompliancerow(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/compliancerow/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCompliancerow(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/audittrail2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, audittrail2Summary());
          return;
        }
        if (path === '/api/audittrail2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAudittrail2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/audittrail2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAudittrail2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cctvreview' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cctvreviewSummary());
          return;
        }
        if (path === '/api/cctvreview' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCctvreview(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cctvreview/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCctvreview(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/guestsafety' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, guestsafetySummary());
          return;
        }
        if (path === '/api/guestsafety' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGuestsafety(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/guestsafety/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGuestsafety(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/aegis' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildAegis());
          return;
        }
        if (path === '/api/aegis/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runAegisSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/aegis/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackAegisFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/aegis/safety/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearAegisSafety(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/aegis/incident/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeAegisIncident(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/aegis/evac/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearAegisEvac(await readBody(req), user.username)); })();
          return;
        }

        // ── Intelligence OS · Oracle (AŞAMA 526–540) ──

        if (path === '/api/datalake' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, datalakeSummary());
          return;
        }
        if (path === '/api/datalake' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDatalake(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/datalake/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDatalake(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/featureflag' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, featureflagSummary());
          return;
        }
        if (path === '/api/featureflag' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFeatureflag(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/featureflag/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFeatureflag(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/modelcard' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, modelcardSummary());
          return;
        }
        if (path === '/api/modelcard' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createModelcard(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/modelcard/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateModelcard(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/promptlab' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, promptlabSummary());
          return;
        }
        if (path === '/api/promptlab' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPromptlab(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/promptlab/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePromptlab(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/evalbench' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, evalbenchSummary());
          return;
        }
        if (path === '/api/evalbench' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEvalbench(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/evalbench/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEvalbench(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/datasetcat' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, datasetcatSummary());
          return;
        }
        if (path === '/api/datasetcat' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDatasetcat(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/datasetcat/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDatasetcat(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/lineage' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, lineageSummary());
          return;
        }
        if (path === '/api/lineage' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLineage(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/lineage/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLineage(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/vectorstore' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, vectorstoreSummary());
          return;
        }
        if (path === '/api/vectorstore' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createVectorstore(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/vectorstore/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateVectorstore(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/insightboard' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, insightboardSummary());
          return;
        }
        if (path === '/api/insightboard' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createInsightboard(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/insightboard/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateInsightboard(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/anomaly' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, anomalySummary());
          return;
        }
        if (path === '/api/anomaly' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAnomaly(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/anomaly/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAnomaly(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/demandcast' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, demandcastSummary());
          return;
        }
        if (path === '/api/demandcast' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDemandcast(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/demandcast/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDemandcast(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/abtest' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, abtestSummary());
          return;
        }
        if (path === '/api/abtest' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAbtest(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/abtest/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAbtest(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/scorecard' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, scorecardSummary());
          return;
        }
        if (path === '/api/scorecard' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createScorecard(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/scorecard/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateScorecard(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/decidlog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, decidlogSummary());
          return;
        }
        if (path === '/api/decidlog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDecidlog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/decidlog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDecidlog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/oracle' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildOracle());
          return;
        }
        if (path === '/api/oracle/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runOracleSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/oracle/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackOracleFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/oracle/anomaly/resolve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, resolveOracleAnomaly(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/oracle/score/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearOracleScoreRed(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/oracle/canary/promote' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, promoteOracleCanary(await readBody(req), user.username)); })();
          return;
        }

        // ── Loyalty OS · Crown (AŞAMA 541–555) ──

        if (path === '/api/memberdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, memberdeskSummary());
          return;
        }
        if (path === '/api/memberdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMemberdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/memberdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMemberdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/tierladder' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, tierladderSummary());
          return;
        }
        if (path === '/api/tierladder' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTierladder(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/tierladder/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTierladder(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pointledger' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pointledgerSummary());
          return;
        }
        if (path === '/api/pointledger' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPointledger(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pointledger/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePointledger(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/perkshop' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, perkshopSummary());
          return;
        }
        if (path === '/api/perkshop' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPerkshop(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/perkshop/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePerkshop(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/referral' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, referralSummary());
          return;
        }
        if (path === '/api/referral' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createReferral(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/referral/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateReferral(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/giftcard' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, giftcardSummary());
          return;
        }
        if (path === '/api/giftcard' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGiftcard(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/giftcard/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGiftcard(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/vipdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, vipdeskSummary());
          return;
        }
        if (path === '/api/vipdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createVipdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/vipdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateVipdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/stayhistory' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, stayhistorySummary());
          return;
        }
        if (path === '/api/stayhistory' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createStayhistory(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/stayhistory/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateStayhistory(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/prefernote' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, prefernoteSummary());
          return;
        }
        if (path === '/api/prefernote' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPrefernote(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/prefernote/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePrefernote(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/npspulse' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, npspulseSummary());
          return;
        }
        if (path === '/api/npspulse' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createNpspulse(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/npspulse/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateNpspulse(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/guestcase' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, guestcaseSummary());
          return;
        }
        if (path === '/api/guestcase' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGuestcase(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/guestcase/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGuestcase(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/praise' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, praiseSummary());
          return;
        }
        if (path === '/api/praise' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPraise(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/praise/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePraise(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/winback' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, winbackSummary());
          return;
        }
        if (path === '/api/winback' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWinback(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/winback/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWinback(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/clubnight' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, clubnightSummary());
          return;
        }
        if (path === '/api/clubnight' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createClubnight(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/clubnight/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateClubnight(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/crown' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildCrown());
          return;
        }
        if (path === '/api/crown/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runCrownSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/crown/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackCrownFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/crown/vip/house' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, houseCrownVip(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/crown/case/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeCrownCase(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/crown/winback/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveCrownWinback(await readBody(req), user.username)); })();
          return;
        }

        // ── Marketing OS · Beacon (AŞAMA 556–570) ──

        if (path === '/api/campdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, campdeskSummary());
          return;
        }
        if (path === '/api/campdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCampdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/campdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCampdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/contentcal' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, contentcalSummary());
          return;
        }
        if (path === '/api/contentcal' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createContentcal(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/contentcal/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateContentcal(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/socialqueue' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, socialqueueSummary());
          return;
        }
        if (path === '/api/socialqueue' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSocialqueue(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/socialqueue/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSocialqueue(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/creatordesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, creatordeskSummary());
          return;
        }
        if (path === '/api/creatordesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCreatordesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/creatordesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCreatordesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/utmtrack' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, utmtrackSummary());
          return;
        }
        if (path === '/api/utmtrack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createUtmtrack(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/utmtrack/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateUtmtrack(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/landing' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, landingSummary());
          return;
        }
        if (path === '/api/landing' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLanding(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/landing/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLanding(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/abcopy' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, abcopySummary());
          return;
        }
        if (path === '/api/abcopy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAbcopy(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/abcopy/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAbcopy(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/seopage' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, seopageSummary());
          return;
        }
        if (path === '/api/seopage' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSeopage(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/seopage/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSeopage(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pushdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pushdeskSummary());
          return;
        }
        if (path === '/api/pushdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPushdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pushdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePushdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/emailblast' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, emailblastSummary());
          return;
        }
        if (path === '/api/emailblast' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEmailblast(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/emailblast/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEmailblast(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/presspack' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, presspackSummary());
          return;
        }
        if (path === '/api/presspack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPresspack(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/presspack/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePresspack(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/brandkit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, brandkitSummary());
          return;
        }
        if (path === '/api/brandkit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBrandkit(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/brandkit/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBrandkit(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/mediabuy' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, mediabuySummary());
          return;
        }
        if (path === '/api/mediabuy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMediabuy(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/mediabuy/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMediabuy(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/leadmagnet' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, leadmagnetSummary());
          return;
        }
        if (path === '/api/leadmagnet' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLeadmagnet(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/leadmagnet/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLeadmagnet(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/beacon' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildBeacon());
          return;
        }
        if (path === '/api/beacon/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runBeaconSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/beacon/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackBeaconFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/beacon/camp/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveBeaconCamp(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/beacon/social/fix' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, fixBeaconSocial(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/beacon/seo/heal' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, healBeaconSeo(await readBody(req), user.username)); })();
          return;
        }

        // ── Treasury OS · Vault (AŞAMA 571–585) ──

        if (path === '/api/treasury' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, treasurySummary());
          return;
        }
        if (path === '/api/treasury' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTreasury(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/treasury/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTreasury(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cashflow' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cashflowSummary());
          return;
        }
        if (path === '/api/cashflow' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCashflow(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cashflow/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCashflow(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/apdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, apdeskSummary());
          return;
        }
        if (path === '/api/apdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createApdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/apdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateApdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ardesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ardeskSummary());
          return;
        }
        if (path === '/api/ardesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createArdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ardesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateArdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/invoicedesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, invoicedeskSummary());
          return;
        }
        if (path === '/api/invoicedesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createInvoicedesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/invoicedesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateInvoicedesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/taxdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, taxdeskSummary());
          return;
        }
        if (path === '/api/taxdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTaxdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/taxdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTaxdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/budgetline' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, budgetlineSummary());
          return;
        }
        if (path === '/api/budgetline' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBudgetline(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/budgetline/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBudgetline(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/fxdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, fxdeskSummary());
          return;
        }
        if (path === '/api/fxdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFxdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/fxdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFxdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/bankrecon' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, bankreconSummary());
          return;
        }
        if (path === '/api/bankrecon' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBankrecon(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/bankrecon/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBankrecon(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/payout' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, payoutSummary());
          return;
        }
        if (path === '/api/payout' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPayout(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/payout/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePayout(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pettycash' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pettycashSummary());
          return;
        }
        if (path === '/api/pettycash' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPettycash(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pettycash/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePettycash(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/costcenter' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, costcenterSummary());
          return;
        }
        if (path === '/api/costcenter' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCostcenter(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/costcenter/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCostcenter(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/glmap' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, glmapSummary());
          return;
        }
        if (path === '/api/glmap' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGlmap(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/glmap/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGlmap(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/closebook' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, closebookSummary());
          return;
        }
        if (path === '/api/closebook' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createClosebook(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/closebook/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateClosebook(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/vault' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildVault());
          return;
        }
        if (path === '/api/vault/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runVaultSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/vault/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackVaultFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/vault/treasury/heal' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, healVaultTreasury(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/vault/ap/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeVaultAp(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/vault/recon/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearVaultRecon(await readBody(req), user.username)); })();
          return;
        }

        // ── Mobility OS · Convoy (AŞAMA 586–600) ──

        if (path === '/api/shuttlelane' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, shuttlelaneSummary());
          return;
        }
        if (path === '/api/shuttlelane' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createShuttlelane(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/shuttlelane/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateShuttlelane(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/fleetdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, fleetdeskSummary());
          return;
        }
        if (path === '/api/fleetdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFleetdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/fleetdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFleetdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/driverost' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, driverostSummary());
          return;
        }
        if (path === '/api/driverost' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDriverost(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/driverost/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDriverost(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/fuelcard' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, fuelcardSummary());
          return;
        }
        if (path === '/api/fuelcard' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFuelcard(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/fuelcard/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFuelcard(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/routeplan' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, routeplanSummary());
          return;
        }
        if (path === '/api/routeplan' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRouteplan(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/routeplan/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRouteplan(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/dispatchboard' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, dispatchboardSummary());
          return;
        }
        if (path === '/api/dispatchboard' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDispatchboard(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/dispatchboard/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDispatchboard(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/gpsping' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, gpspingSummary());
          return;
        }
        if (path === '/api/gpsping' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGpsping(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/gpsping/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGpsping(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/vehiclemaint' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, vehiclemaintSummary());
          return;
        }
        if (path === '/api/vehiclemaint' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createVehiclemaint(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/vehiclemaint/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateVehiclemaint(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/valetops' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, valetopsSummary());
          return;
        }
        if (path === '/api/valetops' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createValetops(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/valetops/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateValetops(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/parkingbay' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, parkingbaySummary());
          return;
        }
        if (path === '/api/parkingbay' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createParkingbay(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/parkingbay/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateParkingbay(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/transferjob' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, transferjobSummary());
          return;
        }
        if (path === '/api/transferjob' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTransferjob(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/transferjob/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTransferjob(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pickupdrop' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pickupdropSummary());
          return;
        }
        if (path === '/api/pickupdrop' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPickupdrop(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pickupdrop/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePickupdrop(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/tollpass' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, tollpassSummary());
          return;
        }
        if (path === '/api/tollpass' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTollpass(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/tollpass/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTollpass(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/lanecontrol' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, lanecontrolSummary());
          return;
        }
        if (path === '/api/lanecontrol' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLanecontrol(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/lanecontrol/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLanecontrol(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/convoy' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildConvoy());
          return;
        }
        if (path === '/api/convoy/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runConvoySweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/convoy/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackConvoyFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/convoy/dispatch/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearConvoyDispatch(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/convoy/fleet/ready' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, readyConvoyFleet(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/convoy/curb/free' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, freeConvoyCurb(await readBody(req), user.username)); })();
          return;
        }

        // ── Housekeeping OS · Linen (AŞAMA 601–615) ──

        if (path === '/api/roomrack' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, roomrackSummary());
          return;
        }
        if (path === '/api/roomrack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRoomrack(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/roomrack/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRoomrack(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/hkboard' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, hkboardSummary());
          return;
        }
        if (path === '/api/hkboard' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHkboard(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/hkboard/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHkboard(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/linenroom' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, linenroomSummary());
          return;
        }
        if (path === '/api/linenroom' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLinenroom(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/linenroom/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLinenroom(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/minibarbay' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, minibarbaySummary());
          return;
        }
        if (path === '/api/minibarbay' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMinibarbay(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/minibarbay/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMinibarbay(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/foundlog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, foundlogSummary());
          return;
        }
        if (path === '/api/foundlog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFoundlog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/foundlog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFoundlog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/keydesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, keydeskSummary());
          return;
        }
        if (path === '/api/keydesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createKeydesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/keydesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateKeydesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/turnup' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, turnupSummary());
          return;
        }
        if (path === '/api/turnup' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTurnup(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/turnup/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTurnup(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/deepclean' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, deepcleanSummary());
          return;
        }
        if (path === '/api/deepclean' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDeepclean(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/deepclean/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDeepclean(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/inspectroom' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, inspectroomSummary());
          return;
        }
        if (path === '/api/inspectroom' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createInspectroom(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/inspectroom/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateInspectroom(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/vipprep' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, vipprepSummary());
          return;
        }
        if (path === '/api/vipprep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createVipprep(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/vipprep/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateVipprep(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/outoforder' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, outoforderSummary());
          return;
        }
        if (path === '/api/outoforder' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOutoforder(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/outoforder/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOutoforder(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/guestrequest' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, guestrequestSummary());
          return;
        }
        if (path === '/api/guestrequest' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGuestrequest(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/guestrequest/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGuestrequest(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/amensave' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, amensaveSummary());
          return;
        }
        if (path === '/api/amensave' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAmensave(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/amensave/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAmensave(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/publicarea' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, publicareaSummary());
          return;
        }
        if (path === '/api/publicarea' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPublicarea(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/publicarea/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePublicarea(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/linen' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildLinen());
          return;
        }
        if (path === '/api/linen/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runLinenSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/linen/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackLinenFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/linen/inspect/pass' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, passLinenInspect(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/linen/ooo/release' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, releaseLinenOoo(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/linen/hk/complete' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, completeLinenHk(await readBody(req), user.username)); })();
          return;
        }

        // ── Front Office OS · Atlas (AŞAMA 616–630) ──

        if (path === '/api/nightaudit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, nightauditSummary());
          return;
        }
        if (path === '/api/nightaudit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createNightaudit(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/nightaudit/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateNightaudit(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/foliodesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, foliodeskSummary());
          return;
        }
        if (path === '/api/foliodesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFoliodesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/foliodesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFoliodesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/deskqueue' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, deskqueueSummary());
          return;
        }
        if (path === '/api/deskqueue' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDeskqueue(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/deskqueue/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDeskqueue(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/wakeall' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, wakeallSummary());
          return;
        }
        if (path === '/api/wakeall' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWakeall(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/wakeall/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWakeall(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/earlycheck' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, earlycheckSummary());
          return;
        }
        if (path === '/api/earlycheck' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEarlycheck(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/earlycheck/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEarlycheck(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/latecheck' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, latecheckSummary());
          return;
        }
        if (path === '/api/latecheck' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLatecheck(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/latecheck/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLatecheck(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/bagstore' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, bagstoreSummary());
          return;
        }
        if (path === '/api/bagstore' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBagstore(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/bagstore/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBagstore(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/callsheet' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, callsheetSummary());
          return;
        }
        if (path === '/api/callsheet' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCallsheet(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/callsheet/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCallsheet(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/arrivalboard' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, arrivalboardSummary());
          return;
        }
        if (path === '/api/arrivalboard' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createArrivalboard(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/arrivalboard/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateArrivalboard(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/departureboard' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, departureboardSummary());
          return;
        }
        if (path === '/api/departureboard' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDepartureboard(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/departureboard/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDepartureboard(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/viparrive' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, viparriveSummary());
          return;
        }
        if (path === '/api/viparrive' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createViparrive(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/viparrive/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateViparrive(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/frontlog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, frontlogSummary());
          return;
        }
        if (path === '/api/frontlog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFrontlog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/frontlog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFrontlog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/moveticket' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, moveticketSummary());
          return;
        }
        if (path === '/api/moveticket' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMoveticket(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/moveticket/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMoveticket(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/conciergejob' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, conciergejobSummary());
          return;
        }
        if (path === '/api/conciergejob' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createConciergejob(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/conciergejob/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateConciergejob(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/atlas' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildAtlas());
          return;
        }
        if (path === '/api/atlas/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runAtlasSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/atlas/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackAtlasFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/atlas/folio/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeAtlasFolio(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/atlas/desk/serve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, serveAtlasDesk(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/atlas/audit/run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runAtlasAudit(await readBody(req), user.username)); })();
          return;
        }

        // ── Konglomerat Ticaret · Empire (AŞAMA 631–645) ──

        if (path === '/api/tybridge' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, tybridgeSummary());
          return;
        }
        if (path === '/api/tybridge' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTybridge(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/tybridge/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTybridge(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/dolaplist' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, dolaplistSummary());
          return;
        }
        if (path === '/api/dolaplist' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDolaplist(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/dolaplist/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDolaplist(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/hephapick' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, hephapickSummary());
          return;
        }
        if (path === '/api/hephapick' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHephapick(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/hephapick/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHephapick(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/tourpack' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, tourpackSummary());
          return;
        }
        if (path === '/api/tourpack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTourpack(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/tourpack/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTourpack(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/rentgear' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, rentgearSummary());
          return;
        }
        if (path === '/api/rentgear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRentgear(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/rentgear/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRentgear(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/wareserve' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, wareserveSummary());
          return;
        }
        if (path === '/api/wareserve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWareserve(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/wareserve/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWareserve(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/staybook' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, staybookSummary());
          return;
        }
        if (path === '/api/staybook' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createStaybook(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/staybook/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateStaybook(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/dazeroom' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, dazeroomSummary());
          return;
        }
        if (path === '/api/dazeroom' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDazeroom(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/dazeroom/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDazeroom(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/keylessdoor' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, keylessdoorSummary());
          return;
        }
        if (path === '/api/keylessdoor' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createKeylessdoor(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/keylessdoor/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateKeylessdoor(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sportslot' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sportslotSummary());
          return;
        }
        if (path === '/api/sportslot' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSportslot(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sportslot/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSportslot(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/arenabook' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, arenabookSummary());
          return;
        }
        if (path === '/api/arenabook' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createArenabook(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/arenabook/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateArenabook(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/nexusgate' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, nexusgateSummary());
          return;
        }
        if (path === '/api/nexusgate' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createNexusgate(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/nexusgate/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateNexusgate(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/mintbundle' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, mintbundleSummary());
          return;
        }
        if (path === '/api/mintbundle' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMintbundle(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/mintbundle/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMintbundle(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/packfolio' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, packfolioSummary());
          return;
        }
        if (path === '/api/packfolio' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPackfolio(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/packfolio/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePackfolio(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/empire' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildEmpire());
          return;
        }
        if (path === '/api/empire/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runEmpireSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/empire/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackEmpireFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/empire/ty/sync' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, syncEmpireTy(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/empire/hepha/ship' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, shipEmpireHepha(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/empire/tour/depart' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, departEmpireTour(await readBody(req), user.username)); })();
          return;
        }

        // ── Retail OS · Bazaar (AŞAMA 646–660) ──

        if (path === '/api/retailfloor' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, retailfloorSummary());
          return;
        }
        if (path === '/api/retailfloor' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRetailfloor(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/retailfloor/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRetailfloor(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/planogram' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, planogramSummary());
          return;
        }
        if (path === '/api/planogram' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPlanogram(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/planogram/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePlanogram(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/shelfscan' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, shelfscanSummary());
          return;
        }
        if (path === '/api/shelfscan' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createShelfscan(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/shelfscan/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateShelfscan(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/priceaudit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, priceauditSummary());
          return;
        }
        if (path === '/api/priceaudit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPriceaudit(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/priceaudit/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePriceaudit(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/shrinklog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, shrinklogSummary());
          return;
        }
        if (path === '/api/shrinklog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createShrinklog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/shrinklog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateShrinklog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/vendorportal' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, vendorportalSummary());
          return;
        }
        if (path === '/api/vendorportal' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createVendorportal(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/vendorportal/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateVendorportal(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/assortmix' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, assortmixSummary());
          return;
        }
        if (path === '/api/assortmix' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAssortmix(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/assortmix/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAssortmix(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/promoplane' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, promoplaneSummary());
          return;
        }
        if (path === '/api/promoplane' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPromoplane(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/promoplane/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePromoplane(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/categorybuy' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, categorybuySummary());
          return;
        }
        if (path === '/api/categorybuy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCategorybuy(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/categorybuy/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCategorybuy(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/demandplan' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, demandplanSummary());
          return;
        }
        if (path === '/api/demandplan' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDemandplan(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/demandplan/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDemandplan(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/stockhealth' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, stockhealthSummary());
          return;
        }
        if (path === '/api/stockhealth' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createStockhealth(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/stockhealth/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateStockhealth(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/returnbay' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, returnbaySummary());
          return;
        }
        if (path === '/api/returnbay' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createReturnbay(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/returnbay/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateReturnbay(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/darkstore' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, darkstoreSummary());
          return;
        }
        if (path === '/api/darkstore' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDarkstore(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/darkstore/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDarkstore(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/poslane' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, poslaneSummary());
          return;
        }
        if (path === '/api/poslane' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPoslane(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/poslane/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePoslane(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/bazaar' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildBazaar());
          return;
        }
        if (path === '/api/bazaar/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runBazaarSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/bazaar/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackBazaarFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/bazaar/stock/heal' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, healBazaarStock(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/bazaar/shrink/review' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, reviewBazaarShrink(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/bazaar/dark/dispatch' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, dispatchBazaarDark(await readBody(req), user.username)); })();
          return;
        }

        // ── Media OS · Studio (AŞAMA 661–675) ──

        if (path === '/api/mediawall' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, mediawallSummary());
          return;
        }
        if (path === '/api/mediawall' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMediawall(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/mediawall/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMediawall(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/contentrights' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, contentrightsSummary());
          return;
        }
        if (path === '/api/contentrights' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createContentrights(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/contentrights/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateContentrights(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/adslot' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, adslotSummary());
          return;
        }
        if (path === '/api/adslot' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAdslot(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/adslot/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAdslot(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sponsorpack' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sponsorpackSummary());
          return;
        }
        if (path === '/api/sponsorpack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSponsorpack(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sponsorpack/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSponsorpack(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/brandambass' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, brandambassSummary());
          return;
        }
        if (path === '/api/brandambass' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBrandambass(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/brandambass/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBrandambass(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/creatorpay' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, creatorpaySummary());
          return;
        }
        if (path === '/api/creatorpay' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCreatorpay(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/creatorpay/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCreatorpay(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ugcqueue' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ugcqueueSummary());
          return;
        }
        if (path === '/api/ugcqueue' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createUgcqueue(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ugcqueue/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateUgcqueue(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pressroom' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pressroomSummary());
          return;
        }
        if (path === '/api/pressroom' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPressroom(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pressroom/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePressroom(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/livecast' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, livecastSummary());
          return;
        }
        if (path === '/api/livecast' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLivecast(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/livecast/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLivecast(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/eventstream' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, eventstreamSummary());
          return;
        }
        if (path === '/api/eventstream' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEventstream(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/eventstream/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEventstream(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/affiliatenet' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, affiliatenetSummary());
          return;
        }
        if (path === '/api/affiliatenet' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAffiliatenet(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/affiliatenet/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAffiliatenet(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/boostdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, boostdeskSummary());
          return;
        }
        if (path === '/api/boostdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBoostdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/boostdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBoostdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/assetlib' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, assetlibSummary());
          return;
        }
        if (path === '/api/assetlib' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAssetlib(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/assetlib/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAssetlib(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/briefdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, briefdeskSummary());
          return;
        }
        if (path === '/api/briefdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBriefdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/briefdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBriefdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/studio' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildStudio());
          return;
        }
        if (path === '/api/studio/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runStudioSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/studio/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackStudioFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/studio/ugc/approve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, approveStudioUgc(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/studio/live/end' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, endStudioLive(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/studio/brief/deliver' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, deliverStudioBrief(await readBody(req), user.username)); })();
          return;
        }

        // ── Green OS · Verdant (AŞAMA 676–690) ──

        if (path === '/api/carbonledger' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, carbonledgerSummary());
          return;
        }
        if (path === '/api/carbonledger' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCarbonledger(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/carbonledger/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCarbonledger(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/wateruse' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, wateruseSummary());
          return;
        }
        if (path === '/api/wateruse' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWateruse(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/wateruse/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWateruse(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/wastesort' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, wastesortSummary());
          return;
        }
        if (path === '/api/wastesort' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWastesort(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/wastesort/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWastesort(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/energybid' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, energybidSummary());
          return;
        }
        if (path === '/api/energybid' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEnergybid(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/energybid/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEnergybid(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/solaryield' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, solaryieldSummary());
          return;
        }
        if (path === '/api/solaryield' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSolaryield(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/solaryield/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSolaryield(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/greenteam' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, greenteamSummary());
          return;
        }
        if (path === '/api/greenteam' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGreenteam(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/greenteam/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGreenteam(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/esgaudit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, esgauditSummary());
          return;
        }
        if (path === '/api/esgaudit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEsgaudit(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/esgaudit/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEsgaudit(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/biosurvey' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, biosurveySummary());
          return;
        }
        if (path === '/api/biosurvey' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBiosurvey(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/biosurvey/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBiosurvey(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/offsetbuy' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, offsetbuySummary());
          return;
        }
        if (path === '/api/offsetbuy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOffsetbuy(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/offsetbuy/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOffsetbuy(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/climategoal' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, climategoalSummary());
          return;
        }
        if (path === '/api/climategoal' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createClimategoal(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/climategoal/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateClimategoal(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/greenbond' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, greenbondSummary());
          return;
        }
        if (path === '/api/greenbond' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGreenbond(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/greenbond/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGreenbond(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/plasticaudit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, plasticauditSummary());
          return;
        }
        if (path === '/api/plasticaudit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPlasticaudit(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/plasticaudit/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePlasticaudit(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/evcharger' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, evchargerSummary());
          return;
        }
        if (path === '/api/evcharger' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEvcharger(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/evcharger/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEvcharger(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/reefwatch' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, reefwatchSummary());
          return;
        }
        if (path === '/api/reefwatch' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createReefwatch(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/reefwatch/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateReefwatch(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/verdant' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildVerdant());
          return;
        }
        if (path === '/api/verdant/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runVerdantSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/verdant/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackVerdantFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/verdant/water/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearVerdantWater(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/verdant/esg/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeVerdantEsg(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/verdant/ev/fix' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, fixVerdantEv(await readBody(req), user.username)); })();
          return;
        }

        // ── Identity OS · Bastion (AŞAMA 691–705) ──

        if (path === '/api/accessgate' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, accessgateSummary());
          return;
        }
        if (path === '/api/accessgate' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAccessgate(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/accessgate/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAccessgate(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/idproof' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, idproofSummary());
          return;
        }
        if (path === '/api/idproof' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createIdproof(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/idproof/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateIdproof(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/rolegrant' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, rolegrantSummary());
          return;
        }
        if (path === '/api/rolegrant' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRolegrant(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/rolegrant/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRolegrant(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sessionguard' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sessionguardSummary());
          return;
        }
        if (path === '/api/sessionguard' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSessionguard(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sessionguard/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSessionguard(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/devicetrust' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, devicetrustSummary());
          return;
        }
        if (path === '/api/devicetrust' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDevicetrust(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/devicetrust/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDevicetrust(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/secretvault' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, secretvaultSummary());
          return;
        }
        if (path === '/api/secretvault' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSecretvault(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/secretvault/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSecretvault(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/mfareg' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, mfaregSummary());
          return;
        }
        if (path === '/api/mfareg' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMfareg(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/mfareg/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMfareg(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ssobridge' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ssobridgeSummary());
          return;
        }
        if (path === '/api/ssobridge' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSsobridge(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ssobridge/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSsobridge(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/privacypol' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, privacypolSummary());
          return;
        }
        if (path === '/api/privacypol' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPrivacypol(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/privacypol/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePrivacypol(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/consentrow' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, consentrowSummary());
          return;
        }
        if (path === '/api/consentrow' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createConsentrow(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/consentrow/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateConsentrow(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/breachlog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, breachlogSummary());
          return;
        }
        if (path === '/api/breachlog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBreachlog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/breachlog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBreachlog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/socqueue' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, socqueueSummary());
          return;
        }
        if (path === '/api/socqueue' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSocqueue(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/socqueue/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSocqueue(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/patchdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, patchdeskSummary());
          return;
        }
        if (path === '/api/patchdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPatchdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/patchdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePatchdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/zerohour' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, zerohourSummary());
          return;
        }
        if (path === '/api/zerohour' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createZerohour(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/zerohour/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateZerohour(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/bastion' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildBastion());
          return;
        }
        if (path === '/api/bastion/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runBastionSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/bastion/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackBastionFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/bastion/access/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeBastionAccess(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/bastion/role/approve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, approveBastionRole(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/bastion/breach/archive' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, archiveBastionBreach(await readBody(req), user.username)); })();
          return;
        }

        // ── Alliance OS · Alliance2 (AŞAMA 706–720) ──

        if (path === '/api/partnerdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, partnerdeskSummary());
          return;
        }
        if (path === '/api/partnerdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPartnerdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/partnerdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePartnerdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/franchise' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, franchiseSummary());
          return;
        }
        if (path === '/api/franchise' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFranchise(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/franchise/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFranchise(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/channelkit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, channelkitSummary());
          return;
        }
        if (path === '/api/channelkit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createChannelkit(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/channelkit/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateChannelkit(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/rebate' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, rebateSummary());
          return;
        }
        if (path === '/api/rebate' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRebate(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/rebate/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRebate(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/coinvest' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, coinvestSummary());
          return;
        }
        if (path === '/api/coinvest' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCoinvest(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/coinvest/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCoinvest(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/slatrack' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, slatrackSummary());
          return;
        }
        if (path === '/api/slatrack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSlatrack(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/slatrack/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSlatrack(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/jointpromo' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, jointpromoSummary());
          return;
        }
        if (path === '/api/jointpromo' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createJointpromo(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/jointpromo/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateJointpromo(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/leadshare' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, leadshareSummary());
          return;
        }
        if (path === '/api/leadshare' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLeadshare(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/leadshare/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLeadshare(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/b2border' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, b2borderSummary());
          return;
        }
        if (path === '/api/b2border' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createB2border(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/b2border/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateB2border(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/wholesale' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, wholesaleSummary());
          return;
        }
        if (path === '/api/wholesale' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWholesale(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/wholesale/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWholesale(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/dealroom' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, dealroomSummary());
          return;
        }
        if (path === '/api/dealroom' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDealroom(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/dealroom/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDealroom(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/contractrow' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, contractrowSummary());
          return;
        }
        if (path === '/api/contractrow' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createContractrow(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/contractrow/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateContractrow(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/commission' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, commissionSummary());
          return;
        }
        if (path === '/api/commission' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCommission(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/commission/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCommission(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/onboardkit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, onboardkitSummary());
          return;
        }
        if (path === '/api/onboardkit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOnboardkit(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/onboardkit/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOnboardkit(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/alliance2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildAlliance2());
          return;
        }
        if (path === '/api/alliance2/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runAlliance2Sweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/alliance2/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackAlliance2Flag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/alliance2/partner/busy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, busyAlliance2Partner(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/alliance2/channel/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeAlliance2Channel(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/alliance2/invest/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveAlliance2Invest(await readBody(req), user.username)); })();
          return;
        }

        // ── Supply OS · Artery (AŞAMA 721–735) ──

        if (path === '/api/inboundpo' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, inboundpoSummary());
          return;
        }
        if (path === '/api/inboundpo' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createInboundpo(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/inboundpo/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateInboundpo(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/outboundso' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, outboundsoSummary());
          return;
        }
        if (path === '/api/outboundso' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOutboundso(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/outboundso/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOutboundso(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/asntrack' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, asntrackSummary());
          return;
        }
        if (path === '/api/asntrack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAsntrack(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/asntrack/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAsntrack(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/dockyard' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, dockyardSummary());
          return;
        }
        if (path === '/api/dockyard' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDockyard(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/dockyard/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDockyard(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/crossdock' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, crossdockSummary());
          return;
        }
        if (path === '/api/crossdock' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCrossdock(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/crossdock/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCrossdock(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/coldchain2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, coldchain2Summary());
          return;
        }
        if (path === '/api/coldchain2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createColdchain2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/coldchain2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateColdchain2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/slotbook' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, slotbookSummary());
          return;
        }
        if (path === '/api/slotbook' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSlotbook(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/slotbook/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSlotbook(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/carrierbid' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, carrierbidSummary());
          return;
        }
        if (path === '/api/carrierbid' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCarrierbid(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/carrierbid/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCarrierbid(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/freightbill' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, freightbillSummary());
          return;
        }
        if (path === '/api/freightbill' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFreightbill(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/freightbill/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFreightbill(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/milestonet' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, milestonetSummary());
          return;
        }
        if (path === '/api/milestonet' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMilestonet(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/milestonet/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMilestonet(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/exceptionlog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, exceptionlogSummary());
          return;
        }
        if (path === '/api/exceptionlog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createExceptionlog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/exceptionlog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateExceptionlog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/inventoryage' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, inventoryageSummary());
          return;
        }
        if (path === '/api/inventoryage' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createInventoryage(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/inventoryage/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateInventoryage(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/replenplan' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, replenplanSummary());
          return;
        }
        if (path === '/api/replenplan' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createReplenplan(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/replenplan/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateReplenplan(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/safetystock' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, safetystockSummary());
          return;
        }
        if (path === '/api/safetystock' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSafetystock(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/safetystock/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSafetystock(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/artery' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildArtery());
          return;
        }
        if (path === '/api/artery/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runArterySweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/artery/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackArteryFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/artery/inbound/run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runArteryInbound(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/artery/asn/busy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, busyArteryAsn(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/artery/dock/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeArteryDock(await readBody(req), user.username)); })();
          return;
        }

        // ── Dominion Seal · Dominion (AŞAMA 736–750) ──

        if (path === '/api/supplierkpi' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, supplierkpiSummary());
          return;
        }
        if (path === '/api/supplierkpi' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSupplierkpi(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/supplierkpi/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSupplierkpi(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cmdpulse' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cmdpulseSummary());
          return;
        }
        if (path === '/api/cmdpulse' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCmdpulse(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cmdpulse/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCmdpulse(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/boardpulse' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, boardpulseSummary());
          return;
        }
        if (path === '/api/boardpulse' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBoardpulse(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/boardpulse/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBoardpulse(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/riskheat' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, riskheatSummary());
          return;
        }
        if (path === '/api/riskheat' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRiskheat(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/riskheat/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRiskheat(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cashpulse' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cashpulseSummary());
          return;
        }
        if (path === '/api/cashpulse' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCashpulse(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cashpulse/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCashpulse(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/opsheat' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, opsheatSummary());
          return;
        }
        if (path === '/api/opsheat' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOpsheat(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/opsheat/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOpsheat(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/guestheat' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, guestheatSummary());
          return;
        }
        if (path === '/api/guestheat' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGuestheat(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/guestheat/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGuestheat(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/brandheat' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, brandheatSummary());
          return;
        }
        if (path === '/api/brandheat' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBrandheat(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/brandheat/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBrandheat(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/agentpulse' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, agentpulseSummary());
          return;
        }
        if (path === '/api/agentpulse' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAgentpulse(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/agentpulse/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAgentpulse(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/systempulse' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, systempulseSummary());
          return;
        }
        if (path === '/api/systempulse' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSystempulse(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/systempulse/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSystempulse(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/alertfuse' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, alertfuseSummary());
          return;
        }
        if (path === '/api/alertfuse' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAlertfuse(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/alertfuse/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAlertfuse(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/warbrief' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, warbriefSummary());
          return;
        }
        if (path === '/api/warbrief' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWarbrief(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/warbrief/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWarbrief(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/decisionhub' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, decisionhubSummary());
          return;
        }
        if (path === '/api/decisionhub' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDecisionhub(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/decisionhub/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDecisionhub(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sealnote' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sealnoteSummary());
          return;
        }
        if (path === '/api/sealnote' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSealnote(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sealnote/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSealnote(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/dominion' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildDominion());
          return;
        }
        if (path === '/api/dominion/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runDominionSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/dominion/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackDominionFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/dominion/supplier/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveDominionSupplier(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/dominion/board/run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runDominionBoard(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/dominion/cash/busy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, busyDominionCash(await readBody(req), user.username)); })();
          return;
        }

        // ── Serenity OS · Serenity (AŞAMA 751–765) ──

        if (path === '/api/legacyflag' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, legacyflagSummary());
          return;
        }
        if (path === '/api/legacyflag' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLegacyflag(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/legacyflag/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLegacyflag(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/calmroom' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, calmroomSummary());
          return;
        }
        if (path === '/api/calmroom' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCalmroom(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/calmroom/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCalmroom(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/quiethours' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, quiethoursSummary());
          return;
        }
        if (path === '/api/quiethours' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createQuiethours(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/quiethours/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateQuiethours(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/scentmood' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, scentmoodSummary());
          return;
        }
        if (path === '/api/scentmood' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createScentmood(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/scentmood/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateScentmood(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pillowmenu2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pillowmenu2Summary());
          return;
        }
        if (path === '/api/pillowmenu2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPillowmenu2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pillowmenu2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePillowmenu2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/bathritual' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, bathritualSummary());
          return;
        }
        if (path === '/api/bathritual' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBathritual(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/bathritual/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBathritual(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sleepscore' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sleepscoreSummary());
          return;
        }
        if (path === '/api/sleepscore' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSleepscore(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sleepscore/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSleepscore(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/welcomeamen' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, welcomeamenSummary());
          return;
        }
        if (path === '/api/welcomeamen' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWelcomeamen(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/welcomeamen/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWelcomeamen(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/farewell' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, farewellSummary());
          return;
        }
        if (path === '/api/farewell' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFarewell(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/farewell/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFarewell(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/memorybook' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, memorybookSummary());
          return;
        }
        if (path === '/api/memorybook' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMemorybook(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/memorybook/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMemorybook(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/carecall' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, carecallSummary());
          return;
        }
        if (path === '/api/carecall' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCarecall(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/carecall/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCarecall(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/surprisegift' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, surprisegiftSummary());
          return;
        }
        if (path === '/api/surprisegift' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSurprisegift(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/surprisegift/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSurprisegift(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/loyaltyhug' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, loyaltyhugSummary());
          return;
        }
        if (path === '/api/loyaltyhug' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLoyaltyhug(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/loyaltyhug/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLoyaltyhug(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/feedbackloop' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, feedbackloopSummary());
          return;
        }
        if (path === '/api/feedbackloop' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFeedbackloop(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/feedbackloop/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFeedbackloop(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/serenity' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildSerenity());
          return;
        }
        if (path === '/api/serenity/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runSerenitySweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/serenity/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackSerenityFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/serenity/legacy/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeSerenityLegacy(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/serenity/quiet/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveSerenityQuiet(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/serenity/pillow/run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runSerenityPillow(await readBody(req), user.username)); })();
          return;
        }

        // ── Circuit OS · Circuit (AŞAMA 766–780) ──

        if (path === '/api/momentmap' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, momentmapSummary());
          return;
        }
        if (path === '/api/momentmap' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMomentmap(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/momentmap/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMomentmap(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/apigateway' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, apigatewaySummary());
          return;
        }
        if (path === '/api/apigateway' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createApigateway(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/apigateway/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateApigateway(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/webhookhub' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, webhookhubSummary());
          return;
        }
        if (path === '/api/webhookhub' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWebhookhub(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/webhookhub/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWebhookhub(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ratelimit2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ratelimit2Summary());
          return;
        }
        if (path === '/api/ratelimit2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRatelimit2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ratelimit2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRatelimit2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/schemareg' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, schemaregSummary());
          return;
        }
        if (path === '/api/schemareg' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSchemareg(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/schemareg/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSchemareg(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/eventbus' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, eventbusSummary());
          return;
        }
        if (path === '/api/eventbus' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEventbus(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/eventbus/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEventbus(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/jobqueue2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, jobqueue2Summary());
          return;
        }
        if (path === '/api/jobqueue2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createJobqueue2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/jobqueue2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateJobqueue2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cachemesh' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cachemeshSummary());
          return;
        }
        if (path === '/api/cachemesh' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCachemesh(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cachemesh/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCachemesh(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cdnedge' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cdnedgeSummary());
          return;
        }
        if (path === '/api/cdnedge' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCdnedge(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cdnedge/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCdnedge(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/observemap' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, observemapSummary());
          return;
        }
        if (path === '/api/observemap' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createObservemap(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/observemap/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateObservemap(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/errorbudget2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, errorbudget2Summary());
          return;
        }
        if (path === '/api/errorbudget2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createErrorbudget2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/errorbudget2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateErrorbudget2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/featuregate' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, featuregateSummary());
          return;
        }
        if (path === '/api/featuregate' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFeaturegate(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/featuregate/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFeaturegate(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/canaryrun' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, canaryrunSummary());
          return;
        }
        if (path === '/api/canaryrun' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCanaryrun(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/canaryrun/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCanaryrun(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/rollback' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, rollbackSummary());
          return;
        }
        if (path === '/api/rollback' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRollback(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/rollback/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRollback(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/circuit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildCircuit());
          return;
        }
        if (path === '/api/circuit/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runCircuitSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/circuit/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackCircuitFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/circuit/moment/busy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, busyCircuitMoment(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/circuit/hook/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeCircuitHook(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/circuit/schema/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveCircuitSchema(await readBody(req), user.username)); })();
          return;
        }

        // ── Agora OS · Agora (AŞAMA 781–795) ──

        if (path === '/api/chaosdrill' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, chaosdrillSummary());
          return;
        }
        if (path === '/api/chaosdrill' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createChaosdrill(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/chaosdrill/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateChaosdrill(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/memberhub' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, memberhubSummary());
          return;
        }
        if (path === '/api/memberhub' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMemberhub(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/memberhub/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMemberhub(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/circle' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, circleSummary());
          return;
        }
        if (path === '/api/circle' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCircle(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/circle/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCircle(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/meetup' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, meetupSummary());
          return;
        }
        if (path === '/api/meetup' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMeetup(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/meetup/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMeetup(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/forummod' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, forummodSummary());
          return;
        }
        if (path === '/api/forummod' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createForummod(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/forummod/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateForummod(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/polldesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, polldeskSummary());
          return;
        }
        if (path === '/api/polldesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPolldesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/polldesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePolldesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/badgeearn' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, badgeearnSummary());
          return;
        }
        if (path === '/api/badgeearn' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBadgeearn(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/badgeearn/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBadgeearn(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/questline' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, questlineSummary());
          return;
        }
        if (path === '/api/questline' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createQuestline(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/questline/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateQuestline(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/volunteer' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, volunteerSummary());
          return;
        }
        if (path === '/api/volunteer' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createVolunteer(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/volunteer/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateVolunteer(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/donation' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, donationSummary());
          return;
        }
        if (path === '/api/donation' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDonation(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/donation/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDonation(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/chapter' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, chapterSummary());
          return;
        }
        if (path === '/api/chapter' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createChapter(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/chapter/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateChapter(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ambassador2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ambassador2Summary());
          return;
        }
        if (path === '/api/ambassador2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAmbassador2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ambassador2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAmbassador2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/storywall' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, storywallSummary());
          return;
        }
        if (path === '/api/storywall' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createStorywall(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/storywall/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateStorywall(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ritualcal' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ritualcalSummary());
          return;
        }
        if (path === '/api/ritualcal' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRitualcal(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ritualcal/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRitualcal(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/agora' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildAgora());
          return;
        }
        if (path === '/api/agora/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runAgoraSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agora/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackAgoraFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agora/drill/run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runAgoraDrill(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agora/circle/busy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, busyAgoraCircle(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agora/badge/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveAgoraBadge(await readBody(req), user.username)); })();
          return;
        }

        // ── Crucible OS · Crucible (AŞAMA 796–810) ──

        if (path === '/api/cohort' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cohortSummary());
          return;
        }
        if (path === '/api/cohort' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCohort(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cohort/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCohort(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/labbench' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, labbenchSummary());
          return;
        }
        if (path === '/api/labbench' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLabbench(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/labbench/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLabbench(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pilotrun' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pilotrunSummary());
          return;
        }
        if (path === '/api/pilotrun' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPilotrun(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pilotrun/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePilotrun(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/prototype' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, prototypeSummary());
          return;
        }
        if (path === '/api/prototype' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPrototype(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/prototype/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePrototype(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/hypothesis' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, hypothesisSummary());
          return;
        }
        if (path === '/api/hypothesis' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHypothesis(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/hypothesis/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHypothesis(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/metricslab' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, metricslabSummary());
          return;
        }
        if (path === '/api/metricslab' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMetricslab(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/metricslab/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMetricslab(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/userboard' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, userboardSummary());
          return;
        }
        if (path === '/api/userboard' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createUserboard(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/userboard/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateUserboard(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/patentdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, patentdeskSummary());
          return;
        }
        if (path === '/api/patentdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPatentdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/patentdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePatentdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sandbox' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sandboxSummary());
          return;
        }
        if (path === '/api/sandbox' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSandbox(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sandbox/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSandbox(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/hackday' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, hackdaySummary());
          return;
        }
        if (path === '/api/hackday' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHackday(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/hackday/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHackday(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/incubate' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, incubateSummary());
          return;
        }
        if (path === '/api/incubate' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createIncubate(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/incubate/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateIncubate(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/spindesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, spindeskSummary());
          return;
        }
        if (path === '/api/spindesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSpindesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/spindesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSpindesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/researchnote' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, researchnoteSummary());
          return;
        }
        if (path === '/api/researchnote' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createResearchnote(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/researchnote/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateResearchnote(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/labbudget' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, labbudgetSummary());
          return;
        }
        if (path === '/api/labbudget' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLabbudget(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/labbudget/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLabbudget(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/crucible' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildCrucible());
          return;
        }
        if (path === '/api/crucible/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runCrucibleSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/crucible/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackCrucibleFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/crucible/pilot/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveCruciblePilot(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/crucible/learn/busy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, busyCrucibleLearn(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/crucible/lab/ship' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, shipCrucibleLab(await readBody(req), user.username)); })();
          return;
        }

        // ── Charter OS · Charter (AŞAMA 811–825) ──

        if (path === '/api/ipvault' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ipvaultSummary());
          return;
        }
        if (path === '/api/ipvault' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createIpvault(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ipvault/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateIpvault(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/legaldesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, legaldeskSummary());
          return;
        }
        if (path === '/api/legaldesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLegaldesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/legaldesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLegaldesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/riskreg' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, riskregSummary());
          return;
        }
        if (path === '/api/riskreg' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRiskreg(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/riskreg/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRiskreg(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/policyhub' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, policyhubSummary());
          return;
        }
        if (path === '/api/policyhub' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPolicyhub(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/policyhub/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePolicyhub(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/claimdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, claimdeskSummary());
          return;
        }
        if (path === '/api/claimdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createClaimdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/claimdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateClaimdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/insurancet' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, insurancetSummary());
          return;
        }
        if (path === '/api/insurancet' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createInsurancet(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/insurancet/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateInsurancet(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/litigation' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, litigationSummary());
          return;
        }
        if (path === '/api/litigation' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLitigation(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/litigation/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLitigation(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/compliance2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, compliance2Summary());
          return;
        }
        if (path === '/api/compliance2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCompliance2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/compliance2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCompliance2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ethicsline' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ethicslineSummary());
          return;
        }
        if (path === '/api/ethicsline' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEthicsline(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ethicsline/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEthicsline(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/kycrow' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, kycrowSummary());
          return;
        }
        if (path === '/api/kycrow' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createKycrow(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/kycrow/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateKycrow(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sanctions' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sanctionsSummary());
          return;
        }
        if (path === '/api/sanctions' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSanctions(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sanctions/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSanctions(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/datapriv' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, dataprivSummary());
          return;
        }
        if (path === '/api/datapriv' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDatapriv(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/datapriv/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDatapriv(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/retentionpol' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, retentionpolSummary());
          return;
        }
        if (path === '/api/retentionpol' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRetentionpol(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/retentionpol/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRetentionpol(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/auditevidence' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, auditevidenceSummary());
          return;
        }
        if (path === '/api/auditevidence' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAuditevidence(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/auditevidence/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAuditevidence(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/charter' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildCharter());
          return;
        }
        if (path === '/api/charter/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runCharterSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/charter/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackCharterFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/charter/ethics/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeCharterEthics(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/charter/risk/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveCharterRisk(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/charter/claim/done' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, doneCharterClaim(await readBody(req), user.username)); })();
          return;
        }

        // ── Phoenix OS · Phoenix (AŞAMA 826–840) ──

        if (path === '/api/boardresolve' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, boardresolveSummary());
          return;
        }
        if (path === '/api/boardresolve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBoardresolve(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/boardresolve/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBoardresolve(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/drplan' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, drplanSummary());
          return;
        }
        if (path === '/api/drplan' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDrplan(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/drplan/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDrplan(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/backupjob' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, backupjobSummary());
          return;
        }
        if (path === '/api/backupjob' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBackupjob(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/backupjob/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBackupjob(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/failover2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, failover2Summary());
          return;
        }
        if (path === '/api/failover2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFailover2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/failover2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFailover2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/runbook' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, runbookSummary());
          return;
        }
        if (path === '/api/runbook' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRunbook(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/runbook/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRunbook(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/warroom2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, warroom2Summary());
          return;
        }
        if (path === '/api/warroom2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWarroom2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/warroom2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWarroom2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/commsbridge2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, commsbridge2Summary());
          return;
        }
        if (path === '/api/commsbridge2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCommsbridge2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/commsbridge2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCommsbridge2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/siteevac' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, siteevacSummary());
          return;
        }
        if (path === '/api/siteevac' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSiteevac(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/siteevac/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSiteevac(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/coldsite' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, coldsiteSummary());
          return;
        }
        if (path === '/api/coldsite' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createColdsite(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/coldsite/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateColdsite(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/hotspare' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, hotspareSummary());
          return;
        }
        if (path === '/api/hotspare' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHotspare(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/hotspare/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHotspare(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/drillscore' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, drillscoreSummary());
          return;
        }
        if (path === '/api/drillscore' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDrillscore(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/drillscore/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDrillscore(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/vendorfail' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, vendorfailSummary());
          return;
        }
        if (path === '/api/vendorfail' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createVendorfail(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/vendorfail/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateVendorfail(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/powercut' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, powercutSummary());
          return;
        }
        if (path === '/api/powercut' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPowercut(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/powercut/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePowercut(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/netsplit' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, netsplitSummary());
          return;
        }
        if (path === '/api/netsplit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createNetsplit(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/netsplit/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateNetsplit(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/phoenix' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildPhoenix());
          return;
        }
        if (path === '/api/phoenix/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runPhoenixSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/phoenix/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackPhoenixFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/phoenix/backup/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closePhoenixBackup(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/phoenix/runbook/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, livePhoenixRunbook(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/phoenix/drill/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closePhoenixDrill(await readBody(req), user.username)); })();
          return;
        }

        // ── Frontier OS · Frontier (AŞAMA 841–855) ──

        if (path === '/api/restorejob' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, restorejobSummary());
          return;
        }
        if (path === '/api/restorejob' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRestorejob(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/restorejob/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRestorejob(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/marketscan' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, marketscanSummary());
          return;
        }
        if (path === '/api/marketscan' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMarketscan(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/marketscan/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMarketscan(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sitehunt' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sitehuntSummary());
          return;
        }
        if (path === '/api/sitehunt' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSitehunt(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sitehunt/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSitehunt(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/capextable' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, capextableSummary());
          return;
        }
        if (path === '/api/capextable' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCapextable(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/capextable/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCapextable(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/softopen' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, softopenSummary());
          return;
        }
        if (path === '/api/softopen' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSoftopen(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/softopen/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSoftopen(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/launchpad' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, launchpadSummary());
          return;
        }
        if (path === '/api/launchpad' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLaunchpad(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/launchpad/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLaunchpad(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/localhire' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, localhireSummary());
          return;
        }
        if (path === '/api/localhire' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLocalhire(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/localhire/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLocalhire(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/permitdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, permitdeskSummary());
          return;
        }
        if (path === '/api/permitdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPermitdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/permitdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePermitdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/landlease' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, landleaseSummary());
          return;
        }
        if (path === '/api/landlease' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLandlease(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/landlease/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLandlease(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/buildphase' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildphaseSummary());
          return;
        }
        if (path === '/api/buildphase' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBuildphase(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/buildphase/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBuildphase(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ffespec' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ffespecSummary());
          return;
        }
        if (path === '/api/ffespec' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFfespec(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ffespec/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFfespec(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/brandrollout' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, brandrolloutSummary());
          return;
        }
        if (path === '/api/brandrollout' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBrandrollout(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/brandrollout/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBrandrollout(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/trainwave' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, trainwaveSummary());
          return;
        }
        if (path === '/api/trainwave' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTrainwave(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/trainwave/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTrainwave(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/golive' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, goliveSummary());
          return;
        }
        if (path === '/api/golive' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGolive(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/golive/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGolive(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/frontier' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildFrontier());
          return;
        }
        if (path === '/api/frontier/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runFrontierSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/frontier/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackFrontierFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/frontier/restore/run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runFrontierRestore(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/frontier/site/busy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, busyFrontierSite(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/frontier/hire/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveFrontierHire(await readBody(req), user.username)); })();
          return;
        }

        // ── Prism OS · Prism (AŞAMA 856–870) ──

        if (path === '/api/postlaunch' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, postlaunchSummary());
          return;
        }
        if (path === '/api/postlaunch' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPostlaunch(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/postlaunch/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePostlaunch(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/qasample' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, qasampleSummary());
          return;
        }
        if (path === '/api/qasample' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createQasample(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/qasample/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateQasample(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/defectlog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, defectlogSummary());
          return;
        }
        if (path === '/api/defectlog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDefectlog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/defectlog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDefectlog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/standardop' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, standardopSummary());
          return;
        }
        if (path === '/api/standardop' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createStandardop(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/standardop/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateStandardop(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/mysteryguest' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, mysteryguestSummary());
          return;
        }
        if (path === '/api/mysteryguest' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMysteryguest(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/mysteryguest/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMysteryguest(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/npsdeep' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, npsdeepSummary());
          return;
        }
        if (path === '/api/npsdeep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createNpsdeep(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/npsdeep/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateNpsdeep(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/servicemark' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, servicemarkSummary());
          return;
        }
        if (path === '/api/servicemark' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createServicemark(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/servicemark/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateServicemark(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/calibdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, calibdeskSummary());
          return;
        }
        if (path === '/api/calibdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCalibdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/calibdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCalibdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/labresult' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, labresultSummary());
          return;
        }
        if (path === '/api/labresult' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLabresult(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/labresult/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLabresult(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/certrenew' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, certrenewSummary());
          return;
        }
        if (path === '/api/certrenew' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCertrenew(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/certrenew/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCertrenew(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/isotrack' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, isotrackSummary());
          return;
        }
        if (path === '/api/isotrack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createIsotrack(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/isotrack/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateIsotrack(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/guestvoice' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, guestvoiceSummary());
          return;
        }
        if (path === '/api/guestvoice' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGuestvoice(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/guestvoice/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGuestvoice(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/fixgate' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, fixgateSummary());
          return;
        }
        if (path === '/api/fixgate' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFixgate(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/fixgate/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFixgate(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/rootcause' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, rootcauseSummary());
          return;
        }
        if (path === '/api/rootcause' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRootcause(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/rootcause/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRootcause(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/prism' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildPrism());
          return;
        }
        if (path === '/api/prism/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runPrismSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/prism/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackPrismFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/prism/mark/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closePrismMark(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/prism/post/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, livePrismPost(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/prism/defect/run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runPrismDefect(await readBody(req), user.username)); })();
          return;
        }

        // ── Monument OS · Monument (AŞAMA 871–885) ──

        if (path === '/api/corrective' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, correctiveSummary());
          return;
        }
        if (path === '/api/corrective' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCorrective(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/corrective/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCorrective(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/archivebox' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, archiveboxSummary());
          return;
        }
        if (path === '/api/archivebox' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createArchivebox(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/archivebox/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateArchivebox(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/oralhistory' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, oralhistorySummary());
          return;
        }
        if (path === '/api/oralhistory' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOralhistory(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/oralhistory/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOralhistory(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/artifact' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, artifactSummary());
          return;
        }
        if (path === '/api/artifact' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createArtifact(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/artifact/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateArtifact(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/timeline' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, timelineSummary());
          return;
        }
        if (path === '/api/timeline' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createTimeline(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/timeline/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateTimeline(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/foundersnote' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, foundersnoteSummary());
          return;
        }
        if (path === '/api/foundersnote' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFoundersnote(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/foundersnote/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFoundersnote(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/brandbible' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, brandbibleSummary());
          return;
        }
        if (path === '/api/brandbible' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBrandbible(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/brandbible/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBrandbible(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/museumdesk' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, museumdeskSummary());
          return;
        }
        if (path === '/api/museumdesk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMuseumdesk(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/museumdesk/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMuseumdesk(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/heritage' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, heritageSummary());
          return;
        }
        if (path === '/api/heritage' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHeritage(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/heritage/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHeritage(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/anniversary' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, anniversarySummary());
          return;
        }
        if (path === '/api/anniversary' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAnniversary(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/anniversary/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAnniversary(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/alumni' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, alumniSummary());
          return;
        }
        if (path === '/api/alumni' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAlumni(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/alumni/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAlumni(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/scholarship' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, scholarshipSummary());
          return;
        }
        if (path === '/api/scholarship' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createScholarship(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/scholarship/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateScholarship(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/foundation' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, foundationSummary());
          return;
        }
        if (path === '/api/foundation' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFoundation(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/foundation/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFoundation(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/legacygift' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, legacygiftSummary());
          return;
        }
        if (path === '/api/legacygift' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLegacygift(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/legacygift/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLegacygift(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/monument' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildMonument());
          return;
        }
        if (path === '/api/monument/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runMonumentSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/monument/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackMonumentFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/monument/corrective/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeMonumentCorrective(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/monument/oral/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveMonumentOral(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/monument/timeline/run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runMonumentTimeline(await readBody(req), user.username)); })();
          return;
        }

        // ── Olympus Finale · Olympus (AŞAMA 886–900) ──

        if (path === '/api/storyvault' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, storyvaultSummary());
          return;
        }
        if (path === '/api/storyvault' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createStoryvault(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/storyvault/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateStoryvault(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/olympulse' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, olympulseSummary());
          return;
        }
        if (path === '/api/olympulse' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOlympulse(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/olympulse/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOlympulse(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/holdingseal' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, holdingsealSummary());
          return;
        }
        if (path === '/api/holdingseal' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHoldingseal(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/holdingseal/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHoldingseal(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/agentcourt' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, agentcourtSummary());
          return;
        }
        if (path === '/api/agentcourt' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAgentcourt(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/agentcourt/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAgentcourt(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/finalbrief' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, finalbriefSummary());
          return;
        }
        if (path === '/api/finalbrief' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFinalbrief(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/finalbrief/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFinalbrief(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/legacycode' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, legacycodeSummary());
          return;
        }
        if (path === '/api/legacycode' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLegacycode(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/legacycode/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLegacycode(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/eternallog' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, eternallogSummary());
          return;
        }
        if (path === '/api/eternallog' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEternallog(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/eternallog/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEternallog(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/summitnote' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, summitnoteSummary());
          return;
        }
        if (path === '/api/summitnote' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSummitnote(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/summitnote/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSummitnote(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/constellate' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, constellateSummary());
          return;
        }
        if (path === '/api/constellate' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createConstellate(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/constellate/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateConstellate(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/mythos' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, mythosSummary());
          return;
        }
        if (path === '/api/mythos' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMythos(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/mythos/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMythos(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/aegisfinal' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, aegisfinalSummary());
          return;
        }
        if (path === '/api/aegisfinal' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAegisfinal(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/aegisfinal/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAegisfinal(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/crownfinal' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, crownfinalSummary());
          return;
        }
        if (path === '/api/crownfinal' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCrownfinal(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/crownfinal/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCrownfinal(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/vaultfinal' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, vaultfinalSummary());
          return;
        }
        if (path === '/api/vaultfinal' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createVaultfinal(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/vaultfinal/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateVaultfinal(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/empirefinal' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, empirefinalSummary());
          return;
        }
        if (path === '/api/empirefinal' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEmpirefinal(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/empirefinal/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEmpirefinal(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/olympus' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildOlympus());
          return;
        }
        if (path === '/api/olympus/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runOlympusSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/olympus/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackOlympusFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/olympus/seal/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeOlympusSeal(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/olympus/brief/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveOlympusBrief(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/olympus/story/busy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, busyOlympusStory(await readBody(req), user.username)); })();
          return;
        }

        // ── Identity OS · Bastion2 (AŞAMA 901–915) ──

        if (path === '/api/accessgate2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, accessgate2Summary());
          return;
        }
        if (path === '/api/accessgate2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAccessgate2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/accessgate2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAccessgate2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/idproof2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, idproof2Summary());
          return;
        }
        if (path === '/api/idproof2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createIdproof2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/idproof2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateIdproof2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/rolegrant2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, rolegrant2Summary());
          return;
        }
        if (path === '/api/rolegrant2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRolegrant2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/rolegrant2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRolegrant2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sessionguard2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sessionguard2Summary());
          return;
        }
        if (path === '/api/sessionguard2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSessionguard2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sessionguard2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSessionguard2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/devicetrust2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, devicetrust2Summary());
          return;
        }
        if (path === '/api/devicetrust2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDevicetrust2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/devicetrust2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDevicetrust2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/secretvault2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, secretvault2Summary());
          return;
        }
        if (path === '/api/secretvault2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSecretvault2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/secretvault2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSecretvault2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/mfareg2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, mfareg2Summary());
          return;
        }
        if (path === '/api/mfareg2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMfareg2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/mfareg2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMfareg2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ssobridge2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ssobridge2Summary());
          return;
        }
        if (path === '/api/ssobridge2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSsobridge2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ssobridge2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSsobridge2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/privacypol2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, privacypol2Summary());
          return;
        }
        if (path === '/api/privacypol2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPrivacypol2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/privacypol2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePrivacypol2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/consentrow2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, consentrow2Summary());
          return;
        }
        if (path === '/api/consentrow2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createConsentrow2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/consentrow2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateConsentrow2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/breachlog2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, breachlog2Summary());
          return;
        }
        if (path === '/api/breachlog2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBreachlog2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/breachlog2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBreachlog2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/socqueue2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, socqueue2Summary());
          return;
        }
        if (path === '/api/socqueue2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSocqueue2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/socqueue2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSocqueue2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/patchdesk2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, patchdesk2Summary());
          return;
        }
        if (path === '/api/patchdesk2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPatchdesk2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/patchdesk2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePatchdesk2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/zerohour2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, zerohour2Summary());
          return;
        }
        if (path === '/api/zerohour2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createZerohour2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/zerohour2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateZerohour2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/bastion2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildBastion2());
          return;
        }
        if (path === '/api/bastion2/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runBastion2Sweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/bastion2/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackBastion2Flag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/bastion2/access/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeBastion2Access(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/bastion2/role/approve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, approveBastion2Role(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/bastion2/breach/archive' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, archiveBastion2Breach(await readBody(req), user.username)); })();
          return;
        }

        // ── Alliance OS · Alliance3 (AŞAMA 916–930) ──

        if (path === '/api/partnerdesk2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, partnerdesk2Summary());
          return;
        }
        if (path === '/api/partnerdesk2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPartnerdesk2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/partnerdesk2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePartnerdesk2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/franchise2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, franchise2Summary());
          return;
        }
        if (path === '/api/franchise2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFranchise2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/franchise2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFranchise2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/channelkit2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, channelkit2Summary());
          return;
        }
        if (path === '/api/channelkit2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createChannelkit2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/channelkit2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateChannelkit2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/rebate2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, rebate2Summary());
          return;
        }
        if (path === '/api/rebate2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRebate2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/rebate2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRebate2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/coinvest2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, coinvest2Summary());
          return;
        }
        if (path === '/api/coinvest2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCoinvest2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/coinvest2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCoinvest2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/slatrack2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, slatrack2Summary());
          return;
        }
        if (path === '/api/slatrack2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSlatrack2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/slatrack2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSlatrack2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/jointpromo2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, jointpromo2Summary());
          return;
        }
        if (path === '/api/jointpromo2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createJointpromo2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/jointpromo2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateJointpromo2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/leadshare2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, leadshare2Summary());
          return;
        }
        if (path === '/api/leadshare2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLeadshare2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/leadshare2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLeadshare2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/b2border2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, b2border2Summary());
          return;
        }
        if (path === '/api/b2border2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createB2border2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/b2border2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateB2border2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/wholesale2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, wholesale2Summary());
          return;
        }
        if (path === '/api/wholesale2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWholesale2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/wholesale2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWholesale2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/dealroom2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, dealroom2Summary());
          return;
        }
        if (path === '/api/dealroom2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDealroom2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/dealroom2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDealroom2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/contractrow2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, contractrow2Summary());
          return;
        }
        if (path === '/api/contractrow2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createContractrow2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/contractrow2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateContractrow2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/commission2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, commission2Summary());
          return;
        }
        if (path === '/api/commission2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCommission2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/commission2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCommission2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/onboardkit2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, onboardkit2Summary());
          return;
        }
        if (path === '/api/onboardkit2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOnboardkit2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/onboardkit2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOnboardkit2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/alliance3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildAlliance3());
          return;
        }
        if (path === '/api/alliance3/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runAlliance3Sweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/alliance3/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackAlliance3Flag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/alliance3/partner/busy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, busyAlliance3Partner(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/alliance3/channel/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeAlliance3Channel(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/alliance3/invest/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveAlliance3Invest(await readBody(req), user.username)); })();
          return;
        }

        // ── Supply OS · Artery2 (AŞAMA 931–945) ──

        if (path === '/api/inboundpo2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, inboundpo2Summary());
          return;
        }
        if (path === '/api/inboundpo2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createInboundpo2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/inboundpo2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateInboundpo2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/outboundso2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, outboundso2Summary());
          return;
        }
        if (path === '/api/outboundso2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOutboundso2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/outboundso2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOutboundso2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/asntrack2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, asntrack2Summary());
          return;
        }
        if (path === '/api/asntrack2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAsntrack2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/asntrack2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAsntrack2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/dockyard2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, dockyard2Summary());
          return;
        }
        if (path === '/api/dockyard2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDockyard2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/dockyard2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDockyard2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/crossdock2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, crossdock2Summary());
          return;
        }
        if (path === '/api/crossdock2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCrossdock2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/crossdock2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCrossdock2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/coldchain22' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, coldchain22Summary());
          return;
        }
        if (path === '/api/coldchain22' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createColdchain22(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/coldchain22/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateColdchain22(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/slotbook2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, slotbook2Summary());
          return;
        }
        if (path === '/api/slotbook2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSlotbook2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/slotbook2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSlotbook2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/carrierbid2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, carrierbid2Summary());
          return;
        }
        if (path === '/api/carrierbid2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCarrierbid2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/carrierbid2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCarrierbid2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/freightbill2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, freightbill2Summary());
          return;
        }
        if (path === '/api/freightbill2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFreightbill2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/freightbill2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFreightbill2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/milestonet2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, milestonet2Summary());
          return;
        }
        if (path === '/api/milestonet2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMilestonet2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/milestonet2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMilestonet2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/exceptionlog2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, exceptionlog2Summary());
          return;
        }
        if (path === '/api/exceptionlog2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createExceptionlog2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/exceptionlog2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateExceptionlog2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/inventoryage2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, inventoryage2Summary());
          return;
        }
        if (path === '/api/inventoryage2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createInventoryage2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/inventoryage2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateInventoryage2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/replenplan2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, replenplan2Summary());
          return;
        }
        if (path === '/api/replenplan2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createReplenplan2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/replenplan2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateReplenplan2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/safetystock2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, safetystock2Summary());
          return;
        }
        if (path === '/api/safetystock2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSafetystock2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/safetystock2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSafetystock2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/artery2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildArtery2());
          return;
        }
        if (path === '/api/artery2/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runArtery2Sweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/artery2/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackArtery2Flag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/artery2/inbound/run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runArtery2Inbound(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/artery2/asn/busy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, busyArtery2Asn(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/artery2/dock/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeArtery2Dock(await readBody(req), user.username)); })();
          return;
        }

        // ── Dominion Seal · Dominion2 (AŞAMA 946–960) ──

        if (path === '/api/supplierkpi2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, supplierkpi2Summary());
          return;
        }
        if (path === '/api/supplierkpi2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSupplierkpi2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/supplierkpi2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSupplierkpi2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cmdpulse2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cmdpulse2Summary());
          return;
        }
        if (path === '/api/cmdpulse2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCmdpulse2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cmdpulse2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCmdpulse2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/boardpulse2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, boardpulse2Summary());
          return;
        }
        if (path === '/api/boardpulse2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBoardpulse2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/boardpulse2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBoardpulse2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/riskheat2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, riskheat2Summary());
          return;
        }
        if (path === '/api/riskheat2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRiskheat2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/riskheat2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRiskheat2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cashpulse2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cashpulse2Summary());
          return;
        }
        if (path === '/api/cashpulse2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCashpulse2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cashpulse2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCashpulse2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/opsheat2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, opsheat2Summary());
          return;
        }
        if (path === '/api/opsheat2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOpsheat2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/opsheat2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOpsheat2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/guestheat2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, guestheat2Summary());
          return;
        }
        if (path === '/api/guestheat2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGuestheat2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/guestheat2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGuestheat2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/brandheat2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, brandheat2Summary());
          return;
        }
        if (path === '/api/brandheat2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBrandheat2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/brandheat2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBrandheat2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/agentpulse2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, agentpulse2Summary());
          return;
        }
        if (path === '/api/agentpulse2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAgentpulse2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/agentpulse2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAgentpulse2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/systempulse2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, systempulse2Summary());
          return;
        }
        if (path === '/api/systempulse2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSystempulse2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/systempulse2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSystempulse2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/alertfuse2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, alertfuse2Summary());
          return;
        }
        if (path === '/api/alertfuse2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAlertfuse2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/alertfuse2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAlertfuse2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/warbrief2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, warbrief2Summary());
          return;
        }
        if (path === '/api/warbrief2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWarbrief2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/warbrief2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWarbrief2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/decisionhub2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, decisionhub2Summary());
          return;
        }
        if (path === '/api/decisionhub2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDecisionhub2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/decisionhub2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDecisionhub2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sealnote2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sealnote2Summary());
          return;
        }
        if (path === '/api/sealnote2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSealnote2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sealnote2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSealnote2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/dominion2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildDominion2());
          return;
        }
        if (path === '/api/dominion2/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runDominion2Sweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/dominion2/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackDominion2Flag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/dominion2/supplier/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveDominion2Supplier(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/dominion2/board/run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runDominion2Board(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/dominion2/cash/busy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, busyDominion2Cash(await readBody(req), user.username)); })();
          return;
        }

        // ── Serenity OS · Serenity2 (AŞAMA 961–975) ──

        if (path === '/api/legacyflag2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, legacyflag2Summary());
          return;
        }
        if (path === '/api/legacyflag2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLegacyflag2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/legacyflag2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLegacyflag2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/calmroom2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, calmroom2Summary());
          return;
        }
        if (path === '/api/calmroom2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCalmroom2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/calmroom2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCalmroom2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/quiethours2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, quiethours2Summary());
          return;
        }
        if (path === '/api/quiethours2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createQuiethours2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/quiethours2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateQuiethours2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/scentmood2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, scentmood2Summary());
          return;
        }
        if (path === '/api/scentmood2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createScentmood2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/scentmood2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateScentmood2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pillowmenu3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pillowmenu3Summary());
          return;
        }
        if (path === '/api/pillowmenu3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPillowmenu3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pillowmenu3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePillowmenu3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/bathritual2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, bathritual2Summary());
          return;
        }
        if (path === '/api/bathritual2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBathritual2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/bathritual2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBathritual2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sleepscore2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sleepscore2Summary());
          return;
        }
        if (path === '/api/sleepscore2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSleepscore2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sleepscore2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSleepscore2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/welcomeamen2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, welcomeamen2Summary());
          return;
        }
        if (path === '/api/welcomeamen2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWelcomeamen2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/welcomeamen2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWelcomeamen2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/farewell2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, farewell2Summary());
          return;
        }
        if (path === '/api/farewell2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFarewell2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/farewell2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFarewell2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/memorybook2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, memorybook2Summary());
          return;
        }
        if (path === '/api/memorybook2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMemorybook2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/memorybook2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMemorybook2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/carecall2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, carecall2Summary());
          return;
        }
        if (path === '/api/carecall2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCarecall2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/carecall2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCarecall2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/surprisegift2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, surprisegift2Summary());
          return;
        }
        if (path === '/api/surprisegift2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSurprisegift2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/surprisegift2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSurprisegift2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/loyaltyhug2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, loyaltyhug2Summary());
          return;
        }
        if (path === '/api/loyaltyhug2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLoyaltyhug2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/loyaltyhug2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLoyaltyhug2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/feedbackloop2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, feedbackloop2Summary());
          return;
        }
        if (path === '/api/feedbackloop2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFeedbackloop2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/feedbackloop2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFeedbackloop2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/serenity2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildSerenity2());
          return;
        }
        if (path === '/api/serenity2/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runSerenity2Sweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/serenity2/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackSerenity2Flag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/serenity2/legacy/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeSerenity2Legacy(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/serenity2/quiet/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveSerenity2Quiet(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/serenity2/pillow/run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runSerenity2Pillow(await readBody(req), user.username)); })();
          return;
        }

        // ── Circuit OS · Circuit2 (AŞAMA 976–990) ──

        if (path === '/api/momentmap2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, momentmap2Summary());
          return;
        }
        if (path === '/api/momentmap2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMomentmap2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/momentmap2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMomentmap2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/apigateway2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, apigateway2Summary());
          return;
        }
        if (path === '/api/apigateway2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createApigateway2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/apigateway2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateApigateway2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/webhookhub2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, webhookhub2Summary());
          return;
        }
        if (path === '/api/webhookhub2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWebhookhub2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/webhookhub2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWebhookhub2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ratelimit22' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ratelimit22Summary());
          return;
        }
        if (path === '/api/ratelimit22' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRatelimit22(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ratelimit22/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRatelimit22(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/schemareg2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, schemareg2Summary());
          return;
        }
        if (path === '/api/schemareg2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSchemareg2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/schemareg2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSchemareg2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/eventbus2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, eventbus2Summary());
          return;
        }
        if (path === '/api/eventbus2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEventbus2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/eventbus2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEventbus2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/jobqueue22' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, jobqueue22Summary());
          return;
        }
        if (path === '/api/jobqueue22' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createJobqueue22(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/jobqueue22/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateJobqueue22(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cachemesh2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cachemesh2Summary());
          return;
        }
        if (path === '/api/cachemesh2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCachemesh2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cachemesh2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCachemesh2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cdnedge2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cdnedge2Summary());
          return;
        }
        if (path === '/api/cdnedge2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCdnedge2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cdnedge2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCdnedge2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/observemap2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, observemap2Summary());
          return;
        }
        if (path === '/api/observemap2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createObservemap2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/observemap2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateObservemap2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/errorbudget3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, errorbudget3Summary());
          return;
        }
        if (path === '/api/errorbudget3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createErrorbudget3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/errorbudget3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateErrorbudget3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/featuregate2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, featuregate2Summary());
          return;
        }
        if (path === '/api/featuregate2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFeaturegate2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/featuregate2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFeaturegate2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/canaryrun2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, canaryrun2Summary());
          return;
        }
        if (path === '/api/canaryrun2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCanaryrun2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/canaryrun2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCanaryrun2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/rollback2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, rollback2Summary());
          return;
        }
        if (path === '/api/rollback2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRollback2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/rollback2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRollback2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/circuit2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildCircuit2());
          return;
        }
        if (path === '/api/circuit2/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runCircuit2Sweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/circuit2/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackCircuit2Flag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/circuit2/moment/busy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, busyCircuit2Moment(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/circuit2/hook/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeCircuit2Hook(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/circuit2/schema/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveCircuit2Schema(await readBody(req), user.username)); })();
          return;
        }

        // ── Agora OS · Agora2 (AŞAMA 991–1005) ──

        if (path === '/api/chaosdrill2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, chaosdrill2Summary());
          return;
        }
        if (path === '/api/chaosdrill2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createChaosdrill2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/chaosdrill2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateChaosdrill2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/memberhub2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, memberhub2Summary());
          return;
        }
        if (path === '/api/memberhub2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMemberhub2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/memberhub2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMemberhub2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/circle2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, circle2Summary());
          return;
        }
        if (path === '/api/circle2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCircle2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/circle2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCircle2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/meetup2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, meetup2Summary());
          return;
        }
        if (path === '/api/meetup2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMeetup2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/meetup2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMeetup2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/forummod2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, forummod2Summary());
          return;
        }
        if (path === '/api/forummod2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createForummod2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/forummod2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateForummod2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/polldesk2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, polldesk2Summary());
          return;
        }
        if (path === '/api/polldesk2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPolldesk2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/polldesk2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePolldesk2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/badgeearn2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, badgeearn2Summary());
          return;
        }
        if (path === '/api/badgeearn2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBadgeearn2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/badgeearn2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBadgeearn2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/questline2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, questline2Summary());
          return;
        }
        if (path === '/api/questline2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createQuestline2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/questline2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateQuestline2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/volunteer2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, volunteer2Summary());
          return;
        }
        if (path === '/api/volunteer2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createVolunteer2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/volunteer2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateVolunteer2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/donation2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, donation2Summary());
          return;
        }
        if (path === '/api/donation2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDonation2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/donation2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDonation2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/chapter2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, chapter2Summary());
          return;
        }
        if (path === '/api/chapter2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createChapter2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/chapter2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateChapter2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ambassador22' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ambassador22Summary());
          return;
        }
        if (path === '/api/ambassador22' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAmbassador22(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ambassador22/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAmbassador22(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/storywall2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, storywall2Summary());
          return;
        }
        if (path === '/api/storywall2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createStorywall2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/storywall2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateStorywall2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ritualcal2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ritualcal2Summary());
          return;
        }
        if (path === '/api/ritualcal2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRitualcal2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ritualcal2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRitualcal2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/agora2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildAgora2());
          return;
        }
        if (path === '/api/agora2/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runAgora2Sweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agora2/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackAgora2Flag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agora2/drill/run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runAgora2Drill(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agora2/circle/busy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, busyAgora2Circle(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agora2/badge/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveAgora2Badge(await readBody(req), user.username)); })();
          return;
        }

        // ── Crucible OS · Crucible2 (AŞAMA 1006–1020) ──

        if (path === '/api/cohort2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cohort2Summary());
          return;
        }
        if (path === '/api/cohort2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCohort2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cohort2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCohort2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/labbench2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, labbench2Summary());
          return;
        }
        if (path === '/api/labbench2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLabbench2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/labbench2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLabbench2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pilotrun2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pilotrun2Summary());
          return;
        }
        if (path === '/api/pilotrun2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPilotrun2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pilotrun2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePilotrun2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/prototype2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, prototype2Summary());
          return;
        }
        if (path === '/api/prototype2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPrototype2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/prototype2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePrototype2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/hypothesis2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, hypothesis2Summary());
          return;
        }
        if (path === '/api/hypothesis2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHypothesis2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/hypothesis2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHypothesis2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/metricslab2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, metricslab2Summary());
          return;
        }
        if (path === '/api/metricslab2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMetricslab2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/metricslab2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMetricslab2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/userboard2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, userboard2Summary());
          return;
        }
        if (path === '/api/userboard2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createUserboard2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/userboard2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateUserboard2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/patentdesk2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, patentdesk2Summary());
          return;
        }
        if (path === '/api/patentdesk2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPatentdesk2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/patentdesk2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePatentdesk2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sandbox2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sandbox2Summary());
          return;
        }
        if (path === '/api/sandbox2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSandbox2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sandbox2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSandbox2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/hackday2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, hackday2Summary());
          return;
        }
        if (path === '/api/hackday2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHackday2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/hackday2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHackday2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/incubate2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, incubate2Summary());
          return;
        }
        if (path === '/api/incubate2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createIncubate2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/incubate2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateIncubate2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/spindesk2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, spindesk2Summary());
          return;
        }
        if (path === '/api/spindesk2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSpindesk2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/spindesk2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSpindesk2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/researchnote2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, researchnote2Summary());
          return;
        }
        if (path === '/api/researchnote2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createResearchnote2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/researchnote2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateResearchnote2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/labbudget2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, labbudget2Summary());
          return;
        }
        if (path === '/api/labbudget2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLabbudget2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/labbudget2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLabbudget2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/crucible2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildCrucible2());
          return;
        }
        if (path === '/api/crucible2/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runCrucible2Sweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/crucible2/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackCrucible2Flag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/crucible2/pilot/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveCrucible2Pilot(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/crucible2/learn/busy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, busyCrucible2Learn(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/crucible2/lab/ship' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, shipCrucible2Lab(await readBody(req), user.username)); })();
          return;
        }

        // ── Charter OS · Charter2 (AŞAMA 1021–1035) ──

        if (path === '/api/ipvault2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ipvault2Summary());
          return;
        }
        if (path === '/api/ipvault2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createIpvault2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ipvault2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateIpvault2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/legaldesk2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, legaldesk2Summary());
          return;
        }
        if (path === '/api/legaldesk2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLegaldesk2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/legaldesk2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLegaldesk2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/riskreg2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, riskreg2Summary());
          return;
        }
        if (path === '/api/riskreg2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRiskreg2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/riskreg2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRiskreg2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/policyhub2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, policyhub2Summary());
          return;
        }
        if (path === '/api/policyhub2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPolicyhub2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/policyhub2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePolicyhub2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/claimdesk2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, claimdesk2Summary());
          return;
        }
        if (path === '/api/claimdesk2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createClaimdesk2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/claimdesk2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateClaimdesk2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/insurancet2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, insurancet2Summary());
          return;
        }
        if (path === '/api/insurancet2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createInsurancet2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/insurancet2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateInsurancet2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/litigation2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, litigation2Summary());
          return;
        }
        if (path === '/api/litigation2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLitigation2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/litigation2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLitigation2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/compliance22' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, compliance22Summary());
          return;
        }
        if (path === '/api/compliance22' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCompliance22(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/compliance22/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCompliance22(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ethicsline2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ethicsline2Summary());
          return;
        }
        if (path === '/api/ethicsline2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEthicsline2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ethicsline2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEthicsline2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/kycrow2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, kycrow2Summary());
          return;
        }
        if (path === '/api/kycrow2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createKycrow2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/kycrow2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateKycrow2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sanctions2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sanctions2Summary());
          return;
        }
        if (path === '/api/sanctions2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSanctions2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sanctions2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSanctions2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/datapriv2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, datapriv2Summary());
          return;
        }
        if (path === '/api/datapriv2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDatapriv2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/datapriv2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDatapriv2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/retentionpol2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, retentionpol2Summary());
          return;
        }
        if (path === '/api/retentionpol2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRetentionpol2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/retentionpol2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRetentionpol2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/auditevidence2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, auditevidence2Summary());
          return;
        }
        if (path === '/api/auditevidence2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAuditevidence2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/auditevidence2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAuditevidence2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/charter2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildCharter2());
          return;
        }
        if (path === '/api/charter2/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runCharter2Sweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/charter2/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackCharter2Flag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/charter2/ethics/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeCharter2Ethics(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/charter2/risk/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveCharter2Risk(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/charter2/claim/done' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, doneCharter2Claim(await readBody(req), user.username)); })();
          return;
        }

        // ── Phoenix OS · Phoenix2 (AŞAMA 1036–1050) ──

        if (path === '/api/boardresolve2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, boardresolve2Summary());
          return;
        }
        if (path === '/api/boardresolve2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBoardresolve2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/boardresolve2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBoardresolve2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/drplan2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, drplan2Summary());
          return;
        }
        if (path === '/api/drplan2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDrplan2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/drplan2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDrplan2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/backupjob2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, backupjob2Summary());
          return;
        }
        if (path === '/api/backupjob2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBackupjob2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/backupjob2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBackupjob2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/failover3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, failover3Summary());
          return;
        }
        if (path === '/api/failover3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFailover3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/failover3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFailover3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/runbook2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, runbook2Summary());
          return;
        }
        if (path === '/api/runbook2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRunbook2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/runbook2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRunbook2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/warroom22' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, warroom22Summary());
          return;
        }
        if (path === '/api/warroom22' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWarroom22(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/warroom22/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWarroom22(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/commsbridge22' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, commsbridge22Summary());
          return;
        }
        if (path === '/api/commsbridge22' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCommsbridge22(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/commsbridge22/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCommsbridge22(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/siteevac2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, siteevac2Summary());
          return;
        }
        if (path === '/api/siteevac2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSiteevac2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/siteevac2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSiteevac2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/coldsite2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, coldsite2Summary());
          return;
        }
        if (path === '/api/coldsite2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createColdsite2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/coldsite2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateColdsite2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/hotspare2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, hotspare2Summary());
          return;
        }
        if (path === '/api/hotspare2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHotspare2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/hotspare2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHotspare2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/drillscore2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, drillscore2Summary());
          return;
        }
        if (path === '/api/drillscore2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDrillscore2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/drillscore2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDrillscore2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/vendorfail2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, vendorfail2Summary());
          return;
        }
        if (path === '/api/vendorfail2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createVendorfail2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/vendorfail2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateVendorfail2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/powercut2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, powercut2Summary());
          return;
        }
        if (path === '/api/powercut2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPowercut2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/powercut2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePowercut2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/netsplit2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, netsplit2Summary());
          return;
        }
        if (path === '/api/netsplit2' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createNetsplit2(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/netsplit2/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateNetsplit2(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/phoenix2' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildPhoenix2());
          return;
        }
        if (path === '/api/phoenix2/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runPhoenix2Sweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/phoenix2/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackPhoenix2Flag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/phoenix2/backup/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closePhoenix2Backup(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/phoenix2/runbook/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, livePhoenix2Runbook(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/phoenix2/drill/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closePhoenix2Drill(await readBody(req), user.username)); })();
          return;
        }

        // ── Elysium OS · Elysium (AŞAMA 1051–1065) ──

        if (path === '/api/accessgate3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, accessgate3Summary());
          return;
        }
        if (path === '/api/accessgate3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAccessgate3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/accessgate3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAccessgate3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/idproof3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, idproof3Summary());
          return;
        }
        if (path === '/api/idproof3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createIdproof3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/idproof3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateIdproof3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/rolegrant3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, rolegrant3Summary());
          return;
        }
        if (path === '/api/rolegrant3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRolegrant3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/rolegrant3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRolegrant3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sessionguard3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sessionguard3Summary());
          return;
        }
        if (path === '/api/sessionguard3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSessionguard3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sessionguard3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSessionguard3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/devicetrust3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, devicetrust3Summary());
          return;
        }
        if (path === '/api/devicetrust3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDevicetrust3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/devicetrust3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDevicetrust3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/secretvault3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, secretvault3Summary());
          return;
        }
        if (path === '/api/secretvault3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSecretvault3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/secretvault3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSecretvault3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/mfareg3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, mfareg3Summary());
          return;
        }
        if (path === '/api/mfareg3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMfareg3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/mfareg3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMfareg3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ssobridge3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ssobridge3Summary());
          return;
        }
        if (path === '/api/ssobridge3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSsobridge3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ssobridge3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSsobridge3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/privacypol3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, privacypol3Summary());
          return;
        }
        if (path === '/api/privacypol3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPrivacypol3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/privacypol3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePrivacypol3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/consentrow3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, consentrow3Summary());
          return;
        }
        if (path === '/api/consentrow3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createConsentrow3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/consentrow3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateConsentrow3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/breachlog3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, breachlog3Summary());
          return;
        }
        if (path === '/api/breachlog3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBreachlog3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/breachlog3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBreachlog3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/socqueue3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, socqueue3Summary());
          return;
        }
        if (path === '/api/socqueue3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSocqueue3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/socqueue3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSocqueue3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/patchdesk3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, patchdesk3Summary());
          return;
        }
        if (path === '/api/patchdesk3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPatchdesk3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/patchdesk3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePatchdesk3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/zerohour3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, zerohour3Summary());
          return;
        }
        if (path === '/api/zerohour3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createZerohour3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/zerohour3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateZerohour3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/elysium' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildElysium());
          return;
        }
        if (path === '/api/elysium/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runElysiumSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/elysium/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackElysiumFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/elysium/access/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeElysiumAccess(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/elysium/role/approve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, approveElysiumRole(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/elysium/breach/archive' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, archiveElysiumBreach(await readBody(req), user.username)); })();
          return;
        }

        // ── Aether OS · Aether (AŞAMA 1066–1080) ──

        if (path === '/api/partnerdesk3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, partnerdesk3Summary());
          return;
        }
        if (path === '/api/partnerdesk3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPartnerdesk3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/partnerdesk3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePartnerdesk3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/franchise3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, franchise3Summary());
          return;
        }
        if (path === '/api/franchise3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFranchise3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/franchise3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFranchise3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/channelkit3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, channelkit3Summary());
          return;
        }
        if (path === '/api/channelkit3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createChannelkit3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/channelkit3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateChannelkit3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/rebate3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, rebate3Summary());
          return;
        }
        if (path === '/api/rebate3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRebate3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/rebate3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRebate3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/coinvest3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, coinvest3Summary());
          return;
        }
        if (path === '/api/coinvest3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCoinvest3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/coinvest3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCoinvest3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/slatrack3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, slatrack3Summary());
          return;
        }
        if (path === '/api/slatrack3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSlatrack3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/slatrack3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSlatrack3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/jointpromo3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, jointpromo3Summary());
          return;
        }
        if (path === '/api/jointpromo3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createJointpromo3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/jointpromo3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateJointpromo3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/leadshare3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, leadshare3Summary());
          return;
        }
        if (path === '/api/leadshare3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLeadshare3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/leadshare3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLeadshare3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/b2border3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, b2border3Summary());
          return;
        }
        if (path === '/api/b2border3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createB2border3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/b2border3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateB2border3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/wholesale3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, wholesale3Summary());
          return;
        }
        if (path === '/api/wholesale3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWholesale3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/wholesale3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWholesale3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/dealroom3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, dealroom3Summary());
          return;
        }
        if (path === '/api/dealroom3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDealroom3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/dealroom3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDealroom3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/contractrow3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, contractrow3Summary());
          return;
        }
        if (path === '/api/contractrow3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createContractrow3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/contractrow3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateContractrow3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/commission3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, commission3Summary());
          return;
        }
        if (path === '/api/commission3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCommission3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/commission3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCommission3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/onboardkit3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, onboardkit3Summary());
          return;
        }
        if (path === '/api/onboardkit3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOnboardkit3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/onboardkit3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOnboardkit3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/aether' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildAether());
          return;
        }
        if (path === '/api/aether/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runAetherSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/aether/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackAetherFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/aether/partner/activate' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, activateAetherPartner(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/aether/deal/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeAetherDeal(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/aether/coinvest/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveAetherCoinvest(await readBody(req), user.username)); })();
          return;
        }

        // ── Helios OS · Helios (AŞAMA 1081–1095) ──

        if (path === '/api/inboundpo3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, inboundpo3Summary());
          return;
        }
        if (path === '/api/inboundpo3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createInboundpo3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/inboundpo3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateInboundpo3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/outboundso3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, outboundso3Summary());
          return;
        }
        if (path === '/api/outboundso3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOutboundso3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/outboundso3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOutboundso3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/asntrack3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, asntrack3Summary());
          return;
        }
        if (path === '/api/asntrack3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAsntrack3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/asntrack3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAsntrack3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/dockyard3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, dockyard3Summary());
          return;
        }
        if (path === '/api/dockyard3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDockyard3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/dockyard3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDockyard3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/crossdock3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, crossdock3Summary());
          return;
        }
        if (path === '/api/crossdock3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCrossdock3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/crossdock3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCrossdock3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/coldchain23' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, coldchain23Summary());
          return;
        }
        if (path === '/api/coldchain23' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createColdchain23(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/coldchain23/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateColdchain23(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/slotbook3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, slotbook3Summary());
          return;
        }
        if (path === '/api/slotbook3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSlotbook3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/slotbook3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSlotbook3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/carrierbid3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, carrierbid3Summary());
          return;
        }
        if (path === '/api/carrierbid3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCarrierbid3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/carrierbid3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCarrierbid3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/freightbill3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, freightbill3Summary());
          return;
        }
        if (path === '/api/freightbill3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFreightbill3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/freightbill3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFreightbill3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/milestonet3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, milestonet3Summary());
          return;
        }
        if (path === '/api/milestonet3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMilestonet3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/milestonet3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMilestonet3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/exceptionlog3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, exceptionlog3Summary());
          return;
        }
        if (path === '/api/exceptionlog3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createExceptionlog3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/exceptionlog3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateExceptionlog3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/inventoryage3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, inventoryage3Summary());
          return;
        }
        if (path === '/api/inventoryage3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createInventoryage3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/inventoryage3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateInventoryage3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/replenplan3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, replenplan3Summary());
          return;
        }
        if (path === '/api/replenplan3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createReplenplan3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/replenplan3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateReplenplan3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/safetystock3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, safetystock3Summary());
          return;
        }
        if (path === '/api/safetystock3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSafetystock3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/safetystock3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSafetystock3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/helios' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildHelios());
          return;
        }
        if (path === '/api/helios/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runHeliosSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/helios/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackHeliosFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/helios/inbound/run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runHeliosInbound(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/helios/asn/busy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, busyHeliosAsn(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/helios/slot/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveHeliosSlot(await readBody(req), user.username)); })();
          return;
        }

        // ── Selene OS · Selene (AŞAMA 1096–1110) ──

        if (path === '/api/supplierkpi3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, supplierkpi3Summary());
          return;
        }
        if (path === '/api/supplierkpi3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSupplierkpi3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/supplierkpi3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSupplierkpi3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cmdpulse3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cmdpulse3Summary());
          return;
        }
        if (path === '/api/cmdpulse3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCmdpulse3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cmdpulse3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCmdpulse3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/boardpulse3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, boardpulse3Summary());
          return;
        }
        if (path === '/api/boardpulse3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBoardpulse3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/boardpulse3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBoardpulse3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/riskheat3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, riskheat3Summary());
          return;
        }
        if (path === '/api/riskheat3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRiskheat3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/riskheat3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRiskheat3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cashpulse3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cashpulse3Summary());
          return;
        }
        if (path === '/api/cashpulse3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCashpulse3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cashpulse3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCashpulse3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/opsheat3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, opsheat3Summary());
          return;
        }
        if (path === '/api/opsheat3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createOpsheat3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/opsheat3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateOpsheat3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/guestheat3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, guestheat3Summary());
          return;
        }
        if (path === '/api/guestheat3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createGuestheat3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/guestheat3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateGuestheat3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/brandheat3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, brandheat3Summary());
          return;
        }
        if (path === '/api/brandheat3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBrandheat3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/brandheat3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBrandheat3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/agentpulse3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, agentpulse3Summary());
          return;
        }
        if (path === '/api/agentpulse3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAgentpulse3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/agentpulse3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAgentpulse3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/systempulse3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, systempulse3Summary());
          return;
        }
        if (path === '/api/systempulse3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSystempulse3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/systempulse3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSystempulse3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/alertfuse3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, alertfuse3Summary());
          return;
        }
        if (path === '/api/alertfuse3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAlertfuse3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/alertfuse3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAlertfuse3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/warbrief3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, warbrief3Summary());
          return;
        }
        if (path === '/api/warbrief3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWarbrief3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/warbrief3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWarbrief3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/decisionhub3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, decisionhub3Summary());
          return;
        }
        if (path === '/api/decisionhub3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDecisionhub3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/decisionhub3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDecisionhub3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sealnote3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sealnote3Summary());
          return;
        }
        if (path === '/api/sealnote3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSealnote3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sealnote3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSealnote3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/selene' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildSelene());
          return;
        }
        if (path === '/api/selene/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runSeleneSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/selene/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackSeleneFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/selene/heat/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeSeleneHeat(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/selene/supplier/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveSeleneSupplier(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/selene/board/run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runSeleneBoard(await readBody(req), user.username)); })();
          return;
        }

        // ── Gaia OS · Gaia (AŞAMA 1111–1125) ──

        if (path === '/api/legacyflag3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, legacyflag3Summary());
          return;
        }
        if (path === '/api/legacyflag3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLegacyflag3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/legacyflag3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLegacyflag3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/calmroom3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, calmroom3Summary());
          return;
        }
        if (path === '/api/calmroom3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCalmroom3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/calmroom3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCalmroom3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/quiethours3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, quiethours3Summary());
          return;
        }
        if (path === '/api/quiethours3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createQuiethours3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/quiethours3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateQuiethours3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/scentmood3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, scentmood3Summary());
          return;
        }
        if (path === '/api/scentmood3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createScentmood3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/scentmood3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateScentmood3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pillowmenu4' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pillowmenu4Summary());
          return;
        }
        if (path === '/api/pillowmenu4' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPillowmenu4(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pillowmenu4/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePillowmenu4(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/bathritual3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, bathritual3Summary());
          return;
        }
        if (path === '/api/bathritual3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBathritual3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/bathritual3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBathritual3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sleepscore3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sleepscore3Summary());
          return;
        }
        if (path === '/api/sleepscore3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSleepscore3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sleepscore3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSleepscore3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/welcomeamen3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, welcomeamen3Summary());
          return;
        }
        if (path === '/api/welcomeamen3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWelcomeamen3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/welcomeamen3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWelcomeamen3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/farewell3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, farewell3Summary());
          return;
        }
        if (path === '/api/farewell3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFarewell3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/farewell3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFarewell3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/memorybook3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, memorybook3Summary());
          return;
        }
        if (path === '/api/memorybook3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMemorybook3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/memorybook3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMemorybook3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/carecall3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, carecall3Summary());
          return;
        }
        if (path === '/api/carecall3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCarecall3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/carecall3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCarecall3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/surprisegift3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, surprisegift3Summary());
          return;
        }
        if (path === '/api/surprisegift3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSurprisegift3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/surprisegift3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSurprisegift3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/loyaltyhug3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, loyaltyhug3Summary());
          return;
        }
        if (path === '/api/loyaltyhug3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLoyaltyhug3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/loyaltyhug3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLoyaltyhug3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/feedbackloop3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, feedbackloop3Summary());
          return;
        }
        if (path === '/api/feedbackloop3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFeedbackloop3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/feedbackloop3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFeedbackloop3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/gaia' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildGaia());
          return;
        }
        if (path === '/api/gaia/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runGaiaSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/gaia/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackGaiaFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/gaia/legacy/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeGaiaLegacy(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/gaia/quiet/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveGaiaQuiet(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/gaia/pillow/run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runGaiaPillow(await readBody(req), user.username)); })();
          return;
        }

        // ── Chronos OS · Chronos (AŞAMA 1126–1140) ──

        if (path === '/api/momentmap3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, momentmap3Summary());
          return;
        }
        if (path === '/api/momentmap3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMomentmap3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/momentmap3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMomentmap3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/apigateway3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, apigateway3Summary());
          return;
        }
        if (path === '/api/apigateway3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createApigateway3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/apigateway3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateApigateway3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/webhookhub3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, webhookhub3Summary());
          return;
        }
        if (path === '/api/webhookhub3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWebhookhub3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/webhookhub3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWebhookhub3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ratelimit23' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ratelimit23Summary());
          return;
        }
        if (path === '/api/ratelimit23' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRatelimit23(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ratelimit23/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRatelimit23(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/schemareg3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, schemareg3Summary());
          return;
        }
        if (path === '/api/schemareg3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSchemareg3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/schemareg3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSchemareg3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/eventbus3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, eventbus3Summary());
          return;
        }
        if (path === '/api/eventbus3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEventbus3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/eventbus3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEventbus3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/jobqueue23' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, jobqueue23Summary());
          return;
        }
        if (path === '/api/jobqueue23' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createJobqueue23(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/jobqueue23/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateJobqueue23(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cachemesh3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cachemesh3Summary());
          return;
        }
        if (path === '/api/cachemesh3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCachemesh3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cachemesh3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCachemesh3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/cdnedge3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cdnedge3Summary());
          return;
        }
        if (path === '/api/cdnedge3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCdnedge3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cdnedge3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCdnedge3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/observemap3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, observemap3Summary());
          return;
        }
        if (path === '/api/observemap3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createObservemap3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/observemap3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateObservemap3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/errorbudget4' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, errorbudget4Summary());
          return;
        }
        if (path === '/api/errorbudget4' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createErrorbudget4(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/errorbudget4/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateErrorbudget4(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/featuregate3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, featuregate3Summary());
          return;
        }
        if (path === '/api/featuregate3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFeaturegate3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/featuregate3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFeaturegate3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/canaryrun3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, canaryrun3Summary());
          return;
        }
        if (path === '/api/canaryrun3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCanaryrun3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/canaryrun3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCanaryrun3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/rollback3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, rollback3Summary());
          return;
        }
        if (path === '/api/rollback3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRollback3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/rollback3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRollback3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/chronos' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildChronos());
          return;
        }
        if (path === '/api/chronos/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runChronosSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/chronos/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackChronosFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/chronos/moment/busy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, busyChronosMoment(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/chronos/hook/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeChronosHook(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/chronos/schema/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveChronosSchema(await readBody(req), user.username)); })();
          return;
        }

        // ── Kairos OS · Kairos (AŞAMA 1141–1155) ──

        if (path === '/api/chaosdrill3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, chaosdrill3Summary());
          return;
        }
        if (path === '/api/chaosdrill3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createChaosdrill3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/chaosdrill3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateChaosdrill3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/memberhub3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, memberhub3Summary());
          return;
        }
        if (path === '/api/memberhub3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMemberhub3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/memberhub3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMemberhub3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/circle3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, circle3Summary());
          return;
        }
        if (path === '/api/circle3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCircle3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/circle3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCircle3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/meetup3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, meetup3Summary());
          return;
        }
        if (path === '/api/meetup3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMeetup3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/meetup3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMeetup3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/forummod3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, forummod3Summary());
          return;
        }
        if (path === '/api/forummod3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createForummod3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/forummod3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateForummod3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/polldesk3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, polldesk3Summary());
          return;
        }
        if (path === '/api/polldesk3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPolldesk3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/polldesk3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePolldesk3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/badgeearn3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, badgeearn3Summary());
          return;
        }
        if (path === '/api/badgeearn3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBadgeearn3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/badgeearn3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBadgeearn3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/questline3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, questline3Summary());
          return;
        }
        if (path === '/api/questline3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createQuestline3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/questline3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateQuestline3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/volunteer3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, volunteer3Summary());
          return;
        }
        if (path === '/api/volunteer3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createVolunteer3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/volunteer3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateVolunteer3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/donation3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, donation3Summary());
          return;
        }
        if (path === '/api/donation3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDonation3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/donation3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDonation3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/chapter3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, chapter3Summary());
          return;
        }
        if (path === '/api/chapter3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createChapter3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/chapter3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateChapter3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ambassador23' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ambassador23Summary());
          return;
        }
        if (path === '/api/ambassador23' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAmbassador23(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ambassador23/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAmbassador23(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/storywall3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, storywall3Summary());
          return;
        }
        if (path === '/api/storywall3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createStorywall3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/storywall3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateStorywall3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ritualcal3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ritualcal3Summary());
          return;
        }
        if (path === '/api/ritualcal3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRitualcal3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ritualcal3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRitualcal3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/kairos' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildKairos());
          return;
        }
        if (path === '/api/kairos/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runKairosSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/kairos/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackKairosFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/kairos/drill/run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runKairosDrill(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/kairos/circle/busy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, busyKairosCircle(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/kairos/badge/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveKairosBadge(await readBody(req), user.username)); })();
          return;
        }

        // ── Logos OS · Logos (AŞAMA 1156–1170) ──

        if (path === '/api/cohort3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cohort3Summary());
          return;
        }
        if (path === '/api/cohort3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCohort3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/cohort3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCohort3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/labbench3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, labbench3Summary());
          return;
        }
        if (path === '/api/labbench3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLabbench3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/labbench3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLabbench3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/pilotrun3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, pilotrun3Summary());
          return;
        }
        if (path === '/api/pilotrun3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPilotrun3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/pilotrun3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePilotrun3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/prototype3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, prototype3Summary());
          return;
        }
        if (path === '/api/prototype3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPrototype3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/prototype3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePrototype3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/hypothesis3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, hypothesis3Summary());
          return;
        }
        if (path === '/api/hypothesis3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHypothesis3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/hypothesis3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHypothesis3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/metricslab3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, metricslab3Summary());
          return;
        }
        if (path === '/api/metricslab3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createMetricslab3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/metricslab3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMetricslab3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/userboard3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, userboard3Summary());
          return;
        }
        if (path === '/api/userboard3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createUserboard3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/userboard3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateUserboard3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/patentdesk3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, patentdesk3Summary());
          return;
        }
        if (path === '/api/patentdesk3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPatentdesk3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/patentdesk3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePatentdesk3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sandbox3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sandbox3Summary());
          return;
        }
        if (path === '/api/sandbox3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSandbox3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sandbox3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSandbox3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/hackday3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, hackday3Summary());
          return;
        }
        if (path === '/api/hackday3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHackday3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/hackday3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHackday3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/incubate3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, incubate3Summary());
          return;
        }
        if (path === '/api/incubate3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createIncubate3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/incubate3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateIncubate3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/spindesk3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, spindesk3Summary());
          return;
        }
        if (path === '/api/spindesk3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSpindesk3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/spindesk3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSpindesk3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/researchnote3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, researchnote3Summary());
          return;
        }
        if (path === '/api/researchnote3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createResearchnote3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/researchnote3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateResearchnote3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/labbudget3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, labbudget3Summary());
          return;
        }
        if (path === '/api/labbudget3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLabbudget3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/labbudget3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLabbudget3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/logos' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildLogos());
          return;
        }
        if (path === '/api/logos/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runLogosSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/logos/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackLogosFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/logos/pilot/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveLogosPilot(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/logos/learn/busy' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, busyLogosLearn(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/logos/lab/ship' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, shipLogosLab(await readBody(req), user.username)); })();
          return;
        }

        // ── Pathos OS · Pathos (AŞAMA 1171–1185) ──

        if (path === '/api/ipvault3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ipvault3Summary());
          return;
        }
        if (path === '/api/ipvault3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createIpvault3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ipvault3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateIpvault3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/legaldesk3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, legaldesk3Summary());
          return;
        }
        if (path === '/api/legaldesk3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLegaldesk3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/legaldesk3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLegaldesk3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/riskreg3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, riskreg3Summary());
          return;
        }
        if (path === '/api/riskreg3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRiskreg3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/riskreg3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRiskreg3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/policyhub3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, policyhub3Summary());
          return;
        }
        if (path === '/api/policyhub3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPolicyhub3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/policyhub3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePolicyhub3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/claimdesk3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, claimdesk3Summary());
          return;
        }
        if (path === '/api/claimdesk3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createClaimdesk3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/claimdesk3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateClaimdesk3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/insurancet3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, insurancet3Summary());
          return;
        }
        if (path === '/api/insurancet3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createInsurancet3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/insurancet3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateInsurancet3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/litigation3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, litigation3Summary());
          return;
        }
        if (path === '/api/litigation3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createLitigation3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/litigation3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateLitigation3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/compliance23' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, compliance23Summary());
          return;
        }
        if (path === '/api/compliance23' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCompliance23(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/compliance23/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCompliance23(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/ethicsline3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, ethicsline3Summary());
          return;
        }
        if (path === '/api/ethicsline3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createEthicsline3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/ethicsline3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateEthicsline3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/kycrow3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, kycrow3Summary());
          return;
        }
        if (path === '/api/kycrow3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createKycrow3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/kycrow3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateKycrow3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/sanctions3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sanctions3Summary());
          return;
        }
        if (path === '/api/sanctions3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSanctions3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/sanctions3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSanctions3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/datapriv3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, datapriv3Summary());
          return;
        }
        if (path === '/api/datapriv3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDatapriv3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/datapriv3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDatapriv3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/retentionpol3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, retentionpol3Summary());
          return;
        }
        if (path === '/api/retentionpol3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRetentionpol3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/retentionpol3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRetentionpol3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/auditevidence3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, auditevidence3Summary());
          return;
        }
        if (path === '/api/auditevidence3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createAuditevidence3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/auditevidence3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateAuditevidence3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/pathos' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildPathos());
          return;
        }
        if (path === '/api/pathos/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runPathosSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/pathos/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackPathosFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/pathos/ip/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closePathosIp(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/pathos/risk/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, livePathosRisk(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/pathos/claim/run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runPathosClaim(await readBody(req), user.username)); })();
          return;
        }

        // ── Apotheosis · Apotheosis (AŞAMA 1186–1200) ──

        if (path === '/api/boardresolve3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, boardresolve3Summary());
          return;
        }
        if (path === '/api/boardresolve3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBoardresolve3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/boardresolve3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBoardresolve3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/drplan3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, drplan3Summary());
          return;
        }
        if (path === '/api/drplan3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDrplan3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/drplan3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDrplan3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/backupjob3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, backupjob3Summary());
          return;
        }
        if (path === '/api/backupjob3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createBackupjob3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/backupjob3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateBackupjob3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/failover4' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, failover4Summary());
          return;
        }
        if (path === '/api/failover4' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createFailover4(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/failover4/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateFailover4(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/runbook3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, runbook3Summary());
          return;
        }
        if (path === '/api/runbook3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createRunbook3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/runbook3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateRunbook3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/warroom23' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, warroom23Summary());
          return;
        }
        if (path === '/api/warroom23' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createWarroom23(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/warroom23/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateWarroom23(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/commsbridge23' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, commsbridge23Summary());
          return;
        }
        if (path === '/api/commsbridge23' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createCommsbridge23(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/commsbridge23/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCommsbridge23(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/siteevac3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, siteevac3Summary());
          return;
        }
        if (path === '/api/siteevac3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createSiteevac3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/siteevac3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateSiteevac3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/coldsite3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, coldsite3Summary());
          return;
        }
        if (path === '/api/coldsite3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createColdsite3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/coldsite3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateColdsite3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/hotspare3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, hotspare3Summary());
          return;
        }
        if (path === '/api/hotspare3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createHotspare3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/hotspare3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateHotspare3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/drillscore3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, drillscore3Summary());
          return;
        }
        if (path === '/api/drillscore3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createDrillscore3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/drillscore3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateDrillscore3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/vendorfail3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, vendorfail3Summary());
          return;
        }
        if (path === '/api/vendorfail3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createVendorfail3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/vendorfail3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateVendorfail3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/powercut3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, powercut3Summary());
          return;
        }
        if (path === '/api/powercut3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createPowercut3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/powercut3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updatePowercut3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        if (path === '/api/netsplit3' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, netsplit3Summary());
          return;
        }
        if (path === '/api/netsplit3' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, { item: createNetsplit3(await readBody(req), user.username) });
          })();
          return;
        }
        if (path.startsWith('/api/netsplit3/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateNetsplit3(path.split('/')[3], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kayıt bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }

        if (path === '/api/apotheosis' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, buildApotheosis());
          return;
        }
        if (path === '/api/apotheosis/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runApotheosisSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/apotheosis/flag/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackApotheosisFlag(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/apotheosis/backup/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeApotheosisBackup(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/apotheosis/runbook/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, liveApotheosisRunbook(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/apotheosis/drill/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeApotheosisDrill(await readBody(req), user.username)); })();
          return;
        }


        // ── Kampüs stack Adım 2–9 ──
        if (path === '/api/campus' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, campusCoreOverview());
          return;
        }
        if (path.startsWith('/api/campus/zones/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateCampusZone(path.split('/')[4], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Zon yok' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }
        if (path === '/api/campus/incident' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, { item: addCampusIncident(await readBody(req), user.username) }); })();
          return;
        }

        if (path === '/api/campus/zone-transition' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, transitionCampusZone(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/campus/incident/resolve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, resolveCampusIncident(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/campus/capacity' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          sendJson(res, 200, campusCapacityRollup(user.username));
          return;
        }

        if (path === '/api/campus/work-order' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, createCampusWorkOrder(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/campus/work-order/complete' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, completeCampusWorkOrder(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/campus/work-order/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runCampusWorkOrderSweep(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/campus/work-order/assign' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, assignCampusWorkOrder(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/campus/work-order/start' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, startCampusWorkOrder(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/campus/work-order/escalate' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, escalateCampusWorkOrder(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/campus/incident/escalate' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, escalateCampusIncident(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/campus/zone/lockdown' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, lockdownCampusZone(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/campus/zone/lockdown/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearCampusZoneLockdown(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/campus/capacity/alert-sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runCampusCapacityAlertSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/stayring' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, stayRingOverview());
          return;
        }
        if (path === '/api/stayring/book' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, createStayBooking(await readBody(req), user.username)); })();
          return;
        }
        if (path.startsWith('/api/stayring/units/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateStayUnit(path.split('/')[4], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Ünite yok' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }
        if (path === '/api/athleteos' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, athleteOsOverview());
          return;
        }
        if (path === '/api/athleteos/plan' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, upsertAthletePlan(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/athleteos/session' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, logAthleteSession(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/athleteos/license' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, issueAthleteLicense(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/athleteos/clearance' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, setAthleteClearance(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/athleteos/injury' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, reportAthleteInjury(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/athleteos/return-to-play' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, advanceReturnToPlay(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/athleteos/rtp-sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runAthleteRtpSweep(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/athleteos/competition' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, registerAthleteCompetition(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/athleteos/competition/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearAthleteForCompetition(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/athleteos/competition/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runAthleteCompetitionClearanceSweep(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/athleteos/coach' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, assignAthleteCoach(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/athleteos/medical-hold' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, placeAthleteMedicalHold(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/athleteos/medical-hold/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearAthleteMedicalHold(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/athleteos/readiness' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, athleteReadinessRollup());
          return;
        }
        if (path === '/api/lifecoach/checkin' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, lifeCoachCheckIn(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/lifecoach/digest' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          sendJson(res, 200, lifeWeeklyDigest(user.username));
          return;
        }

        if (path === '/api/lifecoach/followups/schedule' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, scheduleLifeFollowUps(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/lifecoach/followups/complete' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, completeLifeFollowUp(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/lifecoach/adherence' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, scoreLifePlanAdherence(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/lifecoach/crisis' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, flagLifeCrisis(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/lifecoach/crisis/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearLifeCrisis(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/lifecoach/checkin/missed-sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runLifeMissedCheckInSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/stayring/hk-complete' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, completeStayHk(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/stayring/night-rollup' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          sendJson(res, 200, stayNightRollup(user.username));
          return;
        }

        if (path === '/api/stayring/request' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, createStayGuestRequest(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/stayring/request/complete' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, completeStayGuestRequest(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/stayring/folio/charge' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, postStayFolioCharge(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/stayring/folio/auto' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, autoPostStayFolio(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/stayring/folio/settle' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, settleStayFolio(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/stayring/night-audit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runStayNightAudit(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/stayring/overstay/flag' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, flagStayOverstay(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/stayring/overstay/resolve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, resolveStayOverstay(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/stayring/late-checkout' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, scheduleStayLateCheckout(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/stayring/folio/dispute' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, disputeStayFolioCharge(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/stayring/keyless/revoke' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, revokeStayKeyless(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/lifecoach' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, lifeCoachOverview());
          return;
        }
        if (path === '/api/lifecoach/wearable' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ingestWearable(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/lifecoach/plan' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, createLifePlan(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/marketos' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, marketOsOverview());
          return;
        }
        if (path === '/api/marketos/list' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, createMarketListing(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/marketos/checkout' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, marketCheckout(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/openmall' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, openMallOverview());
          return;
        }
        if (path === '/api/openmall/sale' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, recordMallSale(await readBody(req), user.username)); })();
          return;
        }
        if (path.startsWith('/api/openmall/tenants/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateMallTenant(path.split('/')[4], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Kiracı yok' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }
        if (path === '/api/familycamp' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, familyCampOverview());
          return;
        }
        if (path === '/api/familycamp/checkin' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, familyCheckIn(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/familycamp/checkout' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const body = await readBody(req);
            sendJson(res, 200, familyCheckOut(body.id, user.username));
          })();
          return;
        }
        if (path === '/api/agentbridge' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, agentBridgeOverview());
          return;
        }
        if (path === '/api/agentbridge/ping' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, agentBridgePing(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/agentbridge/broadcast' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, agentBridgeBroadcast(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/agentbridge/channel' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, openAgentBridgeChannel(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentbridge/channel/pulse' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, pulseAgentBridgeChannel(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentbridge/alert' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, escalateAgentBridgeAlert(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentbridge/alert/resolve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, resolveAgentBridgeAlert(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/agentbridge/channel/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeAgentBridgeChannel(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentbridge/alert/sla-sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runAgentBridgeAlertSlaSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentbridge/alert/route' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, routeAgentBridgeAlert(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentbridge/alert/mute' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, muteAgentBridgeAlert(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentbridge/alert/unmute' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, unmuteAgentBridgeAlerts(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentbridge/channel/snooze' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, snoozeAgentBridgeChannel(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentbridge/channel/wake' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, wakeSnoozedAgentBridgeChannels(await readBody(req), user.username)); })();
          return;
        }


        if (path === '/api/stayring/keyless' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, issueStayKeyless(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/stayring/winter' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, setStayWintering(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/stayring/hk' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, createStayHkTask(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/stayring/checkout' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, checkoutStay(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/marketos/channel' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, syncMarketChannel(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/culture' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, cultureSceneOverview());
          return;
        }
        if (path === '/api/culture/event' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, createCultureEvent(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/culture/hold' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, holdCultureTicket(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/culture/live' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, setCultureLive(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/culture/stream/start' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, startCultureStream(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/culture/stream/pulse' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, pulseCultureStream(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/culture/stream/end' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, endCultureStream(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/culture/stage' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, setCultureStageStatus(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/culture/box-office' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          sendJson(res, 200, cultureBoxOfficeRollup(user.username));
          return;
        }

        if (path === '/api/culture/holds/expire' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, expireCultureHolds(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/culture/refund' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, refundCultureSale(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/culture/event/settle' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, settleCultureEvent(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/culture/door/scan' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, scanCultureDoor(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/culture/crew/call' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, callCultureCrew(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/culture/crew/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackCultureCrewCall(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/culture/sale/vip' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, upgradeCultureSaleVip(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/culture/hold/transfer' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, transferCultureHold(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/culture/door/deny' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, denyCultureDoor(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/sportbridge' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, sportBridgeOverview());
          return;
        }
        if (path === '/api/sportbridge/link' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, linkSportProfiles(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/sportbridge/sync-slot' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, syncSlotToSession(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/sportbridge/recovery' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, bridgeRecoveryPlan(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/lifecoach/device' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, registerLifeDevice(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/lifecoach/webhook' && req.method === 'POST') {
          void (async () => {
            const body = await readBody(req);
            const sig = req.headers['x-likya-signature'];
            const demo = req.headers['x-likya-demo'] === '1';
            let verified = false;
            try {
              verified = verifyLifeWebhookSignature(JSON.stringify(body), sig);
            } catch {
              verified = false;
            }
            if (verified || demo) {
              sendJson(res, 200, ingestWearableWebhook(body, {
                actor: demo ? 'demo-webhook' : 'webhook',
                verified: verified || demo,
              }));
              return;
            }
            const authed = requireUser(req, res);
            if (!authed) return;
            sendJson(res, 200, ingestWearableWebhook(body, { actor: authed.username, verified: false }));
          })();
          return;
        }
        if (path === '/api/agentqueue' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, agentQueueOverview());
          return;
        }
        if (path === '/api/agentqueue/enqueue' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, enqueueAgentJob(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentqueue/claim' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, claimAgentJob(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentqueue/complete' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, completeAgentJob(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentqueue/tick' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          sendJson(res, 200, tickAgentQueue(user.username));
          return;
        }

        if (path === '/api/agentqueue/sla-sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runAgentQueueSlaSweep(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/agentqueue/rebalance' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, rebalanceAgentQueue(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentqueue/revive' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, reviveDeadAgentJobs(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentqueue/archive' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, archiveAgentJobs(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentqueue/priority-bump' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, bumpAgentJobPriority(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentqueue/snooze' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, snoozeAgentJob(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentqueue/cancel' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, cancelAgentJob(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentqueue/wake-snoozed' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, wakeSnoozedAgentJobs(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/campusbrief/actions' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          sendJson(res, 200, syncCampusBriefActions(user.username));
          return;
        }
        if (path === '/api/campusbrief/actions/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, ackCampusBriefAction(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/campusbrief/actions/assign' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, assignCampusBriefAction(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/campusbrief/actions/escalate' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, escalateCampusBriefAction(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/campusbrief/actions/snooze' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, snoozeCampusBriefAction(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/campusbrief/actions/wake' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, wakeSnoozedCampusBriefActions(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/campusbrief/actions/dismiss' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, dismissCampusBriefAction(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/campusbrief/publish' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, publishCampusBriefDigest(user.username)); })();
          return;
        }

        if (path === '/api/lifecoach/flags' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          sendJson(res, 200, processLifeFlags(user.username));
          return;
        }
        if (path === '/api/greenpulse' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, greenPulseOverview());
          return;
        }
        if (path === '/api/greenpulse/meter' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, recordGreenMeter(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/greenpulse/incident' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, addGreenIncident(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/campusbrief' && req.method === 'GET') {
          const u = requireUser(req, res);
          if (!u) return;
          sendJson(res, 200, campusBriefOverview(u.username));
          return;
        }
        if (path === '/api/campusbrief/auto' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          sendJson(res, 200, { ...runCampusAutomations(user.username), brief: campusBriefOverview(user.username) });
          return;
        }

        if (path === '/api/agentfleet' && req.method === 'GET') {
          const u = requireUser(req, res);
          if (!u) return;
          sendJson(res, 200, agentFleetOverview());
          return;
        }
        if (path === '/api/agentfleet/ping' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, pingFleetAgent(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentfleet/dispatch' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, dispatchFleetDirective(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/familycamp/book' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, bookFamilyProgram(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/familycamp/emergency' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, familyEmergencyNote(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/familycamp/transfer' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, transferFamilyChild(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/familycamp/pickup-code' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, issueFamilyPickupCode(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/familycamp/authorized-checkout' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, authorizedFamilyCheckout(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/familycamp/safety-sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runFamilySafetySweep(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/familycamp/staff/assign' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, assignFamilyStaff(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/familycamp/roll-call' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runFamilyRollCall(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/familycamp/staff-ratio' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runFamilyStaffRatioSweep(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/familycamp/pickup/noshow' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, flagFamilyPickupNoShow(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/familycamp/program/cancel' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, cancelFamilyProgramBooking(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/familycamp/pickup/expiry-sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runFamilyPickupExpirySweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/culture/confirm' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, confirmCultureTicket(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/culture/release' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, releaseCultureHold(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/marketos/return' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, returnMarketRental(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/marketos/restock' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, restockMarketListing(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/marketos/reconcile' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, reconcileMarketChannels(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/marketos/low-stock' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runMarketLowStockSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/marketos/po' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, createMarketPurchaseOrder(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/marketos/po/receive' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, receiveMarketPurchaseOrder(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/marketos/rental/overdue' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, flagMarketRentalOverdue(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/marketos/rental/damage' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, assessMarketRentalDamage(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/marketos/deposit/settle' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, settleMarketDeposit(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/marketos/rental/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runMarketRentalSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/greenpulse/batch' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, batchRecordGreenMeters(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/openmall/fnb-settle' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, settleMallTenantFnb(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/openmall/day-rollup' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          sendJson(res, 200, mallDayRollup(user.username));
          return;
        }

        if (path === '/api/openmall/rent-run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, generateMallRentRun(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/openmall/invoice/pay' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, payMallInvoice(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/openmall/dunning' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runMallDunningSweep(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/openmall/cam-run' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, generateMallCamRun(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/openmall/lease/hold' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, holdMallLease(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/openmall/lease/release' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, releaseMallLease(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/openmall/invoice/dispute' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, disputeMallInvoice(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/openmall/tenant/pause' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, pauseMallTenant(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/openmall/tenant/resume' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, resumeMallTenant(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/campus/health' && req.method === 'GET') {
          const u = requireUser(req, res);
          if (!u) return;
          sendJson(res, 200, campusHealthCheck());
          return;
        }

        // ── Antalya Extreme Park (AŞAMA 321–325 vizyon / extremepark) ──
        if (path === '/api/extreme' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          sendJson(res, 200, extremeOverview());
          return;
        }
        if (path === '/api/extreme/user-spec' && req.method === 'GET') {
          if (!requireUser(req, res)) return;
          {
            const q = new URL(req.url ?? '', 'http://local');
            sendJson(res, 200, extremeUserSpec(q.searchParams.get('user_id') || 'guest_can'));
          }
          return;
        }
        if (path === '/api/extreme/waiver' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, signExtremeWaiver(await readBody(req), user.username));
          })();
          return;
        }
        if (path === '/api/extreme/slot-weather-check' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, extremeSlotWeatherCheck(await readBody(req), user.username));
          })();
          return;
        }

        if (path === '/api/extreme/weather-hold' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, applyExtremeWeatherHold(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/extreme/weather-clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearExtremeWeatherHold(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/extreme/slot-reserve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, reserveExtremeSlot(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/extreme/slot-cancel' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, cancelExtremeReservation(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/extreme/gear-return' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, returnExtremeGear(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/extreme/gear-issue' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, issueExtremeGear(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/extreme/gear-service-sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runExtremeGearServiceSweep(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/extreme/waitlist' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, joinExtremeWaitlist(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/extreme/waitlist/promote' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, promoteExtremeWaitlist(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/extreme/reservation/check-in' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, checkInExtremeReservation(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/extreme/reservation/no-show' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, markExtremeNoShow(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/extreme/waitlist/expire' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, expireExtremeWaitlist(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentfleet/presence-sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, sweepFleetPresence(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/agentfleet/directive/ack' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, acknowledgeFleetDirective(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentfleet/shift/start' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, startFleetShift(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentfleet/shift/handoff' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, handoffFleetShift(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentfleet/directive/retire' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, retireFleetDirective(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentfleet/park' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, parkFleetAgent(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentfleet/unpark' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, unparkFleetAgents(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentfleet/load-balance' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runFleetLoadBalance(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/agentfleet/shift/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeFleetShift(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/sportbridge/eligibility' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runSportEligibilitySweep(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/sportbridge/gate' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, gateSportSlotAccess(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/sportbridge/comp-hold' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, applySportCompetitionHold(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/sportbridge/recovery/complete' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, completeBridgeRecovery(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/sportbridge/postcomp-sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runSportPostCompSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/sportbridge/hold/snooze' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, snoozeSportHold(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/sportbridge/hold/wake' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, wakeSportHolds(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/sportbridge/gate/escalate' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, escalateSportGate(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/sportbridge/link/archive' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, archiveSportBridgeLink(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/greenpulse/automations' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runGreenPulseAutomations(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/greenpulse/permit' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, createGreenWorkPermit(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/greenpulse/permit/approve' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, approveGreenWorkPermit(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/greenpulse/permit/close' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, closeGreenWorkPermit(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/greenpulse/water-triage' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runWaterLeakTriage(await readBody(req), user.username)); })();
          return;
        }

        if (path === '/api/greenpulse/permit/expiry-sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runGreenPermitExpirySweep(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/greenpulse/curtailment' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, issueGreenCurtailment(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/greenpulse/curtailment/clear' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, clearGreenCurtailment(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/extreme/maas' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, createExtremeMaas(await readBody(req), user.username));
          })();
          return;
        }
        if (path === '/api/extreme/wallet/spend' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            sendJson(res, 200, extremeWalletSpend(await readBody(req), user.username));
          })();
          return;
        }

        if (path === '/api/extreme/maas/renew' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, renewExtremeMaas(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/extreme/wallet/topup' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, topUpExtremeWallet(await readBody(req), user.username)); })();
          return;
        }
        if (path === '/api/extreme/weather-hold/sweep' && req.method === 'POST') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => { sendJson(res, 200, runExtremeWeatherHoldSweep(await readBody(req), user.username)); })();
          return;
        }
        if (path.startsWith('/api/extreme/gear/') && req.method === 'PATCH') {
          const user = requireUser(req, res);
          if (!user) return;
          void (async () => {
            const item = updateExtremeGear(path.split('/')[4], await readBody(req), user.username);
            if (!item) { sendJson(res, 404, { error: 'Ekipman bulunamadı' }); return; }
            sendJson(res, 200, { item });
          })();
          return;
        }


        next();
  };
}

export function platformPlugin() {
  return {
    name: 'likya-platform',
    configureServer(server) {
      server.middlewares.use(createPlatformMiddleware());
    },
  };
}

/** Entegrasyon katmanından kalıcı log yazmak için. */
export function persistWhatsapp(entry) {
  const list = prependItem('whatsapp', entry, 100);
  appendAudit({
    actor: 'REMINDER-AI',
    action: 'whatsapp.send',
    detail: String(entry.body ?? '').slice(0, 100),
    meta: { provider: entry.provider, status: entry.status, guest: entry.guest },
  });
  return list;
}

export function persistNexusEvent(event) {
  const list = prependItem('nexus-events', event, 200);
  if (event.action !== 'protocol_ready') {
    appendAudit({
      actor: 'NEXUS',
      action: `nexus.${event.action}`,
      detail: event.detail || event.deviceId,
      meta: { deviceId: event.deviceId, ok: event.ok },
    });
  }
  return list;
}

export function readWhatsapp() {
  return readCollection('whatsapp', []);
}

export function readNexusEvents() {
  return readCollection('nexus-events', []);
}

export { writeCollection, readCollection };
