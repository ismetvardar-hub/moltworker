# /ship — Launch checklist

Skill: `skills/shipping-and-launch/SKILL.md` + `skills/stop-slop/SKILL.md`

1. `npm run gate` yeşil.
2. `npm run build` yeşil.
3. Slop yok: kullanılmayan import, debug `console.log`, yarım TODO.
4. Commit mesajı net; push `cursor/<feature>-f59e` dalına.
5. PR gövdesi kısa özet + test planı.
6. Demo hesabı bozulmadı mı: `ceo` / `likya2026`.
