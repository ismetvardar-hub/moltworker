# /test — Prove it

Skill: `skills/test-driven-development/SKILL.md`

```bash
npm run typecheck
npm run build
npm run smoke:campus   # kampüs değiştiyse
npm run e2e:campus     # API yüzeyi değiştiyse
npm run gate
```

Kırmızı → yeşil → refaktör. Davranış değişmeden test geçmeli.
